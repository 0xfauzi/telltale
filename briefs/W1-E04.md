You are the experimenter for task W1-E04 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W1-E04 (created from main). This session is itself being recorded by
`telltale run`. The owner approved this experiment's sessions on 2026-09-02: ten short
`claude -p --model sonnet` sessions, no more. Read AGENTS.md, docs/design/02-protocol.md
7.5 (the write-up template), docs/experiments/E01.md (S2 and S6: the numbers this
experiment is built on), experiments/E01/run.py (import its EXPLORE_PROMPT, FIXTURE_REPO
and the fixture-copy helper; do not copy code), docs/log/W1-T1.md and W1-T4.md (the
launcher and the fake agent), src/telltale/launch.py, receiver.py, store.py (the drop
policy and /healthz), tests/integration/fake_agent.py, and ~/.claude/output-styles/
Teacher.md before writing code.

House rules: as little and as simple code as possible; stdlib only; no emoji anywhere;
no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never guess a
number: every number in the write-up is read from a file under experiments/E04/out/;
statistics are median, scaled MAD (1.4826 MAD), min, max and the full sorted list, never
a mean alone; no test files (this is an experiment: experiments/ is excluded from mypy
and deptry, ruff still runs). Commit messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W1-E04; `uv sync`; `uv run ruff check
experiments/E04 && uv run ruff format --check experiments/E04`; `uv run pre-commit run
--all-files`; `git push -u origin task/W1-E04`; `gh pr create --base main --title "W1-E04
perturbation and fail-open under load" --body-file docs/log/W1-E04.md`, PR body ending
"Generated with Claude Code: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc"
(no emoji). experiments/E04/out/ is gitignored except the files you re-include with
`git add -f`: results.json and the per-session meta files, never a transcript or a
stream file.

STOP RULES (the owner's budget): at most 10 agent sessions in total, all `claude -p
--model sonnet`, each capped with `--max-turns 8`. Pilot first: run ONE pair (one ON, one
OFF) and check that each stayed under 60 s and 200,000 tokens (E01 measured S2 at 8.8 s
and 99,661 tokens); if either limit is exceeded, STOP after that pair and report. No
Codex sessions. No session beyond the ten, whatever fails: a failed session counts. The
load arm spends no tokens (fake agent only).

Injected facts. E01 measured S2 (explore-only, sonnet, `--permission-mode acceptEdits`,
EXPLORE_PROMPT, fresh fixture copy) at 8.772 s wall, 3 turns, 99,661 tokens (97 percent
cache reads), exit 0, and S6 (the same prompt with a dead receiver) at 9.685 s: +0.913 s
on a single pair, which is the only overhead number Telltale has and the reason this
experiment exists. E01 also measured that `claude -p` starts from inside a Claude Code
session with CLAUDECODE left in the environment (`drop_claudecode: false`). The launcher
adds 213 ms per capture (W1-T1) and configures four surfaces (hooks, otel_logs,
otel_metrics, stream); the receiver answers every POST 200 and reports drops on
/healthz; the queue is 10,000 deep and a non-droppable batch waits at most 250 ms.
TRAP, and it decides the experiment: YOUR OWN process runs under `telltale run`, so
os.environ carries CLAUDE_CODE_ENABLE_TELEMETRY, OTEL_LOGS_EXPORTER, OTEL_METRICS_EXPORTER,
OTEL_EXPORTER_OTLP_PROTOCOL, OTEL_EXPORTER_OTLP_ENDPOINT, OTEL_LOG_TOOL_DETAILS,
OTEL_LOGS_EXPORT_INTERVAL, OTEL_METRIC_EXPORT_INTERVAL, OTEL_RESOURCE_ATTRIBUTES and
possibly more. An OFF-arm child that inherits them exports telemetry to the
orchestrator's receiver and is not off. The OFF arm's environment removes every
variable whose name starts with OTEL_ or CLAUDE_CODE_ENABLE_TELEMETRY, and the write-up
lists the exact names removed; the ON arm's inner `telltale run` sets its own. Both arms
inherit the owner's global hooks from ~/.claude/settings.json (praxis), which fire in
both and are part of both wall times; say so. TRAP: the inner `telltale run` writes to
the default $TELLTALE_HOME (~/.telltale/telltale.db), the same store the outer capture
writes to; that is intended (they are real captures, experiment "E04") and the busy
timeout handles the two writers; do not set TELLTALE_HOME.

Decision rule, written before the run (copy it into docs/experiments/E04.md verbatim):
(1) Overhead. delta_i = wall_on_i - wall_off_i over 5 interleaved pairs, where wall is
the whole process wall time as the user experiences it (perf_counter around the
subprocess: the launcher process for ON, the claude process for OFF), and separately
delta_api_i from the stream result's duration_ms (the child's own clock). Report the
median and scaled MAD of each delta and the scaled MAD of the OFF arm's wall. If the
median delta exceeds the OFF arm's scaled MAD, the gate report says "the overhead
exceeds run-to-run noise at n = 5" and quotes both numbers; otherwise "not resolved at
n = 5". No threshold is invented: the number is reported, not judged.
(2) Fail-open under load. During a fake-agent capture whose receiver is flooded by a load
generator, every load POST is answered 200 within 300 ms (record the max), the fake
agent's exit code and stdout byte count equal the unloaded run's, and the stream
observations stored for the flooded capture equal the unloaded count, or the difference
is accounted for by /healthz drop counts and diagnostics of kind dropped, listed by
surface. Any POST over 300 ms or unanswered, or a changed exit code or stdout, is a
fail-open violation and the headline of the write-up.
(3) Completeness. In the ON arm, every capture's telltale.capture_ended records
non-zero surfaces_received for hook, otel_logs, otel_metrics and stream; a zero names
the surface and the session in the write-up.

DO:
(1) experiments/E04/run.py: `--pilot` runs one pair; `--all` runs the remaining four
    pairs; order ON, OFF, ON, OFF, ... with a fresh copy of E01's fixture repo for every
    session (both arms). ON: `uv run telltale run --provider claude --level 1 --task-id
    E04 --attempt <k> --experiment E04 -- claude -p --model sonnet --permission-mode
    acceptEdits --max-turns 8 --output-format stream-json --verbose "<EXPLORE_PROMPT>"`
    with stdout captured to out/pair<k>/on.stream.jsonl (never committed). OFF: the same
    `claude -p ...` without the launcher, environment stripped as above, stdout to
    out/pair<k>/off.stream.jsonl. Per session record to out/pair<k>/<arm>.meta.json:
    wall_s, exit_code, num_turns, is_error, duration_ms and usage from the result line,
    stdout_bytes, stderr_bytes, and for ON the capture id (from `telltale sessions`, the
    newest E04 capture) and its capture_ended payload (surfaces_received, duration_ms).
(2) experiments/E04/load.py: `--port P --seconds S --threads T` posts OTLP JSON log
    batches (100 synthetic claude_code.api_request records each, attributes shaped like
    the E01 fixtures) to http://127.0.0.1:P/v1/logs as fast as it can from T threads
    for S seconds, records per POST the status and the latency, and prints count, max
    latency, non-200 count, and the /healthz body at the end. run.py's `--load` arm:
    one unloaded fake-agent capture (`uv run telltale run --provider claude --task-id
    E04-load --attempt 1 --experiment E04 -- bash -c 'echo $OTEL_EXPORTER_OTLP_ENDPOINT >
    endpoint.txt; exec python <fake_agent.py> -p "make the answer 42" --output-format
    stream-json --seed 7'` in a temporary git repository with answer.txt), then the
    same as attempt 2 with load.py started against the endpoint the child wrote, 4
    threads for 20 s, started 1 s after the capture begins; record both captures'
    observation counts by surface, diagnostics by kind, the fake agent's exit code and
    stdout bytes, and load.py's summary, to out/load/results.json.
(3) experiments/E04/out/results.json: every number the write-up quotes, with the file it
    came from.
(4) docs/experiments/E04.md in the 7.5 template: the question, why it matters, the
    decision rule verbatim, exact commands, environment names stripped, the per-pair
    table (pair, wall_on, wall_off, delta, duration_ms_on, duration_ms_off, delta_api,
    tokens_on, tokens_off, turns), the medians and MADs, the load table, what each
    branch of the rule concluded, what was rejected, how sure per claim (measured |
    reasoned | judgement | unknown), and what downstream may now say.
VERIFY (paste in the log): `uv run python experiments/E04/run.py --pilot` (one pair,
both inside the limits, or STOP); `--all`; `--load`; `uv run telltale sessions --limit
14` (five E04 captures and two E04-load captures listed with their durations);
`sqlite3 ~/.telltale/telltale.db "select kind, count(*) from diagnostics where
capture_id in (<the two load capture ids>) group by 1"`; ruff on experiments/E04;
pre-commit.
STOP as above; also STOP if `claude -p` refuses to start in the OFF arm after the
environment strip (report the stderr; do not restore the variables to make it start).
REPORT: docs/log/W1-E04.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE /
NEXT, then EXPLAIN FOR THE OWNER in the Teacher register: what overhead was measured and
how, one real pair with its numbers, why five pairs cannot resolve a small overhead and
what n would (state the formula from design 6.12: MDD = 2.8 s sqrt(2 / n) with s the
scaled MAD, and the n it gives for the measured s and delta), and what the load run did
and did not show. Commits: "W1-E04 runner and load generator", "W1-E04 results and
write-up". Your final message (it is captured): OUTCOME, the per-pair table with the
medians, the load summary line, and anything UNSURE OR NOT DONE.
