You are the implementer for task W1-T4 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W1-T4 (created from main). This session is itself being recorded by
`telltale run`: the build records itself. Read AGENTS.md, docs/design/01-design.md
sections 6.9, 6.11 and 6.12 ("H2 and H3 protocol") and its closing "Amendments from
wave 0" section, docs/design/02-protocol.md (brief and report formats), docs/log/W1-T1.md
(the launcher: how a capture is made, the public functions, the UNSURE list),
src/telltale/launch.py, cli.py, store.py (append, flush, captures, observations, purge),
receiver.py (POST /v1/correlations), allowlist.py (external.correlation fields), and the
S1 fixture's stream.jsonl under fixtures/sources/claude/2.1.257/ (the shape a stream-json
child prints) before writing code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji
anywhere; no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never
guess a number (measure or write "needs measuring"); statistics are median, scaled MAD
(1.4826 MAD), IQR, min, max and the full sorted list, never a mean alone; unknown stays
None; comments state constraints the code cannot show; integration tests only, and you
add exactly one test file, tests/integration/test_experiments.py, plus the live test file
named below. Commit messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W1-T4; `uv sync`; gates green (`uv run mypy .`,
`uv run ruff check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W1-T4`;
`gh pr create --base main --title "W1-T4 repeat runner and fake agent" --body-file
docs/log/W1-T4.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULE: this task must not start any agent session that spends tokens. No `claude -p`,
no `codex exec`. Everything is verified with the fake agent. The one exception is
`claude --version` and `codex --version`, which spend nothing.

Injected facts. The launcher is `uv run telltale run [--provider auto|claude|codex]
[--level N] [--task-id X --attempt N --experiment E] [--commit SHA] -- <argv...>`; it
starts an in-process receiver, records capture_started (provider, argv_shape,
surfaces_configured, task_id, attempt, experiment, repo_id, worktree_id, env_removed),
telltale.environment, telltale.repo.identity, debounced repo.snapshot observations and
capture_ended, tees stdout when the argv contains `--output-format stream-json` or
`--json`, and exits with the child's code; overhead is 213 ms per capture. Stream lines
teed by the launcher are parsed by providers/claude.py as claude.stream.* observations
(assistant messages yield usage plus one observation per tool_use block; user messages
one per tool_result block; the result message carries usage, num_turns, duration_ms and
modelUsage). `POST /v1/correlations` accepts a JSON body that becomes an
external.correlation observation with fields external_system, external_run_id,
component_id, task_id, attempt, provider_session_id (read receiver.py for the exact
route and the capture query parameter). Store: `Store(path).open()`, `.captures()`,
`.observations(capture_id)`, `.activities(capture_id)`, `.evidence(capture_id)`,
`.flush(timeout)`, `.purge(capture_id)`, `.close()`; `telltale sessions` lists captures.
W1-T2 (activities and `telltale show`) is being written in parallel and may not be on
main when you start: build the per-repetition evidence vector from what exists (the
usage fields of claude.stream.result observations and counts of tool_use observations by
name), through a small function `vector(store, capture_id) -> dict[str, float | None]`
that W2-T4 will replace with the real evidence vector; say so in a comment. The
environment fingerprint's runtime_version is None today (W1-T1 UNSURE list): fill it in
the launcher by running `<binary> --version` once and caching the answer under
`$TELLTALE_HOME/versions.json` keyed by the binary's resolved path and mtime, so the
cost is one spawn per binary upgrade rather than per capture; the repeat runner needs
identical fingerprints within a condition and that field is part of the fingerprint.
`telltale purge <capture_id>` does not exist as a command yet: add it (10 lines; it calls
Store.purge and prints the counts). launch.py is 761 lines against the 800-line ratchet:
if your runtime_version change would push it over, first move its reading half (Facts,
facts, link_commits) into src/telltale/facts.py as a separate commit with no behaviour
change.

BRIEF W1-T4: `telltale experiment repeat <spec.json>` that runs N independent captures of
one task under one environment through `telltale run`, records task/attempt correlations
through /v1/correlations, asserts identical fingerprints, and reports robust statistics;
plus the scripted fake agent used by integration tests; plus runtime_version and purge.
OWNS: src/telltale/experiments.py, cli.py (experiment repeat, purge), launch.py or
facts.py (runtime_version only), tests/integration/fake_agent.py,
tests/integration/test_experiments.py, tests/integration/test_live.py, docs/log/W1-T4.md.
READS: launch.py, store.py, receiver.py, providers/claude.py, model.py.
Traps: each repetition is a fresh detached worktree of the target repository at the
same base commit (`git worktree add --detach <dir> <sha>`), removed after the capture
(`git worktree remove --force`); the acceptance command in the spec is run by the
HARNESS after the agent exits, in the worktree, and its exit status is recorded as an
external.outcome observation (kind mechanical_verification, status pass|fail) posted to
the receiver of that capture or appended through the store when the capture has ended
(read how W1-T1 appends launcher observations and do the same). The runner refuses to
compute statistics when two captures in one condition have different environment
fingerprint ids, and prints the differing fields. The fake agent must be a real child
process the launcher wraps: it prints stream-json-shaped lines (system init with a
session_id, assistant messages with tool_use blocks for Read, Edit and Bash, user
messages with tool_result blocks, a result message with usage and num_turns) while
actually editing a file and running a command in its cwd, with `--seed N` changing the
number of tool calls and the token numbers deterministically, `--effort` and `--model`
flags that change its behaviour deterministically (W2-T3 needs between-arm differences),
and a `--fail` flag that makes the acceptance command fail. Its stream lines must parse
through providers/claude.py (check against the S1 fixture's shapes; the test asserts
the observation types stored).
DO:
(1) fake_agent.py as above (about 120 lines; stdlib).
(2) experiments.py: `repeat(spec: dict, home: Path) -> dict` where spec = {task_id,
command: [argv...], repo: path, base_sha, repetitions, provider, level, acceptance:
[argv...], experiment}; for each repetition: worktree, `telltale run --task-id --attempt
--experiment` as a subprocess (the launcher is the only way a capture is made), the
correlation POST or append (external_system "telltale-experiment"), acceptance run and
outcome record, worktree removal; then `vector()` per capture and `stats(values) ->
dict` (n, median, scaled MAD, IQR, min, max, sorted values) per metric; the report dict
carries the spec, the fingerprint id, capture ids, per-repetition vectors, stats, and
the acceptance results. Write it to experiments/<task_id>/report.json only when the
caller asks (the CLI does; tests read the returned dict).
(3) cli.py `experiment repeat <spec.json> [--out DIR]` printing the stats table with a
claim_class column (derived for per-capture numbers; the stats rows are comparative
within the condition, and the table says so) and `purge <capture_id>`.
(4) launch.py or facts.py: runtime_version via the cached `--version` probe; the
fingerprint payload gains the value; `telltale sessions` shows it in the MODEL column's
neighbour or a new column, whichever is smaller.
(5) test_experiments.py: a spec with the fake agent and 5 repetitions on a temporary git
repository (created by the test with one commit): 5 captures exist, attempts 1..5 are
correlated to the task_id, all five fingerprints are equal, the stats table has median
and MAD per metric, the acceptance outcome is recorded for each, the worktrees are gone
afterwards, and one repetition with `--fail` in its command records status fail; a
second spec whose command differs by `--model` between two of the runs is refused with
the differing field named (do this by running two specs and comparing, or by a runner
option, whichever is simpler); `telltale purge` removes one capture and leaves the rest.
(6) test_live.py: two tests marked `live` (skipped unless TELLTALE_LIVE=1) that run one
real `claude -p --model haiku --max-turns 3` and one `codex exec` through the launcher
and assert a capture with model requests; they are NOT run here.
VERIFY (paste outputs in the report): `uv run telltale experiment repeat spec.json` with
the fake agent and 5 repetitions (the table; the fingerprint line; the capture ids);
`uv run telltale sessions` (runtime_version visible for a `claude --version` capture:
run `uv run telltale run -- claude --version` once, which spends no tokens); `uv run
telltale purge <id>`; pytest -m integration; all gates; break-and-restore: make
`vector()` return zeros for missing values and show which assertion fails (unknown must
stay None), then restore.
STOP if the runner would need a real agent to verify (use the fake agent), or if the
launcher cannot wrap the fake agent for a reason in launch.py (report the reason; do
not patch around it silently).
REPORT: docs/log/W1-T4.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE /
NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): what a repetition is, one real table from the fake agent with the numbers
explained, why the fingerprint assertion exists, and what "comparative within the
condition" means. Commits: "W1-T4 fake agent", "W1-T4 repeat runner, purge,
runtime_version". Your final message (it is captured): OUTCOME, the stats table, the
public signatures, and anything UNSURE OR NOT DONE.
