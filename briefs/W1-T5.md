You are the implementer for task W1-T5 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W1-T5 (created from main). This session is itself being recorded by
`telltale run`: the build records itself. Read AGENTS.md, docs/design/01-design.md
section 6.12 ("SeriesCompiler" and "Request clock"; INJECTED below: the column list, the
invariant, the series_id definition, the two missingness policies) and its closing
"Amendments from wave 0" section, docs/log/W1-T2.md (the activities reducer: the activity
types and field names, its UNSURE list), src/telltale/activities.py, model.py (Series,
ColumnSpec, RowMeta), store.py (put_series, activities(capture_id), _read, the ratchet
note), cli.py, report.py (render_table), and the S1 fixture under
fixtures/sources/claude/2.1.257/ before writing code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji
anywhere; no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never
guess a number (measure or write "needs measuring"); unknown stays None, never zero-filled;
comments state constraints the code cannot show; integration tests only, and you add
exactly one test file, tests/integration/test_series.py. Commit messages end with these
two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W1-T5; `uv sync`; gates green (`uv run mypy .`,
`uv run ruff check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W1-T5`;
`gh pr create --base main --title "W1-T5 request-clock series" --body-file
docs/log/W1-T5.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULE: this task must not start any agent session that spends tokens. No `claude -p`,
no `codex exec`. Everything is verified on replayed fixtures and synthetic data.

Injected facts. Activities (W1-T2, read docs/log/W1-T2.md for the authoritative list):
activity types and field names as stored (measured on the replayed S1 fixture, W1-T2):
model_request {input_tokens, cache_read_tokens, cache_creation_tokens, output_tokens,
duration_ms, model, request_index, request_id, query_source, usage_source, cost_usd, clock
("provider"), primary_observation}; verification_run and command {tool_name,
command_norm, category, scope, classifier_version, success (bool or None), duration_ms,
tool_use_id, decision, permission_mode, tool_input_size_bytes, tool_result_size_bytes,
tool_result_content_bytes, clock, primary_observation}; file_edit and file_read
{file_path, tool_name, success, duration_ms, tool_use_id, sizes, clock,
primary_observation}; compaction {trigger, pre_tokens, post_tokens, duration_ms, success};
subagent (S4 only; read docs/log/W1-T2.md); lifecycle: the row with actor "telltale" and
fields.event "capture" carries fields.coverage (a dict capability -> observed | partial |
derived | unavailable, capabilities request_usage, context_window, compaction, tool_calls,
file_paths, commands, exit_codes, subagents, permission_mode, commit_ids),
fields.context_window, fields.context_window_source, fields.models,
fields.surfaces_delivered and started_at/ended_at of the capture; the other lifecycle rows
(session_start, session_end) come from hooks and carry fields.clock "arrival" with
started_at = ingest_ts. Every activity has started_at (never None) and ended_at (None
when unknown). There is no exit_code field: last_verification_exit derives from
verification_run.success (0 when True, 1 when False, None when None). The environment
fingerprint id is NOT an activity field: it is the environment_fingerprint_id COLUMN of
the observation row; row_meta.env_fingerprint_id = that column of the request's
primary_observation, read with `Store.observations_by_id([...])` (the one observation read
this task may make, metadata column only, never the payload), None on replayed fixtures
(no launcher stamped them) and set on launcher captures. Codex activities are being added
by W1-T3 in parallel with the same field names; build against the names above and note
in UNSURE anything Codex-specific you could not test.
`Store.activities(capture_id)` returns dicts with fields and provenance already decoded
from JSON. `Store.put_series(series)` writes through INSERT OR REPLACE keyed on series_id
(a content hash, so a rebuild of identical rows is a no-op). There is no series reader
yet: you add `Store.series(series_id) -> Series | None`, `Store.series_ids(clock=None)
-> list[dict]` (series_id, clock, cohort, rows count, built_at) and
`Store.forecast_runs(series_id) -> list[dict]` (W1-T6 reads them), about 25 lines using
the store's `_read`; store.py is at 798 lines against the 800-line ratchet, so FIRST move
the DDL string `_DDL` (or whatever the schema constant is called) into
src/telltale/schema.py as a separate commit "W1-T5 move schema DDL" with no behaviour
change (the derived-writes-only-in-store hook allows INSERT text only in store.py: the DDL
has CREATE statements only, check with `grep -n "INSERT" src/telltale/schema.py` printing
nothing). Fingerprint: the capture's environment_fingerprint_id sits on every observation
and on the lifecycle activity's fields (W1-T2 says where); a capture with two fingerprints
(model switch) yields a changepoint at the first row after the switch.
Series shape (model.py): `Series(series_id, clock, cohort, columns: list[ColumnSpec(name,
unit, role, coverage)], rows: list[list[float | None]], row_meta: list[RowMeta(row_key,
row_end_ts, env_fingerprint_id, provenance)], changepoints: list[int],
missingness_policy, reducer_version, built_at)`.
Request clock columns, in this order, all role past_covariate except that the target is
chosen at forecast time (W1-T6): fresh_input_tokens, cache_read_tokens, output_tokens
(from the model_request activity's usage fields; coverage observed when the capture's
request_usage capability is observed, else unavailable and the column is all None),
request_duration_ms (Claude: from the model_request duration; Codex: derived from turn
timestamps, coverage partial; None when absent), compaction_before (1 when a compaction
activity ended between the previous request and this one, else 0; coverage from the
compaction capability: unavailable means the column is all None, not zeros),
tool_calls_since_prev, files_edited_since_prev (count of file_edit activities; level 1
only, None at level 0), verification_runs_since_prev, last_verification_exit (0 pass, 1
fail, None when no verification has run yet), env_changed (1 on the first row after a
fingerprint change, else 0). Row = one model_request activity; row_key = the activity id;
row_end_ts = the request's ended_at, or started_at when ended_at is None. Position of any
activity on the clock = its ended_at or started_at; the fold consumes activities in that
order and emits row i from state that has consumed only activities with position <=
row_end_ts[i]: no look-ahead by construction. Row provenance = the activity ids consumed
since the previous row plus the request itself. Invariant checked by `series check`:
`max(position of provenance[i]) <= row_end_ts[i]` and row_end_ts non-decreasing.
series_id = "ser_" + sha256 of the canonical JSON of (clock, cohort, column schema,
reducer_version, rows)[:24]. reducer_version = sha256 of series.py's source at import.
Cohort for the request clock: {capture_id, provider, repo_id, environment_fingerprint_id,
content_level}. Missingness policies: exclude (default; None cells stay None and the
forecaster drops windows containing them: nothing is done at build time except counting
None cells per column and printing the count) and refuse (any None cell in a column with
coverage observed refuses the build with exit 2 naming the column and the first row; a
column whose coverage is unavailable is all None by definition and is reported, not
refused). No policy imputes.

BRIEF W1-T5: `telltale series build --clock request --capture <id> [--policy
exclude|refuse]` and `telltale series check <series_id>`: the prefix-fold compiler that
turns one capture's activities into a Series with no look-ahead, and the invariant
checker; plus the store readers named above.
OWNS: src/telltale/series.py, src/telltale/schema.py (new, DDL only), store.py (readers
and the DDL move only), cli.py (series build, series check), tests/integration/
test_series.py, tests/integration/synthetic_series.py, docs/log/W1-T5.md.
READS: activities.py, model.py, report.py, docs/log/W1-T2.md.
Traps: attempt and change clocks are W3-T1: `series build --clock attempt|change` exits
2 with "not built yet". A capture whose surfaces did not observe a capability gives a
column with coverage unavailable and all None, never zeros. Parallel tool calls and
subagents overlap: sort by position with ties broken by activity id; never force a total
order beyond that. `env_changed` is the only model-visible form of the fingerprint; the
fingerprint id itself lives in row_meta. Do not read observations: every column comes
from activities; if a column's source field does not exist in activities.py, STOP and
report it (do not compute it from observations).
DO:
(1) series.py: `build(store, clock, capture_id, missingness_policy="exclude") -> Series`
    (request clock only), `check(series) -> list[str]` returning invariant violations as
    sentences naming the row, `series_id(...)`, `REDUCER_VERSION`, and a `columns()`
    helper returning the ColumnSpec list with coverage resolved from the capture's
    capability coverage (read it from the lifecycle activity the way W1-T2 stores it).
(2) store.py readers named above; schema.py move first.
(3) cli.py `series build` (prints series_id, row count, each column with its coverage and
    None count, changepoints, and the policy; exit 2 with the reason under refuse) and
    `series check <series_id>` (prints "ok" or the violations, exit 1 on violations);
    `series list`.
(4) tests/integration/synthetic_series.py: `write(store, rows=200, seed=1) -> Series`
    writing a seeded random-walk request-clock Series with the columns above (all
    coverage observed, one changepoint at row 120, missingness exclude) through
    put_series; runnable as `uv run python tests/integration/synthetic_series.py --db PATH
    --rows 200 --seed 1` printing the series_id. W1-T6 uses it.
(5) test_series.py: replay S1 through the receiver (conftest has the helper), rebuild,
    build the series: row count equals the number of model_request activities; each row's
    fresh_input_tokens equals the corresponding model_request's input tokens; the sum of
    verification_runs_since_prev equals the number of verification_run activities; every
    provenance id resolves to an activity of the capture; `check` returns []; building
    twice gives the same series_id; a copy with one row_end_ts moved earlier fails check
    naming the row; policy refuse on a capture with a None cell in an observed column
    exits with the column named (make one by replaying a fixture whose request lacks
    duration if S1 has none, else by a synthetic store write); the synthetic writer
    round-trips through Store.series.
VERIFY (paste outputs in the report; the CLI has no --db flag: every command reads
$TELLTALE_HOME/telltale.db, so `export TELLTALE_HOME=/tmp/t5home; mkdir -p $TELLTALE_HOME`
first): replay S1 with `uv run python -m telltale.receiver --replay
fixtures/sources/claude/2.1.257/S1 --db $TELLTALE_HOME/telltale.db --capture capS1` then
`uv run telltale rebuild`, `uv run telltale series build --clock request --capture capS1`
(row count equals the model_request count you list independently with `sqlite3
$TELLTALE_HOME/telltale.db "select count(*) from activities where
activity_type='model_request' and capture_id='capS1'"`; per-row fresh_input_tokens equal the api_request input_tokens
you list with jq from the fixture's otel_logs.jsonl and paste side by side);
`series check <id>` prints ok; corrupt one row_end_ts in a copy (a 5-line python snippet
against a copied db) and `series check` names the row; `series build --clock attempt`
exits 2; the synthetic writer prints a series_id and `series list` shows it; pytest -m
integration; all gates; break-and-restore: let the fold consume activities with position
<= the NEXT row's end and show which test assertion fails, then restore.
STOP if a column's source activity field does not exist in activities.py (report; do not
compute it from observations), or if W1-T2's lifecycle activity does not carry the
capability coverage (report where it is instead and use that; do not invent coverage).
REPORT: docs/log/W1-T5.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE /
NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): what a clock is, one real table of the first 6 rows of the S1 series with
every column explained from the fixture, what "no look-ahead by construction" means and
how `series check` would catch a violation, and why unavailable is not zero. Commits:
"W1-T5 move schema DDL", "W1-T5 request-clock series". Your final message (it is
captured): OUTCOME, the S1 series table, the public signatures, and anything UNSURE OR
NOT DONE.
