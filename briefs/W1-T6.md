You are the implementer for task W1-T6 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W1-T6 (created from main). This session is itself being recorded by
`telltale run`: the build records itself. Read AGENTS.md, docs/design/01-design.md
section 6.12 in full (the pre-registered constants, "Forecaster interface", "Baselines",
"Rolling-origin backtester", "Where TimesFM-3 bites", "Forecast integration tests") and
its closing "Amendments from wave 0" section (the 6.12 (E03) line), docs/experiments/E03.md
("What it means" and "What downstream may now say"), docs/log/W1-T5.md (the series
compiler: how a Series is built, stored and read back; its UNSURE list), src/telltale/
model.py (Series, ColumnSpec, RowMeta, ForecastResult), store.py (put_series,
put_forecast_run, FORECAST_RUN_COLUMNS, the series readers W1-T5 added), series.py, and
tests/integration/synthetic_series.py if W1-T5 created one (else you create it) before
writing code.

House rules: as little and as simple code as possible; stdlib only in src except
forecast/timesfm.py (numpy, torch, timesfm3 live there and nowhere else: the
forecast-isolation pre-commit hook enforces it, and `FORECASTERS` must register the
TimesFM adapter lazily so `telltale` works without the extra); no emoji anywhere; no em
dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never guess a number
(measure or write "needs measuring"); unknown stays None, never zero-filled; every printed
number is an Evidence field with its claim_class (predictive for forecasts, derived for
metrics computed from actuals); comments state constraints the code cannot show;
integration tests only, and you add exactly one test file,
tests/integration/test_forecast_contracts.py. Commit messages end with these two trailer
lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W1-T6; `uv sync --extra forecast`; gates green
(`uv run mypy .`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run deptry .`,
`uv run pytest -m integration` in the DEFAULT environment, i.e. after a plain `uv sync`,
so the suite proves it needs no torch; `uv run pre-commit run --all-files`); `git push -u
origin task/W1-T6`; `gh pr create --base main --title "W1-T6 forecast core" --body-file
docs/log/W1-T6.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULE: this task must not start any agent session that spends tokens. No `claude -p`,
no `codex exec`. The TimesFM checkpoint download is allowed once (1,322,900,328 bytes,
32.462 s measured by E03) but first reuse E03's cache by exporting
TELLTALE_HF_CACHE=<absolute path of experiments/E03/out/hf> when that directory exists.

Injected facts (measured, E03 on this machine, torch 2.13.0, timesfm 3.0.0, Python 3.12):
one `predict_batch` call costs 0.35 s median on CPU (min 0.148 s, worst seen 0.814 s),
so a 50-origin backtest is about 18 s of model time plus a 1.9 to 2.1 s load; MPS agrees
with CPU to 9.072e-07 relative on one probe and is about 2x faster per call but its load
cost varied 2.0 to 4.9 s, so the adapter defaults to `device="cpu"` and takes
`--device mps` as an opt-in (orchestrator decision: the CPU numbers are the measured cost
model, the MPS agreement was measured on one input). `predict_batch(contexts, horizon,
past_only_covariates=None, past_future_covariates=None, ts_ids=None,
return_quantiles=True, ..., padding_mode=...)` RETURNS A GENERATOR of ForecastOutput
(ts_id, forecast (V,H) or (H,), quantiles (V,H,9) or (H,9)); consume it with `list()`.
The horizon is rounded up to 64 internally so H=1 costs the same as H=64. The model
interpolates interior NaN, trims leading NaN, FORWARD-FILLS trailing NaN and zero-fills an
all-NaN row, silently: the adapter asserts `np.isfinite` on every array before the call
and raises a named error otherwise. The 32-variate cap is not enforced by the model: the
adapter asserts `1 + past_only + past_future <= 32` itself. `padding_mode="none"` and
`"edge"` give forecasts that differ by 1.41 on the E03 probe: the adapter passes
past-future covariates of length n_ctx + H with `padding_mode="edge"` and records
padding_mode in every ForecastRun. A 5-point context runs and returns finite numbers with
no signal that it was short: the adapter enforces c_min itself. Quantiles come back sorted
(0 of 216 steps disordered with sorting off); keep `sort_quantiles` at its default. Point
forecast = quantile index 4 (the median). Weights license string:
`timesfm-non-commercial-license-v1.0` (research, non-production): printed by every
forecast command and stored in every ForecastRun's forecasters JSON. Model construction:
`from timesfm3 import TimesFM3Forecaster, ModelConfig`;
`ModelConfig(checkpoint_path="google/timesfm-3.0-pytorch", device=..., cache_dir=...,
local_files_only=...)` (read experiments/E03/run.py for the exact construction that
worked, and copy it). torch import alone is 1.02 s and 201 MB: never import it outside
timesfm.py, and only inside the adapter's constructor.
Store: `put_forecast_run(mapping)` requires EXACTLY the keys of
store.FORECAST_RUN_COLUMNS (series_id, target, variant, ordering, placebo_seed, horizon,
c_min, stride, forecasters, windows, metrics, decision, scenario, missingness_policy,
warnings, assumptions) and fills forecast_run_id, claim_class and created_at itself;
ordering is 'true' in this task (placebo orderings are W3-T2, so `placebo_seed` is None
and `decision` is None here); JSON columns take dicts and lists.
Pre-registered constants (print them in every report; changing one is a new experiment
id): delta 0.10, w 0.60, k_min 20 windows, c_min 32 rows on the request clock, H in {1, 4},
stride s = H, baseline window 8, threshold tau = q80 of the first c_min rows of the target
unless the registry declares an absolute value.

BRIEF W1-T6: the Forecaster interface, four baselines, the rolling-origin backtester with
its metrics, an echo stub, the TimesFM-3 adapter, the `forecast backtest` command, and
contract tests 1 and 2 of design 6.12.
OWNS: src/telltale/forecast/{__init__,baselines,backtest,timesfm}.py, cli.py (forecast
backtest), tests/integration/test_forecast_contracts.py, tests/integration/
synthetic_series.py (create or extend), docs/log/W1-T6.md.
READS: series.py, store.py, model.py, docs/experiments/E03.md, experiments/E03/run.py.
Traps: every forecaster sees the identical Window (a copy of the context rows; no store
handle); the no-look-ahead test relies on the stub recording exactly what it received.
Baselines are pure functions of the context array behind the same signature. Quantile
index 4 is the point; never average quantiles. A window containing None in the target or
a selected covariate is dropped and counted with its reason; nothing is imputed. Rows
whose target coverage is not observed are excluded before origins are enumerated (ADR-012).
`ctx_start = max(last changepoint <= o, o - 512)`; skip when `o - ctx_start < c_min`
(reason regime_too_short); windows with a changepoint inside [o, o + H) are excluded from
the headline and listed. MASE and RMSE are rejected on purpose (zero denominator on
constant contexts; heavy-tailed counts): MAE of the median forecast per window, aggregated
by mean and median across windows.
DO:
(1) forecast/__init__.py: `Window` dataclass (columns, rows, origin, ctx_start, n_ctx,
    horizon, target); `Forecaster` Protocol `forecast(window, horizon) -> ForecastResult`;
    `TARGETS` registry for the request clock: fresh_input_tokens, output_tokens,
    tool_calls_since_prev, each {clock, unit, nonnegative, c_min 32, horizons [1, 4],
    k_min 20, threshold_rule "q80_first_c_min", variant: the other request-clock columns
    as past-only covariates}; `EchoStub` (persistence plus a fixed offset of 1.0, quantiles
    = point plus (q - 0.5) * 2.0 for the nine q, and it appends every Window it receives to
    `self.seen`); `FORECASTERS: dict[str, Callable[[], Forecaster]]` with persistence,
    rolling_median, rolling_mean, local_drift, echo, and "timesfm" mapped to a function
    that imports forecast.timesfm inside its body.
(2) baselines.py: `persistence`, `rolling_median` (8), `rolling_mean` (8), `local_drift`
    (`y_{o-1} + h (y_{o-1} - y_{o-9}) / 8`; when n_ctx < 9 fall back to persistence and
    add the warning "local_drift undefined below 9 context rows: persistence used"), each
    `(context: list[float], horizon: int) -> list[float]`; `residual_quantiles(context,
    fn)`: the empirical quantiles (0.1..0.9) of fn's own one-step residuals inside the
    context, None with a warning when fewer than 8 residuals exist; a wrapper that turns a
    baseline into a Forecaster producing ForecastResult with point and quantiles = point
    plus residual quantiles (quantiles None when not assessable).
(3) backtest.py: `run(store, series, target, horizon, forecasters, variant=None) -> dict`
    per design 6.12 steps 1 to 6 with ordering 'true': origins `range(c_min, N - H + 1,
    s)`, one Window per origin, every forecaster on the identical window, per-window
    record {origin, ctx_start, n_ctx, actual[H], per-forecaster point[H], quantiles[H][9]
    or None, wall_ms, flags}, dropped windows with reasons; `metrics(windows, tau)`: per
    forecaster MAE per window then mean and median, skill = 1 - E_model / E_best_baseline
    (baselines = the four of (2)), calibration coverage_q for the nine q with `max_q
    |coverage_q - q|`, central 80 percent coverage with n, the flag when any
    `|coverage_q - q| > 2 sqrt(q (1 - q) / n)`, WQS = `2 * sum_q sum_{w,h} rho_q(y,
    yhat_q) / (9 * sum_{w,h} |y|)` with `rho_q(y, yhat) = max(q (y - yhat), (q - 1)(y -
    yhat))` (unnormalized mean pinball loss when sum |y| = 0, flagged), lead time (tau =
    q80 of the first c_min target rows; windows with `y_{o-1} < tau` only; a* and f* as
    defined in 6.12; hit rate, false-alarm rate, median lead, timing error); all
    calibration and WQS fields None with a reason for a forecaster without quantiles;
    `persist(store, ...)` builds the FORECAST_RUN_COLUMNS mapping (forecasters JSON
    carries name, checkpoint, license, device and padding_mode for timesfm) and calls
    put_forecast_run; `report(run) -> str` prints the constants, the command line that
    produced it, one table (forecaster, n_windows, MAE mean, MAE median, skill,
    calibration max deviation, coverage80, WQS, lead hit rate, false alarms, median lead,
    claim_class), the dropped-window reasons with counts, the warnings, and the license
    line whenever timesfm ran.
(4) timesfm.py: `TimesFM(device="cpu", cache_dir=None)` loading the model once per
    process, `forecast(window, horizon)` building the contexts (target row plus past-only
    covariates as a (V, n_ctx) array), asserting finiteness and the variate cap, calling
    predict_batch with `make_positive` from the registry's nonnegative flag, consuming the
    generator, returning ForecastResult with checkpoint, quantiles (H, 9) for the target
    variate, point = quantiles[:, 4], warnings including "horizon covariates beyond step H
    are edge-replicated by the model" when past-future covariates are passed (none are in
    this task: past-only only; keep the sentence in the code for W6-T1); wall_ms measured
    per call with time.perf_counter and returned in warnings-free form through a
    `last_wall_ms` attribute the backtester reads; `LICENSE` and `CHECKPOINT` constants.
    A `batch(windows, horizon)` method that sends all windows of a run in one
    predict_batch call, sorted by length, is optional: implement it only if the per-window
    path takes longer than 60 s on the VERIFY series (measure and say which you did).
(5) cli.py `forecast backtest --series <id> --target <t> [--horizon 1|4] [--forecasters
    a,b,c] [--device cpu|mps]` printing report(run) and the stored forecast_run_id (the
    store is $TELLTALE_HOME/telltale.db like every other command; there is no --db flag); `--horizon` default 1; forecasters default: the four baselines plus
    echo (timesfm only when named).
(6) tests/integration/synthetic_series.py (if W1-T5 did not create it): `write(store,
    rows=200, seed=1) -> Series` writing a seeded random-walk request-clock Series with
    the registry's columns (target coverage observed, one env changepoint at row 120,
    missingness_policy exclude) through put_series; runnable as `uv run python
    tests/integration/synthetic_series.py --db PATH --rows 200 --seed 1` printing the
    series_id.
(7) test_forecast_contracts.py, tests 1 and 2 of design 6.12: (1) no look-ahead: run the
    backtester with EchoStub on the synthetic series, then set every row at index >= each
    origin to 1e9 in a copy of the series and run again: the recorded windows, baseline
    outputs and per-window contexts are identical, and every provenance timestamp in
    row_meta is <= its row_end_ts; (2) metrics: on a fixed 12-row hand-written fixture
    with H=1 and a two-window run, MAE, coverage_q, WQS and lead time equal values you
    compute by hand in the test (show the arithmetic in comments), and persistence on a
    seeded random walk has MAE > 0. Both tests run without torch.
VERIFY (paste outputs in the report; `export TELLTALE_HOME=/tmp/t6home; mkdir -p
$TELLTALE_HOME` first): `uv run python tests/integration/synthetic_series.py --db
$TELLTALE_HOME/telltale.db --rows 200 --seed 1`; `uv run telltale forecast backtest
--series <id> --target fresh_input_tokens` (table with n_windows, MAE per forecaster,
calibration, WQS, lead time; constants printed; a stored forecast_run_id); the same with
`--horizon 4`; `uv run --extra forecast telltale forecast backtest ... --forecasters
persistence,timesfm` (runs; wall_ms per window recorded in the windows JSON, quote the
min, median and max; the license line printed; numbers not interpreted); the same with
`--device mps` (runs or fails with the error verbatim); `uv run pytest -m integration`
in the default environment (contract tests pass, no torch installed: prove it with `uv
run python -c "import torch"` failing); break-and-restore: make the backtester build the
window from rows[ctx_start:o + H] and show test 1 failing, then restore; all gates;
`uv run python -c "import telltale, telltale.forecast"` in the default environment
succeeds (lazy import holds).
STOP if series.py or the store readers do not give you a Series with row_meta and
changepoints (report what is missing; do not read series_snapshots with your own SQL),
or if the TimesFM checkpoint cannot be loaded from E03's cache or downloaded within 10
minutes (build everything else; the adapter raises a named error; say so).
REPORT: docs/log/W1-T6.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE /
NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): what a rolling origin is, one real table from the synthetic series with each
column explained, why a baseline can beat a 330M-parameter model on a random walk (and
why that is the point of the test), what the license line means for later waves. Commits:
"W1-T6 forecaster interface and baselines", "W1-T6 backtester, TimesFM adapter, contract
tests". Your final message (it is captured): OUTCOME, the backtest table, the public
signatures, the measured per-window wall_ms for timesfm, and anything UNSURE OR NOT DONE.
