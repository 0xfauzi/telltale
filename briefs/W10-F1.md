Task W10-F1: an OTLP batch whose service.name is present but unrecognized is refused, not
silently parsed as Claude Code.

OWNS: src/telltale/receiver.py (`_otlp_provider` only), tests/integration/test_privacy.py or
a new tests/integration/test_receiver_routing.py (implementer's choice; state which and
why), docs/log/W10-F1.md.
READS: providers/__init__.py (SERVICE_NAMES, get), store.py (diagnose, DIAGNOSTIC_KINDS).

READ: src/telltale/receiver.py lines 271-307 (`_route` and `_otlp_provider`): the daemon
serves `/v1/logs` and `/v1/metrics` for every provider on one endpoint, so the provider
comes out of the OTLP body's `resource.attributes["service.name"]`, looked up in
`providers.SERVICE_NAMES = {"claude-code": "claude", "codex": "codex", "codex_exec":
"codex"}`; today, ANY value not in that map (or no value at all) falls through to
`self.default_provider`, which the daemon sets to `"claude"`. Lines 330-343
(`_attribute`'s fallback): a record nothing else could attribute is stored under the
pseudo-capture `UNATTRIBUTED` with a `"launcher"` diagnostic naming the surface and
session, which is the existing, correct pattern for "this record parsed but nobody claimed
it": your fix produces the analogous outcome for provider routing, not a new mechanism.
`handle()` at line ~254 calls `_route`, which is inside the same method whose caller (the
HTTP handler's `_diagnose`, line ~575) already catches any exception from `handle()` and
writes a `"parse_failure"` diagnostic, then returns 200: raising from `_otlp_provider` for
this case costs no new code path.

Measured today, 2026-09-14, on the owner's real daemon (`~/.telltale/telltale.db`, which you
DO NOT open; work from a copy or a fresh temp store, per AGENTS.md invariant 7 and the
standing rule that experiments never touch `~/.telltale`): a capture_id literally named
`unattributed` holds 18,897 observations since 2026-09-07, of types `claude.otel.metric`
(14,256), `claude.otel.codex.api_request` (1,717), `claude.otel.codex.browser_use.security_check`
(834), `claude.otel.codex.tool_result` (752), and nine more `claude.otel.codex.*` types.
None of these are in `claude`'s allowlist (they are not Claude Code's vocabulary at all),
so `sanitize` drops nearly every field: the `claude.otel.codex.api_request` payload is `{}`.
Sniffing a live OTLP body confirmed the source: a resource attribute `service.name` =
some value that is not `"claude-code"`, `"codex"`, or `"codex_exec"`, sent by a process
distinct from Claude Code and from the `codex` CLI, pointed at the daemon's port. What
sent it, and whether Telltale should ever capture it, is the owner's decision and is
explicitly OUT of this task: this task only stops the daemon from guessing.

Trap: a block with NO `service.name` attribute at all (no resource, or a resource with no
such key) is a different case from one whose `service.name` IS present but unmapped; only
the second is a refusal. The first keeps returning `self.default_provider`, because there
are real records with no resource attributes at all (a bare test POST, a future surface
that has not set one yet) and treating "absent" as "refuse" would break existing fixture
replays: grep for `_otlp_provider` fixture coverage in test_privacy.py and test_doctor
before assuming which fixtures carry no service.name. Trap: NEVER_PERSIST and the allowlist
already keep this safe from a content leak (the empty `{}` payload proves it); this task is
about correctness and diagnostics, not privacy. Trap: no emoji, no em dashes.

DO:
(1) receiver.py: `_otlp_provider` returns the mapped provider on a match, as today. When a
    block carries a `service.name` attribute whose value is NOT in `SERVICE_NAMES`, raise
    `ValueError(f"unrecognized service.name {value!r}")` rather than falling through.
    `self.default_provider` is returned only when no block carried a `service.name`
    attribute at all (every block scanned, none had the key). Do not change `_route`,
    `handle`, or the existing parse_failure catch: the raise alone is the fix.
(2) A test that replays a synthetic OTLP `/v1/logs` body with `resource.attributes =
    [{"key": "service.name", "value": {"stringValue": "some-other-app"}}]` through the real
    receiver over HTTP: the response is 200, zero observations are stored for that POST, and
    one `parse_failure` diagnostic exists naming `some-other-app`. A second case with NO
    `service.name` key at all still routes to the daemon's default provider (unchanged
    behaviour, proven by a passing assertion, not by absence of a new test).
(3) Re-run the E01 and E02 fixture replays (`python -m telltale.receiver --replay
    fixtures/claude/<v>/S1 --db <tmp>` and the Codex equivalent) and confirm the observation
    counts per type are BYTE IDENTICAL to before this change: those fixtures carry real
    `service.name` values that must still match.
VERIFY: uv run pytest -m integration -q (all pass, the new test named in the report); the
E01/E02 replay counts pasted before and after, identical; uv run mypy . ; uv run ruff check .
&& uv run ruff format --check . ; uv run deptry . ; pre-commit run --all-files.
STOP if any existing fixture's `service.name` is not in `SERVICE_NAMES` and would now be
refused where it previously worked: name the fixture and the value, and do not widen
`SERVICE_NAMES` to admit it without saying so in the report; the owner decides what is a
real provider.
REPORT: docs/log/W10-F1.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR
NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register: the 18,897-row example
above as measured, before and after (a live replay of the synthetic case is enough; you do
not need real production data), and the rejected answer (silently keep defaulting to
claude) and why (it fabricates provenance a reader would trust). Commit message: "W10-F1
an unrecognized OTLP service.name is refused, not defaulted to claude".
