Task W11-T1: `telltale setup claude --print --import-schedule` prints a launchd plist that
reruns the transcript backfill importer on a fixed interval, so a session written by the
Claude desktop app's local-agent-mode feature (or any other root the owner names) keeps
landing in the store without a live OTel/hook path.

OWNS: src/telltale/setup_import.py (new), a thin wire-up in src/telltale/cli.py (as few
lines as the ratchet allows: see the trap below), docs/design/03-capture-howto.md (one
paragraph), tests/integration/test_doctor_setup.py (extend), docs/log/W11-T1.md.
READS: src/telltale/setup_daemon.py (the pattern to follow exactly), src/telltale/
cli_import.py (the importer's own CLI surface, `add_commands` and `command`),
src/telltale/importer.py (`import_files`, the idempotent "already imported" counter).

READ: src/telltale/setup_daemon.py in full (105 lines): `plist()` builds a launchd agent
dict with plistlib (never a formatted string, because a path containing a space or an `&`
is exactly what plistlib exists to escape correctly, and the home directory here has one:
`~/Library/Application Support/Claude/...`), `binary()` resolves an absolute path to the
running `telltale` because launchd's environment has no PATH, `INSTRUCTIONS` is an f-string
telling the owner the three commands (save the plist, `launchctl load`, `launchctl unload`)
and stating the AGENTS.md invariant 7 refusal in words: Telltale prints, the owner's shell
runs `launchctl`, `--apply` prints a refusal. Your new module is this same shape for a
different job. src/telltale/cli.py lines 241-260 (`setup`): where `--daemon` triggers
`setup_daemon.plist` and appends `INSTRUCTIONS`; line 630 (`"--daemon"` argparse wiring).
src/telltale/cli_import.py `add_commands` (line 350) and `command` (line 404): the exact
argv shape `telltale import claude-transcripts --root DIR [--since DATE] [--level N]`
already takes, and `importer.import_files`'s own idempotence (`already_stored` counter in
its return dict): a rerun of the identical command is always safe, which is the property
this whole task rests on and which you verify rather than assume.

Measured today, 2026-09-14, by the orchestrator, on the real store (`~/.telltale`, which
this task's own tests never touch: use a temp `TELLTALE_HOME` throughout, exactly as
test_doctor_setup.py already does for the daemon case): `telltale import claude-transcripts
--root ~/Library/"Application Support"/Claude/local-agent-mode-sessions` found 131 files
under a root the CLI's existing recursive `os.walk` + `*.jsonl` match (importer_scan.py
`_files`) already discovers with zero code changes; a real (non-dry-run) import wrote
15,248 observations across 61 sessions, second run added 0 (idempotent, confirmed). This
task adds NO new parsing or discovery code: the importer already works against this root.
It adds only the print-only scheduling wrapper.

Trap: `src/telltale/cli.py` is 810 lines, already over the 800-line ratchet (a frozen
legacy file: `scripts/precommit/file_length_ratchet.py` blocks it from growing past its
CURRENT length, not from 800 specifically, since it is already over). Any net line growth
in cli.py fails pre-commit. Put every real line in `setup_import.py`; if cli.py's own
wiring cannot be made net-zero by trimming, say so in the report and show the exact diff
stat, do not fight the hook by deleting unrelated lines. Trap: the plist's `ProgramArguments`
for a periodic job needs `StartInterval` (an integer, seconds), not `RunAtLoad`+`KeepAlive`
alone (those are for a long-running daemon, not a job that starts, runs to completion, and
exits repeatedly): read `man launchd.plist` conventions the daemon plist already models
(`KeepAlive` restarts a job that dies; a one-shot import is different, and RunAtLoad should
still be true so the owner gets one immediate catch-up run). Trap: the import root and
`TELLTALE_HOME` both go in `ProgramArguments`/`EnvironmentVariables`, not assumed from the
owner's shell, for exactly the reason setup_daemon.py's docstring gives for the daemon's own
`TELLTALE_HOME`. Trap: no emoji, no em dashes.

DO:
(1) setup_import.py: `plist(root: Path, interval_s: int, home: Path, binary: str, kind:
    str) -> str` (plistlib, `Label` distinct from the daemon's, e.g.
    "com.telltale.import.<slugified root or a caller-given name>"; `ProgramArguments =
    [binary, "import", kind, "--root", str(root)]`; `StartInterval`; `RunAtLoad = True`;
    `EnvironmentVariables = {"TELLTALE_HOME": str(home)}`; `StandardOutPath`/
    `StandardErrorPath` under `home`, named after the label so two scheduled imports do not
    collide). `INSTRUCTIONS` in the same register as the daemon's: PRINTED only, the three
    `launchctl` lines, the AGENTS.md invariant 7 sentence. `binary()` may be imported from
    `setup_daemon` rather than duplicated (it has no daemon-specific logic in it: check
    before duplicating).
(2) cli.py: a `--import-schedule PATH` flag on `setup` (or the smallest wiring that reaches
    `setup_import.plist` with a root and an interval; your judgement on the flag name and
    whether interval is a second flag with a sensible default, e.g. 3600) alongside the
    existing `--daemon` handling, printed the same way (plist first, then INSTRUCTIONS).
    `--apply` continues to refuse, unchanged.
(3) docs/design/03-capture-howto.md: one paragraph, Teacher register: what a desktop app
    that writes session transcripts to disk (rather than exporting live telemetry) needs
    instead of the daemon: a scheduled rerun of the same idempotent import command, and the
    real numbers from this task's own VERIFY as the example.
VERIFY: `telltale setup claude --print --import-schedule ~/Library/"Application
Support"/Claude/local-agent-mode-sessions` on a temp TELLTALE_HOME prints valid plist XML
(`plutil -lint` on the extracted `<?xml ... </plist>` block passes) with the given root and
a `StartInterval`, writes nothing under the temp home or a fake HOME (grep the temp dirs
for the plist's Label after the command exits: absent); `telltale setup claude --apply
--import-schedule ...` exits non-zero with the existing refusal text, unchanged; a new
test_doctor_setup.py case asserts the printed plist parses and contains a `StartInterval`
key and the given root path. uv run pytest -m integration -q ; uv run mypy . ; uv run ruff
check . && uv run ruff format --check . ; uv run deptry . ; pre-commit run --all-files
(paste the file-length ratchet's line for cli.py, before and after, in the report).
STOP if cli.py cannot take the wiring without net growth and no offsetting trim exists
in it: report the exact lines needed and ask rather than touch a file outside OWNS to make
room.
REPORT: docs/log/W11-T1.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR
NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register: the local-agent-mode
backfill (61 sessions, 15,248 observations, idempotent rerun measured at 0 added) as the
real example this mechanism exists for. Commit message: "W11-T1 print-only launchd
scheduling for periodic transcript backfill".
