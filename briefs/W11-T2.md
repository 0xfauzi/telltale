Task W11-T2: a new provider for the OpenAI Codex desktop app (`service.name =
"codex-app-server"`), so its OTLP traffic is captured instead of refused, with the same
privacy discipline E01/E02 used for the first two providers.

OWNS: src/telltale/providers/codex_app.py (new), the codex_app entries of
sanitize.ALLOWLIST (or a new allowlist module the existing ones already split into, follow
whichever pattern src/telltale/allowlist.py and allowlist_codex.py use), one line each in
providers/__init__.py (`KNOWN`) and providers/__init__.py or wherever `SERVICE_NAMES` lives
(add `"codex-app-server": "codex_app"`), tests/integration/test_privacy.py (extend),
docs/log/W11-T2.md.
READS: providers/claude.py and providers/codex.py (the shape to follow: `CAPABILITIES`,
`parse(surface, raw, ctx)`, no `launch()` needed here, see below), sanitize.py (NEVER_PERSIST,
REFUSED, Kind), receiver.py (`_otlp_provider`, the routing this task makes succeed instead
of refuse).

READ: src/telltale/sanitize.py lines 67-91 (NEVER_PERSIST and REFUSED, verbatim, injected
below) and the Kind enum (SCALAR, PATH, COMMAND, ENUM, ID, SIZE) with MAX_ENUM=64,
MAX_ID=128. src/telltale/receiver.py `_otlp_provider` (design 01, 6.6): the provider comes
out of `resource.attributes["service.name"]` in every `/v1/logs` and `/v1/metrics` POST;
today `"codex-app-server"` is refused with a `parse_failure` diagnostic (W10-F1), which is
the CORRECT and SAFE state this task is improving on, not fixing: nothing is broken, and
if this task cannot finish safely it should stop rather than ship a partial allowlist.
providers/codex.py in full: the closest template, since both providers speak the same OTLP
log-record shape (`event.name`, `resource.attributes`, `logRecords[].attributes`), and
`providers.otlp_attrs` / the dot-to-underscore attribute conversion it already uses is
reusable, not reinvented.

Measured today, 2026-09-14, by the orchestrator, from a brief (about 25 second) swap of a
schema-only sniffer onto the daemon's own port while the app was actively running, port
restored immediately after: `service.name` on every record is the literal string
`codex-app-server`. NO field VALUES were read or recorded by that sniff, only attribute
KEY NAMES, on purpose: this brief hands you the shape, not a shortcut past reading real
values carefully yourself. Six distinct OTel log-record attribute-key sets were seen (the
`/v1/metrics` batch carried none, an empty attribute set on every point, and needs its own
check in your own pass, not an assumption from this one):

1. `agent_name, app.version, arguments, auth_mode, call_id, conversation.id, duration_ms,
   event.name, event.timestamp, mcp_server, mcp_server_origin, model, originator, output,
   output_truncated, slug, success, terminal.type, tool_name, tool_namespace,
   tool_result_seq, user.account_id, user.email` -- an MCP tool-call-shaped event.
   `arguments` and `output` are EXACT matches for the existing NEVER_PERSIST set (sanitize.py
   line 67-74): they are already refused regardless of any allowlist entry. Do not add
   either name to any allowlist; do not invent a differently-spelled key for the same data.
2. `app.version, auth_mode, cache_write_token_count, cached_token_count, conversation.id,
   event.kind, event.name, event.timestamp, input_token_count, model,
   model_reasoning_effort, originator, output_token_count, reasoning_token_count, slug,
   terminal.type, tool_token_count, ttft_ms, user.account_id, user.email` -- a turn or
   request-completion-shaped event: token counts and timing, no content-shaped field name.
3. `app.version, auth_mode, call_id, conversation.id, decision, event.name, event.timestamp,
   model, originator, slug, source, terminal.type, tool_name, tool_namespace,
   user.account_id, user.email` -- a tool-decision-shaped event, no content-shaped field.
4. `app.version, auth.connection_reused, auth.env_codex_api_key_enabled,
   auth.env_codex_api_key_present, auth.env_openai_api_key_present,
   auth.env_refresh_token_url_override_present, auth_mode, conversation.id, duration_ms,
   event.name, event.timestamp, model, originator, slug, success, terminal.type,
   user.account_id, user.email` -- a session/auth-start-shaped event; the four `auth.env_*`
   fields are booleans about whether an environment variable is SET, never the variable's
   value (confirm this with your own pass; do not assume).
5, 6. Two `claude_code.*`-named records also appeared in the same brief sniff: this
   machine's own running Claude Code sessions, caught incidentally because both point at
   the same port. They are not this app's traffic and are not this task's concern; they
   confirm nothing more than that the sniff window briefly saw the whole port's traffic.

`user.account_id` and `user.email` are on every one of this app's events, exactly as
Claude's `user.account_id`/`user.email`/`organization.id`/`user.account_uuid`/`user.id` are
on every Claude OTel event (E01; sanitize.py's REFUSED comment). `providers.otlp_attrs`
turns OTLP dots into underscores before the sanitizer sees a key, so `user.account_id`
becomes `user_account_id`, already a member of `REFUSED`: reusing that conversion utility
is what makes this refusal automatic rather than something you write again.

Trap: `event.name`'s VALUES (not just the key) are unmeasured. Before writing a single
allowlist entry or choosing observation_type names, do your OWN brief sniff pass (the exact
method: `launchctl unload ~/Library/LaunchAgents/com.telltale.daemon.plist`, start a
listener on 127.0.0.1:47311 that prints attribute KEYS and, for `event.name` and
`event.kind` ONLY, the VALUE too (a symbolic event identifier is not content, the same way
Claude's `event.name` already is not), wait under 30 seconds while the app is active,
`launchctl load` immediately after; report the exact wall time the daemon was down). If the
app is not active when you run, STOP and report rather than wait indefinitely or guess the
values from the shapes above. Trap: `output_truncated`'s type is unconfirmed here (a bool
flag, or a truncated PREVIEW STRING of the output itself, which the name alone does not
settle) -- read its actual value type in your own pass before deciding its Kind; if it is
ever a string of any length, it is content and goes in NEVER_PERSIST, not the allowlist.
Trap: `mcp_server`, `mcp_server_origin`, `tool_namespace`, `slug`, `originator`, `agent_name`
are symbolic names (ENUM-shaped, bounded at 64 chars), not free text: verify each one's
observed length and character shape before allowlisting as ENUM rather than assuming.
Trap: `conversation.id` and `call_id` are ID-shaped (bounded at 128, like Claude's
`session.id`): never PATH, never free text. Trap: no emoji, no em dashes.

DO:
(1) Your own brief sniff pass (STOP condition above governs it) to get real `event.name`
    and `event.kind` values and confirm `output_truncated`'s type. Record the exact values
    seen and the daemon-down wall time in the report.
(2) providers/codex_app.py: `CAPABILITIES` (per-attribute observed|unavailable, following
    codex.py's shape), `parse(surface, raw, ctx) -> list[Observation]` for otel_logs and
    otel_metrics, observation_type `"codex_app.otel." + event.name` (or `.metric` for a
    metric point, matching claude.py's own convention), reusing `providers.otlp_attrs` and
    the existing OTLP-to-Observation plumbing rather than duplicating it. No `launch()`:
    this provider is never launched by Telltale, only received; give it whatever the
    `Provider` protocol requires as a minimum stub, and say in the report what that was.
(3) sanitize.py or a new allowlist_codex_app.py (follow whichever split the codebase
    already uses for codex's own entries): one allowlist entry per observation_type you
    named in step 1/2, keeping ONLY fields you have positively classified as safe by the
    traps above; every field you are not confident about is LEFT OUT (dropped, diagnosed
    as unknown_field, safe by the sanitizer's own fail-closed default) rather than guessed
    into the allowlist. `arguments` and `output` are never added anywhere.
(4) providers/__init__.py: `KNOWN = ("claude", "codex", "codex_app")`; the SERVICE_NAMES
    (or wherever it lives) entry `"codex-app-server": "codex_app"`.
(5) test_privacy.py: a new case building a synthetic OTLP body shaped exactly like shape 1
    above (the MCP tool-call event), with `arguments` and `output` holding fake probe
    strings (reuse the existing TELLTALEFAKE convention) and `user.account_id`/`user.email`
    holding fake values, replayed through the real receiver: the probes are absent from the
    stored payload and from the db/wal bytes, `user_account_id`/`user_email` are absent and
    counted under REFUSED not unknown_field, and at least one field you did allowlist (e.g.
    `duration_ms` or `success`) IS present, proving the case is not accidentally refusing
    everything. Do the same for one more of the four shapes.
VERIFY: uv run pytest -m integration -q (all pass, new tests named); a live replay: with
the daemon running normally (not swapped) after your merge would take effect, `sessions`
would show a `codex_app` capture the next time the app is active -- you cannot verify this
live from your worktree (the daemon on the owner's machine runs merged `main`, not your
branch) and should not attempt to swap the port again: say so in the report instead, and
give the exact command the orchestrator should run after merge to confirm it. uv run mypy .
; uv run ruff check . && uv run ruff format --check . ; uv run deptry . ; pre-commit run
--all-files.
STOP if the app is not sending anything when you attempt your own sniff pass: report what
you have (the six shapes above are already enough to write CAPABILITIES and the provider
skeleton) and either proceed with observation_type names inferred conservatively as
`codex_app.otel.unknown_<n>` pending real names, or leave the parser refusing cleanly and
say exactly what is missing. STOP and do not allowlist any field whose safety you are not
certain of; leaving it unlisted is always the safe default and is not a failure to report.
REPORT: docs/log/W11-T2.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR
NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register: the MCP tool-call shape
above as the real example, the exact reasoning for every field you allowlisted and every
field you deliberately left out, and the rejected answer (map `codex-app-server` onto the
existing `codex` provider and reuse its allowlist) and why (different vocabulary, different
risk profile: reusing it would either drop everything as unknown or, worse, allowlist a
field the CLI's schema never had a reason to guard). Commit message: "W11-T2 codex_app
provider: the OpenAI Codex desktop app, privacy-reviewed field by field".
