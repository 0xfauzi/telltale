Task W11-T2: a new provider for the OpenAI Codex desktop app (`service.name =
"codex-app-server"`), so its OTLP traffic is captured instead of refused, covering ONLY the
event shapes measured below, with everything else left unallowlisted on purpose.

OWNS: src/telltale/providers/codex_app.py (new), the codex_app entries of
sanitize.ALLOWLIST (or a new allowlist module, following whichever split allowlist.py and
allowlist_codex.py already use), one line each in providers/__init__.py (`KNOWN`) and
wherever `SERVICE_NAMES` lives (add `"codex-app-server": "codex_app"`), tests/integration/
test_privacy.py (extend), docs/log/W11-T2.md.
READS: providers/claude.py and providers/codex.py (the shape to follow: `CAPABILITIES`,
`parse(surface, raw, ctx)`, no real `launch()` needed, see below), sanitize.py
(NEVER_PERSIST, REFUSED, Kind), receiver.py (`_otlp_provider`, the routing this task makes
succeed for four shapes instead of refusing everything).

READ: src/telltale/sanitize.py lines 67-91 (NEVER_PERSIST and REFUSED, verbatim, injected
below) and the Kind enum (SCALAR, PATH, COMMAND, ENUM, ID, SIZE; MAX_ENUM=64, MAX_ID=128).
src/telltale/receiver.py `_otlp_provider`: the provider comes out of
`resource.attributes["service.name"]` on every `/v1/logs` and `/v1/metrics` POST; today
`"codex-app-server"` is refused (`parse_failure`, W10-F1), which is the CORRECT and SAFE
state this task narrows, not fixes: nothing is broken today, and any event shape this task
cannot classify with confidence should STAY refused rather than ship a guess.
providers/codex.py in full: the closest template (same OTLP log-record wire shape,
`providers.otlp_attrs` reusable for the dot-to-underscore conversion).

Measured today, 2026-09-14, by the orchestrator, in two brief (about 25 seconds each) swaps
of a schema-only sniffer onto the daemon's own port while the app was actively running,
port restored immediately after both. NO field VALUES were read in either pass except
`event.name` and `event.kind`, which are symbolic event identifiers, the same way Claude's
own `event.name` already is not content. This brief hands you what was measured; it does
NOT ask you to repeat either swap. Doing so yourself is explicitly OUT of scope for this
task: your session runs with bypassPermissions and no fresh owner go-ahead for that specific
action, and the orchestrator already spent the owner's two approved windows measuring what
follows.

FOUR real attribute-key shapes (pass 1), names not yet certainly matched to any of them:

1. `agent_name, app.version, arguments, auth_mode, call_id, conversation.id, duration_ms,
   event.name, event.timestamp, mcp_server, mcp_server_origin, model, originator, output,
   output_truncated, slug, success, terminal.type, tool_name, tool_namespace,
   tool_result_seq, user.account_id, user.email` -- an MCP tool-call-shaped event.
   `arguments` and `output` are EXACT matches for NEVER_PERSIST (sanitize.py line 67-74):
   already refused regardless of any allowlist entry. Never add either name to any
   allowlist; never invent a differently-spelled key for the same data.
2. `app.version, auth_mode, cache_write_token_count, cached_token_count, conversation.id,
   event.kind, event.name, event.timestamp, input_token_count, model,
   model_reasoning_effort, originator, output_token_count, reasoning_token_count, slug,
   terminal.type, tool_token_count, ttft_ms, user.account_id, user.email` -- token counts
   and timing, no content-shaped field name.
3. `app.version, auth_mode, call_id, conversation.id, decision, event.name, event.timestamp,
   model, originator, slug, source, terminal.type, tool_name, tool_namespace,
   user.account_id, user.email` -- a tool-decision-shaped event, no content-shaped field.
4. `app.version, auth.connection_reused, auth.env_codex_api_key_enabled,
   auth.env_codex_api_key_present, auth.env_openai_api_key_present,
   auth.env_refresh_token_url_override_present, auth_mode, conversation.id, duration_ms,
   event.name, event.timestamp, model, originator, slug, success, terminal.type,
   user.account_id, user.email` -- a session/auth-start-shaped event; the four `auth.env_*`
   fields are booleans about whether an environment variable is SET, never its value (this
   was confirmed on the wire: pass 2 read `output_truncated`'s runtime type across the live
   records it saw and it was never a string; the `auth.env_*` fields were not re-checked by
   type and you should treat that specific claim as inferred from the name, not measured,
   and allowlist them only if you independently agree a `*_present`/`*_enabled` name cannot
   reasonably hold anything but a boolean).

TWELVE real `event.name` VALUES (pass 1's incidental find, cross-referenced against a
now-deleted 2026-09-14 capture of this same app's traffic that arrived before W10-F1's
fix, back when the vocabulary was visible under the wrong provider's observation types):
`codex.api_request`, `codex.tool_result`, `codex.tool_decision`,
`codex.browser_use.security_check`, `codex.websocket_connect`, `codex.websocket_request`,
`codex.sse_event`, `codex.conversation_starts`, `codex.agent_communication`,
`codex.startup_phase`, `codex.turn_ttft`, `codex.user_prompt`. Pass 2 confirmed
`codex.api_request` fires on the wire today (one live record, no attributes captured in
that pass beyond the four filtered above). NONE of these twelve names has a confirmed
attribute-key list attached to it: you do not know which of the four shapes above (if any)
`codex.api_request` actually is, and you do not know the shape of any of the other eleven
at all, `codex.tool_result` included.

Trap, the one that matters most: `codex.tool_result` is a name, not a measured shape. By
analogy with Claude Code's own `tool_result` (which carries `tool_input`/`tool_response`,
both NEVER_PERSIST), an event named `tool_result` on ANY provider is exactly the shape most
likely to carry raw content. You have no field list for it. Do not allowlist it, do not
guess its fields from shape 1's `output`/`arguments` names, and do not assume it is safe
because NEVER_PERSIST would catch a field literally named `output` -- a differently-spelled
content field (`result`, `response_text`, `payload`, anything) would not be. Leave every
observation this event produces unallowlisted: the sanitizer's fail-closed default already
drops it safely, and that is the correct outcome for this task, not a gap to close.
`codex.browser_use.security_check`, `.websocket_connect`, `.websocket_request`,
`.sse_event`, `.conversation_starts`, `.agent_communication`, `.startup_phase`,
`.user_prompt`, `.turn_ttft` are the same situation: named, not shaped. Leave them
unallowlisted too.

Trap: `user.account_id` and `user.email` are on every one of the four measured shapes,
exactly as Claude's five identity attributes are on every one of its events. `providers.
otlp_attrs` converts OTLP dots to underscores before the sanitizer sees a key, so
`user.account_id` becomes `user_account_id`, already in `REFUSED`: reusing that conversion
utility is what makes this refusal automatic. Trap: `mcp_server`, `mcp_server_origin`,
`tool_namespace`, `slug`, `originator`, `agent_name` are symbolic (ENUM-shaped, bounded at
64), not free text, but their actual observed lengths were not measured: allowlist them as
ENUM (which truncates and flags a paragraph-sized value the same way Claude's own ENUM
fields do, per Kind's own bound) rather than SCALAR, so an unexpectedly long value is
bounded and recorded, not silently kept whole. Trap: `conversation.id` and `call_id` are
ID-shaped (bounded at 128, like Claude's `session.id`): never PATH, never free text. Trap:
`output_truncated`'s runtime type was checked in pass 2 across the records seen and was
never a string; allowlist it as a bool-shaped SCALAR on that basis, and if your own reading
of the field name still worries you, leave it out instead: the default is always safe.
Trap: no emoji, no em dashes.

DO:
(1) providers/codex_app.py: `CAPABILITIES` naming ONLY the four measured shapes as
    observed/partial, everything else unavailable; `parse(surface, raw, ctx) ->
    list[Observation]` for otel_logs and otel_metrics, reusing `providers.otlp_attrs` and
    the existing OTLP-to-Observation plumbing (do not duplicate it). Choose
    observation_type names that describe the SHAPE, not a guessed event.name match, e.g.
    `codex_app.otel.mcp_tool_call`, `codex_app.otel.turn_usage`, `codex_app.otel.
    tool_decision`, `codex_app.otel.session_start` (your judgement on the exact words; say
    in the report why each name was chosen and that it is a shape-name, not a confirmed
    event.name). A log record whose `event.name` is none of the twelve you cannot classify,
    or is one of the eleven with no known shape, produces no Observation from this parser
    (falls through to the receiver's own unattributed/diagnostic handling, unchanged). No
    `launch()`: this provider is never launched by Telltale, only received; give it
    whatever minimum the `Provider` protocol needs and say in the report what that was.
(2) sanitize.py or a new allowlist_codex_app.py: one allowlist entry per shape in step 1,
    keeping ONLY the fields positively classified above; every field not covered by this
    brief is left OUT (dropped, diagnosed as unknown_field, safe by the sanitizer's own
    default), never guessed in.
(3) providers/__init__.py: `KNOWN = ("claude", "codex", "codex_app")`; the SERVICE_NAMES
    entry `"codex-app-server": "codex_app"`.
(4) test_privacy.py: a synthetic OTLP body shaped exactly like shape 1, with `arguments`
    and `output` holding fake probe strings (the existing TELLTALEFAKE convention) and
    `user.account_id`/`user.email` holding fake values, replayed through the real receiver:
    probes absent from the stored payload and the db/wal bytes, `user_account_id`/
    `user_email` absent and counted under REFUSED not unknown_field, and at least one field
    you did allowlist (`duration_ms` or `success`) present, proving the case is not
    accidentally refusing everything. One more case for shape 2, 3, or 4. A THIRD case
    replays a synthetic `codex.tool_result` record (any plausible fields, including one
    named something other than `output`/`arguments` that still sounds content-shaped, e.g.
    `result_text`) and asserts it produces ZERO stored observations and no leaked content:
    this is the test that proves the STOP-and-leave-unallowlisted decision actually holds.
VERIFY: uv run pytest -m integration -q (all pass, new tests named); uv run mypy . ; uv run
ruff check . && uv run ruff format --check . ; uv run deptry . ; pre-commit run --all-files.
The daemon is not touched by this task at any point; do not run `launchctl unload` or any
variant of it. After merge, the orchestrator will confirm live capture with `telltale
sessions` the next time the app is active; say so in the report rather than attempting it.
STOP and leave a field, a shape, or an event.name out of the allowlist whenever this brief
did not measure it with confidence. That is not a failure to report, it is the correct
outcome for anything this brief did not hand you measured.
REPORT: docs/log/W11-T2.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR
NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register: shape 1 (the MCP
tool-call event) as the real example, the reasoning for every field kept and every field
left out, and the rejected answer (allowlist all twelve known event names using the four
measured shapes as a best guess for the other eight) and why (a guess that happens to be
wrong about `codex.tool_result` is exactly the failure mode NEVER_PERSIST cannot always
catch, since it only guards field NAMES it already knows to watch for). Commit message:
"W11-T2 codex_app provider: four measured shapes, eight names left deliberately unbuilt".
