You are the implementer for task W2-E05 of the Telltale build: the H2 pilot, an
experiment that starts real agent sessions. You are running as a headless Claude Code
session inside a git worktree of github.com/0xfauzi/telltale on branch task/W2-E05
(created from main). This session is itself being recorded by `telltale run`. Read
AGENTS.md, docs/design/01-design.md section 6.12 "H2 and H3 protocol" (the statistics,
the MDD formula, the demotion rule, N_needed), docs/experiments/README.md (the write-up
template), docs/experiments/E01.md (scenario S1 and its fixture repository),
docs/experiments/E04.md (the model write-up of real sessions driven through the
launcher), docs/log/W1-T4.md (the repeat runner: spec keys, worktrees, the report
dict), docs/log/W2-T4.md (VECTOR and cohorts), docs/log/W2-T3.md (telltale.stats),
src/telltale/experiments.py, stats.py, cohorts.py, experiments/E01/run.py (FIX_PROMPT
and how the fixture repository is prepared), experiments/E01/fixture_repo/ and
experiments/E04/run.py before writing code.

House rules: as little and as simple code as possible; no emoji anywhere; no em dashes
anywhere (hyphen, colon or full stop); Python 3.12, uv only; never guess a number
(measure or write "needs measuring"); statistics are n, median, scaled MAD (1.4826
MAD), IQR, min, max and the full sorted list, never a mean alone; unknown stays None;
the words cause, impact and would are refused in anything written about these
sessions. Commit messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W2-E05; `uv sync`; gates green (`uv run mypy .`,
`uv run ruff check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W2-E05`;
`gh pr create --base main --title "W2-E05 repeated task variance (H2 pilot)"
--body-file docs/log/W2-E05.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULES. The owner approved exactly five real sessions for this task on 2026-09-02:
five repetitions of `claude -p --model sonnet` on the fix-the-test task, started by
`telltale experiment repeat`, and nothing else that spends tokens (no pilot session of
your own, no retry of a failed repetition: a failure is a result and is reported).
E01 measured one such session at 19.5 s of wall time, 7 turns and 246,950 tokens: if
the first repetition passes 3 minutes of wall time or 600,000 tokens, kill the runner
and report. These captures are the owner's data: TELLTALE_HOME stays unset so they
land in ~/.telltale, and nothing is purged. Never write under ~/.claude or ~/.codex.
The base commit, the prompt and the acceptance command are fixed below; changing any
of them after seeing a result is a new experiment id, not this one.

Injected facts.
- The task. experiments/E01/fixture_repo/ holds pkg/calc.py (divide with an unguarded
  zero), tests/test_calc.py (one failing test), pyproject.toml, .python-version and
  secrets_note.txt (the privacy probes; leave them in: the privacy claim is re-tested
  by their presence). Build experiments/E05/out/repo from a copy of it with `git init`,
  the E01 git identity, one commit, and record its sha as base_sha. The prompt is
  E01's FIX_PROMPT, verbatim: "Run the tests with `uv run pytest`, fix the bug in
  pkg/calc.py so the failing test passes, run the tests again, and stop."
- The command in the spec: ["claude", "-p", "--model", "sonnet", "--permission-mode",
  "acceptEdits", "--output-format", "stream-json", "--verbose", FIX_PROMPT]. The
  launcher adds `--settings` (the http hooks) and `--session-id` itself and configures
  OTel through the child's environment; E01 measured that `claude -p` starts from
  inside a Claude Code session with CLAUDECODE still set, and E04 ran ten such
  sessions through the launcher this way. The acceptance command, run by the HARNESS
  in the worktree after the agent exits: [<this worktree's .venv/bin/python>, "-m",
  "pytest", "-q"] (pytest is in the dev group; the fixture needs nothing installed).
- The runner. `uv run telltale experiment repeat spec.json --out experiments/E05/out`
  with spec keys task_id "E05", experiment "E05", repo, base_sha, command, acceptance,
  repetitions 5, provider "claude", level 1. Each repetition is a fresh detached
  worktree of the target repository at base_sha under $TELLTALE_HOME/worktrees,
  removed afterwards; attempts 1..5 are correlated to the task through
  /v1/correlations; the acceptance result is an external.outcome observation; the
  runner refuses statistics when two fingerprints differ and prints the fields. It
  writes out/E05/report.json.
- The vector is a placeholder. `experiments.vector()` still reads the stream result's
  usage and the tool_use counts (W1-T4 wrote it that way and said W2-T4 would replace
  it; W2-T4 did not touch experiments.py). Since W2-T6, every launcher capture reduces
  itself at capture end, so the evidence rows exist when the runner reads them. DO (1)
  replaces the placeholder with the real vector: the 22 metrics of cohorts.VECTOR read
  from the evidence table (value, or None when the row's value is None), keyed
  "family.metric", plus the three stream facts the placeholder already had that are
  not measures (duration_ms, num_turns, and the tool call counts by name). A capture
  with no evidence rows is refused with its id, not zero-filled and not None-filled.
  test_experiments.py (W1-T4 and W2-T3) asserts on the old keys; update only those
  assertions, and keep the fake-agent expectations exact (the fake agent's numbers are
  deterministic per seed).
- The decision rule, from design 6.12, written here before the run. Per measure over
  the five captures: n, median, s (scaled MAD), MDD = 2.8 s sqrt(2 / n), demoted from
  repository comparison while MDD > 0.25 median, N_needed = ceil(2 (2.8 s / (0.25
  median))^2). s = 0 means "no within-condition variation observed at n = 5", neither
  demoted nor endorsed. median = 0 with s > 0 is demoted with N_needed "not computable
  at median 0". A measure with fewer than 5 known values reports its n and is not
  judged. Scaling to N = min(20, N_needed) is NOT this task: the report lists N_needed
  per measure and the orchestrator takes the list to the owner.

BRIEF W2-E05: five independent captures of one task under one environment through the
repeat runner, the real evidence vector per capture, the within-condition statistics
and the demotion table, and the decision file.
OWNS: src/telltale/experiments.py (vector only), tests/integration/test_experiments.py
(the vector assertions only), experiments/E05/run.py, experiments/E05/out/ (spec.json,
report.json, decision.json re-included in .gitignore the way E04 re-includes its
results; the repository copy and the worktrees are not committed), docs/experiments/
E05.md, docs/log/W2-E05.md.
READS: stats.py, cohorts.py, experiments_env.py, store.py, cli.py.
Traps: the fingerprint must be identical across the five: the runner asserts it, and
if it refuses, the report shows the differing fields and the statistics are not
computed by hand. The acceptance result is what the harness measured, not what the
agent said; if a repetition fails acceptance, its numbers stay in the table with the
fail beside them. Wall time is the wrapper's (perf_counter around the subprocess) and
the child's own duration_ms both, as E04 did. The five sessions run serially; do not
parallelise them.
DO:
(1) experiments.py `vector()` as above, one commit: "W2-E05 real evidence vector in
    the repeat runner".
(2) experiments/E05/run.py: prepares out/repo, writes out/spec.json, runs the repeat
    command as a subprocess and records its wall time, then reads out/E05/report.json
    and computes the decision table with telltale.stats (median, scaled MAD, MDD,
    demoted, N_needed per measure) into out/decision.json.
(3) docs/experiments/E05.md per the 7.5 template: the decision rule paragraph above
    copied verbatim, the five sessions (capture id, attempt, wall, duration_ms,
    tokens, acceptance), the fingerprint id, the per-measure table with the sorted
    values, the demotion table, "what we rejected", "how sure" per claim, and "what
    downstream may now say" (comparative within this condition; cohort: five sonnet
    fix-the-test sessions on 2026-09-02; nothing about any other task).
VERIFY (paste outputs in the report): the runner's printed tables (five captures,
attempts 1..5, the fingerprint line, acceptance results); `uv run telltale sessions
--limit 8` (the five listed with MODEL sonnet); `uv run telltale show <attempt 1>`
(the measures present, coverage first); `uv run telltale vector <attempt 1>` (the
same 22 numbers the runner used); the decision table; pytest -m integration; all
gates; break-and-restore: make vector() return 0 for a missing evidence row and show
which fake-agent assertion fails, then restore.
STOP if `claude -p` refuses to start under the launcher (report the exact stderr; do
not edit any settings file), or if the first repetition passes the limits above.
REPORT: docs/log/W2-E05.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT
DONE / NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): what a within-condition floor is, one measure's five values and its MDD
computed by hand, which measures are demoted at n = 5 and what N_needed says about
them, and why "no variation at n = 5" is not "no variation". Commits: "W2-E05 real
evidence vector in the repeat runner", "W2-E05 repeated task variance". Your final
message (it is captured): OUTCOME, the demotion table, and anything UNSURE OR NOT DONE.
