You are the implementer for task W2-E06 of the Telltale build: the H3 pilot, an
experiment that starts real agent sessions. You are running as a headless Claude Code
session inside a git worktree of github.com/0xfauzi/telltale on branch task/W2-E06
(created from main after W2-E05 merged). This session is itself being recorded by
`telltale run`. Read AGENTS.md, docs/design/01-design.md section 6.12 "H2 and H3
protocol", docs/experiments/README.md, docs/experiments/E05.md (the same task, one
condition: its numbers are the floors this experiment compares against),
docs/log/W2-E05.md, docs/log/W2-T3.md (the environment runner and telltale.stats),
src/telltale/experiments_env.py, stats.py, experiments/E05/run.py (reuse its
repository preparation by import; do not copy it) before writing code.

House rules: as W2-E05 (as little code as possible; no emoji; no em dashes; uv only;
never guess a number; robust statistics with the full sorted lists; unknown stays
None; cause, impact and would are refused). Commit messages end with these two trailer
lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: branch task/W2-E06; `uv sync`; gates green (`uv run mypy .`, `uv run ruff
check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W2-E06`;
`gh pr create --base main --title "W2-E06 environment sensitivity (H3 pilot)"
--body-file docs/log/W2-E06.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULES. The owner approved exactly ten real sessions for this task on 2026-09-02:
two arms of five, `claude -p --model sonnet` on the E05 fix-the-test task, differing
in one launch flag, `--effort low` against `--effort high`, started by `telltale
experiment environment`, and nothing else that spends tokens. If the first session
passes 3 minutes of wall time or 600,000 tokens, kill the runner and report. These
captures are the owner's data: TELLTALE_HOME stays unset so they land in ~/.telltale,
and nothing is purged. Never write under ~/.claude or ~/.codex. Prompt, base commit,
acceptance command and the two effort values are fixed; changing one after a result
is a new experiment id.

Injected facts.
- The task, prompt, acceptance and repository preparation are E05's; the base_sha is
  a fresh commit of the same fixture files under experiments/E06/out/repo (a copy, so
  the two experiments' worktrees never share a repository).
- The runner: `uv run telltale experiment environment spec.json --out
  experiments/E06/out` with spec keys task_id "E06", experiment "E06", repo, base_sha,
  acceptance, repetitions_per_arm 5, provider "claude", level 1, factor "effort", arms
  [{name "low", command [... "--effort", "low" ...]}, {name "high", command [...
  "--effort", "high" ...]}] where the command is E05's with `--effort <value>` inserted
  after `--model sonnet`. Claude Code 2.1.258 accepts low, medium, high, xhigh and max
  for `--effort`. The runner asserts that every capture in an arm carries one
  fingerprint and that the two arms' fingerprints differ in exactly the field
  `effort`, prints the per-arm tables and the between-arm table (HL shift, Cliff's
  delta, exact Mann-Whitney p by enumeration, MDD, N_needed, demoted, label), and
  refuses otherwise. The vector it reads is the real one after W2-E05.
- The decision rule, from design 6.12, written here before the run. Per measure: the
  factor has a material environment effect iff |HL shift| > MDD, where MDD = 2.8 s
  sqrt(2 / n) with s the pooled scaled MAD the runner prints; otherwise "not resolved
  at n = 5", never "no effect". A measure whose s is 0 in both arms and whose values
  are identical across arms reads "no variation and no shift at n = 5". The per-arm
  demotion rule of E05 applies to each arm's own floors. A material effect makes the
  effort fingerprint a changepoint for that measure (design 6.12); the write-up says
  which measures, and says that a changepoint is a statement about this task on this
  day.

Injected on 2026-09-02 evening, from W2-E05's first attempt: (a) run the runner in the
FOREGROUND of your Bash tool with a long timeout (the ten sessions take about 3 to 6
minutes; the tool allows 10), never as a background task: a headless session that ends
its turn while a background task runs kills the task, and E05 lost its fifth outcome that
way. (b) Before the run, prove the acceptance command in a scratch worktree of the base
commit: it must exit non-zero on the unfixed code and 0 after the one-line fix to
pkg/calc.py, and both exit codes go in the report; E05's first attempt recorded fail on
every repetition and the write-up has to be able to say whether that was the agent or the
harness. (c) `telltale sessions` and the store are the record of what ran; if anything
stops early, recover from the store (the captures, correlations and outcomes are there)
and never start a replacement session.

BRIEF W2-E06: two arms of five real sessions differing only in `--effort`, the
between-arm statistics, and the decision file naming which measures show a material
environment effect at n = 5 and which are not resolved.
OWNS: experiments/E06/run.py, experiments/E06/out/ (spec.json, environment.json,
decision.json re-included as E04 does), docs/experiments/E06.md, docs/log/W2-E06.md.
READS: experiments_env.py, experiments.py, stats.py, experiments/E05/run.py.
Traps: the ten sessions run serially, low arm then high arm as the runner orders them;
a failed acceptance stays in its arm's table with the fail beside it; the high arm may
take longer and cost more, and that is a measurement this experiment reports, not a
reason to cap it. A spec that differs in anything but `--effort` is refused by the
runner: do not edit the refusal away.
DO:
(1) experiments/E06/run.py: prepare out/repo (import E05's helper), write out/spec.json,
    run the environment command as a subprocess with its wall time, read the runner's
    JSON, compute the decision per measure (the inequality with its values) into
    out/decision.json.
(2) docs/experiments/E06.md per the 7.5 template: the rule copied verbatim, the ten
    sessions with their arms, the two fingerprint ids and the differing field, the
    per-arm tables, the between-arm table, the labels with the inequalities beside
    them, "what we rejected", "how sure", and "what downstream may now say"
    (comparative between two stated conditions of one task; nothing about another
    task, model or day).
VERIFY (paste outputs): the runner's printed tables; `uv run telltale sessions --limit
12` (ten sessions, five per arm); the decision table; pytest -m integration; all
gates.
STOP if the runner refuses the spec (paste the refusal; do not change the arms), or if
the first session passes the limits.
REPORT: docs/log/W2-E06.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT
DONE / NEXT, then EXPLAIN FOR THE OWNER in the Teacher register: what a factor effect
is and what a changepoint is, one measure's two arms with its HL shift and MDD computed
by hand, and the difference between "not resolved at n = 5" and "no effect". Commit:
"W2-E06 environment sensitivity". Your final message (it is captured): OUTCOME, the
between-arm table, and anything UNSURE OR NOT DONE.

Amendment, 2026-09-02 (after the E05 pilot): E05's five sessions never did the task
because the spec launched `claude -p` with `--permission-mode acceptEdits`, under which
a Bash call needs an approval nobody can give in headless mode (the transcripts show
"This command requires approval" three times per session). Every command in this
experiment carries `--permission-mode bypassPermissions --max-turns 40`, the flags E01
used, and the runner refuses any arm whose claude argv has `-p` without
bypassPermissions (W2-E05's corrected runner does this; reuse it, do not re-implement).
Dispatch order: this brief is dispatched only after the owner approves the corrected
E05 re-run and its first session shows a file edit in `telltale timeline`; one good
session on the shared runner path proves the flag for both experiments.
