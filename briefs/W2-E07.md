You are the implementer for task W2-E07 of the Telltale build: an experiment, not a
feature. You are running as a headless Claude Code session inside a git worktree of
github.com/0xfauzi/telltale on branch task/W2-E07 (created from main). This session is
itself being recorded by `telltale run`: the build records itself. Read AGENTS.md,
docs/design/01-design.md section 6.12 in full (the pre-registered constants, the
backtester, the metrics, the decision rule), docs/experiments/README.md (the write-up
template), docs/experiments/E03.md (TimesFM-3 on this machine: timings and the device
decision), docs/experiments/E04.md (the model of a decision file written from a run
directory), docs/log/W1-T6.md (the backtester and the adapter), docs/log/W2-T5.md
(readiness), docs/log/W2-T7.md (the verification columns), src/telltale/forecast/
__init__.py, backtest.py, readiness.py, timesfm.py, src/telltale/cli_forecast.py,
experiments/E03/run.py and experiments/E04/run.py before writing code.

House rules: as little and as simple code as possible; the runner is a script under
experiments/E07/ and may import telltale; no emoji anywhere; no em dashes anywhere
(hyphen, colon or full stop); Python 3.12, uv only; never guess a number (measure or
write "needs measuring"); statistics are median, scaled MAD (1.4826 MAD), IQR, min, max
and the full sorted list, never a mean alone, except where design 6.12 defines a metric
as a mean (mean MAE across windows) and then both mean and median are printed; unknown
stays None; the words cause, impact and would are refused in anything this task
writes about a forecast. Commit messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W2-E07; `uv sync --extra forecast`; gates
green (`uv run mypy .`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run
deptry .`, `uv run pytest -m integration`, `uv run pre-commit run --all-files`); `git
push -u origin task/W2-E07`; `gh pr create --base main --title "W2-E07 request-clock
backtests on the build's own captures" --body-file docs/log/W2-E07.md`, PR body ending
"Generated with Claude Code: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc"
(no emoji).

STOP RULES: this task must not start any agent session that spends tokens. It must
never write to ~/.telltale/telltale.db: the first thing run.py does is copy that file
into experiments/E07/out/home/telltale.db and point TELLTALE_HOME at out/home for
everything after; it refuses to run when TELLTALE_HOME resolves to ~/.telltale. Do not
change c_min, k_min, delta, w, H, the stride, the baseline window or the threshold rule
to admit more data: a capture that fails readiness is excluded and listed, and "fewer
than three captures pass" is a valid outcome that gets written up as such.

Injected facts, measured on 2026-09-02 unless a file is named.
- The data. The build's own sessions, captured through the launcher, are in
  ~/.telltale/telltale.db; the capture_started payload of each carries experiment
  "build", task_id and attempt. On the evening of 2026-09-02 nine of them had 65 or
  more model requests (175, 158, 130, 120, 111, 86, 80, 73, 65) and more exist by the
  time you run (the W2-T4, W2-T6, W2-T7 and this session's own captures); enumerate
  at run time, after `telltale rebuild` on the copy, from the activities table
  (activity_type model_request, grouped by capture) joined to capture_started, and
  keep only experiment "build" captures: replayed fixtures and imported transcripts
  are not launcher captures and are excluded with that reason. Every build capture
  shares one cohort: provider claude, runtime 2.1.258, model opus, content level 1;
  never pool across providers, and there is nothing else to pool with.
- Readiness after W2-T7. W2-T5 measured 52 model requests as the shortest request
  clock that yields k_min = 20 windows at H = 1 and 112 at H = 4. Before W2-T7 every
  long capture lost all its windows to the None rows of last_verification_exit; W2-T7
  replaced that column, and docs/log/W2-T7.md carries the before-and-after window
  counts: read them and do not re-derive them. On the 175-request capture,
  fresh_input_tokens and tool_calls_since_prev have scaled MAD 0 (check 7 fails: eight
  of ten Claude requests carry at most 2 fresh input tokens, and most turns make one
  tool call) and output_tokens has scaled MAD 409.2. Expect output_tokens to be the
  only assessable target on Claude captures and report the readiness verdict per
  capture, per target and per horizon whatever it turns out to be.
- The commands. `uv run telltale series build --clock request --capture <id>` (prints
  the series id), `uv run telltale forecast readiness --series <id> --target <t>
  [--horizon 1|4]` (exit 0 when ready), `uv run telltale forecast backtest --series
  <id> --target <t> [--horizon 1|4] [--forecasters a,b,c] [--device cpu|mps]` (stores
  one forecast_runs row, prints the report with the license line, returns the run id).
  Forecasters: persistence, rolling_median, rolling_mean, local_drift, echo (the stub)
  and timesfm; the default list is the first five. Call these through their Python
  functions (backtest.run, backtest.persist, backtest.metrics, readiness.check) from
  run.py rather than through subprocess, so every number lands in one JSON.
- TimesFM-3. `uv sync --extra forecast`. The checkpoint google/timesfm-3.0-pytorch is
  already in ~/.cache/huggingface/hub/models--google--timesfm-3.0-pytorch (downloaded
  2026-09-02 11:09; the adapter reads TELLTALE_HF_CACHE for another cache directory and
  otherwise uses the Hugging Face default). E03 measured 0.3071 s median per
  predict_batch call on CPU (min 0.148, max 0.522, 108 calls), 0.37 s at context 512,
  and the same cost for every horizon from 1 to 64; MPS was about twice as fast with
  agreement 9.1e-7 against CPU. All windows of one run go to one predict_batch call
  (design 6.12), so a run costs one call plus the model load; run everything on cpu by
  default and take `--device mps` as a flag, recording wall_ms per run either way.
  License: timesfm-non-commercial-license-v1.0, printed in every report.
- The decision rule (design 6.12), and what half of it is missing. Per (target, H,
  capture): E_M = mean MAE of timesfm in true order, E_B = min over the four baselines,
  W_MB = share of windows where timesfm beats the best baseline; baseline sufficient
  iff E_M > (1 - delta) E_B or W_MB < w, with delta 0.10 and w 0.60. The placebo half
  (E_P, W_MP, the two-halves clause) needs block_shuffle, which is W3-T2 and does not
  exist: this experiment can label "baseline sufficient" or "not baseline sufficient at
  this n, placebo pending", never "temporal evolution" or "conditional prediction".
  Print the inequalities with their values beside every label. Constants printed in
  every output: delta 0.10, w 0.60, k_min 20, c_min 32, H in {1, 4}, stride H, baseline
  window 8, tau q80 of the first c_min rows, relative resolution 0.25.

BRIEF W2-E07: run readiness and the request-clock backtester, with TimesFM-3 and the
baselines, on every build capture in a copy of the home store, and write the decision
file that says per target and horizon which captures were assessable, what the
baselines and the model did on each, and which branch of the rule each took.
OWNS: experiments/E07/run.py, experiments/E07/out/ (summary.json and the per-run JSON
re-included in .gitignore the way E04 re-includes its results; the copied database is
NOT committed: add `experiments/E07/out/home/` to .gitignore), docs/experiments/E07.md,
docs/log/W2-E07.md.
READS: everything under src/telltale/forecast/, series.py, store.py, cli_forecast.py.
Traps: the copied database contains every capture on this disk including the E04 load
runs and the fake-agent captures; the experiment "build" filter is what keeps them
out, and the exclusion table lists them with the reason. A run's windows are stored
in forecast_runs as JSON; read them back from the store for the summary rather than
keeping a second copy in memory, so the decision file is traceable to rows anyone can
query. The lead-time and calibration metrics are "not assessable" when their
preconditions fail (fewer than 8 residuals, no window with y below tau); print the
string, never a number in that cell. Per-capture tables come first and the pooled row
(all windows of the cohort concatenated) last, labelled pooled, because a pooled mean
hides which capture carried it. The write-up follows the 7.5 template exactly, the
decision rule paragraph is copied from this brief before the run, and the "how sure"
table has one row per claim.
DO:
(1) experiments/E07/run.py [--device cpu|mps] [--forecasters ...]: copy the store,
    rebuild, enumerate, per capture: series build, readiness per target and horizon,
    backtest each ready (target, H) with the five defaults plus timesfm, persist, read
    the run back, compute E_M, E_B, W_MB, the label and the inequalities; write
    out/<capture_id>/<target>-H<h>.json and out/summary.json (constants, the
    exclusion table, per-capture rows, the pooled row per target and horizon, wall
    times per run and the whole run's wall time).
(2) docs/experiments/E07.md: the template, with the readiness exclusion table (every
    capture, every target, the first failing check with its measured and needed
    numbers), the per-capture backtest tables (n_windows, mean and median MAE per
    forecaster, skill against the best baseline, W_MB, calibration flag, WQS, lead
    time or "not assessable"), the pooled rows, the labels with their inequalities,
    and the "what downstream may now say" paragraph naming the claim class
    (predictive, request clock, cohort of build sessions, placebo pending).
VERIFY (paste outputs in the report): `uv run --extra forecast python
experiments/E07/run.py` from a clean out/ (the whole run; its wall time; the summary
table it prints); `python -m json.tool experiments/E07/out/summary.json | head -60`;
`sqlite3 experiments/E07/out/home/telltale.db "select count(*) from forecast_runs"`
equal to the number of (capture, target, H) rows the summary calls assessed; every
number in docs/experiments/E07.md found in out/ (say how you checked: a script that
greps each number is fine); `uv run python -c "import telltale"` in the default
environment (no torch) still works; `uv run ruff check experiments/E07`; all gates; `git
status` shows no database and no file over 1 MB staged.
STOP if fewer than three captures pass readiness for every target (write the decision
file with the counts and the failing checks: that is the result), if the timesfm import
fails in the extra environment (run the baselines and the stub, and say so in the
headline), or if any label would need the placebo (write "placebo pending" instead).
REPORT: docs/log/W2-E07.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT
DONE / NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): what a rolling origin is, one real window from one real capture with the
five forecasts and the actual beside them, what "baseline sufficient" means and why it
is not a failure, and what the placebo will add when W3-T2 lands. Commit: "W2-E07
request-clock backtests". Your final message (it is captured): OUTCOME, the per-target
summary table, and anything UNSURE OR NOT DONE.
