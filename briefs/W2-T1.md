You are the implementer for task W2-T1 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W2-T1 (created from main). This session is itself being recorded by
`telltale run`: the build records itself. Read AGENTS.md, docs/design/01-design.md
sections 6.10 and 6.11 in full and its "Amendments from wave 1" section,
docs/spec/telltale-architecture.md section 13 and Appendix B, docs/gates/wave-1.md (the
two defects and the two findings this task fixes are measured there), docs/log/W1-T2.md,
W1-T3.md and W1-T5.md (UNSURE and NEXT lists), src/telltale/measures.py, activities.py,
activities_codex.py, correlate.py, commands.py, report.py, model.py (the Evidence
constructors and their ValueErrors), store.py (replace_evidence, evidence), the goldens
under fixtures/golden/ and tests/integration/test_capture_to_summary.py before writing
code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji
anywhere; no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never
guess a number (measure or write "needs measuring"); unknown stays None, never zero-filled:
a metric whose inputs have coverage unavailable is an Evidence with value None and
coverage unavailable, and a count over an empty set is 0 only when the capability was
observed; every derived number is an Evidence written through store.replace_evidence
with claim_class derived, its coverage, a non-empty source list of the activity ids it
read, assumptions and warnings; comments state constraints the code cannot show;
integration tests only, no new test file: extend tests/integration/
test_capture_to_summary.py. Commit messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W2-T1; `uv sync`; gates green (`uv run mypy .`,
`uv run ruff check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W2-T1`;
`gh pr create --base main --title "W2-T1 measures and the session summary" --body-file
docs/log/W2-T1.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULE: this task must not start any agent session that spends tokens. No `claude -p`,
no `codex exec`. Verification is on replayed fixtures and on a COPY of the build's own
store: `mkdir -p /tmp/w2t1home && cp ~/.telltale/telltale.db /tmp/w2t1home/telltale.db`
and `export TELLTALE_HOME=/tmp/w2t1home` (never point TELLTALE_HOME at ~/.telltale: that
store is the orchestrator's record of the build and a rebuild there is a write).

Injected facts (measured in wave 1). Activity types and fields as stored: model_request
{input_tokens, cache_read_tokens, cache_creation_tokens, output_tokens, duration_ms,
model, request_index, request_id, query_source, usage_source, cost_usd, clock,
primary_observation; Codex adds total_input_tokens, reasoning_output_tokens and lacks
request_id, duration_ms, cost_usd}; command and verification_run {tool_name, command_norm,
category, scope, classifier_version, success (bool or None), duration_ms, tool_use_id,
decision, permission_mode, sizes, clock, primary_observation; Codex adds exit_code};
file_edit and file_read {file_path, tool_name, success, duration_ms, tool_use_id, sizes};
compaction {trigger, pre_tokens, post_tokens, duration_ms, success}; subagent {agent_id,
spawned_by_tool_use_id, start_source, ...; read activities.py _subagents}; turn (Codex
only; turn totals, never summed into requests); repo_snapshot (one per
telltale.repo.snapshot observation: read activities.py for its fields, which are the
observation payload: trigger, head, dirty_tree_hash, diff_hash, files_changed,
additions, deletions, per_file); repo_commit; lifecycle (the actor "telltale" row with
fields.event "capture" carries fields.coverage per capability, context_window,
context_window_source, models, surfaces_delivered, started_at and ended_at of the
capture; the session_end row from the stream result carries is_error, num_turns,
stop_reason). Coverage words per capability are on that capture row.
Defect 1 (docs/gates/wave-1.md): Claude Code 2.1.258 emits claude.stream.system.
task_started for every Bash call with payload task_type "local_bash" and
is_backgrounded false; the reducer turned 5 of them into 5 subagent activities on a
build session that spawned no subagent (capture cap_01M1GPSMW1ZRXVADWZPF0KZ9H3 in the
copied store; the raw stream has 29 Bash and 2 Write tool_use blocks and nothing else).
A subagent starts from claude.hook.SubagentStart, from an OTel api_request whose
query_source or agent name says so, or from task_started whose task_type is NOT
local_bash; never from a local_bash task. The 2.1.257 S4 fixture has one real subagent
and its task_started must still count.
Defect 2 (same capture): 32 conflict diagnostics, one per request, because the stream's
per-assistant-message usage (secondary) was compared with the OTel api_request usage
(primary) per request, and a request yields several assistant messages (103 against 17
output tokens). Sum the stream usage per request before comparing, or compare only when
one message maps to one request; a conflict diagnostic must mean the two surfaces
disagree about the same quantity.
Finding 3: `UV_CACHE_DIR=/x uv run pytest && ...` classifies as git (Codex S1 and S6):
commands.classify finds no category for the leading env assignment and falls through to
the next segment. A leading `NAME=value` prefix is skipped before classification and
`agent_test_runs` on Codex S1 becomes 2 (the report says which two commands).
Finding 4: a `git commit` command with a multi-line message goes through the shlex
fallback (one token) so no stored command_norm contains "git commit"; classify the
fallback by its first two words when they are lowercase identifiers (`git commit` ->
git) and record `normalization: fallback` as today.
Finding 5: a session ended by the subscription limit has a stream result with subtype
success and is_error true: the summary carries session.is_error and session.stop_reason
from the session_end lifecycle row (values as stored, no prose).
S1 (Claude, 2.1.257) facts for the goldens: 7 requests, 14 fresh input, 227790 cache
read, 936 output, 18210 cache creation; 3 pytest runs all piped into `tail` so every
surface saw tail's exit status (the first run's output said "1 failed, 1 passed" and no
surface recorded it); 1 file read, 1 file edit of pkg/calc.py, 1 other command; 0
compactions with coverage observed. S7: 4 compactions with pre-tokens 34181, 35113,
38398, 42366 and post-tokens None, 3909, 8138, 5193 (one failed). S4: one subagent with
attributed_total_tokens 92479 and provider_total_tokens 20093, never summed. Codex S1: 7
responses, fresh 25683, cached 111104, output 714, denominator_source
codex.rollout.event_msg.task_started.model_context_window with context_window 258400.
Definitions (design 6.11, pre-registered): verification: agent_test_runs,
failed_test_runs, fail_to_pass_cycles (a failed verification followed by a passing one of
the same category), edits_after_last_successful_test, edit_epochs_with_verification and
edit_epochs_without (an epoch is a maximal run of file_edits with no verification_run
between them), last_verification_repo_hash (dirty_tree_hash of the latest snapshot at
the time), targeted_vs_full counts (scope). Edit turnover: unique_files_changed,
file_revisits (an edit to a file edited earlier in the capture), max_diff_lines and
final_diff_lines (additions plus deletions from snapshots), reversions (a snapshot
diff_hash equal to an earlier one after an edit), post_failure_revisits (an edit to a
file within N = 3 edits after a failed verification). Exploration:
unique_files_read_before_first_edit, unique_files_read, search_ops (Grep, Glob, rg,
grep, find), directories_traversed (distinct parent directories of read paths),
read_to_edit_ratio, explored_to_final_ratio (distinct directories read over distinct
directories edited; None when nothing was edited). Context: totals by type
(fresh_input = input_tokens, cache_read, cache_creation, output), model_requests,
compactions, pre_compaction_tokens, post_compaction_tokens (when observed),
occupancy_ratio = the largest per-request (input + cache_read + cache_creation) over the
context_window, only with a trustworthy denominator, with denominator_source recorded,
else None. Stable-state work intervals: maximal intervals where the latest snapshot
diff_hash and the latest verification signature (category, success, repo hash) are both
unchanged while model_request or tool_call activities continue; report count, durations,
tokens inside, and what ended each (edit, verification, capture end); unavailable when
the capture has no snapshots. Delegation: subagent_count, subagent_tokens (attributed),
direct_vs_delegated_tool_calls. Each metric is one Evidence with the coverage of the
capabilities it needed (the weakest word wins) and source = the activity ids it read.

BRIEF W2-T1: every neutral measure of spec 13 as Evidence, the complete Appendix B
session summary, and the four reducer fixes above.
OWNS: src/telltale/measures.py, activities.py (subagent rule, conflict rule), correlate.py
(only what those two rules need), commands.py (env prefix, fallback classification),
report.py (show renders the new fields), fixtures/golden/** (regenerate; every changed
number explained), tests/integration/test_capture_to_summary.py (extend), docs/log/W2-T1.md.
READS: model.py, store.py, series.py, experiments.py (vector() will be replaced by W2-T4;
do not touch it).
Traps: measures.py is 394 lines and activities.py 760 against the 800-line ratchet: put
the interval and epoch machinery in a new module (measures_intervals.py or similar) rather
than growing either file past 800. A metric whose inputs are unavailable is None with
coverage unavailable and a warning naming the capability, never 0. Every Evidence.source
must resolve to an activity of the capture (the test resolves each). `rebuild` must stay
idempotent: run twice, byte-identical rows, including evidence ids (derive them from the
capture id and metric name, not from a fresh ULID). Do not read observations from
measures.py: activities only. Do not print anything from measures.py.
DO:
(1) measures.py (plus one helper module): `summarize(store, capture_id)` writing every
    metric above through replace_evidence; `summary(store, capture_id)` returning the
    Appendix B dict with coverage first, the session block (provider, model, runtime,
    is_error, stop_reason, duration_ms), usage, work, verification, exploration, context,
    stable_state, delegation, forecast_readiness (unchanged from W1-T5's reading),
    claim_class derived, warnings, diagnostics count last.
(2) activities.py: the subagent rule (task_type) and the conflict rule; a comment naming
    the 2.1.258 measurement.
(3) commands.py: env-prefix skipping and fallback classification; CLASSIFIER_VERSION and
    the normalization version change accordingly (they are on every stored row).
(4) report.py: `show` renders the new blocks; `explain` works for every new metric.
(5) goldens regenerated for Claude S1, S4, S7 and Codex S1, S3, S6, with a table in the
    log: metric, old value, new value, where in the fixture the new value comes from.
(6) tests: S1 verification block (fail_to_pass_cycles 0 with the tail warning,
    edits_after_last_successful_test 0, edit_epochs_with_verification 1);
    S2-like explore-only (replay S2: unique_files_changed 0, exploration numbers
    non-zero); a capture replayed without hooks and without repo snapshots reports the
    stable-state and diff metrics as None with coverage unavailable, not 0; S4 gives
    subagent_count 1 and a hand-built capture containing only task_started local_bash
    observations gives subagent_count 0; Codex S1 agent_test_runs 2; every evidence
    source resolves; rebuild twice idempotent including evidence rows.
VERIFY (paste outputs in the report): on the copied store, `uv run telltale rebuild` then
`uv run telltale show cap_01M1GPSMW1ZRXVADWZPF0KZ9H3` (delegation.subagent_count 0, the
usage block unchanged: 32 requests, 64 fresh, 6570582 cache read, 27598 output; conflict
diagnostics for that capture 0 after rebuild, shown with sqlite3), `uv run telltale
timeline` on it (no subagent rows), `uv run telltale explain cap_01M1GPSMW1ZRXVADWZPF0KZ9H3
stable_state_work_intervals` (walks to snapshots and verification runs); on a fresh home,
replay Claude S1 and `show` (verification block as in DO 6; usage totals unchanged from
W1-T2), `explain <S1> fail_to_pass_cycles`; replay Codex S1 and `show` (agent_test_runs
2); pytest -m integration; all gates; break-and-restore: make the subagent rule accept
local_bash again and show the test failing, then restore.
STOP if a spec 13 metric cannot be computed from activities without a new observation
type (report the gap; do not read observations), or if a golden number changes for a
reason you cannot trace to a fixture line (report it; do not commit the golden).
REPORT: docs/log/W2-T1.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE /
NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): what a stable-state interval is with one real interval from the build
capture, why a count of zero and "unavailable" are different answers, what the tail
warning means for anyone reading failed_test_runs, and what the subagent defect would
have done to a comparison had it survived. Commits: "W2-T1 reducer fixes: subagents,
conflicts, classifier", "W2-T1 measures and the session summary". Your final message (it
is captured): OUTCOME, the show output for the build capture, the public signatures, and
anything UNSURE OR NOT DONE.
