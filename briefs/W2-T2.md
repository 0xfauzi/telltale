You are the implementer for task W2-T2 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W2-T2 (created from main). This session is itself being recorded by
`telltale run`: the build records itself. Read AGENTS.md, docs/design/00-digest.md 2.1
("Transcript JSONL") and 2.2 ("Rollout files"), docs/design/01-design.md 6.3, 6.4, 6.7
and its "Amendments from wave 0" and "wave 1" sections, docs/design/02-protocol.md (the
owner decision: backfill was approved AFTER a dry run whose counts the owner sees first),
docs/log/W0-T4.md and W1-T3.md (how the providers parse and their DRIFT lists),
src/telltale/providers/claude.py, providers/codex.py (its rollout surface already
parses E02's rollout fixtures), providers/__init__.py (ParseCtx, prose()), allowlist.py,
allowlist_codex.py, sanitize.py (NEVER_PERSIST), receiver.py (the replay path and
`POST /v1/stream/codex?surface=rollout`), store.py, cli.py, tests/integration/conftest.py
and test_privacy.py before writing code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji
anywhere; no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never
guess a number (measure or write "needs measuring"); unknown stays None; comments state
constraints the code cannot show; integration tests only, and you add exactly one test
file, tests/integration/test_import.py, plus the import cases in test_privacy.py. Commit
messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W2-T2; `uv sync`; gates green (`uv run mypy .`,
`uv run ruff check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W2-T2`;
`gh pr create --base main --title "W2-T2 backfill importers" --body-file
docs/log/W2-T2.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULES: this task must not start any agent session that spends tokens. It must not
WRITE anywhere under ~/.claude or ~/.codex (read-only, invariant 7), and it must not run
the real import of the owner's files: the only command it runs against ~/.claude/projects
and ~/.codex/sessions is `--dry-run`, whose counts go in the report for the owner. The
real import is the owner's decision at the wave 2 gate. Real transcript lines you read
while learning the shapes are never copied into the repository: fixtures are synthetic,
hand-written, and contain the privacy probes.

Injected facts. Volume on this machine (measured 2026-09-02): ~/.claude/projects holds
303 project directories and 1803 .jsonl files, 1.7 GB; ~/.codex/sessions holds 1452
rollout-*.jsonl files, 2.9 GB. A dry run must therefore stream files line by line and
never load a file whole. Transcript JSONL (~/.claude/projects/<slug>/<session>.jsonl):
assistant lines carry requestId, message.usage (input_tokens, cache_read_input_tokens,
cache_creation_input_tokens, output_tokens, and cache_creation.ephemeral_1h_input_tokens
and ephemeral_5m_input_tokens), message.model, effort, version, gitBranch, cwd,
sessionId, uuid, parentUuid, timestamp; `isSidechain` true marks subagent lines; content
blocks are tool_use {id, name, input (FULL contents: Edit old_string and new_string,
Write content, Bash command)} and tool_result {tool_use_id, is_error, content (FULL
output)}; user lines carry the prompt text; a compaction line is type system, subtype
compact_boundary, compactMetadata {trigger, preTokens, postTokens,
cumulativeDroppedTokens, durationMs}; there are also summary lines and file-history
snapshot lines. Only allowlisted facts survive: the same field names the live surfaces
use (usage tokens, model, request id, tool name, file_path relativized, command
normalized, sizes, is_error, compaction numbers), and the prompt, the response text,
tool contents and outputs never reach sanitize (NEVER_PERSIST containers are lifted the
way claude.parse does for the stream). Rollout files (~/.codex/sessions/YYYY/MM/DD/
rollout-<ts>-<id>.jsonl): session_meta, turn_context, event_msg (token_count with
info.total_token_usage, last_token_usage and model_context_window; task_started with
model_context_window; task_complete), response_item (function_call, custom_tool_call,
custom_tool_call_output, reasoning: reasoning is dropped before sanitize and counted in a
diagnostic); codex.parse("rollout") exists and is the parser to reuse. Capture id of an
imported session: "imp_" + sha256(provider + ":" + session_id)[:24], so a second import
is a no-op; surfaces_configured is ["transcript"] or ["rollout"]; coverage is computed
from that by the existing reducer; `telltale sessions` shows a BACKFILL column. The
observation types are claude.transcript.<kind> and codex.rollout.<kind> (the latter
already exist). Compaction post_tokens is OBSERVED on transcripts (the only surface that
carries it), so a transcript capture's compaction coverage is observed.
Store: `Store.append(observations)`, `diagnose(...)`, `captures()`; the receiver is not
involved: the importer appends through the store directly, in batches of 500, after
parse and sanitize, and emits telltale.capture_started and telltale.capture_ended
observations for each imported session (provider, argv_shape "backfill",
surfaces_configured, the source file's path hashed, the file's byte size and line count,
first and last provider timestamps) so every capture has the same skeleton.

BRIEF W2-T2: `telltale import claude-transcripts [--root DIR] [--since DATE] [--project
SLUG] [--dry-run]` and `telltale import codex-rollouts [--root DIR] [--since DATE]
[--dry-run]`: read the owner's existing local session files through the same providers
and sanitizer, one capture per session, idempotent, backfill-labelled.
OWNS: src/telltale/importer.py, cli.py (import), providers/claude.py parse("transcript"),
the transcript entries of allowlist.py (or a new allowlist_transcript.py if allowlist.py
would pass 800 lines), correlate.ROLES entries for claude.transcript.* only (W2-T1 is
editing the rules in that file in parallel: add table rows, change nothing else),
fixtures/sources/claude/2.1.257/transcript/ and fixtures/sources/codex/0.150.1/
rollout-import/ (synthetic, 30 to 60 lines each, every probe string from
fixtures/sources/claude/2.1.257/S3/MANIFEST.md present), tests/integration/test_import.py,
the import cases of test_privacy.py, docs/log/W2-T2.md.
READS: sanitize.py, store.py, activities.py, measures.py.
Traps: the transcript's tool_use inputs hold full file contents; the parser lifts only
file_path, command (through commands.normalize), sizes and git_commit_id, and the privacy
test proves the probes never reach the database bytes. `isSidechain` lines are subagent
activity: keep the fact, never sum their tokens into the main thread's (spec 13.6).
`--since` compares the file's first provider timestamp, not its mtime. A file that
fails to parse is one diagnostics row (kind parse_failure) and the import continues; the
dry run reports it under "unreadable". A transcript directory slug is not a path: never
reconstruct or store the project path, only its sha256 and the repo_id when a cwd line
gives one that exists on this machine (relativized through the normal path rule). Never
follow symlinks out of --root.
DO:
(1) providers/claude.py parse("transcript", line, ctx): one observation per transcript
    line kind (assistant usage, one per tool_use block, one per tool_result block, user
    prompt length only, compact_boundary, summary presence only), DRIFT entries for every
    field seen in real files that the digest does not name.
(2) importer.py: `scan(root, provider, since, project) -> Iterator[Source]` (path, bytes,
    lines, session_id, first_ts, last_ts, readable), `dry_run(...) -> dict` (files,
    sessions, bytes, lines, date range, per-project or per-day counts, unreadable
    files, already-imported count against the store), `import_files(store, sources,
    level) -> dict` (captures created, skipped as existing, observations, diagnostics).
(3) cli.py `import` subcommand printing the dry-run table or the import result;
    `sessions` gains the BACKFILL column.
(4) fixtures and tests: the synthetic transcript contains an Edit with old_string and
    new_string holding the probes, a Bash tool_result whose content holds a probe and an
    absolute home path, a user prompt with a probe, a compaction line, and a sidechain
    assistant line; the synthetic rollout contains a reasoning item with a probe and a
    command_execution with aggregated_output holding a probe. test_import.py: dry run
    counts equal the fixture's files and sessions and writes nothing; import creates one
    capture per session with usage and, for the transcript, compaction post_tokens
    observed; `show` on it reads; second import creates 0 captures; --since excludes;
    the sidechain tokens are not in the main usage; the privacy cases in test_privacy.py
    scan the database bytes for every probe and the home path after an import at level
    1 and level 0.
VERIFY (paste outputs in the report): `uv run telltale import claude-transcripts --root
fixtures/sources/claude/2.1.257/transcript --dry-run` (counts, no write: prove with an
ls of $TELLTALE_HOME before and after); the real import of the fixture (`show` has usage
and compaction with post_tokens observed; `sessions` shows BACKFILL yes); the import
again (0 new); `uv run telltale import codex-rollouts --root fixtures/sources/codex/
0.150.1/rollout-import --dry-run` and the import (`show` has cached tokens and the
denominator_source); privacy test on the synthetic fixtures at both levels; `strings
$TELLTALE_HOME/telltale.db | grep -c TELLTALEFAKE` (0); then, read-only, `uv run telltale
import claude-transcripts --dry-run` and `uv run telltale import codex-rollouts --dry-run`
against the owner's real directories (default roots): paste the two count tables in the
report under a heading "Dry run for the owner" and change nothing; pytest -m
integration; all gates.
STOP if a transcript field needed for usage is absent in the real files for the versions
present (report the version and field; coverage unavailable), or if a probe reaches the
database from a field the allowlist does not cover (report the field; do not widen the
allowlist).
REPORT: docs/log/W2-T2.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE /
NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): what backfill is and is not (no hooks, no OTel, so which capabilities it
can never observe), what the dry-run numbers mean and what the real import would write
(sizes in the store, not in the source directories), and how a probe string was proven
absent. Commits: "W2-T2 transcript parser and importer", "W2-T2 rollout import, dry run,
privacy cases". Your final message (it is captured): OUTCOME, the two dry-run tables,
the public signatures, and anything UNSURE OR NOT DONE.
