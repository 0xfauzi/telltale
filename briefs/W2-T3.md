You are the implementer for task W2-T3 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W2-T3 (created from main). This session is itself being recorded by
`telltale run`: the build records itself. Read AGENTS.md, docs/design/01-design.md 6.12
"H2 and H3 protocol" (INJECTED below: the statistics, the MDD formula, the demotion rule,
N_needed) and its "Amendments from wave 1" section, docs/log/W1-T4.md (the repeat runner
you extend: its spec shape, `repeat`, `vector`, `stats`, `one_fingerprint`, its UNSURE
list), src/telltale/experiments.py, env.py (the fingerprint payload fields), launch.py
(how a fingerprint is emitted), cli.py, report.py, tests/integration/fake_agent.py and
test_experiments.py before writing code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji
anywhere; no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never
guess a number (measure or write "needs measuring"); statistics are median, scaled MAD
(1.4826 MAD), IQR, min, max and the full sorted list per arm, never a mean alone;
unknown stays None; every printed number carries its claim class; comments state
constraints the code cannot show; integration tests only, no new test file: extend
tests/integration/test_experiments.py. Commit messages end with these two trailer lines
exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W2-T3; `uv sync`; gates green (`uv run mypy .`,
`uv run ruff check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W2-T3`;
`gh pr create --base main --title "W2-T3 environment runner" --body-file
docs/log/W2-T3.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULE: this task must not start any agent session that spends tokens. No `claude -p`,
no `codex exec`. Everything is verified with the fake agent.

Injected facts. The repeat runner (W1-T4): `repeat(spec, home, out=None) -> dict` with
spec keys task_id, experiment, repo, base_sha, command, acceptance, repetitions, provider,
level; each repetition is a detached worktree at base_sha, `telltale run --task-id
--attempt --experiment` as a subprocess, a correlation and an outcome posted through the
runner's own receiver attributed by `?capture=`, worktree removal; `vector(store,
capture_id) -> dict[str, float | None]` (usage fields and tool counts; W2-T4 replaces it
with the evidence vector); `stats(values) -> {n, unknown, median, mad_scaled, iqr, min,
max, values}`; `one_fingerprint(store, capture_ids)` refuses across differing
fingerprints and names the differing fields. The environment fingerprint payload (env.py)
has the fields provider, runtime_version, model, effort, tool_set_hash, mcp_names_hash,
instruction_hashes, settings_hash, sandbox_posture, capture_modes, content_level; model
and effort are read from the child's argv (`--model`, `--effort`, last occurrence wins).
The fake agent (tests/integration/fake_agent.py): `--effort low|medium|high` sets rank
0, 1, 2 and `--model haiku|sonnet|opus` changes token numbers by the model name's
length; reads per run = _MIN_READS + seed % _MAX_EXTRA_READS + rank, input_tokens per
turn = 100 + 13 * seed + 50 * rank + len(model) + turn, so a higher effort has MORE
reads and MORE input tokens by construction (read the file for the constants and put the
expected direction in the test as a fact about the fake agent, not about any real
agent); without `--seed` a seed is drawn per run, which is what gives within-arm
variance. Pre-registered constants (design 6.12, print them in every report): pilot 5
per arm; between arms Hodges-Lehmann shift (median of all pairwise differences b - a),
Cliff's delta ((#(b > a) - #(b < a)) / (n_a n_b)), exact Mann-Whitney p (two-sided, by
full enumeration of the rank arrangements for n_a, n_b <= 20 each, using the U statistic
with ties handled by mid-ranks and the enumeration done over the observed values, so no
normal approximation at these n; refuse above 20 per arm with the words "exact test not
computed above n = 20 per arm"); minimal detectable median difference at the current n,
`MDD = 2.8 s sqrt(2 / n)` with n the per-arm count and s the robust spread; s for two
arms is defined here as the root mean square of the two arms' scaled MADs,
`s = sqrt((s_a^2 + s_b^2) / 2)`, and the report prints both s_a and s_b beside it;
demotion: a measure is withheld from repository comparison while `MDD > 0.25 median`
(median = the median of both arms pooled) and the report prints `N_needed = ceil(2 (2.8 s
/ (0.25 median))^2)`; per factor: "material environment effect" iff `|HL shift| > MDD`,
otherwise "not resolved at n", never "no effect". Every between-arm row is claim_class
comparative and names the factor and both arm fingerprints.

Injected on 2026-09-02 after the wave 2 row 1 merges (main 9f9ec2e and later): W2-T1 (measures), W2-T2 (importers) and W1-E04 are on main. cli.py was split by the orchestrator: the `series` and `forecast` subcommands live in src/telltale/cli_forecast.py (registered through `add_commands`, dispatched through its `series` and `forecast` functions) and the helpers both files share are src/telltale/cli_common.py (`store()`, `known()`, `refuse()`, `REFUSED`). cli.py is 636 lines, cli_forecast.py 173. `experiment environment` goes in cli.py beside `experiment repeat`, through the same `experiment` subparser and the `_COMMANDS` table.

BRIEF W2-T3: `telltale experiment environment <spec.json> [--out DIR]`: arms that differ in
exactly one launch flag, fingerprint assertions within and between arms, per-arm robust
statistics through `repeat`, and the between-arm statistics above.
OWNS: src/telltale/experiments.py (environment runner and the statistics; if the file
would pass 800 lines, put the statistics in src/telltale/stats.py first as a separate
commit), cli.py (experiment environment), report.py (the two tables), tests/integration/
test_experiments.py (extend), docs/log/W2-T3.md.
READS: launch.py, env.py, store.py, fake_agent.py (do not change its behaviour: W1-T4's
tests pin it; you may add a flag only if a test needs a shape that does not exist).
Traps: the spec declares the factor and the arms; the runner checks BEFORE running
anything that the arms' commands differ in exactly the declared flag's value (compare
argv lists token by token; any other difference is refused with the tokens named) and
AFTER running that every capture within an arm shares one fingerprint id and that the
two arms' fingerprint payloads differ in exactly the declared field (model or effort;
a spec may also declare content_level as the factor, which is a `level` difference
rather than an argv one); otherwise it refuses to compute between-arm statistics and
prints the differing fields. Repetitions run through `repeat` unchanged (one call per
arm with the arm's task_id suffix), so the per-arm tables are its tables. Exact
Mann-Whitney by enumeration: n_a = n_b = 5 is 252 arrangements and n = 20 each is
137846528820, which is why the refusal above 20 exists; do not approximate. The report
never says "effect of", "cause" or "impact": the label vocabulary is the two strings
above.
DO:
(1) experiments.py `environment(spec, home, out=None) -> dict` where spec = {task_id,
    experiment, repo, base_sha, acceptance, repetitions_per_arm, provider, level,
    factor: "model" | "effort" | "content_level", arms: [{name, command, level?}, ...]}
    (exactly two arms in this task; refuse otherwise with the count named); the report
    dict carries the spec, per-arm reports (from repeat), the fingerprint assertion
    result with the differing fields, and per metric: HL shift, Cliff's delta, exact p,
    s_a, s_b, s, MDD, median, N_needed, demoted (bool), label.
(2) the statistics as pure functions with docstrings that state the formula:
    `hodges_lehmann(a, b)`, `cliffs_delta(a, b)`, `mann_whitney_exact(a, b) -> (U, p)`,
    `mdd(s, n)`, `n_needed(s, median)`.
(3) cli.py `experiment environment <spec.json> [--out DIR]` printing the constants, the
    fingerprint assertion line (both ids and the differing field), the two per-arm tables
    from repeat, and the between-arm table (metric, claim_class, HL shift, delta, p, MDD,
    N_needed, label) followed by one paragraph saying what "not resolved at n" means;
    writes experiments/<task_id>/environment.json when --out is given.
(4) tests: two fake-agent arms, effort low against high, 5 each, on a temporary
    repository: the fingerprint assertion passes naming effort; HL shift for
    tool_calls.Read and input_tokens has the sign the fake agent's constants give
    (compute the expected sign from the constants in the test, do not hard-code a
    number from a run); MDD and N_needed are present per metric; a spec whose arms differ
    in two flags is refused before any capture exists (naming both); a spec whose arms
    differ in level while declaring effort is refused after the runs naming
    content_level; the exact Mann-Whitney function reproduces a hand-computed p on two
    3-element lists written in the test (show the enumeration count 20 and the p in a
    comment); mann_whitney_exact refuses at n = 21.
VERIFY (paste outputs in the report): `uv run telltale experiment environment spec.json
--out /tmp/e` with the two fake-agent arms (the tables; the fingerprint line; the
labels); the refused specs with their messages; `uv run python -c` calling
mann_whitney_exact on the hand-computed lists; pytest -m integration; all gates;
break-and-restore: make the between-arm check ignore effort and show which assertion
fails, then restore.
STOP if the runner would need a real agent to verify (use the fake agent) or if the
fingerprint payload does not carry the declared factor as a field (report which field
is missing from env.py; do not compute the difference from argv instead).
REPORT: docs/log/W2-T3.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE /
NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): what an arm is, one real between-arm table from the fake agent with each
column explained, why "not resolved at n" is the honest label and what N_needed tells
the owner to budget, and why the fake agent's numbers prove the harness and nothing
about any real agent. Commits: "W2-T3 between-arm statistics", "W2-T3 environment
runner". Your final message (it is captured): OUTCOME, the between-arm table, the
public signatures, and anything UNSURE OR NOT DONE.
