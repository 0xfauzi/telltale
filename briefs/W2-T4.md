You are the implementer for task W2-T4 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W2-T4 (created from main). This session is itself being recorded by
`telltale run`: the build records itself. Read AGENTS.md, docs/design/01-design.md
section 6.11 (its last paragraph is the cohort rule) and 6.13, docs/spec/
telltale-architecture.md section 13.7 (the evidence vector) and Appendix B,
docs/design/02-protocol.md (brief and report formats), docs/log/W2-T1.md (every
metric: its definition, its coverage rule, the S1 and S2 numbers it measured),
src/telltale/measures.py (SUMMARY_BLOCKS, summary(), how an Evidence is written),
measures_spec13.py (the Metric table), model.py (Evidence and its constructors),
report.py (render_table, show, explain, experiment), cli.py and cli_common.py (the
reading commands and the helpers they share), store.py (captures, activities,
evidence, observations), tests/integration/conftest.py (the replay helper) and
tests/integration/fake_agent.py before writing code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji
anywhere; no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never
guess a number (measure or write "needs measuring"); unknown stays None and is never
zero-filled; every derived or comparative number is an Evidence built through the
constructors in model.py; a claim class is never upgraded; comments state constraints
the code cannot show; integration tests only, and you add exactly one test file,
tests/integration/test_compare.py. Commit messages end with these two trailer lines
exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W2-T4; `uv sync`; gates green (`uv run mypy .`,
`uv run ruff check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W2-T4`;
`gh pr create --base main --title "W2-T4 compare, cohorts and the evidence vector"
--body-file docs/log/W2-T4.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULE: this task must not start any agent session that spends tokens. No `claude -p`,
no `codex exec`. Everything is verified with replayed fixtures and the fake agent.

Injected facts, all measured on 2026-09-02 unless a file is named.
- The metrics. measures.py writes one Evidence row per metric per capture (claim_class
  derived, coverage, source, value None when its coverage is unavailable); SUMMARY_BLOCKS
  names them: usage (model_requests, fresh_input_tokens, cache_read_tokens,
  cache_creation_tokens, output_tokens), work (unique_files_read, unique_files_changed,
  file_revisits, max_diff_lines, final_diff_lines, reversions, post_failure_revisits,
  stable_state_work_intervals), verification (agent_test_runs, failed_test_runs,
  fail_to_pass_cycles, edits_after_last_successful_test, edit_epochs_with_verification,
  edit_epochs_without_verification, targeted_test_runs, full_test_runs), exploration
  (unique_files_read_before_first_edit, unique_files_read, search_ops,
  directories_traversed, read_to_edit_ratio, explored_to_final_ratio), context
  (compactions, pre_compaction_tokens, post_compaction_tokens, occupancy_ratio),
  stable_state (stable_state_work_intervals, stable_state_total_ms, stable_state_max_ms,
  stable_state_tokens, stable_state_ended_by_edit, stable_state_ended_by_verification,
  stable_state_ended_by_capture_end), delegation (subagent_count, subagent_tokens,
  direct_tool_calls, delegated_tool_calls). Read them from the evidence table; never
  recompute them.
- The vector. Spec 13.7 names six families, each "raw + cohort percentile" except
  compactions, which is "raw count + coverage". The family-to-metric table is a
  decision taken here so the vector is one constant, VECTOR in measures.py, and not a
  judgement per call: context_token_burden = (model_requests, fresh_input_tokens,
  cache_read_tokens, output_tokens); edit_turnover = (unique_files_changed,
  file_revisits, max_diff_lines, final_diff_lines, reversions); verification_cycles =
  (agent_test_runs, failed_test_runs, fail_to_pass_cycles,
  edit_epochs_without_verification); exploration_scope = (unique_files_read,
  search_ops, directories_traversed, read_to_edit_ratio); stable_state_work =
  (stable_state_work_intervals, stable_state_total_ms, stable_state_tokens);
  compactions = (compactions, pre_compaction_tokens), no percentile. 22 entries.
- The cohort rule (design 6.11): same provider, runtime major version, model and
  content level, n >= 10, imported (backfill) captures excluded unless asked. Where
  each key lives, measured on the home store and on a probe capture: provider is on the
  capture_start lifecycle activity's fields and on the telltale.capture_started payload;
  runtime_version is the session block's value, which measures.py takes from the
  session_start lifecycle field claude_code_version (a real session: "2.1.258"; the
  fake agent: "fake-agent", printed by its init message) and NOT from the
  telltale.environment fingerprint, whose runtime_version is None for every
  fake-agent capture (cap_01M1GSPD57128SJ7P6V1N9367V and its E04 siblings) because the
  launcher probes `<binary> --version` only for a known agent binary; the major
  version is the text before the first "." when the string starts with a digit, else
  the whole string; model is the fingerprint's model when the launcher parsed one from
  argv (a probe with `--model sonnet` in the fake agent's argv recorded "sonnet"), else
  the session block's models when that list has exactly one entry, else no cohort for
  that capture with the reason "mixed models" or "model unknown"; content_level is the
  fingerprint payload's content_level (1 on every launcher capture), and a capture with
  no fingerprint has no cohort with the reason "content level unknown". A replayed
  fixture (the conftest replay helper) carries no telltale.environment observation and
  no telltale.capture_started, so every replayed capture is "no cohort": that is the
  correct answer for it and not a gap to patch; confirm it with one sqlite3 query on
  the test store and paste the query. A backfill capture is one whose capture_started
  surfaces_configured is ["transcript"] or ["rollout"] (the BACKFILL column of
  `telltale sessions` shows it); `--include-backfill` on compare and vector admits them.
- Percentile: mid-rank, over the cohort members whose value for that metric is not
  None: percentile = 100 * (count below + 0.5 * count equal) / n_metric, one decimal,
  where the capture itself is a member; the cohort's n >= 10 is required first and
  n_metric >= 10 is required per metric. Each percentile is Evidence.comparative(...)
  with cohort = {provider, runtime_major, model, content_level, n, n_metric, captures:
  the sorted member ids}, source = the evidence ids of the member rows read (never
  empty), coverage = the weakest coverage word among the member rows, assumptions
  naming the four keys and the exclusion of backfill, warnings when any member's
  coverage is partial. Percentiles are NOT written to the store: a percentile is a
  statement about a cohort on a date, it changes whenever a capture is added, and a
  stored row would freeze one comparison as if it were a property of the capture; the
  reducer's idempotence (two rebuilds, identical rows) is the invariant that decides
  it. They are built through the constructor so its checks apply, printed with their
  claim_class, and returned to the test. Say this in the module docstring.
- Making a cohort. A fake-agent capture through the launcher takes 0.26 s wall on this
  machine (n = 1, `/usr/bin/time`, seed 1, in a temporary repository with one empty
  commit), and `experiment repeat` runs the same command for every repetition
  (experiments.py `_launch` passes spec["command"] unchanged), so ten captures with ten
  seeds come from a loop over `uv run telltale run --provider claude -- python
  tests/integration/fake_agent.py --seed K --model sonnet --output-format stream-json`
  for K in 1..10, inside one temporary repository, with TELLTALE_HOME set to a temporary
  directory. Until W2-T6 lands (in flight the same day), the launcher does not run the
  reducers at capture end: run `uv run telltale rebuild` once after the loop and say so
  in the report; if W2-T6 has merged by the time you read this, the rebuild is a no-op
  and you say that instead.
- Line counts on main at dispatch: cli.py about 640 (W2-T3 lands `experiment
  environment` there before you start; the 800-line ratchet applies), cli_common.py 42,
  report.py 353, measures.py 362. The `compare` and `vector` commands go in cli.py
  through `cli_common.store()`, `cli_common.known()` and the `_COMMANDS` table; if
  cli.py would pass 800 lines, move the `import` command group into cli_import.py first
  as a separate commit with no behaviour change.

BRIEF W2-T4: `telltale vector <capture>` prints the spec 13.7 evidence vector of one
capture with a percentile column that is filled only from a qualifying cohort;
`telltale compare <a> <b>` prints two vectors side by side with both coverages on every
row and a difference only where both values exist.
OWNS: src/telltale/measures.py (VECTOR, cohort_keys, cohort, vector, percentile: about
120 lines; if measures.py would pass 800 lines put them in src/telltale/cohorts.py
instead and say so), src/telltale/report.py (compare and vector renderers), cli.py
(compare, vector), tests/integration/test_compare.py, docs/log/W2-T4.md.
READS: store.py, model.py, measures_spec13.py, activities.py output, conftest.py,
fake_agent.py, experiments.py.
Traps: the value column and the coverage column are read from the same evidence row
and printed together; a row whose coverage is unavailable prints "-" for the value,
never 0. A difference (b minus a) is printed only when both values are not None; it is
an Evidence built through the comparative constructor with both evidence ids as its
source, printed, not stored. The percentile column reads "no cohort (n=K)" with K the
number of captures sharing the four keys (K counts the capture itself), or the reason
string when a key is unknown; it never reads 0, blank or "n/a". The cohort is computed
from the store as it is when the command runs; two runs after a capture was added may
legitimately differ, and the report explains why that is not a bug. Unknown stays None
all the way to the JSON output.
DO:
(1) measures.py: `VECTOR` (the table above); `cohort_keys(store, capture_id) -> dict`
    (the four keys and the backfill flag, or the reason string for the key that is
    unknown); `cohort(store, capture_id, include_backfill=False) -> list[str]` (the
    member capture ids, the capture itself included, in id order); `vector(store,
    capture_id, include_backfill=False) -> dict` shaped {family: {metric: {value, unit,
    coverage, claim_class, evidence_id, percentile}}} where percentile is {value, n,
    n_metric, claim_class: "comparative"}, {"no_cohort": K} or {"no_cohort": reason};
    and `percentile(values, own) -> float` as a pure function of a list of numbers.
(2) report.py: `vector(built) -> str` (table: family, metric, value, unit, coverage,
    claim_class, percentile) and `compare(a, b) -> str` (table: family, metric, a, b,
    a coverage, b coverage, difference, claim_class, a percentile, b percentile), both
    through render_table; `--json` prints the dicts.
(3) cli.py: `vector <capture> [--json] [--include-backfill]` and `compare <a> <b>
    [--json] [--include-backfill]`, in the `_COMMANDS` table.
(4) test_compare.py: (a) replay S1 and S2 through the receiver (conftest), rebuild:
    `vector` of S1 has 22 entries, every value equals the evidence row's value, every
    percentile is {"no_cohort": "content level unknown"} (a replayed capture has no
    fingerprint), and `compare` S1 S2 prints both coverages on every row and a
    difference only where both values are not None (assert on the returned dict, then
    on one printed row); (b) ten fake-agent captures with seeds 1..10 through `telltale
    run` in a temporary repository under a temporary TELLTALE_HOME (the loop above,
    from the test, then rebuild), then `vector` of the seed-5 capture: the cohort has n
    10, the percentile of fresh_input_tokens equals the mid-rank the test computes from
    the ten evidence values, the cohort dict lists the ten ids, and every percentile is
    an Evidence with claim_class comparative and a non-empty source; with nine captures
    (purge one) the percentile is {"no_cohort": 9}; one more capture run with `--model
    opus` is in no cohort with the nine others; `--include-backfill` is exercised on one
    imported fixture capture (`telltale import claude-transcripts --root
    fixtures/sources/claude/2.1.257/transcript`): excluded by default, admitted with the
    flag, and in no cohort either way because its content level is unknown (assert the
    reason string).
VERIFY (paste outputs in the report): `uv run telltale compare <S1> <S2>` on a
temporary home with the two fixtures replayed and rebuilt (the table; the S2 column
shows unique_files_changed 0 and non-zero exploration numbers, the S1 column matches
the numbers in docs/log/W2-T1.md, and every percentile cell says no cohort with its
reason); `uv run telltale vector <seed-5 capture>` after the ten-seed loop (percentile
column filled, n 10; beside it the ten fresh_input_tokens values from `telltale show`
and the mid-rank computed by hand in the report); the same with nine captures ("no
cohort (n=9)"); `uv run telltale vector <capture> --json | python -m json.tool | head`
(None prints as null, never 0); pytest -m integration; all gates; break-and-restore:
make `percentile()` count None values as 0 and show which assertion fails, then
restore; make the cohort ignore the model key and show which assertion fails, then
restore.
STOP if a spec 13.7 family cannot be filled from the evidence table without reading
activities or observations (report the family; do not compute it here), or if the
fake-agent loop cannot produce ten captures with equal cohort keys (report the
fingerprint payloads that differ; do not relax a key).
REPORT: docs/log/W2-T4.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE /
NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): what a cohort is and why n = 10 is a gate and not a statistic, one real
vector from the ten-seed loop with the percentile computed by hand, why a percentile is
printed and not stored, and what "no cohort" protects the reader from. Commits:
"W2-T4 cohorts and the evidence vector", "W2-T4 compare". Your final message (it is
captured): OUTCOME, the compare table, the public signatures, and anything UNSURE OR
NOT DONE.
