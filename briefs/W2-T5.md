You are the implementer for task W2-T5 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W2-T5 (created from main). This session is itself being recorded by
`telltale run`: the build records itself. Read AGENTS.md, docs/design/01-design.md 6.12
("Readiness checklist" is INJECTED below verbatim; also "Rolling-origin backtester" and
"SeriesCompiler") and its "Amendments" sections, docs/log/W1-T5.md and W1-T6.md (UNSURE
lists: the zero-window run, the refuse policy on last_verification_exit, the lead-time
denominators), docs/log/W2-T1.md (the summary you add a field to), src/telltale/series.py
(build, check, Refused, the refuse policy), forecast/__init__.py (TARGETS, TargetSpec,
constants), forecast/backtest.py (_plan, _at_origin, _regime_start, _tau, FORECASTABLE,
the drop reasons), measures.py (summary() and the forecast_readiness field as W2-T1 left
it), cli.py (forecast and series commands), tests/integration/synthetic_series.py and
test_forecast_contracts.py before writing code.

House rules: as little and as simple code as possible; stdlib only in src (numpy, torch
and timesfm3 only in forecast/timesfm.py); no emoji anywhere; no em dashes anywhere
(hyphen, colon or full stop); Python 3.12, uv only; never guess a number: a failing check
names the number it measured and the number it needed; unknown stays None, never
zero-filled; no policy imputes; comments state constraints the code cannot show;
integration tests only, no new test file: extend tests/integration/
test_forecast_contracts.py. Commit messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W2-T5; `uv sync`; gates green (`uv run mypy .`,
`uv run ruff check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration` in the default environment, `uv run pre-commit run --all-files`); `git push
-u origin task/W2-T5`; `gh pr create --base main --title "W2-T5 readiness preflight and
missingness policies" --body-file docs/log/W2-T5.md`, PR body ending "Generated with
Claude Code: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULE: this task must not start any agent session that spends tokens. Verification
is on the synthetic series, replayed fixtures, and a COPY of the build's store (`mkdir -p
/tmp/w2t5home && cp ~/.telltale/telltale.db /tmp/w2t5home/telltale.db && export
TELLTALE_HOME=/tmp/w2t5home`; never write to ~/.telltale).

Injected facts. Pre-registered constants (forecast/__init__.py): c_min 32 on the request
clock, H in {1, 4}, stride s = H, k_min 20, placebo block B = max(2, H), baseline window
8, threshold tau = q80 of the first c_min rows of the target unless the registry
declares an absolute value. The backtester (W1-T6) enumerates origins `o in range(c_min,
N - H + 1, s)`, sets `ctx_start = max(last changepoint <= o, o - 512)`, skips an origin
as regime_too_short when `o - ctx_start < c_min`, excludes windows with a changepoint
inside [o, o + H) from the headline, drops any window containing None in the target or a
selected covariate with its reason, and writes even a zero-window run. The series
compiler (W1-T5) builds the request clock with policy exclude (None cells stay None and
are counted per column) or refuse (any None in a column with coverage observed refuses
the build at exit 2 naming the column and the first row; a column with coverage
unavailable is all None and is reported, not refused); on every real capture
last_verification_exit's first rows are None until a test has run, so refuse fires on
essentially every capture. The summary's forecast_readiness today (measures._readiness)
is a per-clock boolean from activity presence: request_clock true when a model_request
exists. The synthetic writer gives 200 rows with one changepoint at row 120; the longest
real capture on this disk has 32 model requests. tests/integration/
test_forecast_contracts.py runs without torch.
The readiness checklist (design 6.12, verbatim): `readiness(series, target)` prints
pass/fail per line and sets `forecast_readiness[clock]` in the session summary:
(1) coverage: target and variant columns observed or derived-from-observed on retained
rows; (2) windows: `floor((N - H - c_min) / s) + 1 >= k_min` after changepoint
exclusions; (3) missingness: zero None cells inside any retained window under policy
exclude; (4) baselines: n_ctx >= 9 at every origin; (5) changepoints: at least one regime
with >= c_min + H rows; (6) placebo: n_ctx >= 2B at every origin; (7) variation: MAD of
the target over retained rows > 0; (8) threshold: tau computable or declared. Missingness
policies are exclude (default) and refuse; no policy imputes.

Injected on 2026-09-02 after the wave 2 row 1 merges (main 9f9ec2e and later): W2-T1 (measures), W2-T2 (importers) and W1-E04 are on main. cli.py was split by the orchestrator: the `series` and `forecast` subcommands live in src/telltale/cli_forecast.py (registered through `add_commands`, dispatched through its `series` and `forecast` functions) and the helpers both files share are src/telltale/cli_common.py (`store()`, `known()`, `refuse()`, `REFUSED`). cli.py is 636 lines, cli_forecast.py 173. `forecast readiness` goes in cli_forecast.py beside `forecast backtest`, NOT in cli.py; wherever this brief says cli.py for the readiness command, read cli_forecast.py.

BRIEF W2-T5: `telltale forecast readiness --series <id> --target <t> [--horizon 1|4]`
implementing the eight checks, the two missingness policies proven end to end (build
under refuse, drop-with-reason under exclude, and check 3), and forecast_readiness in
the session summary computed from the checklist rather than from activity presence.
OWNS: src/telltale/forecast/readiness.py (new; backtest.py is 689 lines and does not
grow), cli.py (forecast readiness), measures.py (only the forecast_readiness field and
the one call that fills it), report.py (show renders the field), tests/integration/
test_forecast_contracts.py (extend), docs/log/W2-T5.md.
READS: series.py, backtest.py (reuse _plan, _regime_start and _tau through public
wrappers if they are private: add a thin public function in backtest.py rather than
copying the origin arithmetic; the origins the checklist counts must be the origins the
backtester would run), model.py, store.py.
Traps: check 2 counts windows AFTER the changepoint exclusions and the regime_too_short
skips, so it must use the backtester's own plan, not the formula alone; print both the
formula's number and the planned number when they differ. Check 1 reads column coverage
words from the Series (observed or derived), never recomputes them. Check 3 is evaluated
over the retained windows of the plan: a None outside every window is not a failure
(policy exclude excludes it), a None inside one is. Check 7 uses the scaled MAD
(1.4826 MAD) and fails on exactly 0. Check 8 fails when the first c_min rows of the
target contain a None (tau not computable) unless the registry declares an absolute
threshold (none does today: say so). The summary field for a clock whose series cannot
be built (no activities, or a refusal) says why, with the Refused message, not a
boolean. The summary must stay rebuildable and idempotent: readiness is computed from
the series that `series build` would produce for the capture under policy exclude, for
every target in TARGETS, at H = 1; the field is {target: {ready: bool, failed:
[check names with measured and needed numbers], windows: n}} per clock, plus the
attempt_clock and change_clock entries left as W1-T2 wrote them with the note that the
clocks are W3-T1's.
DO:
(1) forecast/readiness.py: `check(series, target, horizon=1) -> list[Check]` with
    Check(name, passed, measured, needed, detail) for the eight checks in order, and
    `ready(checks) -> bool`; a `summary_field(store, capture_id) -> dict` that builds the
    request-clock series under exclude and evaluates every registry target, catching
    series.Refused into the field's text.
(2) cli.py `forecast readiness --series <id> --target <t> [--horizon 1|4]`: one line per
    check (`pass` or `FAIL`, the measured number, the needed number, the detail), exit 0
    when all pass and 1 otherwise.
(3) measures.py: forecast_readiness filled from readiness.summary_field; report.py
    renders it under the existing key.
(4) tests: the synthetic 200-row series passes all eight at H = 1 and at H = 4 (state
    the planned window counts); a 40-row synthetic series (write it with --rows 40)
    fails check 2 naming windows available against 20 needed; a copy of the synthetic
    series with one None inside a retained window fails check 3 naming the row and
    column, while the same None placed before c_min in a covariate row outside every
    window does not fail check 3 (and check 8 stays as the target dictates); a series
    with a constant target fails check 7 with measured 0; `series build --policy refuse`
    on the replayed S1 capture exits 2 naming last_verification_exit and row 0 (W1-T5's
    measured behaviour, asserted rather than changed); `show` on S1 carries
    forecast_readiness.request_clock with ready false and the windows check named
    (7 rows against c_min 32); the backtester on a series with a None in a window drops
    that window with reason "none_in_window" (or the reason string backtest.py uses:
    assert the real one) and the readiness check 3 names the same window.
VERIFY (paste outputs in the report): `uv run python tests/integration/
synthetic_series.py --db $TELLTALE_HOME/telltale.db --rows 200 --seed 1` then `uv run
telltale forecast readiness --series <id> --target fresh_input_tokens` (eight pass lines
with numbers); the same with `--horizon 4`; `--rows 40 --seed 2` then readiness (check 2
FAIL with the two numbers); on the copied store, `uv run telltale rebuild` then `uv run
telltale show cap_01M1GPSMW1ZRXVADWZPF0KZ9H3` (forecast_readiness.request_clock: 32
requests against c_min 32 and H 1 gives the planned window count; paste what it says and
whether that capture is ready for anything); replay S1 and `show` (the failing check
named); pytest -m integration; all gates; break-and-restore: make check 3 ignore None
inside windows and show the test failing, then restore.
STOP if the backtester's origin plan is not reachable without copying its arithmetic
(report which function needs a public wrapper and add the wrapper; do not duplicate), or
if a check's needed number is not fixed by the constants above (report it; do not pick
one).
REPORT: docs/log/W2-T5.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE /
NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): what readiness is for (why a forecast is refused before it is wrong), the
eight lines for the synthetic series and for the real build capture side by side, what
the two missingness policies do to the same gap, and how many requests a real capture
needs before the request clock can be backtested at all. Commits: "W2-T5 readiness
checklist", "W2-T5 readiness in the summary and the policy tests". Your final message
(it is captured): OUTCOME, the two readiness printouts, the public signatures, and
anything UNSURE OR NOT DONE.
