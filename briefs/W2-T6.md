You are the implementer for task W2-T6 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W2-T6 (created from main). This session is itself being recorded by
`telltale run`: the build records itself. Read AGENTS.md, docs/gates/wave-1.md (the
commit-linkage defect and the 2.1.258 drift list are measured there), docs/design/
01-design.md 6.8 and 6.9 and its "Amendments" sections, docs/spec/telltale-architecture.md
section 12 (commit linkage: the confidence ladder, "temporal proximity alone is never a
link"), docs/log/W0-T3.md, W0-T4.md, W1-T1.md, W1-T4.md (UNSURE lists), src/telltale/
repo.py (commits_since, _link, _tree_match, _candidates, snapshot), launch.py (the
capture-end linkage and link_commits), facts.py, receiver.py (the file-mutation snapshot
callback), providers/claude.py (names_file_mutation, the stream system subtypes, DRIFT),
allowlist.py, sanitize.py before writing code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji
anywhere; no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never
guess a number (measure or write "needs measuring"); unknown stays None; comments state
constraints the code cannot show; integration tests only, and you add exactly one test
file, tests/integration/test_commit_link.py. Commit messages end with these two trailer
lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W2-T6; `uv sync`; gates green (`uv run mypy .`,
`uv run ruff check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W2-T6`;
`gh pr create --base main --title "W2-T6 commit linkage and 2.1.258 drift" --body-file
docs/log/W2-T6.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULE: this task must not start any agent session that spends tokens. Verification
uses the fake agent, `telltale run -- bash -c '...'` children, and a COPY of the build's
store: `mkdir -p /tmp/w2t6home && cp ~/.telltale/telltale.db /tmp/w2t6home/telltale.db`,
`export TELLTALE_HOME=/tmp/w2t6home`; never write to ~/.telltale.

Injected facts (measured on capture cap_01M1GPSMW1ZRXVADWZPF0KZ9H3 of the copied store,
a real build session on Claude Code 2.1.258 that made one commit and one push). The
capture's repo.identity head was e2aa6a95; three file_mutation snapshots (all at head
e2aa6a95, diff_hash 52039e43..., 2 files) were taken after two Write calls; the agent then
changed more files through Bash (ruff --fix, edits by pre-commit) with no hook-visible
mutation and therefore no snapshot; the capture_end snapshot shows head 3512c938 with an
empty diff (diff_hash e3b0c442..., 0 files). No telltale.repo.commit observation was
written: `provider_reported` needs git_commit_id and no 2.1.258 tool_result carried one
(payload keys seen: command, description_length, duration_ms, event_name,
event_sequence, event_timestamp, normalization_version, service_version, success,
terminal_type, tool_input_size_bytes, tool_name, tool_result_size_bytes, tool_use_id,
file_path; E01's S8 fixture on 2.1.257 DID carry git_commit_id once, so whether 2.1.258
dropped it or the compound `git add && git commit` form hides it needs measuring, and the
build's own future captures will answer it: look for git_commit_id in any capture of
the copied store and say what you found); `_tree_match` compares a commit's (parent,
patch) with snapshot (head, diff_hash) states and had no snapshot of the final tree.
The stream on 2.1.258 emits system subtypes vcs_state_changed (1 per commit),
code_change_published, task_started (task_type local_bash for every Bash call),
task_notification, thinking_tokens, hook_started, hook_progress, hook_response: they
reach the store as claude.stream.system.<subtype> through the allowlist gate, and
vcs_state_changed is exactly the signal a commit gives. 2.1.258 OTel drift, all dropped
as unknown_field today: api_request.cost_usd_micros, organization_id, user_account_id,
user_account_uuid, user_email, user_id; a new event hook_execution_complete (with
organization_id, user_account_id, user_account_uuid, user_email); assistant_response.model,
query_source, request_id; Stop hook fields background_tasks and session_crons. Identity
fields (organization_id, user_*) stay dropped on purpose; the rest are facts. repo.py is
797 lines against the 800-line ratchet and launch.py 782: both changes move code out
before they add.
Spec 12.3 rungs, in order: explicit, provider_reported, tree_match_during,
tree_match_after, heuristic. A snapshot inside the capture whose head is the commit and
whose diff is empty is the working tree AT that commit, taken by Telltale itself: that
is a tree match (during), not temporal proximity. A snapshot after a `git commit`
command is what makes it available at commit time rather than only at capture end.

BRIEF W2-T6: commits made during a capture link through the ladder without provider
help, and the 2.1.258 drift fields are allowlisted or explicitly dropped.
OWNS: src/telltale/repo.py and a new repo_link.py (move commits_since, _link,
_tree_match, _candidates, _commit_facts, _commit_stats there first, no behaviour change,
separate commit), launch.py (snapshot trigger on commit commands and on
vcs_state_changed; if launch.py would pass 800 lines move link_commits and
reported_commits into repo_link.py), receiver.py (the mutation callback also fires on
claude.stream.system.vcs_state_changed and on a Bash tool_result whose command_norm
starts with `git commit` or contains `&& git commit`), providers/claude.py
(names_file_mutation, DRIFT entries), allowlist.py (the drift fields: cost_usd_micros
as SIZE-like scalar, assistant_response.model/query_source/request_id, Stop
background_tasks and session_crons as counts only, hook_execution_complete as
claude.otel.hook_execution_complete with hook_event_name, duration_ms, success; identity
fields not added), tests/integration/test_commit_link.py, docs/log/W2-T6.md.
READS: store.py, activities.py (repo_commit activities), facts.py, cli.py (sessions
--link-commits; change nothing there unless a signature moves).
Traps: `git worktree` captures: the snapshot runs in the child's cwd, which is the
worktree; `_candidates` walks local refs since the capture start; a rebase or amend
produces a commit whose parent is not a snapshot head (report what the ladder does with
it; do not invent a rung). The debounced snapshot (2 s) after a commit command must
complete before capture_end or the capture_end snapshot covers it; either way the
commit links. Never link by timestamp alone; the heuristic rung stays opt-in.
DO:
(1) repo_link.py: the move, then `_tree_match` accepts a snapshot state (head == sha,
    diff_hash == the empty-diff hash) as tree_match_during when the snapshot is inside
    the window and tree_match_after when it is the capture_end snapshot or later.
(2) receiver.py and providers/claude.py: the two new mutation triggers; the snapshot
    payload records trigger "commit_command" or "vcs_state_changed".
(3) allowlist.py: the drift fields; DRIFT list in claude.py updated with the 2.1.258
    facts above, each with the capture id it was measured on.
(4) test_commit_link.py: `telltale run -- bash -c 'echo x >> f && git add f && git
    commit -q -m one'` in a temporary repository (one commit made INSIDE the capture by
    the child): after the capture, exactly one telltale.repo.commit observation with
    link_confidence tree_match_during (or tree_match_after: assert which and why in the
    test), the sha equal to the repository's HEAD, and `sessions --link-commits` shows
    COMMITS 1; a commit made BEFORE the capture starts does not link; a commit made by a
    second, uncaptured process during the capture in the same worktree links only with
    `heuristic` requested (or not at all: assert the actual behaviour and explain it);
    replay Claude S8 (2.1.257, git_commit_id present) still links as provider_reported.
VERIFY (paste outputs in the report): the test's own commands run by hand with their
outputs; on the copied store, `uv run telltale sessions --link-commits --limit 8`
(the W1-T5 attempt 2 capture cannot gain a link retroactively because its worktree is
gone: say so, and show what the ladder reports for it); `uv run telltale run -- bash -c
'...'` from the test pasted; a replay of Claude S1 shows 0 unknown_field diagnostics for
the fields you allowlisted and the identity fields still dropped (sqlite3 query pasted);
pytest -m integration; all gates; break-and-restore: remove the empty-diff acceptance
and show the new test failing, then restore.
STOP if git's own behaviour makes the empty-diff state unobservable in a worktree (report
the command and its output), or if a drift field's meaning cannot be established from
the record itself (leave it dropped and say so).
REPORT: docs/log/W2-T6.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE /
NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): what the ladder is and why time alone is not a rung, the one real commit
from the test with its rung explained, and what the 2.1.258 drift means for the fixtures
captured on 2.1.257. Commits: "W2-T6 move commit linkage into repo_link.py", "W2-T6 link
commits from snapshots; 2.1.258 drift fields". Your final message (it is captured):
OUTCOME, the sessions table with the linked commit, the public signatures, and anything
UNSURE OR NOT DONE.
