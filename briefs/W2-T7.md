You are the implementer for task W2-T7 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W2-T7 (created from main). This session is itself being recorded by
`telltale run`: the build records itself. Read AGENTS.md, docs/design/01-design.md
section 6.12 ("SeriesCompiler", "Request clock", "Readiness checklist") and its
"Amendments from wave 1" section, docs/design/02-protocol.md (brief and report formats),
docs/log/W1-T5.md (the compiler: the fold, the two policies, and everything it says
about last_exit), docs/log/W2-T5.md (readiness: checks 2 and 3, and "How long a real
capture has to be"), src/telltale/series.py, src/telltale/forecast/readiness.py and
backtest.py (how a window is dropped for a None), src/telltale/forecast/__init__.py
(REQUEST_VARIANT), tests/integration/test_series.py, tests/integration/
synthetic_series.py and tests/integration/test_forecast_contracts.py before writing
code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji
anywhere; no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never
guess a number (measure or write "needs measuring"); unknown stays None and is never
zero-filled; comments state constraints the code cannot show; integration tests only,
and you add no test file: the changes go in test_series.py and test_forecast_contracts.py.
Commit messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: you are already on branch task/W2-T7; `uv sync`; gates green (`uv run mypy .`,
`uv run ruff check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W2-T7`;
`gh pr create --base main --title "W2-T7 verification columns of the request clock"
--body-file docs/log/W2-T7.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULE: this task must not start any agent session that spends tokens. No `claude -p`,
no `codex exec`. Everything is verified on replayed fixtures and on a COPY of the
build's own store.

Injected facts, measured on 2026-09-02 on a rebuilt copy of the build's store with the
W2-T5 readiness command (main eb3304f).
- Nine captured build sessions have more than the 52 model requests W2-T5 measured as
  the shortest backtestable request clock at H = 1: cap_01M1GRH92A2D99YWKC1MBF0FSC
  (175 requests), cap_01M1GRH8P1D277RBEWQJYZG1G7 (158), cap_01M1HE3XS4E3S2XTSNB99C7WQT
  (130), cap_01M1FVAAWQNWBBG9G318YE6WVM (120), cap_01M1GRSFRXES0N7ZJFYZ3BK84Q (111),
  cap_01M1GQSRAC1APK7ZM8S8G3YJTA (86), cap_01M1HE3ZDFB9WWW60Z5XZ89F7Z (80),
  cap_01M1HE3ZCX63HRX8PAWJDYSNG6 (73), cap_01M1FW3DA7M6YK37QM4KAY308H (65). W2-T5's
  report says the longest capture on this disk has 32: that sentence was wrong when it
  was written and the nine above are the measurement that replaces it.
- Every one of the nine fails readiness for every target with `windows 0 of 20` and the
  drop reason `missing_context_value(last_verification_exit)` on every planned origin
  (143 of 143 for the 175-request capture). The None cells sit in rows 0..k-1 of
  last_verification_exit, before the capture's first verification run: k is 106, 76,
  64, 50, 110, 30, 45, 17 and 63 for the nine captures in the order above. Context
  windows start at row 0 (no changepoints, fewer than 512 rows), so every window reads
  row 0 and policy exclude drops all of them. The column report of the 175-request
  capture shows 0 nulls in each of the other nine columns.
- The cause is an encoding, not a gap in observation. Design 6.12 wrote
  last_verification_exit as 0/1/None with None meaning "no verification has run yet".
  That state is observed, not unknown: the capture saw every command and none of them
  was a verification. A None there writes a known fact as an unknown, and the exclude
  policy, which is right to refuse unknowns, refuses the fact along with them. W1-T5's
  report (its last_exit section) and the wave 1 amendment in 01-design.md (the line
  beginning "Under refuse, last_verification_exit refuses nearly every capture") both
  saw the refusal and called it structural; neither changed the encoding.
- Variation on the same 175-request Claude capture: fresh_input_tokens and
  tool_calls_since_prev have scaled MAD 0 (tau 2 by the q80 rule: eight of ten requests
  carry at most 2 fresh input tokens, the cache pattern of Claude Code), output_tokens
  has scaled MAD 409.2. That is a fact about the targets and not about this task: leave
  the registry alone and state it in the report so E07 expects it.

BRIEF W2-T7: replace the request clock's last_verification_exit column by two columns
that carry the same three states with no None: verification_seen (0/1: at least one
verification_run activity precedes this row in the capture) and
last_verification_failed (0/1: the most recent verification_run before this row failed;
0 when none has run, which verification_seen disambiguates). Coverage of both is
("commands", "tool_calls") as before; when that capability is unavailable both columns
are all None, as every column is.
OWNS: src/telltale/series.py, docs/design/01-design.md (the request-clock column list
in 6.12, and a new "Amendments from wave 2 (2026-09-02)" section with this change),
src/telltale/forecast/__init__.py (the comment that names the flags),
tests/integration/test_series.py, tests/integration/test_forecast_contracts.py (only
where the column name appears), tests/integration/synthetic_series.py (only if it
names the column), docs/log/W2-T7.md.
READS: readiness.py, backtest.py, store.py, activities.py.
Traps: the fold's `_State.last_exit` is the one counter `reset` leaves alone; the two
new counters are the same kind of thing and outlive a row. `_exit`'s rule ("the
previous answer when nothing said": a verification_run whose success is unknown keeps
the previous state) stays; an unknown success does not flip last_verification_failed
and does not make it None, and verification_seen still turns 1 because a run happened.
The series_id of every capture changes (the column schema is part of it) and the series
reducer version changes: say so; series snapshots stored before this change are not
rebuilt by anything and `series list` shows both generations, which is correct. W1-T5's
refuse-policy test used row 0 of the old column as its None: it needs a real None from
somewhere else. Measure where one exists on the fixtures (the Codex S1 replay's
request_duration_ms is the candidate, which W1-T3 called partial; paste the column
report that shows its nulls); if no replayed fixture has a None cell, exercise
`--policy refuse` at the store level with a Series written the way synthetic_series.py
writes one, carrying one None, and the CLI path on a capture with no None asserting
exit 0, and say so. The readiness drop reason and the check-3 detail text name the
column; tests that assert on them assert the new name.
DO:
(1) series.py: the column table (eleven columns, the two new ones where
    last_verification_exit was), the fold state, the row.
(2) 01-design.md: the request-clock list in 6.12 reads "verification_seen (0/1),
    last_verification_failed (0/1)"; the new amendments section states the measurement
    above, the reasoning (a known state was written as unknown) and the rule it adds:
    a state the capture observed is a number, and None is reserved for what no surface
    delivered.
(3) tests: test_series.py's column assertions (the old `[None, 0, 0, 0, 0, 0, 0]` becomes
    the two columns' expected lists, derived from the S1 fixture's timeline by hand and
    explained), the refuse test as above, and any readiness or contracts test that names
    the column.
VERIFY (paste outputs in the report): on a COPY of the home store (`cp
~/.telltale/telltale.db $TELLTALE_HOME/` under a temporary TELLTALE_HOME, never the
original) `uv run telltale rebuild`, then for cap_01M1GRH92A2D99YWKC1MBF0FSC: `uv run
telltale series build --clock request --capture <id>` (eleven columns, 0 nulls in
each), `uv run telltale forecast readiness --series <id> --target output_tokens` (the
whole checklist with its verdict; 143 windows planned and the number retained),
the same for fresh_input_tokens (variation FAIL with MAD 0 is the honest answer), and
`uv run telltale forecast backtest --series <id> --target output_tokens` with the
default forecasters (the table; the numbers are not interpreted here, E07 does that);
a loop over the nine captures giving a table of capture, requests, windows planned,
windows retained, ready targets; replay S1 and `series build` (the two columns' values
by row, matched against `telltale timeline`); `series check` ok; pytest -m integration;
all gates; break-and-restore: make verification_seen stay 0 after a verification and
show which test fails, then restore.
STOP if a verification_run activity carries no success field on any fixture (report
which; the previous-value rule above is what you then report), or if the amendment
would need any column beyond the two named here.
REPORT: docs/log/W2-T7.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE /
NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): the difference between "unknown" and "known to be none", the 106-row
example, what the change does to the nine captures (a before-and-after table of
windows), and what it does not do (the targets' variation). Commit: "W2-T7 verification
columns of the request clock". Your final message (it is captured): OUTCOME, the
before-and-after table, and anything UNSURE OR NOT DONE.
