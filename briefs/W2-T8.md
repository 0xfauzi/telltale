You are the implementer for task W2-T8 of the Telltale build: a privacy defect found by
the backfill import on 2026-09-02, to be fixed and remediated. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W2-T8 (created from main). This session is itself being recorded by
`telltale run`. Read AGENTS.md, docs/design/01-design.md sections 6.3 (the "never
persisted" list), 6.4 (sanitization and COMMAND normalization) and 6.5 (the store; "no
other deletion path exists"), docs/design/02-protocol.md, docs/log/W0-T2.md and
W0-T5.md (the sanitizer and the privacy test), docs/log/W2-T1.md (normalization v2),
src/telltale/commands.py, sanitize.py, allowlist.py (every field of Kind.COMMAND),
store.py, cli.py, cli_common.py, tests/integration/test_privacy.py and conftest.py
before writing code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji
anywhere; no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never
guess a number; a test that passes against the un-fixed code is not a test (break the
fix, watch the test fail, restore it); integration tests only, and you add no test file:
the new cases go in test_privacy.py. Commit messages end with these two trailer lines
exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: branch task/W2-T8; `uv sync`; gates green (`uv run mypy .`, `uv run ruff
check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W2-T8`;
`gh pr create --base main --title "W2-T8 command normalization keeps only flag-shaped
tokens; resanitize" --body-file docs/log/W2-T8.md`, PR body ending "Generated with
Claude Code: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc" (no emoji).

STOP RULE: no agent session that spends tokens. Never write to ~/.telltale/telltale.db:
verify the remediation on a COPY under a temporary TELLTALE_HOME (the orchestrator runs
it on the real store after the merge). Never write under ~/.claude or ~/.codex.

Injected facts, measured on 2026-09-02 on the home store after the backfill import
(3152 imported captures, 37 launcher captures, 1,038,932 observations).
- The defect. A byte scan of the store for the privacy probes (`TELLTALEFAKE`,
  `sk-ant-api03-`) finds 10 observation rows, all in a COMMAND field
  (`payload.command`), across claude.stream.assistant, claude.hook.PreToolUse,
  claude.hook.PostToolUse, claude.otel.tool_decision and claude.transcript.assistant,
  in launcher captures and imported ones alike. Every one is a shell command in which
  a QUOTED STRING BEGINNING WITH "-" reached the normalizer as one shlex token, and
  commands.py `_normalize_token` keeps any token that starts with "-" verbatim after
  splitting at "=" (design 6.4's "every token starting with - with any =value
  stripped"), with no shape check. The stored forms (the probes are fake strings
  planted by E01 and E02 for exactly this purpose) include
  `... -----BEGIN TELLTALEFAKE PRIVATE KEY-----` from a heredoc that wrote a fixture,
  and `echo --- sk-ant not TELLTALEFAKE --- ; grep -rho _ fixtures/sources/claude`,
  where the whole quoted echo argument survived as one "flag". Paths, bare words and
  `=value` behaved: `<outside>/e9671acd`, `_`, `mk_rollout.py`.
- The second defect, visible in the same rows: the private-key header survived
  although sanitize.py has `_KEY_HEADER = r"-----BEGIN [A-Z0-9 ]{0,40}PRIVATE
  KEY[ A-Z0-9]{0,20}-----"`. Measure where the scrub runs: if it never sees the
  normalized command string, that is the gap; if it sees it, find why the pattern
  missed (one stored form ends `KEY---`, which may be the 512-character bound cutting
  the trailer). Report which, with the code path.
- Where the field lives. `normalization_version` is stored beside every normalized
  command (`cmdnorm-v1` on the oldest captures, `cmdnorm-v2` since W2-T1). The
  allowlist marks the fields of Kind.COMMAND; enumerate them from allowlist.py rather
  than from this list (Bash `command` on the stream, hooks, OTel tool details,
  transcript assistant, and Codex command_execution).
- The store's rule. Observations are immutable and append-only, and `purge <capture>`
  is the only deletion path. Purging the ten captures is the wrong remedy: three of
  them are the build's own launcher captures with hundreds of requests, and the same
  shape exists in any command that quoted an argument beginning with "-". A stricter
  sanitizer applied to an already-sanitized string can only remove information, so
  the remedy is one sanctioned in-place rewrite, named, versioned and recorded: that
  is what "resanitize" below is, and it is the ONE UPDATE of observations in the
  codebase, kept in store.py by a pre-commit pygrep exactly like the INSERT rule.
- Line counts on main: commands.py 358, sanitize.py 407, store.py 752, cli.py 733,
  test_privacy.py 475. store.py and cli.py are within 50 lines of the 800 ratchet: if
  either would pass it, move a coherent block out first as a separate commit with no
  behaviour change (store.py: the read helpers; cli.py: the import command group into
  cli_import.py).

BRIEF W2-T8: a token beginning with "-" survives normalization only when it is
flag-shaped; the secret scrub runs on every normalized command; `telltale resanitize`
rewrites every stored command field whose normalization_version is older than the
current one through the new rule, in place, and records what it did.
OWNS: src/telltale/commands.py (NORMALIZATION_VERSION cmdnorm-v3; the flag rule),
sanitize.py (scrub on the normalized command; the header pattern if it is the gap),
store.py (`resanitize(capture_id=None) -> dict` with the one UPDATE), cli.py
(`resanitize [capture_id]`), .pre-commit-config.yaml (the pygrep
`UPDATE\s+observations` allowed only in store.py, with a comment naming this task),
tests/integration/test_privacy.py (the new cases), docs/design/01-design.md (6.4 and
6.5 amendment lines under "Amendments from wave 2"), docs/log/W2-T8.md.
READS: allowlist.py (Kind.COMMAND fields), receiver.py, importer.py, providers/*.py.
Traps: the flag rule is a shape, not a list: `^--?[A-Za-z0-9][A-Za-z0-9_.:+-]{0,31}$`
after the `=value` split (so `-k`, `--max-turns`, `-rho`, `--output-format` survive
and `--- sk-ant not TELLTALEFAKE ---`, `-----BEGIN ...` and any token with a space
become `_`). Bare tokens keep the existing two-per-segment rule; do not widen the
change beyond the flag shape and the scrub. `classify()` reads the normal form and
must classify every fixture command the same as before: assert the golden timelines
are unchanged except where a leaked token became `_`. Resanitize re-normalizes the
STORED string (tokens are whitespace-separated; paths are already relativized; `_`
stays `_`), never anything raw, and then scrubs; it rewrites `payload` (the field and
`normalization_version`) and appends `resanitize:cmdnorm-v3` to `redaction.redacted`
when a token changed, in one transaction per capture, and writes one diagnostics row
per capture (kind `dropped`, detail "resanitize cmdnorm-vN to cmdnorm-v3: K field(s)
rewritten") only when K > 0. Idempotent: a second run rewrites 0. Every other column
of the row is untouched; observation ids never change.
DO:
(1) commands.py and sanitize.py: the flag shape; NORMALIZATION_VERSION "cmdnorm-v3";
    the scrub applied to the normalized command in the one place every COMMAND field
    passes through (find it; do not add a second call site).
(2) store.py `resanitize`, cli.py `resanitize`, the pre-commit pygrep.
(3) test_privacy.py: post through the receiver (never call parse() directly) a Bash
    tool_use whose command is `echo "--- sk-ant-api03-TELLTALEFAKE0000000000000000000000000000000000000 ---"`,
    one whose command is `python3 - <<EOF` with a heredoc body containing `-----BEGIN
    TELLTALEFAKE PRIVATE KEY-----`, one with `grep -r AKIATELLTALEFAKE00001 .`, and
    one with `git commit -m "-TELLTALEFAKE"`, on the stream and the hook surfaces;
    close; scan the db and wal bytes: none of the probes present, `-k`-style flags
    still present in the stored command; then, in a second store built under a
    monkeypatched NORMALIZATION_VERSION "cmdnorm-v2" with the old flag rule (a fixture
    written by the test through the store, or the old rule kept as a private function
    for the test to use, whichever is smaller), run `resanitize` and scan again: 0
    probes, the diagnostics row present, a second run rewrites 0.
(4) 01-design.md amendments: 6.4 (the flag shape and the scrub position, with the
    measurement above) and 6.5 (resanitize is the one UPDATE; why purge was wrong).
VERIFY (paste outputs in the report): on a COPY of the home store (`cp
~/.telltale/telltale.db $TELLTALE_HOME/`, temporary TELLTALE_HOME; the copy is 1.4 GB,
so give it disk and time): `strings $TELLTALE_HOME/telltale.db | grep -c
"TELLTALEFAKE\|sk-ant-api03-"` before (10 or more), `uv run telltale resanitize` (its
printed counts per capture and the wall time), the same grep after (0), the
diagnostics rows it wrote, a second `resanitize` (0 rewritten), and `uv run telltale
timeline <capture>` for one of the rewritten launcher captures showing the command
with `_` where the string was; the four probe commands above through a receiver on a
fresh store with the bytes scan; pytest -m integration; all gates; break-and-restore:
restore the old flag rule and show which privacy assertion fails, then fix it again.
STOP if a Kind.COMMAND field turns out not to pass through the normalizer at all
(report the surface and field; do not add a call site without saying so), or if the
resanitize of the copy takes more than 10 minutes (report the rate and stop).
REPORT: docs/log/W2-T8.md: OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT
DONE / NEXT, then EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/
Teacher.md): what a shell token is and why a quoted string can start with a dash, the
one real leaked command with the probe shown as the fake string it is, what
resanitize does and does not touch, and why the store gets one UPDATE and not a
second deletion path. Commits: "W2-T8 flag-shaped tokens and the scrub on commands",
"W2-T8 resanitize". Your final message (it is captured): OUTCOME, the before-and-after
byte counts on the copy, and anything UNSURE OR NOT DONE.
