You are the implementer for task W3-E08 of the Telltale build: an experiment that
starts no agent sessions. You are running as a headless Claude Code session inside a
git worktree of github.com/0xfauzi/telltale on branch task/W3-E08 (created from main).
This session is itself being recorded by `telltale run`. Read AGENTS.md,
docs/design/01-design.md 6.12 in full with every wave 2 and wave 3 amendment line,
docs/design/04-forecast-reading-guide.md, docs/design/02-protocol.md 7.5 (the
experiment write-up template), docs/experiments/E07.md and docs/log/W2-E07.md (the
request-clock backtests this experiment extends; its runner experiments/E07/run.py is
the machinery to reuse: copy_store, census, build_series, readiness_of, backtest_once,
pooled_row, pairs), docs/log/W3-T2.md (placebo, decide, ablate, candidate),
docs/log/W3-T1.md and W3-T4.md (the attempt and change clocks), src/telltale/forecast/
{__init__,backtest,placebo,decide,ablate,readiness}.py, cli_forecast.py,
series_lineage.py, tests/integration/test_forecast_decision.py before writing code.

House rules: as little and as simple code as possible; no emoji; no em dashes anywhere
(hyphen, colon or full stop); Python 3.12, uv only; never guess a number; every number
in the write-up traceable to a file under experiments/E08/out/; the pre-registered
constants are printed and never changed after a result. COMMIT BEFORE ANY BRANCH
TOGGLING. Commit messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: branch task/W3-E08; gates green (`uv run mypy .`, `uv run ruff check .`,
`uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m integration`,
`uv run pre-commit run --all-files`); `git push -u origin task/W3-E08`; `gh pr create
--base main --title "W3-E08 temporal validity on captured data" --body-file
docs/log/W3-E08.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc".

STOP RULES: no agent session that spends tokens. Never write to ~/.telltale/telltale.db:
the runner copies it (`.backup`, 1.4 GB) to experiments/E08/out/home and points
TELLTALE_HOME there exactly the way E07's runner does, refusing to open the real
store. Never write under ~/.claude or ~/.codex. Pilot before scale: run ONE pair
(one capture, output_tokens, H = 1) end to end, true order plus its ten placebo runs,
record the wall time, project the full run from the window counts, and STOP with the
projection in the report if it exceeds 90 minutes. Run the runner in the FOREGROUND as
an ordinary Bash tool call (up to 600000 ms per call; split the run into per-pair
invocations that resume from out/ so no single call needs more), never as a background
task, and never end your turn while it runs.

Injected facts, measured 2026-09-02:
- E07's store copy at experiments/E07/out/home is gone (its worktree was removed after
  the merge; out/ is gitignored). E07's per-capture and pooled JSON under
  experiments/E07/out/ survive and are the record of its 19 true-order runs. This
  experiment rebuilds on a fresh copy of the home store, which now holds 3,209
  captures and, since E07, the E05 and E06 sessions, W3-T0's refused-call rule and
  W3-T3's masked-exit rule, so readiness counts may differ from E07's: report both
  and say why they differ.
- E07 measured TimesFM-3 at 90.1 to 108.2 ms median per window on CPU, and the whole
  E07 run at 120.665 s wall. A placebo costs 10 model runs per pair (PLACEBO_SEEDS 5
  times two block sizes, B = max(2, H) and the B = 1 row control), each on the same
  windows as the true run, so a pair with W windows costs about 11 W model calls.
  E07's pooled H = 1 row had 1,017 windows and its H = 4 row 157; the per-capture
  runs are smaller. Project before you run.
- The commands (W3-T2): `telltale forecast readiness --series ID --target T
  [--horizon 1|4]`, `telltale forecast backtest ...`, `telltale forecast placebo
  --series ID --target T [--horizon] [--forecasters timesfm,persistence,...] --model
  timesfm [--device cpu]` (prints the label with every inequality and reuses a stored
  true-order run when one matches), `telltale forecast ablate --series ID --target T`
  on a change-clock series. `decide` withholds the label until a placebo exists.
  TimesFM needs `uv run --extra forecast`; the checkpoint is cached under
  ~/.cache/huggingface (TELLTALE_HF_CACHE); device cpu.
- Clocks: `telltale series build --clock attempt --repo <repo_id>` on the home store
  gives 23 rows (this repository, repo_id d9f783b1d318c0d65ec6b86fe729f6ff1a3581983e
  4287a71f6510eb80d84d6c) with verification_passed and accepted known on 18 of them;
  `--clock change` gives 19 rows with the three path columns known on 4. C_MIN_SHORT
  is 16 on these clocks and K_MIN 20, so at H = 1 the attempt clock has at most 23 -
  16 - 1 + 1 = 7 origins and the change clock 4: "not assessable" is the expected
  outcome and must be written with these counts, never by moving a constant. The
  registry (forecast/__init__.py TARGETS) carries the change-clock targets
  attempts_to_land, fresh_input_tokens_total, verification_cycles and the candidate
  targets; if no attempt-clock target is registered, say so and register nothing.
- The pre-registered constants: DELTA 0.10, W 0.60, K_MIN 20, C_MIN 32 (request) and
  C_MIN_SHORT 16, HORIZONS (1, 4), stride H, BASELINE_WINDOW 8, tau q80 of the first
  c_min rows, PLACEBO_SEEDS 5, PLACEBO_ROW_BLOCK 1, block B = max(2, H), demotion
  resolution 0.25. Print them in every table.
- Decision labels (design 6.12, W3-T2): not assessable; baseline sufficient (E_M >
  (1 - delta) E_B or W_MB < w); temporal evolution (the two placebo inequalities hold
  and hold in each half of the origin range); conditional prediction (otherwise;
  "level prediction: the gain is distributional" for a covariate-free variant). E07
  found all 19 pairs baseline sufficient on the true-order numbers alone.

BRIEF W3-E08: on captured data, the request clock's pairs get their placebo and their
label with the inequalities shown; the attempt and change clocks get their readiness
verdicts with the counts; the A/B/C ablation is attempted on the change clock and
refused with the count; and the write-up says what the laboratory may now claim.
OWNS: experiments/E08/ (run.py reusing E07's functions by import, out/ gitignored
except the JSON you re-include on purpose), docs/experiments/E08.md, docs/log/W3-E08.md.
READS: everything; edit nothing in src/ or tests/ (an src defect you hit is reported
in UNSURE OR NOT DONE with the command that shows it, not fixed here).
DO:
(1) run.py: copy the store; census the captures as E07 did; for every (capture,
    target, H) pair that passes readiness on the request clock, and for the pooled
    rows by cohort, run the true-order backtest with timesfm plus the four baselines
    (reuse E07's backtest_once) and then `forecast placebo` for the same pair; store
    every run through the CLI or backtest.persist; write out/<capture>/<target>-H<h>.json
    with the decision (label, every inequality with both sides, per-half values,
    E_M, E_B, E_P, W_MB, W_MP, n_windows, seeds, block sizes, the persistence
    validity check per seed, wall_ms per forecaster) and out/summary.json.
(2) The lineage clocks: build both on the copy; `forecast readiness` for every
    registered target at H = 1 and 4; the ablation attempt; record every refusal
    verbatim with the numbers it printed.
(3) docs/experiments/E08.md in the 7.5 template: the question; the decision rules
    written before the run (quote 6.12); what we did (commands, constants, the copy's
    capture counts, wall time per stage, the pilot projection against the measured
    total); what we measured (a table per target and horizon: capture, n_windows,
    E_M, E_B, E_P median and range, W_MB, W_MP, persistence under placebo against true
    order, label; the pooled rows; the lineage clocks' counts); what it means: how
    many pairs took each label, whether any placebo run was invalid; what we
    rejected; how sure (measured / reasoned / judgement / unknown per claim); what
    downstream may now say, in one paragraph, with the claim class.
(4) docs/log/W3-E08.md in the REPORT order, with EXPLAIN FOR THE OWNER in the Teacher
    register (~/.claude/output-styles/Teacher.md): what the placebo destroyed and what
    it kept, one pair's real numbers walked through the four labels, and why "not
    assessable" on the lineage clocks is a count and not a failure.
VERIFY (paste outputs): the pilot pair's wall time and the projection; `uv run
telltale forecast placebo ...` output for one pair on the copy; the persistence
validity line per seed for that pair; out/summary.json's label counts equal to the
table's; every number in E08.md found in an out/ file (a small check_numbers.py as
E07 had); pytest -m integration; every gate.
STOP if the projection exceeds 90 minutes (report it), if any placebo run reports
persistence better under shuffle after you have confirmed the true rows were not
shuffled (report the pair and the numbers), or if TimesFM cannot load (report the
error; run the baselines-only placebo and say the model column is absent).
REPORT: docs/log/W3-E08.md; commits: "W3-E08 runner", "W3-E08 temporal validity on
captured data". Final message: OUTCOME with the label counts, the total wall time, and
anything UNSURE OR NOT DONE.
