You are the implementer for task W3-E08b of the Telltale build: a pre-registered
amendment to the decision rule, then a re-labelling of E08's stored numbers under it.
No agent sessions. You are running as a headless Claude Code session inside a git
worktree of github.com/0xfauzi/telltale on branch task/W3-E08b (created from main).
This session is itself being recorded by `telltale run`. Read AGENTS.md,
docs/design/01-design.md 6.12 ("Chronology placebo", "Decision per (target, clock,
variant)") with the wave 3 amendments, docs/design/04-forecast-reading-guide.md,
docs/experiments/E08.md and docs/log/W3-E08.md (the finding: 0 valid placebos of 38,
shuffling the past makes persistence better, median ratio 0.8176), docs/gates/
wave-3.md ("Owner decisions requested", item 1: approved on 2026-09-02 as option (b)),
src/telltale/forecast/{placebo,decide,__init__}.py, cli_forecast.py,
experiments/E08/{run,aggregate,shared}.py and the JSON under experiments/E08/out/,
tests/integration/test_forecast_decision.py before writing code.

House rules: as little and as simple code as possible; no emoji; no em dashes anywhere
(hyphen, colon or full stop); Python 3.12, uv only; never guess a number; a test that
passes against the un-fixed code is not a test; integration tests only, under
tests/integration/. COMMIT BEFORE ANY BRANCH TOGGLING. Commit messages end with these
two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: branch task/W3-E08b; gates green (`uv run mypy .`, `uv run ruff check .`,
`uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m integration`,
`uv run pre-commit run --all-files`); `git push -u origin task/W3-E08b`; `gh pr
create --base main --title "W3-E08b the true-order comparison alone earns baseline
sufficient" --body-file docs/log/W3-E08b.md`, PR body ending "Generated with Claude
Code: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc".

STOP RULES: no agent session that spends tokens. Never write to ~/.telltale/telltale.db
and never under ~/.claude or ~/.codex. ORDER IS THE POINT: the amendment (design text
and code) is committed BEFORE any E08b number exists, and the report shows the two
commit hashes in that order. No model runs are needed: E08's per-pair JSON carries
E_M, E_B, E_P, W_MB, W_MP, n_windows and the validity check per seed for all 38 rows.

The amendment, pre-registered here (copy it verbatim into 01-design.md under
"Amendments from wave 3" as "6.12 (W3-E08b, pre-registered before the re-labelling,
owner-approved 2026-09-02)"):
> The placebo validity check (persistence must be worse under every placebo run)
> gates the two labels that read the placebo, temporal evolution and conditional
> prediction. It does not gate "baseline sufficient", which is a paired comparison of
> the model against the best baseline on the same true-order windows and reads no
> placebo. An invalid placebo therefore yields: "baseline sufficient" when E_M >
> (1 - delta) E_B or W_MB < w, with the warning "placebo invalid: the positive labels
> were not assessable"; otherwise "not assessable: placebo invalid" with the
> inequalities shown. "Not assessable" for n_windows < k_min is unchanged and comes
> first. E08's 38 rows are re-labelled under this rule as E08b; E08's labels stand as
> the record of the rule before the amendment.

Injected facts, measured 2026-09-02:
- decide._label already tests "baseline sufficient" before it looks at the placebo;
  the refusal lives in placebo.py (line ~250: `if not check["valid"]` writes no label
  and warns with INVALID) and in experiments/E08/run.py, which labels an invalid row
  "not assessable" itself. Find every place the validity check reaches a label (grep
  `valid` in forecast/ and cli_forecast.py) and make the amendment the one rule.
- Expected count, from E08's own aggregates (`e07_baseline_only_labels`): 35 of the
  36 per-capture pairs are baseline sufficient on the true-order numbers and 1 is not;
  the 2 pooled rows are labelled by the same terms. State this expectation in the
  report BEFORE running and then show the measured count. A mismatch is a finding,
  not something to tune away.

BRIEF W3-E08b: the amendment, its test, and E08b.
OWNS: src/telltale/forecast/placebo.py, decide.py, __init__.py (the warning string),
cli_forecast.py (the printed line), tests/integration/test_forecast_decision.py,
docs/design/01-design.md (the amendment block above; nothing else), experiments/E08b/
(run.py reading experiments/E08/out/*.json, out/summary.json re-included in git),
docs/experiments/E08b.md, docs/log/W3-E08b.md.
DO:
(1) Commit 1, "W3-E08b pre-register the amendment": the 01-design.md block and the
    code, with two tests: a true-order run that is baseline sufficient plus an invalid
    placebo yields "baseline sufficient" with the warning; a true-order run where the
    model beats the baselines plus an invalid placebo yields "not assessable: placebo
    invalid" with the inequalities. Break-and-restore: revert the placebo.py change
    and show which test fails.
(2) Commit 2, "W3-E08b re-label E08 under the amendment": experiments/E08b/run.py
    applies `decide` (the same function the CLI uses, not a re-implementation) to
    E08's stored per-pair terms and writes out/summary.json with the label counts,
    every row's inequalities, and the per-half values; docs/experiments/E08b.md in
    the 7.5 template (question; the rule as pre-registered, quoting the block and
    commit 1's hash; what we did; what we measured, the 38 rows in a table with label
    and the two inequalities; what it means; what we rejected: "run the placebos
    again" (nothing about them changes) and "drop the validity check" (the positive
    labels still need a control); how sure; what downstream may now say: the model
    does not beat rolling_median on output_tokens per request, on 35 of 36 captures
    and both pooled rows, claim class predictive, coverage observed, with the caveat
    that this target carries no recency).
VERIFY (paste): the two commit hashes in order; the two tests' names; the
break-and-restore; `uv run python experiments/E08b/run.py` output with the label
counts; `uv run telltale forecast placebo` is NOT re-run (say so); pytest -m
integration; every gate.
STOP if the E08 JSON lacks a term `decide` needs (report which and for how many rows;
label those rows "not assessable: term missing" and say so).
REPORT: docs/log/W3-E08b.md in the REPORT order with EXPLAIN FOR THE OWNER in the
Teacher register (~/.claude/output-styles/Teacher.md): why a control that fails can
still leave one comparison standing, with one row's real numbers, and why the
amendment had to be written down before the numbers were looked at. Final message:
the label counts and anything UNSURE OR NOT DONE.
