You are the implementer for task W3-T0 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W3-T0 (created from main). This session is itself being recorded by
`telltale run`. Read AGENTS.md, docs/design/01-design.md 6.5, 6.10 and 6.11 with the
wave 2 amendments, docs/design/02-protocol.md, docs/log/W2-T4.md (cohorts),
docs/log/W2-E05.md (the two reducer findings, quoted below), docs/log/W2-T8.md
(store_reads.py), src/telltale/cohorts.py, store_reads.py, store.py, activities.py,
correlate.py, measures.py, measures_spec13.py, importer.py,
tests/integration/{conftest,fake_agent,test_compare,test_capture_to_summary}.py before
writing code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji; no
em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never guess a
number: measure it and paste the measurement; a test that passes against the un-fixed
code is not a test (break the fix, watch it fail, restore it); integration tests only,
under tests/integration/. Commit messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: branch task/W3-T0; `uv sync`; gates green (`uv run mypy .`, `uv run ruff
check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W3-T0`;
`gh pr create --base main --title "W3-T0 reads that scale with the capture; refused
calls are not test runs" --body-file docs/log/W3-T0.md`, PR body ending "Generated
with Claude Code: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc".

STOP RULE: no agent session that spends tokens. Never write to ~/.telltale/telltale.db:
measure on a COPY (`sqlite3 ~/.telltale/telltale.db ".backup <copy>"`, 1.43 GB, under a
temporary TELLTALE_HOME). Never write under ~/.claude or ~/.codex.

Injected facts, measured 2026-09-02:
- `telltale vector <capture>` takes 10.9 s on the home store (3190 captures, 1.04
  million observations) and took 245 ms at 36 captures (W2-T4). `cohorts._members`
  reads every capture's payloads to compute its cohort keys (provider, runtime_major,
  model, content_level), each from a handful of observations per capture (the session
  block, the environment fingerprint) out of hundreds. The read path is
  `store_reads.Reads.observations(capture_id)`, which returns every observation of the
  capture. The observations table has the (capture_id, observation_id) index; store.py
  line 447 records what T8 measured about it. Whether an index on observation_type or
  on (observation_type, capture_id) exists: check the DDL (`sqlite3 <copy> .schema
  observations`) and paste it.
- Finding 1 (W2-E05): `failed_test_runs` counts a refused tool call as a failed test.
  All five E05 pilot captures report agent_test_runs 3 and failed_test_runs 3, and no
  test ever ran: `activities._tool_outcome` takes success from OTel tool_result.success,
  from a PostToolUseFailure hook, or from the stream's is_error; a permission denial
  sets is_error by the same route. The distinction is in the capture: three
  `claude.stream.system.permission_denied` observations per capture, one per refused
  tool_use id, and `permission_denials` in the stream result. Look at the real rows in
  the copy: captures cap_01M1HKD4YPR80ZR2GVPNGTJ8AR .. cap_01M1HKFNEEBBAVH30CEKW3VBPJ
  (task_id E05). A verification_run that never executed is not a run; it is a refused
  call, and "0 test runs, 3 refused calls" is the true statement.
- Finding 2 (W2-E05): `correlate.USAGE_KEYS` uses the OTel spellings; the stream spells
  two of them `cache_read_input_tokens` and `cache_creation_input_tokens`
  (`STREAM_USAGE_KEYS` exists in correlate.py and `conflicts()` already uses it), so a
  stream-only capture (the fake agent, any capture without OTel api_request) has
  cache_read_tokens and cache_creation_tokens None at coverage partial when the
  stream carried them. `activities._request` (line 351) and line 688 fill from
  USAGE_KEYS only.
- Finding 3 (wave 2 gate): the importer writes diagnostics of kind `dropped` for the
  transcript line kinds it does not parse (attachment, last-prompt, queue-operation,
  atis-latch; 1802 rows on the home store) with wording that reads like the receiver's
  queue drops. The kind vocabulary is CHECK-constrained and stays; the detail text must
  say what it is: "importer: unparsed line kind <kind>: <n>".
- Ratchets (800 lines): activities.py 730, store.py 796, cohorts.py 489, correlate.py
  422, importer.py 717, measures_spec13.py 649, fake_agent.py (measure). store.py may
  not take more than 4 lines; if an index needs a migration, `CREATE INDEX IF NOT
  EXISTS` at open is the whole mechanism, and existing stores get it on next open.

BRIEF W3-T0: reads scale with the capture, not the store; a refused tool call is not a
verification run; stream usage keys fill the usage fields; importer diagnostics say
what they are.
OWNS: src/telltale/store_reads.py, store.py (DDL only), cohorts.py, activities.py,
correlate.py, measures.py or measures_spec13.py (only if a count is added), importer.py
(the detail text), tests/integration/fake_agent.py (a `--deny` mode), test_compare.py,
test_capture_to_summary.py, test_import.py (the wording), fixtures/golden/* (only lines
that change, each explained), docs/design/01-design.md (amendment lines under a new
"Amendments from wave 3" heading), docs/log/W3-T0.md.
READS: everything else. Do not touch series.py, forecast/, cli.py, report.py: W3-T1 and
W3-T2 own them this wave.
DO:
(1) Measure first, on the copy: `time uv run telltale vector <a build capture>` and
    `EXPLAIN QUERY PLAN` of the statements `_members` runs; paste both. Then the
    mechanism: a read that returns only the observation types a caller names
    (`Reads.observations(capture_id, types=(...))` or a `Reads.by_type(...)`), backed by
    an index the query plan actually uses (paste the plan after), and `_members` and
    `cohort_keys` reading only what they need. Acceptance, stated before the change:
    `telltale vector` under 1.0 s on the 3190-capture copy, and the wall time at 36
    captures (a temporary home with the S1/S4 fixtures replayed 36 times, or the
    test_compare cohort fixture) within 2x of its 245 ms. Paste both.
(2) Refused calls: a tool_call whose tool_use id has a permission_denied observation
    (or whose stream result is a denial: find every shape in the E05 rows) gets
    `outcome: "refused"` and `executed: false` in its fields; no verification_run
    activity is built for it; the command's category is still classified. Measures:
    agent_test_runs and failed_test_runs count executed runs only; a new derived
    Evidence `refused_tool_calls` (count, coverage from the stream surface) goes in the
    summary's work block, NOT in the spec 13.7 vector (22 stays 22). The five E05 pilot
    captures on the copy, after `telltale rebuild`, must show agent_test_runs 0,
    failed_test_runs 0, refused_tool_calls 3.
(3) Usage keys: fill from STREAM_USAGE_KEYS when the OTel key is absent, in both places;
    a fake-agent capture then has cache_read_tokens non-None at the coverage the
    stream surface earns (say which). Goldens: regenerate only what changes, explain
    each changed number.
(4) fake_agent.py `--deny`: emits, for one Bash tool_use, the same stream lines a real
    denial produces (copy the shape from the sanitized E05 rows and the provider's
    parser; if the raw line shape is not recoverable from the store, take it from the
    transcript of session 2761a993-8865-47f5-9f4e-080fa00be634 under
    ~/.claude/projects/-Users-wumpinihussein--telltale-worktrees-E05-E05-1/, read-only).
    Tests: a `--deny` capture through the launcher shows one refused tool_call, zero
    verification runs, refused_tool_calls 1; the vector test suite still passes; the
    cohort tests still pass at the same counts.
(5) importer.py wording plus its test line; 01-design.md amendments for 6.5 (typed
    reads), 6.10 (refused calls), 6.11 (refused_tool_calls in the work block).
VERIFY (paste outputs): the two timings and two query plans; `telltale show` on one
E05 pilot capture on the copy after rebuild (agent_test_runs 0, refused_tool_calls 3);
`telltale show` on a fake-agent capture (cache_read_tokens not null); `uv run telltale
run --provider claude -- python tests/integration/fake_agent.py --output-format
stream-json --deny` then show; pytest -m integration; every gate; break-and-restore for
(1) (remove the type filter: the timing test fails), (2) (restore is_error as the only
route: the refused test fails), (3) (restore USAGE_KEYS only: the fake-agent usage test
fails).
STOP if the query plan cannot use an index without changing the observations DDL
beyond one CREATE INDEX (report the plan), or if a denial shape cannot be recovered
from the store or the transcript (report what the rows hold).
REPORT: docs/log/W3-T0.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE
OR NOT DONE / NEXT, then EXPLAIN FOR THE OWNER in the Teacher register
(~/.claude/output-styles/Teacher.md): why reading a whole capture to learn its model
name gets slower with every session recorded, what a refused call is and why "0 test
runs, 3 refused calls" is the honest sentence, and one real number for each. Commits:
"W3-T0 typed reads and the index the plan uses", "W3-T0 a refused call is not a run",
"W3-T0 stream usage keys and importer wording". Final message: OUTCOME, the two
timings, anything UNSURE OR NOT DONE.
