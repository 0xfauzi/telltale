You are the implementer for task W3-T1 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W3-T1 (created from main). This session is itself being recorded by
`telltale run`. Read AGENTS.md, docs/design/01-design.md 6.3 (external.* and
telltale.repo.commit), 6.8, 6.10, 6.12 "SeriesCompiler", "Attempt clock", "Change
clock" (the columns are INJECTED there: use them verbatim), the wave 2 amendments,
docs/design/02-protocol.md, docs/spec/telltale-architecture.md sections 10.3 and 12,
docs/log/W1-T5.md (request clock), W2-T6.md (commit linkage), W2-T5.md (policies),
src/telltale/series.py, repo.py, activities.py, correlate.py, store.py, store_reads.py,
cli.py, cli_common.py, cli_forecast.py, launch.py (how --task-id/--attempt become
external.correlation), experiments.py (`_correlate`, `_outcome`, `_external`: how the
runner posts correlations and outcomes), allowlist.py (external.correlation,
external.outcome, telltale.repo.commit fields), tests/integration/{conftest,fake_agent,
test_series,test_commit_link,test_experiments}.py before writing code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji; no
em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never guess a
number; unknown stays None and nothing zero-fills; a test that passes against the
un-fixed code is not a test; integration tests only, under tests/integration/. Commit
messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: branch task/W3-T1; `uv sync`; gates green (`uv run mypy .`, `uv run ruff
check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W3-T1`;
`gh pr create --base main --title "W3-T1 attempt and change clocks; telltale outcome"
--body-file docs/log/W3-T1.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc".

STOP RULE: no agent session that spends tokens: the fake agent only. Never write to
~/.telltale/telltale.db (read it for shapes if useful; a copy under a temporary
TELLTALE_HOME for anything else). Never write under ~/.claude or ~/.codex.

Injected facts, measured 2026-09-02:
- series.py is 550 lines. `build(store, clock, capture_id, missingness_policy)` builds
  the request clock and refuses "attempt" and "change" by name at lines 159-160 ("the
  {clock} clock is not built yet (W3-T1)"); test_series.py line 299 asserts that
  refusal and must be replaced, not deleted. The request clock is a prefix fold
  (`_fold`, `_State`) over one capture's activities; `check()` verifies
  `max(provider_ts of provenance) <= row_end_ts` and non-decreasing row_end_ts;
  `series_id` is the sha256 of clock, cohort, columns, reducer_version and rows.
- An attempt is a capture with an external.correlation carrying task_id and attempt
  (activities.py builds a "correlation" activity per such observation, and an
  "outcome" activity per external.outcome; a "repo_commit" activity per
  telltale.repo.commit with link_confidence in {explicit, provider_reported,
  tree_match_during, tree_match_after, heuristic}). The launcher writes the
  correlation from `--task-id X --attempt N --experiment E` (every build capture of
  this repository has one: 37 launcher captures, task ids like W2-T4, attempts 1-3),
  and the experiment runner posts correlations and outcomes through
  `/v1/correlations` and `/v1/outcomes` (experiments.py `_external`). NO build capture
  has an outcome yet: the merge protocol has not posted any. The E05 pilot captures
  (task_id E05, attempts 1-5) each have one external.outcome (kind
  mechanical_verification, status fail).
- The attempt clock (design 6.12): one row per attempt in a repository lineage, in
  start order; columns attempt_of_component, model_requests, fresh_input_tokens_total,
  compactions, duration_ms, verification_passed (0/1 from outcome kind
  mechanical_verification), review_fail_count (adversarial_review), accepted (0/1 from
  merge_decision; the join key to the change clock), env_changed. c_min 16 on this
  clock and on the change clock (forecast/__init__.py C_MIN is 32 for the request
  clock; the TargetSpec registry for the new clocks belongs to W3-T2: do not edit
  forecast/).
- The change clock: one row per change landed on the tracked base; columns
  files_changed, lines_added, lines_removed, subsystems_touched (distinct top-level
  directories), test_files_changed, dependency_delta (lockfile or manifest changed),
  attempts_to_land, fresh_input_tokens_total, cache_read_tokens_total, compactions,
  verification_cycles, edit_turnover_ratio, stable_state_intervals, unique_files_read
  (summed over the attempts that landed it), env_changed. A change row needs a
  repo_commit activity with link_confidence explicit, provider_reported or
  tree_match_during; tree_match_after and heuristic links produce rows flagged
  low_confidence in row_meta, which the backtester excludes by default (W3-T2 reads the
  flag; you set it). Temporal proximity alone is never a link.
- repo.py (609 lines) has identity, snapshot, overlaps and the git helpers; the commit
  linkage lives where W2-T6 put it (find it: `grep -n link_confidence src/telltale/`).
  The per-commit numbers a change row needs (files, additions, deletions, paths for
  subsystems and test files, lockfile or manifest paths) come from the
  telltale.repo.commit payload as stored; if a needed field is not in that payload,
  the column is None with coverage unavailable and the report says which field is
  missing. Do not read git at series-build time: a series is rebuildable from the
  store alone.
- cli.py is 764 lines against the 800 ratchet and cli_forecast.py 173, cli_common.py
  42. Before adding `outcome` and the clock options: move the `import` command group
  out of cli.py into cli_import.py as a separate commit with no behaviour change
  ("W3-T1 move the import commands out of cli.py"), the way cli_forecast.py was split.
- `telltale outcome --kind mechanical_verification|adversarial_review|merge_decision|
  revert_or_repair|runtime_signal --status <s> --task-id X --attempt N [--categories
  a,b] [--external-run-id ID]` posts to a running receiver when one is given
  (`--receiver http://127.0.0.1:PORT`) and otherwise appends through the store of
  TELLTALE_HOME: the only CLI write path besides run and import. The observation goes
  to the capture that carries the matching correlation (task_id, attempt), and is
  refused with the reason when no capture or more than one capture carries it
  (absence is not zero and duplicate is not one).
- Data for VERIFY: the fake agent (tests/integration/fake_agent.py, `--seed` changes
  its tool calls; the launcher `telltale run --provider claude --task-id T --attempt N
  -- python tests/integration/fake_agent.py --output-format stream-json`).

BRIEF W3-T1: `series build --clock attempt|change` for a repository lineage, the
low_confidence flag, and `telltale outcome`, so the build's own merge protocol can
record outcomes from now on.
OWNS: src/telltale/series.py (or a new series_lineage.py if series.py would pass 800:
it is at 550), cli.py, cli_import.py (new, the moved import group), cli_common.py,
report.py only for a `series build` print if needed, tests/integration/test_series.py
(extend; a new test_outcome.py is allowed under tests/integration/),
docs/design/01-design.md (amendment lines under "Amendments from wave 3"),
docs/log/W3-T1.md.
READS: activities.py, correlate.py, repo.py, store.py, store_reads.py, forecast/*
(never edit: W3-T2 owns forecast/ and report.py's forecast text this wave; W3-T0 owns
store_reads.py, activities.py, correlate.py, cohorts.py this wave, so if you need a
read helper, add it in series.py and say so in the report).
DO:
(1) cli_import.py split (separate commit, no behaviour change; `telltale import ...`
    output byte-identical on the fixture import).
(2) series.py: `build(store, clock, key, missingness_policy)` where key is a capture id
    on the request clock and a repo_id on the attempt and change clocks (keep the
    request-clock behaviour byte-identical: test_series.py stays green except the
    refusal test, which becomes a test that the two clocks build). Attempt rows from
    the captures whose repo identity has that repo_id and which carry a correlation,
    sorted by capture start; the prefix-fold rule holds (row i sees only activities at
    positions <= i; outcomes observed later are recorded at the row where they were
    observed, never back-filled). Change rows from repo_commit activities on the
    tracked base with the link ladder above; attempts_to_land from the correlations
    of the captures linked to the commit. `check()` covers both clocks. `series_id`
    unchanged in form. Every column's coverage from the capabilities of the captures
    it read; None where nothing observed.
(3) cli.py: `series build --clock request|attempt|change (<capture>|--repo
    <repo_id>) [--policy exclude|refuse]` printing series_id, rows, columns with
    coverage, changepoints, dropped rows with reasons, and low_confidence row count;
    `telltale outcome` as specified; `sessions` output unchanged.
(4) Tests, through the launcher and the receiver, never by writing activities by hand:
    six fake-agent attempts over two task ids (the launcher's --task-id/--attempt),
    outcomes posted with `telltale outcome` (some through a running receiver, some
    through the store), then `series build --clock attempt --repo <id>`: 6 rows,
    verification_passed and accepted filled where posted and None where not, in start
    order; the change clock from a temporary repository with three commits linked
    through the launcher's commit linkage (W2-T6's test_commit_link.py shows how) plus
    one commit that only tree_match_after links: 3 rows plus one flagged
    low_confidence; `check` finds nothing; an outcome for a (task_id, attempt) nobody
    carries is refused with the reason, and one two captures carry is refused naming
    both capture ids.
(5) 01-design.md amendments: 6.12 "Attempt clock" and "Change clock" with what was
    measured (which repo_commit fields exist; which columns are None for which reason
    on the build's own captures), and 6.13 for `outcome` and the split.
VERIFY (paste outputs): `uv run telltale series build --clock attempt --repo <this
repository's repo_id>` on a COPY of the home store (rows = the number of launcher
captures with a correlation; verification_passed all None: no outcomes exist yet; say
so); the same for `--clock change` (rows = commits linked at explicit,
provider_reported or tree_match_during confidence; paste the count and the
low_confidence count); `uv run telltale series check <id>` ok on both; `uv run
telltale outcome --kind merge_decision --status merged --task-id W2-T8 --attempt 1` on
the copy, then the attempt series shows accepted 1 on that row; pytest -m integration;
every gate; break-and-restore: let a change row read an activity after its row_end_ts
(check names the row), and drop the duplicate refusal (the duplicate test fails).
STOP if a column of the attempt or change clock cannot be built from activities without
a new observation type or a git call at build time (report the column and what it
needs; leave it None with coverage unavailable and the reason in column_report).
REPORT: docs/log/W3-T1.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE
OR NOT DONE / NEXT, then EXPLAIN FOR THE OWNER in the Teacher register
(~/.claude/output-styles/Teacher.md): what a clock is and why three of them exist,
what an attempt row and a change row hold with one real row of each from the build's
own store, why an outcome observed later is recorded where it was observed, and what
`telltale outcome` is for. Commits: "W3-T1 move the import commands out of cli.py",
"W3-T1 attempt and change clocks", "W3-T1 telltale outcome". Final message: OUTCOME,
the row counts on the copy, anything UNSURE OR NOT DONE.
