You are the implementer for task W3-T2 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W3-T2 (created from main). This session is itself being recorded by
`telltale run`. Read AGENTS.md, docs/design/01-design.md 6.12 in full ("Pre-registered
constants", "Rolling-origin backtester", "Chronology placebo", "Decision per (target,
clock, variant)", "A/B/C ablation", "One-step candidate conditioning", "Forecast
integration tests") plus the wave 2 amendments, docs/design/02-protocol.md,
docs/experiments/E07.md and docs/log/W2-E07.md (how the labels were computed ad hoc
there), docs/log/W1-T6.md and W2-T5.md, src/telltale/forecast/{__init__,baselines,
backtest,readiness,timesfm}.py, cli_forecast.py, cli_common.py, series.py (the Series
shape; do not edit it), store.py (`put_forecast_run`, the forecast_runs CHECKs:
ordering in true|placebo_block|placebo_row, claim_class predictive),
tests/integration/{conftest,synthetic_series,test_forecast_contracts}.py before writing
code.

House rules: as little and as simple code as possible; stdlib only outside
forecast/timesfm.py (numpy stays behind the extra; baselines and placebo are pure
Python); no emoji; no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv
only; never guess a number; unknown stays None; a test that passes against the un-fixed
code is not a test; integration tests only, under tests/integration/. Commit messages
end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: branch task/W3-T2; `uv sync`; gates green (`uv run mypy .`, `uv run ruff
check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`, and the forecast-isolation hook);
`git push -u origin task/W3-T2`; `gh pr create --base main --title "W3-T2 placebo,
decision rule, ablation, candidate protocol" --body-file docs/log/W3-T2.md`, PR body
ending "Generated with Claude Code: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc".

STOP RULE: no agent session that spends tokens. Never write to ~/.telltale/telltale.db
(a copy under a temporary TELLTALE_HOME if you want real series). Never write under
~/.claude or ~/.codex. TimesFM is not needed for any VERIFY line here: baselines and
the stub only; do not download or load the checkpoint.

Injected facts, measured 2026-09-02:
- forecast/__init__.py holds the pre-registered constants: DELTA 0.10, W 0.60, K_MIN
  20, C_MIN 32, HORIZONS (1, 4), BASELINE_WINDOW 8, THRESHOLD_RULE q80_first_c_min,
  MAX_CONTEXT 512, QUANTILE_LEVELS 0.1..0.9, POINT_INDEX 4, ALARM_INDEX 7,
  REQUEST_VARIANT "request_past_only", BASELINE_NAMES (persistence, rolling_median,
  rolling_mean, local_drift), ECHO stub, DEFAULT_FORECASTERS; `Window`, `Forecaster`
  protocol, `TargetSpec`, `make(name, device)`. Missing there: placebo constants
  (B = max(2, H), R = 5 seeds, and the B = 1 control), the demotion resolution 0.25,
  c_min 16 for the attempt and change clocks, and TargetSpec entries for the change
  clock targets attempts_to_land, fresh_input_tokens_total, verification_cycles (the
  A/B/C targets) and for the candidate targets. Add them there; changing a constant
  after a result needs a new experiment id, so every one is printed in every report.
- backtest.py is 727 lines against the 800 ratchet: 73 lines of headroom, which is not
  enough for anything here. New modules: forecast/placebo.py (block_shuffle and the
  placebo runs), forecast/decide.py (the decision function), forecast/ablate.py,
  forecast/candidate.py. `backtest.run(store, series, target, variant, forecasters,
  ordering="true")` at line 123 produces the ForecastRun dict that `persist` writes;
  `metrics` at 418; `report` at 660 renders the text. readiness.py 434, cli_forecast.py
  173 (`add_commands(subcommands)`, `series`, `forecast`), cli_common.py 42 (`store()`,
  `known()`, `refuse()`, REFUSED 2).
- E07 (docs/experiments/E07.md) labelled 19 (target, horizon) pairs "baseline
  sufficient" with the rule "E_M > 0.9 E_B or W_MB < 0.6" computed by its runner, and
  never ran a placebo. Your decide() replaces that computation; do not re-run E07 (W3-E08
  will); the forecast_runs rows it stored are true-order runs your placebo command can
  pair with.
- The ablation needs a change-clock Series with the A/B/C columns (files_changed,
  lines_added, lines_removed, subsystems_touched, test_files_changed, dependency_delta;
  fresh_input_tokens_total, cache_read_tokens_total, compactions, attempts_to_land,
  env_changed; verification_cycles, edit_turnover_ratio, stable_state_intervals,
  unique_files_read). W3-T1 is building the real change clock in parallel and you must
  not depend on it or edit series.py: tests/integration/synthetic_series.py writes a
  synthetic Series into a store (test_series.py line 310 round-trips it), so extend it
  to write a change-clock series with these columns and a `clock` of "change".
- report.py is owned by W3-T1 this wave; the forecast text lives in forecast/ (the
  existing `backtest.report`). The word refusal (cause, impact, would; ADR-014) is a
  check in the forecast text renderer that raises before anything is printed or stored,
  applied to every forecast report string this package produces, including the
  assumptions and the mandatory candidate sentence.

BRIEF W3-T2: block-shuffle placebo, the decision function with its inequalities, the
A/B/C ablation runner, the one-step candidate protocol, contract test 3, and the
owner's reading guide.
OWNS: src/telltale/forecast/{__init__,placebo,decide,ablate,candidate}.py, backtest.py
(only to call the new modules; it may not grow past 800), cli_forecast.py (`forecast
placebo`, `forecast ablate`), tests/integration/test_forecast_contracts.py (test 3 and
the placebo and decision tests), tests/integration/synthetic_series.py (the change-clock
writer), docs/design/04-forecast-reading-guide.md, docs/design/01-design.md (amendment
lines under "Amendments from wave 3"), docs/log/W3-T2.md.
READS: series.py, store.py, cli.py, report.py (never edit).
Traps: the placebo shuffles the CONTEXT only; origin, test rows and actuals stay true,
so every placebo window is paired with its true-order twin by origin. Baselines run on
the shuffled contexts too, and persistence must get worse; if it does not, the placebo
is broken and the run is invalid (refuse, do not label). The decision function takes
numbers and returns the label plus every inequality it evaluated with the values on
both sides; the report prints them. "temporal evolution" requires the two inequalities
to hold separately in each half of the origin range. A covariate-free variant that
lands on "conditional prediction" says "level prediction: the gain is distributional".
The candidate protocol refuses attempts_to_land as a target and carries the sentence
"The difference between the conditioned and unconditioned forecast measures how much
the candidate's known features change the forecast; it is not the effect of merging
the candidate, because only one future is observed." in assumptions; the renderer's
word refusal must accept that sentence (it contains none of the three words) and
reject a report containing "would".
DO:
(1) placebo.py: `block_shuffle(rows, block, seed)` (consecutive blocks of `block` whole
    rows, permuted with random.Random(seed), concatenated: the multiset of rows and the
    contemporaneous covariate-target relations are preserved exactly); `run(store,
    series, target, variant, forecasters, block, seeds)` producing one forecast_runs row
    per (ordering placebo_block or placebo_row, seed) with the same origins as the true
    run, the per-window records paired by origin, and the persistence check.
(2) decide.py: `decide(true_run, placebo_runs, baselines) -> Decision` with label in
    {not assessable, baseline sufficient, temporal evolution, conditional prediction},
    the values E_M, E_B, E_P, W_MB, W_MP, n_windows, k_min, and the inequalities with
    both sides, plus the per-half values; the level-prediction wording; JSON-able.
(3) ablate.py: A, B, C variants on a change-clock Series, identical origins and actuals
    across variants, target excluded from its own covariates, all covariates past-only,
    at most 15 variates, the "adds temporal information" rule (E_C <= (1 - delta)
    min(E_A, E_B) and W >= w) else "diagnostic only", the winning variant's placebo
    reported beside it.
(4) candidate.py: `conditioned(store, series, target, origin_range, a_block)`: the
    unconditioned run (context [ctx_start, o), H = 1, past-only covariates) and the
    conditioned run (same context and origin plus row o's A block as past-future
    covariates of length n_ctx + 1 with edge padding), paired by origin, the paired
    median difference in MAE and pinball loss, each run's calibration, the sentence,
    the forbidden target, the origin limit `o <= N - 3` for rework_within_3.
(5) cli_forecast.py: `forecast placebo --series <id> --target <t> [--forecasters ...]`
    (runs true order if no stored true run matches, then the placebos, stores every
    run, prints the decision with its inequalities and the constants) and `forecast
    ablate --series <change series id> --target <t>`; `forecast backtest` prints "placebo
    not run: label withheld" until a placebo exists for the pair.
(6) Contract test 3 in test_forecast_contracts.py, through the store and the CLI where
    it can be: (a) a stub worse than persistence is labelled baseline sufficient; (b)
    a stub better than the baselines but unchanged under placebo is labelled conditional
    prediction (mechanism: a stub whose forecast is a function of the multiset of its
    context, such as the full-context mean, on an i.i.d. noise series where the
    window-8 baselines lose to the full-context mean; the placebo cannot change a
    multiset function, so E_P = E_M; say in the test why this is the right stub); (c)
    the persisted rows contain missingness_policy, the placebo definition (block, seeds),
    the baseline outputs, the license line, claim_class predictive, and for a candidate
    run the sentence; (d) `block_shuffle` on a seeded random walk yields the same sorted
    rows and persistence MAE on shuffled contexts exceeds true order; (e) a report text
    containing "would" raises, and the candidate sentence passes.
(7) docs/design/04-forecast-reading-guide.md for the owner in the Teacher register:
    how to read a backtest table, a placebo table and an ablation table; what each
    label licenses one to say and what it does not; one worked example with the
    synthetic series' real numbers. 01-design.md amendments for 6.12 (module layout,
    the constants added, anything measured that differs from the text).
VERIFY (paste outputs): `uv run telltale forecast placebo` on the synthetic 200-row
request series with the stub and baselines (persistence worse under every seed; the
label printed with the inequalities and the constants); `uv run telltale forecast
ablate` on the synthetic 60-row change series (A, B, C tables paired per window, the
rule's verdict, the placebo of the winner beside it); pytest -m integration (contract
test 3 passes; name the tests); a report string containing "would" raises; every gate;
break-and-restore: shuffle the test rows too (test (d) fails), drop the per-half clause
(a test must fail: write it so it does), let attempts_to_land through as a candidate
target (the refusal test fails).
STOP if persistence is not worse under placebo on the seeded random walk after you
have checked the shuffle touches only the context (report the numbers), or if a
decision requires a number the ForecastRun rows do not carry (report which).
REPORT: docs/log/W3-T2.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE
OR NOT DONE / NEXT, then EXPLAIN FOR THE OWNER in the Teacher register
(~/.claude/output-styles/Teacher.md): what a placebo is here and why shuffling the past
is the honest control, what each of the four labels means with the synthetic series'
real numbers, and why "would" is refused. Commits: "W3-T2 placebo and the decision
function", "W3-T2 ablation and the candidate protocol", "W3-T2 reading guide". Final
message: OUTCOME, the labels the synthetic runs earned with their inequalities,
anything UNSURE OR NOT DONE.
