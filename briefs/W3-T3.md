You are the implementer for task W3-T3 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W3-T3 (created from main). This session is itself being recorded by
`telltale run`. Read AGENTS.md, docs/design/01-design.md 6.10, 6.11, 6.12 "Request
clock" and the wave 2 and wave 3 amendment blocks, docs/design/02-protocol.md,
docs/log/W3-T0.md (refused calls; the index measurement; its UNSURE list), docs/log/
W2-E05.md and docs/experiments/E05.md (the piped pytest finding), docs/log/W2-T7.md
(the Codex request_duration_ms finding), src/telltale/activities.py, commands.py,
correlate.py, series.py, measures.py, measures_spec13.py, store.py (DDL and indexes),
providers/codex.py (turn timestamps), tests/integration/{fake_agent,
test_capture_to_summary,test_series}.py before writing code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji; no
em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never guess a
number; unknown stays None and nothing zero-fills; a test that passes against the
un-fixed code is not a test (break the fix, watch it fail, restore it); integration
tests only, under tests/integration/. COMMIT BEFORE ANY BRANCH TOGGLING: the previous
task lost uncommitted work to `git checkout HEAD --`. Commit messages end with these
two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: branch task/W3-T3; `uv sync`; gates green (`uv run mypy .`, `uv run ruff
check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W3-T3`;
`gh pr create --base main --title "W3-T3 the reducer says unknown where it cannot know"
--body-file docs/log/W3-T3.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc".

STOP RULE: no agent session that spends tokens. Never write to ~/.telltale/telltale.db
(a `.backup` copy under a temporary TELLTALE_HOME for measurements). Never write under
~/.claude or ~/.codex. Two other implementers run in parallel: W3-T2 owns forecast/,
cli_forecast.py, tests/integration/synthetic_series.py and test_forecast_contracts.py;
W3-T4 owns launch.py, repo.py, allowlist.py, series_lineage.py and their tests. Do not
edit any of those. Write your design amendment lines to a NEW file
docs/design/amendments/W3-T3.md (the orchestrator folds them into 01-design.md at the
gate), never to 01-design.md.

Injected facts, measured 2026-09-02:
- Piped verification. Every E05 re-run session ran `uv run pytest 2>&1 | tail -50`
  first, and the tests failed, yet the verification_run's outcome is "ok" and
  fail_to_pass_cycles is 0 on all five captures where the truth is 1. The tool's exit
  code is the pipeline's last command's, and providers/claude.py derives an exit code
  only from the "Exit code N" text of an is_error result (`_EXIT_CODE`, line 72;
  `exit_code_source` "stream_text"), so a masked failure looks like success. The normal
  form keeps the structural tokens (`_STRUCTURAL = "<>&|;()"`), so the shape is visible
  after normalization: `uv run pytest _ >& _ | tail -50`. Rule: the exit status of a
  chain belongs to the classified segment only when that segment is last, or when only
  `&&` follows it (a failure stops an `&&` chain with its own code). When `|`, `;` or
  `||` follows the classified segment, the outcome is unknown: the verification_run
  carries `exit_masked: true` and `outcome` None; agent_test_runs still counts it;
  failed_test_runs and fail_to_pass_cycles count only known outcomes and their Evidence
  carries coverage partial and a warning naming the masked count when any run is
  masked. The 22 metrics of spec 13.7 stay 22.
- Refused Read and Edit. W3-T0 made a refused Bash call `outcome: refused`, `executed:
  false`, and no verification_run. `activities._tool_type(name, category, refused)`
  (line 513) still turns a refused Read into a file_read and a refused Edit into a
  file_edit. Same rule, one type over: a refused call did nothing, so it is a tool_call
  with outcome refused and nothing else, and unique_files_read, the exploration family
  and the edit family exclude it. The orchestrator decided this; no owner question.
- Codex request_duration_ms. series.py line 74 maps `request_duration_ms` to the
  request_usage capability, so on a Codex capture the column is labelled observed with
  every cell None (W2-T7). Measure what the Codex surfaces carry (exec_json
  turn.started and turn.completed provider_ts; the rollout's task_started and
  task_complete; providers/codex.py `_duration_ms` at line 503) on the replayed Codex
  fixtures, and do one of two things, stating which: derive the request duration from
  the turn timestamps in activities.py with coverage derived and `duration_source`
  named, or leave it None with the column's coverage unavailable. Either way a series
  column whose every value is None is never labelled observed: the column coverage is
  the worst of the capability coverage and "at least one value present", and
  column_report says which rule fired.
- The obs_by_type index. W3-T0 measured that `obs_by_type` is a strict prefix of
  `obs_by_type_capture`, that no SELECT in src/ filters on observation_type alone, that
  keeping both costs the writer +16.8 percent, and that dropping it costs the reads
  nothing (0.238 s against 0.237 s). A `DROP INDEX IF EXISTS obs_by_type` at open is
  the whole migration and fits in store.py's headroom (796 lines).
- Ratchets: activities.py 798 (you must move a coherent block out FIRST, as a separate
  no-behaviour-change commit: the tool-call reducers into activities_tools.py, or the
  request reducers into activities_requests.py; say which and why), commands.py 507,
  series.py measure it, measures_spec13.py 717, store.py 796.

BRIEF W3-T3: the reducer says unknown where it cannot know: a masked exit status is
unknown, a refused file tool did nothing, an all-None column is never observed, and
the writer stops paying for an index nobody reads.
OWNS: src/telltale/activities.py and the module you split out of it, commands.py
(`exit_masked(command_norm) -> bool`), series.py (column coverage rule), store.py
(the DROP INDEX line), measures.py and measures_spec13.py (the coverage and warnings of
failed_test_runs and fail_to_pass_cycles; the exploration and edit families excluding
refused calls), tests/integration/fake_agent.py (a `--pipe` mode whose first test run
is `uv run pytest 2>&1 | tail -50` with a successful tool_result while the tests
failed, and a `--deny-read` mode), test_capture_to_summary.py, test_series.py,
fixtures/golden/* (only lines that change, each explained), docs/design/amendments/
W3-T3.md, docs/log/W3-T3.md.
READS: everything else; edit nothing owned by W3-T2 or W3-T4.
DO:
(1) The split commit.
(2) commands.exit_masked and the verification_run fields; the measures' coverage and
    warnings; the E05r2 captures on a store copy, rebuilt, must show fail_to_pass_cycles
    with coverage partial and the warning "1 of 2 verification runs has a masked exit
    status" (or the exact count you measure) rather than 0 at coverage observed.
(3) Refused file tools: `_tool_type` returns tool_call for a refused call of any tool;
    the families exclude it; the E05 pilot captures still show refused_tool_calls 3.
(4) The Codex duration decision with its measurement; the series column coverage rule
    with column_report naming it; the Codex S1 golden series (if one exists) updated
    with the changed coverage word explained.
(5) The DROP INDEX line with the writer timing before and after on the selfcheck
    (`uv run python -m telltale.store --selfcheck`), pasted.
VERIFY (paste outputs): fake agent `--pipe` through the launcher, then `telltale show`
(agent_test_runs 2, failed_test_runs with coverage partial and the warning, the
verification_run in `timeline` showing outcome "-" not "ok"); fake agent `--deny-read`,
then show (unique_files_read 0, refused_tool_calls 1); `telltale series build --clock
request <a Codex fixture capture>` showing request_duration_ms with the coverage word
your decision earns and column_report's reason; the selfcheck timings; pytest -m
integration; every gate; break-and-restore for (2), (3) and the coverage rule (name
the failing test each time).
STOP if the Codex turn timestamps are absent on every surface (report the surfaces
and leave the column unavailable), or if the split of activities.py cannot be done
without behaviour change (report what couples the halves).
REPORT: docs/log/W3-T3.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE
OR NOT DONE / NEXT, then EXPLAIN FOR THE OWNER in the Teacher register
(~/.claude/output-styles/Teacher.md): what a pipeline does to an exit status, with the
real E05 command; why "unknown" is the only honest word there; and what an index costs
a recorder. Commits: "W3-T3 split activities.py", "W3-T3 masked exit status is
unknown", "W3-T3 refused file tools and the column coverage rule", "W3-T3 drop the
index nobody reads". Final message: OUTCOME and anything UNSURE OR NOT DONE.
