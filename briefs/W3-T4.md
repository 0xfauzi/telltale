You are the implementer for task W3-T4 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W3-T4 (created from main). This session is itself being recorded by
`telltale run`. Read AGENTS.md, docs/design/01-design.md 6.3, 6.4 (PATH handling), 6.8,
6.12 "Change clock" and the wave 2 and wave 3 amendment blocks, docs/design/
02-protocol.md, docs/log/W2-T6.md (commit linkage), W3-T1.md (the three unbuildable
change columns), W2-T4.md and W2-T6.md (the two unknown_field metric attributes),
src/telltale/launch.py (where telltale.repo.commit is emitted and how a snapshot's
per_file list is bounded: lines around 700-720), repo.py, allowlist.py
(telltale.repo.commit, telltale.repo.snapshot and claude.otel.metric blocks),
sanitize.py (how a PATH is relativized), series_lineage.py (the change clock's column
builders), tests/integration/{test_commit_link,test_series_lineage}.py before writing
code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji; no
em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never guess a
number; unknown stays None and nothing zero-fills; a test that passes against the
un-fixed code is not a test; integration tests only, under tests/integration/. COMMIT
BEFORE ANY BRANCH TOGGLING. Commit messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: branch task/W3-T4; `uv sync`; gates green (`uv run mypy .`, `uv run ruff
check .`, `uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m
integration`, `uv run pre-commit run --all-files`); `git push -u origin task/W3-T4`;
`gh pr create --base main --title "W3-T4 commits carry their paths; the change clock
fills its last three columns" --body-file docs/log/W3-T4.md`, PR body ending
"Generated with Claude Code: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc".

STOP RULE: no agent session that spends tokens. Never write to ~/.telltale/telltale.db
(a `.backup` copy under a temporary TELLTALE_HOME for measurements). Never write under
~/.claude or ~/.codex. Two other implementers run in parallel: W3-T2 owns forecast/,
cli_forecast.py, tests/integration/synthetic_series.py and test_forecast_contracts.py;
W3-T3 owns activities.py, commands.py, series.py, store.py, measures*.py,
fake_agent.py, test_capture_to_summary.py and test_series.py. Do not edit any of those.
Write your design amendment lines to a NEW file docs/design/amendments/W3-T4.md, never
to 01-design.md.

Injected facts, measured 2026-09-02:
- The change clock (W3-T1, series_lineage.py, 769 lines) builds 7 rows on the build's
  own repository and leaves subsystems_touched, test_files_changed and dependency_delta
  None on every row because telltale.repo.commit carries sha, parents, tree,
  committed_ts, files_changed, additions, deletions and link_confidence, and no paths.
  telltale.repo.snapshot already carries a bounded `per_file` list ([{path,
  additions, deletions, patch_hash}], truncated with `per_file_truncated` so the 8 KB
  payload bound cannot drop the list whole; files_changed keeps the true count).
- The three columns, design 6.12: subsystems_touched = distinct top-level directories
  of the changed paths; test_files_changed = paths under a tests directory or matching
  test_*.py, *_test.py, *.test.* or *.spec.*; dependency_delta = 1 when any changed path
  is a lockfile or manifest (uv.lock, pyproject.toml, requirements*.txt, poetry.lock,
  package.json, package-lock.json, yarn.lock, pnpm-lock.yaml, Cargo.toml, Cargo.lock,
  go.mod, go.sum, Gemfile, Gemfile.lock) and 0 when none is; all three None when the
  list was truncated (the truth is not in the store) and None for every commit
  observed before this task (observations are immutable; the old rows stay unknown
  and the column's coverage word says partial). Paths are repo-relative PATH values
  through the sanitizer like the snapshot's.
- Two claude.otel.metric attributes, service_version and terminal_type, are dropped as
  unknown_field on every metric point (746 rows at the wave 2 count). One allowlist
  line each (Kind.ENUM), and the drift list in providers/claude.py if it names them.
- Ratchets: allowlist.py 798 and launch.py 790 lines against 800. Both need a
  no-behaviour-change split FIRST, each its own commit: allowlist.py's Telltale-emitted
  block (telltale.*, external.*, policy.*) into allowlist_telltale.py merged into the
  one ALLOWLIST dict at import, and launch.py's commit-linkage block into
  launch_commits.py (or repo_commits.py; say which). test_commit_link.py must stay
  green unchanged across the split.

BRIEF W3-T4: telltale.repo.commit carries a bounded per_file list; the change clock
fills subsystems_touched, test_files_changed and dependency_delta from it; the two
metric attributes stop being unknown fields.
OWNS: src/telltale/launch.py and the module split out of it, repo.py, allowlist.py and
allowlist_telltale.py, providers/claude.py (DRIFT list only), series_lineage.py,
tests/integration/test_commit_link.py, test_series_lineage.py, test_privacy.py (only
if a per_file path case is needed: the probe paths must not survive),
docs/design/amendments/W3-T4.md, docs/log/W3-T4.md.
READS: everything else; edit nothing owned by W3-T2 or W3-T3.
DO:
(1) The two split commits.
(2) The commit observation's per_file (same shape, bound and truncation flag as the
    snapshot's; `git show --numstat --format= <sha>` or the diff of parents, no patch
    bodies), the allowlist entries, the sanitizer path rule applied.
(3) series_lineage.py: the three columns from per_file with the rules above; the
    coverage word partial when any row lacks a list; column_report naming why.
(4) The metric allowlist lines; the unknown_field count on a replayed Claude fixture
    with metrics before and after, pasted.
VERIFY (paste outputs): a temporary repository with three linked commits through the
launcher (test_commit_link.py shows how) where one commit touches tests/ and one
touches pyproject.toml: `telltale series build --clock change --repo <id>` shows
subsystems_touched, test_files_changed and dependency_delta filled with the values you
computed by hand from the commits; on a COPY of the home store the same command shows
the three columns None on every old row with coverage partial and column_report's
reason; `strings <db> | grep -c "<the absolute temp path>"` is 0 (paths are relative);
the unknown_field counts; pytest -m integration; every gate; break-and-restore for the
dependency rule and the truncation rule (name the failing tests).
STOP if a per_file list cannot be bounded inside the 8 KB payload for the build's own
commits (measure the largest: paste the count and bytes) without dropping the truth
somewhere other than the truncation flag.
REPORT: docs/log/W3-T4.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE
OR NOT DONE / NEXT, then EXPLAIN FOR THE OWNER in the Teacher register
(~/.claude/output-styles/Teacher.md): why a commit's paths are safe to keep when its
patch is not, what the three columns say about a change with one real row from this
repository, and why rows from before this task stay unknown. Commits: "W3-T4 split
allowlist.py and launch.py", "W3-T4 commits carry their paths", "W3-T4 the change
clock's last three columns", "W3-T4 metric attributes". Final message: OUTCOME and
anything UNSURE OR NOT DONE.
