Task W4-E12 (attempt 2): run the full blinded diagnosis, six sessions in both arms, on
the material store rebuilt after W4-F3, at the token bound the owner raised after the
pilot, and write the result into docs/experiments/E12.md beside the pilot.

OWNS: experiments/E12/run.py (the stop bound constant only), experiments/E12/protocol.md
(one amendment paragraph, see DO 1), experiments/E12/out/** (gitignored; copy summary.json
and the per-run JSON files into experiments/E12/results/ so the write-up's numbers are
tracked), docs/experiments/E12.md, docs/log/W4-E12.md (append an "Attempt 2" section; the
attempt 1 text stays as written).
READS: everything else. Do not edit src/, tests/, the key, the questionnaire or the
material store.

READ: experiments/E12/protocol.md in full (the rule, the questionnaire, the bounds, the
table of checks: the last row is the store you run on, 30 of 36 with Q2 None on all six);
docs/experiments/E12.md as it stands (the pilot: arm S 6 of 10, arm R 9 of 10, R at
588,848 tokens against the 400,000 bound); docs/log/W4-E12.md NEXT; briefs/W4-E12.md (the
attempt 1 brief: the blinding rule and the reads-outside-material check still apply);
experiments/E12/run.py (`--skip-existing`, `--arm`, `--only`, `--scan`).
Trap: the material store at experiments/E12/out/store is gitignored and is copied into
your worktree by the orchestrator; check `ls experiments/E12/out/store/telltale.db` and
`ls experiments/E12/out/raw/*.jsonl` (six files) before anything else, and STOP if either
is missing. Trap: the pilot pair is re-run inside the full set (protocol "Sessions"), so
the store ends with 14 E12 reviewer captures and the write-up scores the 12 of this
attempt. Trap: the bound is a budget guard, not part of the decision rule; the rule, the
key and the questionnaire are unchanged and the write-up says so in one sentence. Trap:
never read the raw streams yourself to "check" an answer; the key is the key.

DO:
(1) protocol.md: under "Sessions", append one paragraph: "Amended 2026-09-03 after the
    pilot: the per-session token bound is 2,000,000 (was 400,000). The pilot's raw arm
    spent 588,848 tokens on the 1.44 MB stream (cache reads 534,249) and the largest
    stream is 1.97 MB; the owner approved the raised bound on 2026-09-03. The wall bound
    of 10 minutes and the answered-questions bound are unchanged." run.py: the constant to match; summary.json's
    `bounds.stop_tokens` must print 2000000.
(2) Run `uv run python experiments/E12/run.py` (all six sessions, both arms). If a session
    crosses a bound, the runner's own stop applies: record it and continue with the arm
    the protocol allows; do not edit the bound again.
(3) Score under the pre-registered rule: per question and arm, agreement of 6, unknowns
    tallied separately; labels "summary sufficient", "summary insufficient", "neither".
    Cost: wall and tokens per session, median and the full sorted list per arm, plus the
    launcher's total_cost_usd per session from the capture's stream result (the
    orchestrator read 0.488 and 0.922 USD for the pilot pair from
    `claude.stream.result.total_cost_usd`).
(4) docs/experiments/E12.md: keep the pilot section, add the full run in the 7.5 template
    order. In "What it means": per question, which label and why, with Q2 expected to be
    "declared unknown" in arm S on all six (the null is the designed answer; an arm S
    reviewer who answers a number there is a finding about the summary's legibility).
    Q8's wording issue from the pilot stays as written and is discussed, not rescored.
    Blinding: report task-id mentions in arm S material and reads outside the material
    per session, as attempt 1 did.
(5) `uv run python experiments/E12/check_numbers.py` on the write-up; every unmatched
    number explained.

VERIFY: `uv run telltale sessions --limit 40` shows 14 E12 reviewer captures with task ids
E12-<n>-<S|R> (2 pilot + 12); `python3 -c` over experiments/E12/results/summary.json
prints `bounds.stop_tokens == 2000000` and sessions_per_arm 6 with 6 scored per arm;
per run, `reads.outside_count == 0` or the exception named; `grep -c` of each session's
task id in its arm S material printed per session; the gates (`uv run pytest -q -m
integration`, `uv run mypy .`, `uv run ruff check .` read in full, `uv run ruff format
--check .`, `uv run deptry .`, `pre-commit run --all-files`); `git diff --stat main -- src
tests` empty.

STOP if the material store's `make_key.py experiments/E12/manifest.json --check
--material` does not print 30 of 36 before the run (the store you were given is not the
one the protocol names). STOP after the first arm R session that crosses 10 minutes.

REPORT: docs/log/W4-E12.md, "Attempt 2" section, OUTCOME / WHAT WAS WRONG / WHAT CHANGED /
UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register. Commit
message: "W4-E12 blinded session diagnosis: the full run".

ADDENDUM for attempt 5 (written by the orchestrator on 2026-09-03 after attempt 4):
attempt 4 amended protocol.md and run.py as DO (1) asks (both edits are in this worktree,
uncommitted: keep them, and check that summary.json will print stop_tokens 2000000), then
launched reviewer sessions during an Anthropic API overload. Every reviewer launched in
that window failed identically: ten `api_retry` observations at HTTP 529, then
`api_retries_exhausted`, stream result `terminal_reason: api_error`, `total_cost_usd 0`,
every token counter zero, model recorded as `<synthetic>`. Those captures are launches,
not sessions. Before running: delete every `experiments/E12/out/<session>-<arm>.json`
whose capture has `terminal_reason` api_error or a token total of 0 (list them in the
report with their capture ids), then run with `--skip-existing` so the sessions that did
complete are kept. The write-up's cost section reports the failed launches separately
(count, capture ids, zero tokens) and never averages them into an arm. If a reviewer
session fails with 529 again, wait 600 seconds and re-run that session once more; after
a second failure of the same session, stop and report. The pilot pair of 10:11 and 10:14
stays the pilot; the six sessions of this attempt are the full set.
