Task W4-F3: a verification count whose inputs include a run with no observed outcome is
None, not 0, and a tool call's own error survives on a masked chain.

OWNS: src/telltale/activities_tools.py (`_tool_outcome` only), src/telltale/measures_intervals.py
(`failed`, `epochs`, `fail_to_pass`, `post_failure_revisits`), src/telltale/measures_spec13.py
(the verification and work Metrics, `_stated`, `_masked`), src/telltale/report.py (`_outcome`),
tests/integration/test_commands.py and tests/integration/test_capture_to_summary.py (extend),
fixtures/golden/** only where a number changes, docs/design/amendments/W4-F3.md, docs/log/W4-F3.md.
READS: everything else. Never edit commands.py, commands_shell.py, activities.py, store.py,
sanitize.py or any provider module. No new dependency.

READ: src/telltale/activities_tools.py lines 105-210 (`_tool_outcome` returns before it reads
`success` when the chain is masked); src/telltale/measures_intervals.py lines 60-200 (`failed`
returns None for a masked run because the row has neither `exit_code` nor `success`, and
`epochs`, `fail_to_pass`, `post_failure_revisits` count over the runs `failed` can read, so a
masked run silently counts as neither); src/telltale/measures_spec13.py lines 340-430 (the
Metrics print `len(failed)` with coverage from `_stated` and a warning from `_masked`);
src/telltale/report.py `_outcome`; docs/design/01-design.md 6.10, 6.11 and the folded
amendment W4-T3 (what `exit_masked` means); AGENTS.md invariant 5 and the last Verification
line ("absence is not zero").

WHY (measured on the E12 material store, experiments/E12/out/store, six build sessions, and
on the E12 pilot, docs/experiments/E12.md on branch task/W4-E12): the arm S reviewer, reading
only `telltale show`, `timeline` and `vector` for W4-T1/1, answered "0 failed pytest runs"
(the raw stream says 6), "yes, an edit after the last pytest run" (raw: no) and "0 tool
errors" (raw: 1). The summary printed `failed_test_runs: 0` while its own warning said
"11 of 11 verification runs have a masked exit status": the 0 is a count over nothing.
The tool error is on a masked chain (`... uv run mypy . 2>&1 | tail -2`, stream tool_result
`is_error` true, `exit_code` 1 from the result text); `_tool_outcome` returned before it read
it, so the timeline shows no failed row at all. Every one of the six material captures has
every test run masked:

  capture   runs masked tests tests_masked known_success unknown_after_last_success is_error_results
  E12-M1      38     35    10           10             3                          2                3
  E12-M2      21     20    11           11             1                         15                1
  E12-M3      10      7     6            6             2                          1                1
  E12-M4      12     12    11           11             0                         12                6
  E12-M5      27     20     2            2             5                          2                4
  E12-M6      16     15    11           11             1                          7                1

  (is_error_results = stream tool_result rows with is_error true; today the timeline of
  E12-M2 has 74 "ok" rows, 128 "-" rows and 0 "failed" rows.)

Trap: `_stated` decides observed-versus-partial by the presence of `success` or `exit_code`
on the row. Once (2) writes `success` onto masked rows, `_stated` must also treat
`exit_masked` as not stated, or the coverage word gets stronger while the value gets weaker.
Trap: `failed()` must return None for an `exit_masked` row BEFORE it reads `exit_code` or
`success`; without that guard the call-level success of `tail` counts as the check passing.
Trap: the exit code stays OFF a masked row (W4-T3 decided that; the observation still holds
it). Trap: a refusal (`outcome: refused`) is not an error and not a failed check; do not
count it in either. Trap: coverage stays `partial` for these metrics; do not invent
`unavailable` (the runs were observed; their outcomes were not).

DO:
(1) measures_intervals.py: `failed(run)` returns None when `run.fields.get("exit_masked")`
    is true, then reads exit_code, then success, as now. Add `outcomes_known(runs) -> bool`
    (every run in scope has `failed(run)` not None). `fail_to_pass(runs)` returns None
    when not `outcomes_known(runs)`. `post_failure_revisits(activities)` returns None when
    any verification_run has `failed` None. `epochs(...)`: `edits_after_last_success` is
    None when a run with unknown outcome exists at or after the last known success, or
    when no known success exists and at least one run's outcome is unknown; keep the
    existing 0 when there are runs and every outcome is known and none succeeded. Return
    types become `int | None`; the docstrings state the rule.
(2) activities_tools.py `_tool_outcome`: on a masked chain, still put `exit_masked` True
    and then fall through to the same `success` routes as an unmasked call (OTel success,
    PostToolUseFailure, stream is_error). The docstring's "says nothing else about the
    outcome" paragraph changes to say what the row now says: the call's own outcome, and
    nothing about the check.
(3) measures_spec13.py: `failed_test_runs` is None when not `outcomes_known(tests)`;
    `fail_to_pass_cycles` and `post_failure_revisits` and `edits_after_last_successful_test`
    take the None from (1). Every one of those four keeps coverage from `_stated` (partial
    here) and the `_masked` warning, and `_masked` gains one sentence with the counts the
    value used to hide: "of the K runs whose outcome a surface stated, J failed" (K and J
    computed, never estimated). `_stated` treats an `exit_masked` row as not stated.
    `_ids` for a None value: the runs read (all of them), never empty.
(4) report.py `_outcome`: for a row with `exit_masked` true, the word is the call-level
    word followed by ", check masked" ("ok, check masked", "failed, check masked", or
    "check masked" alone when no surface stated the call's outcome). Unmasked rows are
    unchanged.
(5) Tests, in the files named in OWNS: (a) the fake agent's `--long-chain` capture
    (test_commands.py, W4-F2) asserts `failed_test_runs` None, `fail_to_pass_cycles` None,
    coverage partial, the warning naming the masked count and the "of the K runs" sentence,
    and the timeline row reading "ok, check masked" or "failed, check masked" as the fake
    agent's result states; (b) a capture where every verification run states its outcome
    keeps its integers (the existing S1 golden does this if S1 has no masked run: say which
    fixture proved it). Break-and-restore for (a): remove the `exit_masked` guard in
    `failed()`, watch (a) fail, restore; paste the failing assertion into the report.
(6) docs/design/amendments/W4-F3.md: the rule of (1) and (3) as an amendment to 6.11, the
    rule of (2) and (4) as an amendment to 6.10, each with the measured example above.
    Regenerate a golden only if a number in it changes, and explain each changed number.

VERIFY (paste every output into the report):
- `mkdir -p /tmp/w4f3 && sqlite3 experiments/E12/out/store/telltale.db ".backup /tmp/w4f3/telltale.db" && TELLTALE_HOME=/tmp/w4f3 uv run telltale rebuild` (six captures rebuilt), then for each of the six material captures (ids in experiments/E12/manifest.json under material_capture_id) `TELLTALE_HOME=/tmp/w4f3 uv run telltale show <cap>`: `failed_test_runs`, `fail_to_pass_cycles`, `edits_after_last_successful_test` and `post_failure_revisits` are null on all six (the table above says every one has masked test runs and an unknown run after its last known success), coverage partial, and the warning carries the "of the K runs" sentence with K equal to runs minus masked from the table.
- `TELLTALE_HOME=/tmp/w4f3 uv run telltale timeline <cap> | grep -c "failed"` equals is_error_results in the table (3, 1, 1, 6, 4, 1) and `grep -c "check masked"` equals masked (35, 20, 7, 12, 20, 15). If a count differs, find the row and say why; do not adjust the table.
- `TELLTALE_HOME=/tmp/w4f3 uv run telltale explain <E12-M2 cap> failed_test_runs` walks to the 11 test runs and their observations.
- Rebuild the material store in place (`TELLTALE_HOME=experiments/E12/out/store uv run telltale rebuild`) and run `uv run python experiments/E12/make_key.py --check --material`: 30 of 36 agree, and Q2 is withheld on all six (today it prints 31 of 36 because W3-E08/1's Q2 reads 0 = 0 over two masked runs; that agreement was a coincidence and the report says so).
- `uv run pytest -q -m integration` (all pass; name any golden that changed and why); `uv run mypy .`; `uv run ruff check .` (read the whole output, never through tail); `uv run ruff format --check .`; `uv run deptry .`; `pre-commit run --all-files`.
- Break-and-restore as in DO (5).

STOP if making `failed()` None for masked rows changes a number in a fixture golden that
has NO masked run (that would mean the guard reads the wrong field: report it, do not
regenerate the golden). STOP if `_tool_outcome` cannot read the call's success on a masked
chain without reading the exit code (report which surface).

REPORT: docs/log/W4-F3.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR
NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register
(~/.claude/output-styles/Teacher.md): idea before name, the W4-T1/1 example with its real
values, how each number was measured, the answer rejected (printing the count over the stated
runs as the value) and why. Commit message: "W4-F3 unknown outcomes make a verification count None; a tool error survives a masked chain".
