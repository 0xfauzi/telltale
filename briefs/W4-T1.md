You are the implementer for task W4-T1 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W4-T1 (created from main). This session is itself being recorded by
`telltale run`. Read AGENTS.md, docs/spec/telltale-architecture.md sections 14.3
(probes and interventions, quoted below) and 16, docs/design/01-design.md 6.12 "H2
and H3 protocol" and 6.3 (external.correlation and external.outcome), the wave 2 and
3 amendments, docs/design/02-protocol.md, docs/log/W1-T4.md, W2-T3.md, W2-E05.md,
W2-E06.md (the repeat and environment runners and what they learned: foreground,
bypassPermissions, from_store, finish), src/telltale/experiments.py (629 lines),
experiments_env.py (410), experiments_measure.py (234), stats.py, cli.py (659),
report.py (635), allowlist.py and allowlist_telltale.py (the external.* fields),
tests/integration/{fake_agent,test_experiments}.py before writing code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji;
no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never guess
a number; unknown stays None; a test that passes against the un-fixed code is not a
test; integration tests only, under tests/integration/. COMMIT BEFORE ANY BRANCH
TOGGLING. Design amendment lines go to a NEW file docs/design/amendments/W4-T1.md.
Commit messages end with these two trailer lines exactly:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: branch task/W4-T1; gates green (`uv run mypy .`, `uv run ruff check .`,
`uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m integration`,
`uv run pre-commit run --all-files`); `git push -u origin task/W4-T1`; `gh pr create
--base main --title "W4-T1 probe runner and correctness scoring" --body-file
docs/log/W4-T1.md`, PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc".

STOP RULES: no agent session that spends tokens: the fake agent only. Never write to
~/.telltale/telltale.db (a temporary TELLTALE_HOME for every run). Never write under
~/.claude or ~/.codex. Another implementer, W4-T2, runs in parallel and owns
report.py's profile renderer, a new profile.py, cli.py's `profile` command and
cohorts.py; W3-E08b owns forecast/ and cli_forecast.py. Touch cli.py only to register
your commands (one import and one dispatch line each), so the merge conflict is one
hunk.

Spec 14.3, verbatim: "Fixed read-only probes are executed on selected repository
versions; where practical, the same functional task template is run before and after
a deliberately scoped refactor. The environment fingerprint is held constant or
differences are explicit. Example probes: locate the implementation behind an
interface; identify tests covering a module; trace a public API to persistence;
identify callers of a changed contract. Measure correctness of the probe answer as
well as work quantities. Cheap behaviour with a wrong answer is not an improvement."

Injected facts, measured 2026-09-02:
- The repeat runner (experiments.repeat) is the pattern: a spec JSON, one fresh
  detached worktree per repetition at base_sha, `telltale run --task-id T --attempt
  N --experiment E -- <command>`, correlations and outcomes posted through the
  launcher's receiver (`_external`), the acceptance command run by the harness,
  `from_store` to rebuild a report without launching, `finish` for a repetition the
  runner never closed, `_approvable` refusing headless claude without
  bypassPermissions, line-buffered stdout. The environment runner
  (experiments_env.environment) adds arms that differ in exactly one declared factor
  and asserts it on the fingerprints; today the factor must be an argv flag
  (`_one_declared_difference`, `_declares`).
- The child's stdout: the launcher tees stream-json to the receiver and echoes it
  unchanged. The probe runner must read the final `result` message's text from the
  child's stdout to score it, and store ONLY the score: the answer text never reaches
  the store (NEVER_PERSIST covers assistant text; the runner is a harness and reads
  stdout the way the repeat runner reads the exit code).
- external.outcome fields are in allowlist_telltale.py (kind in
  {mechanical_verification, adversarial_review, merge_decision, revert_or_repair,
  runtime_signal}, status, categories, timestamp, external_run_id, component_id,
  attempt). Check what `categories` admits (a bounded list of ENUM-kind strings) and
  put the score there as `precision:<0..1>`, `recall:<0..1>`, `probe:<id>`, or state
  what the allowlist refuses and what you did instead. No new observation type.
- experiments.py is 629 lines: the probe runner is a new module experiments_probe.py,
  and the intervention runner extends experiments_env.py with a factor "base_sha" (the
  two arms differ in the repository commit and in nothing else; the fingerprint
  assertion excludes repo state by construction, since the fingerprint carries no
  commit; say so in the amendment).
- The fake agent (tests/integration/fake_agent.py) has `--seed`, `--effort`,
  `--model`, `--deny`, `--pipe`, `--deny-read` modes and prints stream-json. It needs
  an `--answer "path/a.py,path/b.py"` mode whose final result text lists those paths,
  after reading them (a Read tool_use per path), so a probe score is testable end to
  end.

BRIEF W4-T1: `telltale experiment probe <spec.json>`: fixed read-only probes with
deterministic answer keys, scored for correctness, and before/after intervention runs
under matched fingerprints (spec 14.3, 14.4 of the plan's registry: E09 and E10).
OWNS: src/telltale/experiments_probe.py (new), experiments_env.py (the base_sha
factor), cli.py (registration lines only), report.py (a `probe` renderer, or a new
report_probe.py if report.py would pass 700 lines: it is at 635 and W4-T2 adds to it),
tests/integration/fake_agent.py (`--answer`), tests/integration/test_experiments.py
or a new test_probe.py, docs/design/amendments/W4-T1.md, docs/log/W4-T1.md.
Spec shape: {task_id, experiment, repo, base_sha, provider, level, command (the
claude argv with the prompt taken from each probe), probes: [{probe_id, prompt,
answer_key: {paths: [...repo-relative...], symbols: [...]}}], repetitions}; an
intervention spec is an environment spec whose arms differ in base_sha (before and
after the refactor) with the same probes or the same task.
Scoring: the agent's final text is searched for each answer-key path (exact
repo-relative match, and basename match reported separately) and symbol (whole word);
precision = matched / mentioned-candidates is NOT computable without knowing what the
agent mentioned, so define precision over the answer-key vocabulary: matched_keys /
(matched_keys + wrong_keys) where wrong_keys are paths mentioned in the text that
exist in the repository and are not in the key (measure them by scanning the text for
repo-relative paths that exist at base_sha); recall = matched_keys / len(key). State
both definitions in the amendment and print both numbers with n. A probe whose text
is empty (no result message) scores None, never 0.
Per probe and repetition: the evidence vector (experiments_measure.vector) plus
precision, recall and the score outcome; per probe: n, median, s, MDD, N_needed per
measure (stats.py) exactly as the repeat runner reports; an intervention: the
between-arm table (HL shift, Cliff's delta, exact MW p, MDD) per measure and per
score, paired by probe.
VERIFY (paste): a spec with 2 probes and 3 repetitions of the fake agent with
`--answer` (one probe answered exactly, one with a wrong extra path): the printed
scores equal the ones you compute by hand (write them in the report before running);
an intervention spec on a temporary repository with two commits (arm "before" at the
first, "after" at the second, same probe): the paired report with the fingerprint
assertion passing and the factor named base_sha; a spec whose arms differ in base_sha
AND an argv flag is refused naming both; the answer text is absent from the store
bytes (`strings <db> | grep -c "<a phrase from the fake answer>"` is 0); pytest -m
integration; every gate; break-and-restore for the scoring (name the failing test).
STOP if the outcome allowlist cannot carry a score without a new observation type
(report the field and the bound; store nothing and print the score in the report
only), or if the runner would need a real session to verify (fake agent only).
REPORT: docs/log/W4-T1.md in the REPORT order with EXPLAIN FOR THE OWNER in the
Teacher register (~/.claude/output-styles/Teacher.md): what a probe is, why the
answer is scored and the text thrown away, one fake probe's real numbers, and why
"cheap behaviour with a wrong answer is not an improvement" is a rule and not a
slogan. Commits: "W4-T1 probe runner and scoring", "W4-T1 intervention arms differ
in base_sha". Final message: OUTCOME, the two verified score tables, anything UNSURE
OR NOT DONE.
