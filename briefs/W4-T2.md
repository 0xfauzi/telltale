You are the implementer for task W4-T2 of the Telltale build. You are running as a
headless Claude Code session inside a git worktree of github.com/0xfauzi/telltale on
branch task/W4-T2 (created from main). This session is itself being recorded by
`telltale run`. Read AGENTS.md, docs/spec/telltale-architecture.md section 16
(repository work profiles, quoted below), 13.7 and 17.2, docs/design/01-design.md
6.11 (the cohort rule), 6.12 "Change clock", the wave 2 and 3 amendments,
docs/design/02-protocol.md, docs/log/W2-T4.md (cohorts, percentiles, VECTOR),
W3-T0.md (typed reads), W3-T1.md and W3-T4.md (the change clock and per_file),
src/telltale/cohorts.py (536 lines), measures.py, measures_spec13.py, report.py (635),
series_lineage.py, series_paths.py, store_reads.py, cli.py (659),
tests/integration/test_compare.py before writing code.

House rules: as little and as simple code as possible; stdlib only in src; no emoji;
no em dashes anywhere (hyphen, colon or full stop); Python 3.12, uv only; never guess
a number; unknown stays None and nothing zero-fills; a test that passes against the
un-fixed code is not a test; integration tests only, under tests/integration/.
COMMIT BEFORE ANY BRANCH TOGGLING. Design amendment lines go to a NEW file
docs/design/amendments/W4-T2.md. Commit messages end with these two trailer lines:
Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc
Delivery: branch task/W4-T2; gates green (`uv run mypy .`, `uv run ruff check .`,
`uv run ruff format --check .`, `uv run deptry .`, `uv run pytest -m integration`,
`uv run pre-commit run --all-files`); `git push -u origin task/W4-T2`; `gh pr create
--base main --title "W4-T2 repository work profiles" --body-file docs/log/W4-T2.md`,
PR body ending "Generated with Claude Code:
https://claude.ai/code/session_01NTNBPJ14MSzn9Cjxps46yc".

STOP RULES: no agent session that spends tokens: the fake agent only. Never write to
~/.telltale/telltale.db (a `.backup` copy under a temporary TELLTALE_HOME for the
real-data VERIFY). Never write under ~/.claude or ~/.codex. W4-T1 runs in parallel
and owns experiments_probe.py, experiments_env.py and a probe renderer; W3-E08b owns
forecast/ and cli_forecast.py. Touch cli.py only to register `profile` (one import
and one dispatch line), so the merge conflict is one hunk.

Spec 16, verbatim: "Natural-history aggregation is useful descriptively but is not
called maintainability. For each path/subsystem, Telltale reports cohort-qualified
observed distributions and sample composition." Allowed: "Sessions touching payments
used 2.1x the matched-cohort median fresh token burden (n=31; same model/runtime
family)." Not allowed: "payments has maintainability 0.17." Spec 17.2: no
quality_score or difficulty_score anywhere; every result carries claim_class,
coverage, cohort, provenance and warnings.

Injected facts, measured 2026-09-02:
- Cohorts (W2-T4): keys provider, runtime_major, model, content_level; n >= 10 per
  cohort and per metric; mid-rank percentile; percentiles are Evidence.comparative
  and are never stored; imported captures are outside every cohort. The home store
  holds 37+ launcher captures of this repository (repo_id d9f783b1d318c0d65ec6b86fe
  729f6ff1a3581983e4287a71f6510eb80d84d6c), one cohort of n = 23 (claude, runtime
  major 2, opus, content level 1) plus the E05/E06 sonnet sessions on the fixture
  repository, and 3152 imported captures outside every cohort.
- Which paths a capture touched: file_edit activities carry repo-relative paths;
  file_read activities carry paths; a linked telltale.repo.commit carries per_file
  since W3-T4 (4 of 19 change rows on the home store). A "subsystem" is a top-level
  directory, the rule series_paths.py uses; a path prefix given by the user is the
  other grouping.
- The evidence vector (spec 13.7, cohorts.VECTOR) is 6 families and 22 metrics, all
  Evidence.derived per capture in the evidence table; W3-T0 made per-type reads fast
  (`telltale vector` 0.85 s on the 3190-capture store).
- report.py is 635 lines; a profile renderer goes in a new report_profile.py; the
  aggregation in a new profile.py (measures.py is 359; keep the profile out of it).

BRIEF W4-T2: `telltale profile <repo_id> [--path PREFIX] [--by week|subsystem]
[--include-backfill]`: the natural history of the evidence vector over one
repository's captures, grouped by ISO week (default) or by subsystem touched,
cohort-qualified, comparative evidence only, never a score.
OWNS: src/telltale/profile.py (new), report_profile.py (new), cli.py (registration
lines), tests/integration/test_profile.py (new), fixtures if a synthetic set is
needed (the fake agent through the launcher with `--seed`, `--model`, `--effort` to
make cohorts), docs/design/amendments/W4-T2.md, docs/log/W4-T2.md.
Rules: a row is (group, metric): n captures, median, scaled MAD, min, max, and the
cohort-relative column "ratio to matched-cohort median" ONLY when the group's captures
and the cohort share the four keys and the cohort has n >= 10 outside the group
(spec 16's "2.1x the matched-cohort median (n=31; same model/runtime family)");
otherwise the column is blank with the reason ("no cohort (n=k)"). Every printed
number is an Evidence.comparative built in memory with the cohort recorded and never
stored (as percentiles are). Coverage per row is the weakest coverage among the
captures' evidence; a metric with every value None prints "-" with the coverage
word. Sample composition per group: captures, providers, models, runtime majors,
content levels, how many imported (excluded unless --include-backfill, and then
listed as their own cohort-less rows). The words maintainability, quality, difficulty
and score never appear in the output (a check in the renderer that raises, the way
ADR-014's word refusal works in forecast/).
VERIFY (paste): `uv run telltale profile d9f783b1d318c0d65ec6b86fe729f6ff1a3581983e
4287a71f6510eb80d84d6c` on a COPY of the home store (a table per metric by week with
n; the cohort column filled where the n = 23 cohort qualifies and blank with the
reason elsewhere); `--by subsystem` (src, tests, docs, briefs, experiments as
groups, from file_edit paths); `--path src/telltale/forecast` (the captures that
edited under it, with n); a temporary home with 12 fake-agent captures in one cohort
and 3 in another: the ratio column present for the first and "no cohort (n=3)" for
the second; the output byte-scan for "maintainability" and "score" is 0 and a
renderer string containing "score" raises; pytest -m integration; every gate;
break-and-restore for the n >= 10 gate (name the failing test).
STOP if a per-path grouping cannot be built from activities without reading git at
profile time (report which paths are missing and why; group by subsystem only).
REPORT: docs/log/W4-T2.md in the REPORT order with EXPLAIN FOR THE OWNER in the
Teacher register (~/.claude/output-styles/Teacher.md): what a natural-history profile
is and is not, one real row from this repository's profile with its cohort and n,
and why "2.1x the cohort median" is allowed and "maintainability 0.17" is refused.
Commits: "W4-T2 profile aggregation", "W4-T2 profile renderer and command". Final
message: OUTCOME, one real profile row, anything UNSURE OR NOT DONE.
