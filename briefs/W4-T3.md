Task W4-T3: a verification command anywhere in a shell chain is a verification run.
Today `commands.classify` lets the first segment with a known category win and treats a
newline-joined chain as one segment, so `uv sync -q | tail -2; uv run pytest`, `uv run
ruff format . && uv run ruff check . && uv run mypy . && uv run pytest`, and a
`python3 - <<'PY' ... PY` script followed on the next line by `uv run pytest` are all
recorded as something other than a test run. Measured by the orchestrator on the six
sessions chosen for E12 (experiments/E12/key.json, derived from the raw stream-json by
experiments/E12/make_key.py): the streams hold 52 pytest executions and Telltale's
`agent_test_runs` sums to 14 (per capture: W4-T2/1 10 against 4, W4-T1/1 11 against 1,
W3-E08b/1 6 against 3, W3-V/1 11 against 0, W3-E08/1 3 against 2, W3-T4/1 11 against 4).
A recorder that tells a reviewer the verifier session ran no tests is wrong about the
one thing spec 13 is for.

OWNS: src/telltale/commands.py, the activities reducer's use of classify and
exit_masked (src/telltale/activities*.py), fixtures/golden/** (regenerated, each changed
number explained), tests/integration/test_commands*.py or the test file that holds the
classifier's tests today, docs/design/amendments/W4-T3.md.
READS: docs/design/01-design.md 6.10 and the wave 2 and wave 3 amendments on
classification, masking (W3-T3) and verification columns (W2-T7); docs/log/W3-T3.md
(the masked-exit rule and how it was measured); experiments/E12/make_key.py (the
orchestrator's independent rule: a segment split on newlines, `&&`, `||`, `;` and `|`,
heredoc bodies excluded, whose head is pytest or `uv run pytest` or `python -m pytest`
or `.venv/bin/pytest`); experiments/E12/key.json (the counts to reproduce).

READ commands.py in full before changing it: `_CLASSIFY_RULES` is part of
CLASSIFIER_VERSION on purpose, and `exit_masked` reads the same segments as `classify`.
Traps:
- A newline inside a Bash command is a `;` to the shell. The normalizer must treat it
  as a separator BEFORE shlex sees it, and must not split a newline inside quotes or
  inside a heredoc body (a heredoc body is data, and `git commit -m "<two lines>"` is
  one segment; W2-T1 measured that case). Measure with real commands from the six E12
  streams (experiments/E12/out/raw/ on the orchestrator's machine; the key file holds
  the counts if the raw files are not there).
- One chain can hold several verification categories (`ruff format && ruff check &&
  mypy && pytest`). The activity is ONE verification_run and needs one category and one
  scope: take the highest-priority verification category present, in the order test,
  benchmark, typecheck, lint, format, build, security_scan, and record every
  verification category the chain held in a new `categories` field so nothing is
  hidden; scope comes from the chosen segment. A non-verification chain keeps the
  first known category (the rule that stands today).
- `exit_masked` is judged for the CHOSEN segment: masked when any separator after it is
  not `&&` (W3-T3's rule, unchanged), and a newline after it counts as `;`.
- Every rule change changes CLASSIFIER_VERSION and the reducer version, which is
  correct and means every capture must be rebuilt: the orchestrator runs `telltale
  rebuild` on the home store after the merge (measured 183 s for 3223 captures). Say
  so in the report; do not run it on the owner's store yourself.
- The goldens under fixtures/golden/ will move where a fixture chain held a later test
  segment; explain each changed number from the fixture line that produced it.
- Never edit ~/.claude or ~/.codex; never write to ~/.telltale/telltale.db except
  through the launcher when a test runs it under a temporary TELLTALE_HOME.

DO:
(1) commands.py: newline as a separator in `normalize` (outside quotes and heredoc
    bodies); `classify` returns the highest-priority verification category present
    across all segments, else the first known category; a helper that returns every
    category the chain held; `_CLASSIFY_RULES` names the new rules (the version
    changes by itself). `exit_masked` takes the chosen segment's index.
(2) The reducer writes `categories` on verification_run activities and keeps
    `category` and `scope` as today.
(3) Tests, each failing on the un-fixed code: `uv sync -q | tail -2; uv run pytest -m
    integration` classifies as test with exit masked false (pytest is last); `uv run
    ruff format . && uv run ruff check . && uv run mypy . && uv run pytest -q` is test,
    categories [format, lint, typecheck, test], not masked; a two-line command `cd
    /tmp/x\nuv run pytest -q 2>&1 | tail -5` is test and masked; a `python3 - <<'PY'`
    script whose body contains the words `uv run pytest` followed after the closing
    tag by a real `uv run pytest` line is ONE test run, not two; `git commit -m
    "<two lines>"` is one git segment. Through the real launcher with a crafted stream
    (the shape W3-V used, see docs/log/W3-V.md break attempt 1), assert
    agent_test_runs on the capture.
(4) docs/design/amendments/W4-T3.md: the rule, the measurement that forced it (the
    52 against 14), and what CLASSIFIER_VERSION's change costs (a rebuild).

VERIFY (paste): the tests above, each shown failing before the change and passing
after; `uv run telltale rebuild <capture>` on a COPY of the home store (`sqlite3
~/.telltale/telltale.db ".backup /tmp/w4t3/telltale.db"`, TELLTALE_HOME=/tmp/w4t3)
for the six E12 captures, then `telltale show <capture>` for each: agent_test_runs
equals the key's Q1 for all six (10, 11, 6, 11, 3, 11) or every difference is named
with the command that causes it and which side is right; failed_test_runs and
fail_to_pass_cycles on the same six with their coverage words (masked runs stay
partial); the five W2-E05 re-run captures still say "2 of 2 verification runs have a
masked exit status"; `uv run pytest -m integration`; every gate; the goldens diff
pasted with each number explained.
STOP if the newline rule cannot be made safe for quoted strings and heredoc bodies with
shlex (report the shape that breaks and the count of such commands in the six
streams); do not ship a rule that splits inside quotes.
REPORT: docs/log/W4-T3.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED /
UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register
(~/.claude/output-styles/Teacher.md): idea before name, one real example with real
values, how each number was measured, the answer rejected and why. Commit message
"W4-T3 a verification command anywhere in a chain is a verification run", with the
two trailer lines the orchestrator's dispatch prompt gives.
