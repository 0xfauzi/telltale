Task W4-T4: a stream-only capture reports output_tokens from a message-start snapshot
and calls it a count. Measured by the orchestrator on 2026-09-03 on the six E12
sessions captured fresh through the launcher from their archived stream-json
(experiments/E12/build_store.py, store experiments/E12/out/store): `telltale show` says
output_tokens 1902, 912, 636, 1620, 1421, 1271 where the sessions' own `result` records
say 141206, 122438, 67988, 99305, 153943, 89125, and the launcher captures of the same
sessions in the home store (OTel as the primary surface) say the latter exactly. On
W4-T2/1's stream: 257 assistant records over 170 message ids; every record of one
message carries an identical usage; output_tokens per message runs from 1 to 21 (the
count at the moment the message started streaming) and sums to 1902; input_tokens (340),
cache_read (35097982) and cache_creation (318963) per message ARE final and equal the
OTel figures; stop_reason is None on all 257 records. The `result` record carries
usage.output_tokens 141206 and modelUsage per model with outputTokens 141206 and
thinkingTokens 60251. A recorder that reports 1902 output tokens for a 141206-token
session at coverage observed is wrong by a factor of 74 and says it is sure.

OWNS: src/telltale/providers/claude.py (the stream parse of assistant usage and of the
result record) and its sanitize.ALLOWLIST entries, src/telltale/activities*.py
(model_request from the stream surface, the session-total activity), src/telltale/
measures.py (usage section), src/telltale/series.py (request-clock output_tokens
coverage), src/telltale/importer.py and the transcript parse ONLY if the measurement in
DO (1) shows transcripts carry the same snapshot, fixtures/golden/** (regenerated, each
changed number explained), tests (a new test file or test_capture_to_summary.py),
docs/design/amendments/W4-T4.md.
READS: docs/design/01-design.md 6.10 (model_request rules, primary and secondary
surfaces, the conflict rule) and 6.11 (usage totals); docs/design/00-digest.md 2.1 (the
stream-json result shape: usage, modelUsage); docs/log/W1-T2.md and W2-T2.md (how the
reducer and the backfill importer read usage today); experiments/E12/out/raw/*.jsonl
(the six streams, on the orchestrator's machine); experiments/E12/key.json (Q10 is the
result record's output_tokens per session); experiments/E12/manifest.json
(material_capture_id names each session's stream-only capture in
experiments/E12/out/store).

Traps:
- Unknown stays unknown. A per-request output_tokens that no surface counted is None,
  not the snapshot and not zero. The capture total may come from the result record,
  which is the provider's figure for the whole session (the split per request is
  unknown: say so in the coverage words and the Evidence warnings), and never from
  summing snapshots.
- The OTel surface is primary and unchanged: a launcher capture with OTel keeps its
  per-request output_tokens and total; the home store's numbers must not move (VERIFY
  checks it on a copy).
- The transcript backfill: measure BEFORE touching it. The six launcher captures know
  their provider_session_id; the transcripts are under ~/.claude/projects/<slug>/
  <session_id>.jsonl (READ ONLY; never write under ~/.claude). Compare the transcript's
  per-message usage.output_tokens with the stream's snapshot and with the result total
  for each of the six. If transcripts carry final counts, the importer is right and the
  scope ends at the stream. If they carry the snapshot, the owner's backfill captures are
  wrong the same way, the importer gets the same rule, and the report counts the affected
  backfill captures on a `.backup` copy, never on the live store.
- Series: the request-clock column output_tokens on a stream-only capture is None per
  row; the missingness policy decides (exclude or refuse); nothing imputes; the readiness
  check names the column.
- Conflict rule (6.10): when OTel and the stream both deliver a request, OTel wins and
  NO conflict diagnostic is written for the snapshot, because a snapshot is not a
  measurement of the same quantity. Say so in the amendment.
- Goldens: the fixtures replay OTel and the stream together, and OTel is primary, so the
  golden usage numbers should not move; if one does, explain it from the fixture line.
- Never edit ~/.claude or ~/.codex; never write to ~/.telltale/telltale.db except
  through the launcher under a temporary TELLTALE_HOME.

DO:
(1) The measurement first, as a stand-alone script (experiments/E12/usage_check.py or
    under docs/log evidence): for each of the six sessions, the stream snapshot sum, the
    result total, the OTel sum read from a `.backup` copy of the home store, and the
    transcript sum when the transcript is found. One table in the report.
(2) providers/claude.py: the stream assistant usage observation names output_tokens for
    what it is (a field name that says snapshot, or the field dropped) so nothing
    downstream can mistake it; the result record's usage and modelUsage totals become an
    observation (allowlist entries as needed; content never).
(3) activities: model_request from the stream surface carries output_tokens None with
    provenance; the lifecycle or a session-total activity carries the result totals.
(4) measures: usage.output_tokens is the OTel sum when the OTel surface delivered the
    requests; else the result record's total with coverage partial and a warning that
    per-request output tokens were not observable on this surface and the total is the
    provider's session figure; else None with coverage unavailable. Do not add a
    thinking-token field unless the design already has one.
(5) series: request-clock output_tokens None per row on stream-only captures; readiness
    names it.
(6) Tests, each failing on the un-fixed code: a launcher capture from a crafted stream
    (the fake_agent shape W4-T3's test_commands.py uses) whose assistant records carry
    small usage snapshots and whose result carries a large total: `show` says the total
    with coverage partial and the warning, the request series column is None, and the
    OTel-backed fixture capture is byte-identical to its golden.
(7) docs/design/amendments/W4-T4.md: the rule, the measurement that forced it, what a
    rebuild can and cannot fix for stream-only captures already on disk.

VERIFY (paste): `uv run python experiments/E12/build_store.py` then
`TELLTALE_HOME=experiments/E12/out/store uv run python experiments/E12/make_key.py
experiments/E12/manifest.json --check --material` shows Q10 agree on all six (141206,
122438, 67988, 99305, 153943, 89125) and Q1 unchanged (10, 10, 5, 11, 2, 11); on a
`.backup` copy of the home store, rebuilt, `show` of the six original captures
(capture_id in the manifest) still says those same output_tokens and the five W2-E05
re-run captures' usage is unchanged; `uv run pytest -m integration`; every gate; the
goldens diff explained; the transcript table.
STOP if a stream has no result record and no other surface delivered output tokens:
that capture's output_tokens is None; count how many of the six that is (expected 0).
STOP if the fix would need the raw stream re-read for captures already on disk: then
existing stream-only captures stay as they are, say so, and count them on the copy.
REPORT: docs/log/W4-T4.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED /
UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register
(~/.claude/output-styles/Teacher.md): idea before name, one real example with real
values, how each number was measured, the answer rejected and why. Commit message
"W4-T4 a stream snapshot is not an output token count", with the two trailer lines the
orchestrator's dispatch prompt gives.
