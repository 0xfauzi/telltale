Task W4-T5: an `instructions` factor for the environment runner and the probe
intervention runner, so that E10 (spec 14.4: the same probes before and after an
AGENTS.md rewrite on a branch) can run under the fingerprint assertion instead of being
refused by it. Today `experiments_env.FACTORS` is ("model", "effort", "content_level",
"base_sha"), and the base_sha factor's post-run assertion is inverted: the arms must
differ in NO fingerprint field. Its own comment says what that does to E10: a commit
that also touched AGENTS.md or CLAUDE.md "is caught there and named", because
`instruction_hashes` is a fingerprint field read out of each arm's worktree. Right for
a code refactor, and it refuses the one intervention the owner approved on 2026-09-03.

OWNS: src/telltale/experiments_env.py (FACTORS, the pre-run and post-run checks for
the new factor, the assertion text), src/telltale/experiments_probe.py and
src/telltale/cli_probe.py (the intervention spec accepts `factor: "instructions"`),
src/telltale/report_probe.py where the report names the factor, the test file that
holds today's intervention and environment tests (tests/integration/test_experiments.py
is at exactly 800 lines, the file-length ratchet's limit: split it first, the constants
and `_assert_statistics` away from the tests, as docs/log/W4-T4.md NEXT 5 says),
docs/design/amendments/W4-T5.md.
READS: docs/design/01-design.md 6.8 (the instruction surfaces the fingerprint hashes),
6.12 "H2 and H3 protocol", docs/spec/telltale-architecture.md 14.4; docs/log/W2-T3.md
(the environment runner and its two assertions), docs/log/W4-T1.md (the intervention
runner and why base_sha's assertion is inverted); src/telltale/env.py (the list of
instruction surfaces: read it from the code, never restate it); experiments/E09/spec.json
(the probes E10 reuses).

Traps:
- The factor's value is a commit, like base_sha, and its assertion is the mirror of
  base_sha's: after the runs the two arms' fingerprint payloads must differ in
  `instruction_hashes` and in NOTHING else. Before any run, the mechanism that makes
  this more than a hope: `git diff --name-only <shaA> <shaB>` in the spec's repo must be
  non-empty and every path in it must be an instruction surface env.py hashes (AGENTS.md,
  CLAUDE.md, the rules directory: whatever env.py lists); refuse otherwise, naming the
  path. A commit that changes code AND AGENTS.md is two factors and is refused.
- The arms' commands must be identical (no flag differs): the pre-run check that
  base_sha already has applies unchanged.
- Home instruction files are hashed into `instruction_hashes` too and are the same for
  both arms; the post-run check compares the payload field as a whole, so the report
  names WHICH paths inside the field differed, not only that the field did.
- The probe intervention runner pairs probes across arms by probe id; nothing about
  scoring changes. The environment runner's statistics do not change.
- VERIFY needs a temp repo with two commits differing only in AGENTS.md, and a third
  commit that also changes a source file for the refusal case; W4-T1's intervention
  test already builds temp repos with two commits: extend that helper.
- Never edit ~/.claude or ~/.codex; never write to ~/.telltale/telltale.db except
  through the launcher under a temporary TELLTALE_HOME.

DO:
(1) experiments_env.py: `INSTRUCTIONS_FACTOR = "instructions"` in FACTORS; the arm's
    value is a commit (arm key base_sha, as for the repository factor; the spec's
    `factor` says which assertion applies); pre-run: the diff check above; post-run:
    expected differing fields == ["instruction_hashes"]; the assertion text says what
    was proved, as `_ASSERTED` does for the other two cases.
(2) The probe intervention runner accepts the factor and reuses (1).
(3) Tests, each failing on the un-fixed code: an intervention spec with factor
    instructions over two temp-repo commits differing only in AGENTS.md runs (fake
    agent) and reports differing_fields ["instruction_hashes"] with the path named; the
    same two commits under factor base_sha are refused naming instruction_hashes (the
    behaviour that stands today, kept and asserted); a factor-instructions spec whose
    commits also differ in a .py file is refused before any run, naming the path; the
    environment runner under the same factor with the fake agent.
(4) docs/design/amendments/W4-T5.md: the rule, and why the assertion is the mirror of
    base_sha's rather than a relaxation of it.

VERIFY (paste): the tests above, each shown failing before the change and passing
after; `uv run telltale experiment intervention <spec>` on a temp repo with the fake
agent and factor instructions prints the paired report with differing_fields
["instruction_hashes"]; with a code change in the second commit it prints the refusal
naming the path and creates no database; `uv run pytest -m integration`; every gate;
`wc -l tests/integration/test_experiments.py` under 800 after the split.
STOP if env.py's instruction surfaces cannot be read as a list from code (report the
list you would have had to duplicate and where it lives).
REPORT: docs/log/W4-T5.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED /
UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register
(~/.claude/output-styles/Teacher.md): idea before name, one real example with real
values, how each number was measured, the answer rejected and why. Commit message
"W4-T5 an instructions factor for the intervention runners", with the two trailer
lines the orchestrator's dispatch prompt gives.
