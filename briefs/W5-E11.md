Task W5-E11: run the one-step candidate-conditioned protocol over every landed change of
this repository with a matched capture, TimesFM-3 against the baselines, and write the
decision file, including the "not assessable" outcome if that is what the data gives.

OWNS: experiments/E11/{run.py,check_numbers.py,out/}, docs/experiments/E11.md, docs/log/W5-E11.md.
READS: everything under src/telltale/forecast, series_lineage.py, docs/design/01-design.md 6.12,
docs/experiments/E08.md (the change-clock readiness table and the placebo validity failure),
docs/log/W5-T1.md.

READ (measured by the orchestrator on 2026-09-03 on the home store; the run measures again):
- Change clock of this repository: `telltale series build --clock change --repo
  d9f783b1d318c0d65ec6b86fe729f6ff1a3581983e4287a71f6510eb80d84d6c` gives 38 rows, one
  changepoint at row 29 (runtime_version 2.1.257 to 2.1.259), so the regimes are 29 and 9
  rows; c_min on the change clock is 16 and H is 1, so origins inside the first regime are
  16 to 28 (13 windows) against k_min 20; the second regime admits none. Path columns
  partial (19 of 38 rows carry no per_file list).
- Outcomes on record: mechanical_verification pass 72 / fail 22, merge_decision merged 33,
  no revert_or_repair, no duration before W5-T1. So `merge_verification_failed` is 0 on
  every landed change that has an outcome (the orchestrator merges only after a pass),
  `rework_within_3` is 0 or None everywhere, `merge_verification_ms` is None everywhere
  before W5-T1's `--duration-ms` and known only for outcomes posted after it.
- E08 found the chronology placebo invalid on this repository's request-clock data
  (persistence improved under the shuffle in 8 of 10 runs of the median pair), and on
  the change clock 23 rows gave 7 origins at H = 1.
Decision rule, written before the run (design 6.12 "One-step candidate conditioning" and
the readiness checklist): per target in {merge_verification_ms, merge_verification_failed,
rework_within_3}: (a) readiness fails -> "not assessable", the failing check and its two
numbers printed; (b) n_windows < k_min -> "not assessable" with n_windows and k_min; (c)
otherwise the paired median difference in MAE and in pinball loss between conditioned and
unconditioned, each run's calibration, and the label from `decide` on the conditioned
run's true order against its placebo; candidate conditioning "changes the forecast" iff
the paired median MAE difference is negative and its sign holds in both halves of the
origin range; never "improves prediction" without both halves, and never a causal word.
Trap: do not lower c_min, k_min or the origin limit to admit windows; the STOP outcome is
a result. Trap: pool nothing across repositories or providers; there is one repository.
Trap: TimesFM runs under `uv run --extra forecast`; record device, wall_ms per window and
the license line; the weights are non-commercial (owner acknowledged at the wave 5
gate: quote the date from docs/gates/wave-5.md).

DO: run.py enumerates the change series (build it fresh, print series_id and the
readiness table per target), runs `forecast candidate` for each target with
persistence, rolling_median, rolling_mean, local_drift and timesfm, on both halves of the
origin range, writes out/<target>.json with every window, and writes docs/experiments/E11.md
in the 7.5 template with: the decision rule above quoted, what was done (commands,
series_id, fingerprint ids, n rows, n windows, wall), what was measured (tables per
target: readiness, n_windows, MAE per forecaster per run, paired differences, calibration,
placebo validity), what it means (which branch each target took), what was rejected, how
sure, what downstream may now say (claim class predictive for a forecast number,
associative for the paired difference, and the mandatory sentence). check_numbers.py as
E10's: every number in the write-up matched to out/ or explained.

VERIFY: `uv run --extra forecast python experiments/E11/run.py` (runs to the end; prints
per-target outcomes); docs/experiments/E11.md exists with every number traced;
`uv run python experiments/E11/check_numbers.py` (unmatched numbers explained in the
file); `grep -n -i -w "cause\|impact\|would" docs/experiments/E11.md` (only inside quoted
rule text, if at all); the gates.

STOP if fewer than k_min windows exist for every target: write the decision file saying
so with the counts and what would make it assessable (36 rows inside one regime at H = 1,
or a rework or a failed merge verification on record), and stop. That is the expected
outcome on the data of 2026-09-03 and it is a valid result.

REPORT: docs/log/W5-E11.md, plus EXPLAIN FOR THE OWNER in the Teacher register. Commit
message: "W5-E11 one-step candidate conditioning on the change clock".
