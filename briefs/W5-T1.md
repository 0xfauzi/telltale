Task W5-T1: the candidate feature extractor (facts known before merge, from a git diff),
the three post-merge columns of the change clock, the TimesFM adapter reading past-future
covariates, and `telltale forecast candidate` printing the paired one-step report with the
mandatory sentence and the license line.

OWNS: src/telltale/forecast/candidate.py, src/telltale/forecast/timesfm.py (past-future
covariates only), src/telltale/series_lineage.py (the three post-merge columns and their
provenance), src/telltale/cli_forecast.py (the `candidate` subcommand), src/telltale/cli_outcome.py
and the `external.outcome` entry of src/telltale/allowlist_telltale.py (one optional field,
`duration_ms`), tests/integration/test_forecast_candidate.py (new), tests/integration/synthetic_series.py
(only if a column name moves), tests/integration/fake_agent.py (only if a flag is needed),
docs/design/amendments/W5-T1.md, docs/log/W5-T1.md.
READS: src/telltale/forecast/{__init__,backtest,readiness,decide}.py, src/telltale/repo.py,
src/telltale/series_paths.py, src/telltale/store.py, src/telltale/model.py. Never edit
store.py, model.py, sanitize.py, activities*.py, measures*.py, or forecast/backtest.py.

READ (facts checked by the orchestrator on 2026-09-03; use them, do not rediscover them):
- docs/design/01-design.md 6.12 "One-step candidate conditioning" (the targets, the
  forbidden target, the sentence, edge padding, `o <= N - 3`) and "Where TimesFM-3 bites"
  (the 32-variate cap, `padding_mode="edge"`, `past_future_covariates` must span context
  plus horizon), and the wave 3 amendment paragraphs on candidate.py.
- src/telltale/forecast/candidate.py: `conditioned(store, series, target, forecasters,
  model, *, origin_range=None, a_block=ABLATION_A, past_only=ABLATION_C, c_min=None)`
  (lines 85-147) already builds the unconditioned and conditioned runs through
  `backtester.run(..., prepare=...)` with `_attach` putting row o's A block on
  `Window.future` at length n_ctx + H (lines 199-205) and refusing a missing column or an
  unknown cell; `report(found)` (291-329) ends in `refuse_words`. The A block is a tuple
  of COLUMN NAMES read out of series rows; nothing reads a git diff. `CANDIDATE_SENTENCE`
  is forecast/__init__.py:275-279; `REFUSED_WORDS` and `refuse_words` are 361-393.
- src/telltale/forecast/timesfm.py: `_arrays` (159-180) builds the target and
  `past_only_covariates` only; `Window.future` is never read; `padding_mode` is recorded
  as a string (line 61, 100). So today the conditioned and unconditioned runs are the
  same run under every forecaster, and candidate.py warns `NO_FUTURE_READER` (74-78).
- src/telltale/series_lineage.py: `_CHANGE_COLUMNS` (119-135, fifteen names ending in
  env_changed), `_change_row` (720-746), `_changes` (669-698, one row per repo_commit
  sha, sorted by committed_ts), `LADDER` and `LOW_CONFIDENCE_RUNGS` (61-73). The three
  post-merge columns `merge_verification_ms`, `merge_verification_failed`,
  `rework_within_3` exist ONLY in the registry (forecast/__init__.py:207-221) and in
  tests/integration/synthetic_series.py (160-162, 280-282); the backtester refuses them
  on a real series with "has no column" (docs/experiments/E08.md:338-341).
- src/telltale/repo.py: `git_numstat(cwd, *args)` (334-342), `totals` (345-357, both None
  on a binary file), `per_file` (360-382: path, additions, deletions), `_Change` (76-84,
  old_path set on a rename). src/telltale/series_paths.py `columns(payload) ->
  (subsystems_touched, test_files_changed, dependency_delta)` (61-84) reads a payload's
  `per_file` list; `_TEST_DIRS`, `_TEST_GLOBS`, `_MANIFESTS` (42-53).
- src/telltale/cli_outcome.py `_post_outcome` (111-145): sanitize then `store.append`,
  `store.flush`, `store.rebuild(capture_id)`; `_OUTCOME_TYPE = "external.outcome"`; the
  allowlist entry is in allowlist_telltale.py. The home store today holds outcomes
  mechanical_verification pass 72 / fail 22 and merge_decision merged 33, none with a
  duration and no revert_or_repair at all (sqlite over ~/.telltale/telltale.db, 2026-09-03).
- The change clock of this repository today (`telltale series build --clock change --repo
  d9f783b1d318c0d65ec6b86fe729f6ff1a3581983e4287a71f6510eb80d84d6c`): 38 rows, one
  changepoint at row 29 (runtime_version 2.1.257 to 2.1.259), path columns partial (19
  of 38 rows carry no per_file list), low_confidence rows 0.
- src/telltale/cli_forecast.py: `_forecast_commands` (396-448) registers backtest,
  readiness, placebo, ablate; `forecast(args)` (477-491) dispatches; handlers take
  `(series_id, target, horizon, names, device, model)`; `_factory` (311) and `_model`
  (315) build forecasters. `backtest._licences` (786-794) is the license line and
  `candidate.report` does not print it.
Trap: `git diff --numstat base...head` (three dots) is the candidate's own changes
against the merge base; two dots would count main's drift as the candidate's. Trap: a
merge commit has no single diff; refuse a head that is a merge of base with a named
reason. Trap: the extractor reads paths and numstat only, never file contents; a binary
file makes lines_added and lines_removed None, not 0. Trap: `rework_within_3` is a
delayed label: None until three later changes exist, whatever the outcomes say. Trap:
the 32-variate cap counts target + past-only + past-future; assert it in the adapter and
never subsample. Trap: `past_future_covariates` must be length n_ctx + H per variate; the
adapter asserts that length and passes `padding_mode="edge"`; the assertion message names
the variate. Trap: `attempts_to_land` as a candidate target is refused (`FORBIDDEN`,
candidate.py:67-71) and stays refused. Trap: the readiness `variation` check will fail on
`merge_verification_failed` on this repository (every landed change passed); that is the
correct output, not a bug to route around.

DO:
(1) candidate.py `features(cwd, base, head) -> dict[str, int | None]`: the A block of a
    candidate from `git diff --numstat -z base...head` through repo.git_numstat and a
    per_file list shaped like repo.per_file, then `series_paths.columns` for the three
    path columns; keys exactly `ABLATION_A`'s names (files_changed, lines_added,
    lines_removed, subsystems_touched, test_files_changed, dependency_delta); plus
    `base_sha`, `head_sha`, `merge_base` resolved with `git rev-parse` and `git
    merge-base`; refuses (named exception) when head is a merge of base or base is not an
    ancestor. `advise_row(series, features) -> list[float | None]`: the A block as a
    past-future covariate row for origin N (the next change), used by (5) and by W5-T2.
(2) series_lineage.py: append `merge_verification_ms`, `merge_verification_failed`,
    `rework_within_3` to `_CHANGE_COLUMNS` before `env_changed`, filled in `_change_row`
    from the outcomes of the attempt that landed the change (the capture's
    external.outcome activities): `merge_verification_ms` = the mechanical_verification
    outcome's `duration_ms` when stated, else None; `merge_verification_failed` = 1 when
    that outcome's status is fail, 0 when pass, None when no such outcome; `rework_within_3`
    = 1 when a revert_or_repair outcome naming this change's task_id and attempt is
    recorded at or before the third following change, 0 when three following changes
    exist and none does, None otherwise. Provenance ids for each. `unknown_columns`
    gains a sentence for each None reason (no outcome; no duration stated; fewer than
    three later changes). Column units: ms, flag, flag. Update `tests/integration/
    synthetic_series.py` only if the column ORDER matters to it (say which).
(3) cli_outcome.py: `--duration-ms N` (optional, whole milliseconds) on `telltale outcome`,
    carried in the payload as `duration_ms`; allowlist_telltale.py `external.outcome`
    gains `"duration_ms": Kind.SIZE`. Nothing else in the payload changes.
(4) timesfm.py: `_arrays` reads `Window.future` when present into `past_future_covariates`
    (one array per named covariate, length n_ctx + H, asserted), passes them to
    `predict_batch` with `padding_mode="edge"`, asserts `1 + past_only + past_future <=
    32`, and records the past-future names in the result's `covariates` so a stored run
    says what the model saw. A window without `future` behaves exactly as today.
(5) cli_forecast.py `forecast candidate --series <id> --target <t> [--forecasters a,b]
    [--device] [--base <ref> --head <ref>]`: runs `candidate.conditioned` and prints
    `candidate.report` with the license line appended (reuse `backtest._licences`);
    with --base/--head, prints the extracted A block as a table (value or "unknown" per
    column) and the one-step forecast at origin N conditioned on it, both runs' point
    and quantiles, and the sentence. A target not in the candidate set, or
    `attempts_to_land`, exits 2 with the refusal text. `candidate.report` prints the
    license line whether or not a TimesFM run is present (it prints "no licensed model in
    this run" otherwise).
(6) A covariate-reading stub in forecast/__init__.py is NOT yours; put `FutureEcho` in
    test_forecast_candidate.py (a Forecaster whose point forecast is the last context
    value plus the future covariate's last value), registered for the test only, so the
    protocol is exercised end to end without TimesFM.
(7) tests/integration/test_forecast_candidate.py: (a) `features` on a temp repo with
    three commits (base, a candidate touching tests/test_x.py, pkg/a.py and
    pyproject.toml, and a binary file) asserts the six values computed by hand in the
    test from the numstat it wrote, `dependency_delta` 1, `test_files_changed` 1,
    `subsystems_touched` 3, and None on the binary lines; a merge head is refused with
    the reason; (b) six fake-agent attempts over two task ids through `telltale run`
    with outcomes posted by `telltale outcome` including `--duration-ms`, plus one
    `revert_or_repair` naming attempt 1 of the first task, then `series build --clock
    change` on the temp repo: the three columns carry the stated values, `rework_within_3`
    is 1 on the named change, 0 on a change with three later ones, None on the last two;
    (c) `conditioned` with `FutureEcho` on synthetic_series.write_change: conditioned MAE
    differs from unconditioned (the future reader reads), `NO_FUTURE_READER` absent, the
    sentence present in both runs' assumptions, `attempts_to_land` refused; (d) with the
    baselines only, the two runs are identical and `NO_FUTURE_READER` is present.
    Break-and-restore for (b): make `merge_verification_failed` read status "fail" as 0
    and watch (b) fail; for (c): make `_attach` return the window unchanged and watch (c)
    fail. Paste both failures.
(8) docs/design/amendments/W5-T1.md: the extractor, the three columns with their None
    rules, `--duration-ms`, the adapter's past-future handling, the CLI. State that the
    E11 series on this repository today cannot vary `merge_verification_failed` and has no
    rework event, with the counts from READ.

VERIFY: `uv run telltale forecast candidate --series <the change series built on this
repository> --target merge_verification_failed --forecasters persistence,rolling_median`
(prints both runs, the paired difference, the sentence, the license line, and a
readiness refusal or a "not assessable" label with the inequalities, whichever the data
gives; paste it); the same with `--target attempts_to_land` (exit 2, the FORBIDDEN text);
`uv run telltale forecast candidate --series <id> --target merge_verification_failed
--base main --head <this branch>` (the A block table with six values, none of them
invented: compare with `git diff --numstat main...HEAD | wc -l` and paste both);
`uv run --extra forecast telltale forecast candidate ... --forecasters timesfm` on
synthetic_series.write_change (runs; conditioned and unconditioned differ; wall_ms
recorded; numbers not interpreted); `uv run telltale outcome --kind mechanical_verification
--status pass --task-id X --attempt 1 --duration-ms 1234 --db <tmp>` then `series build
--clock change` shows 1234 in `merge_verification_ms` on the matching row; pytest -m
integration; mypy; ruff check (read in full); ruff format --check; deptry; pre-commit
(forecast-isolation passes: torch and timesfm3 imports only in forecast/timesfm.py).

STOP if `predict_batch` in timesfm 3.0.0 rejects `past_future_covariates` shaped
(n_ctx + H,) with `padding_mode="edge"` (paste the error; do not reshape by guessing).
STOP if a post-merge column would need to read observations directly rather than
activities (report the gap).

REPORT: docs/log/W5-T1.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR
NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register
(~/.claude/output-styles/Teacher.md): idea before name, one real example with real
values (the A block of this branch against main), how each number was measured, the
answer rejected (reading file contents for the A block) and why. Commit message:
"W5-T1 candidate features, post-merge columns, past-future covariates, forecast candidate".
