Task W5-T2: `telltale advise --head <ref> [--base <ref>]`: the shadow advisory, printed
and stored as a `policy.advisory` observation, never posted anywhere, never a
recommendation without its label and the sentence.

OWNS: src/telltale/cli_advise.py (new; registered from cli.py with one line each in the
parser and the dispatch table), src/telltale/report_advise.py (new), the `policy.advisory`
entry of src/telltale/allowlist_telltale.py, src/telltale/store_reads.py only if `show`
needs one read for advisories, tests/integration/test_advise.py (new),
docs/design/amendments/W5-T2.md, docs/log/W5-T2.md.
READS: src/telltale/forecast/{candidate,backtest,readiness,decide}.py (W5-T1's
`features`, `advise_row`, `conditioned`, `report`), src/telltale/cli_outcome.py (the
direct-append path: sanitize, Store.append, flush, rebuild), src/telltale/store.py
(put_forecast_run, forecast_runs reads), src/telltale/model.py. Never edit store.py,
model.py, sanitize.py, forecast/*.py.

READ: docs/spec/telltale-architecture.md 14.6 ("Forecasts remain shadow-only during
primary evaluation. When a policy begins acting on them, Telltale writes a
policy.intervention observation containing the advisory ID, action taken and policy
version.") and 17.2; docs/design/01-design.md 6.3 (`policy.intervention` {advisory_id,
action, policy_version, external_system}) and 6.12 "One-step candidate conditioning";
allowlist_telltale.py:128-133 (the `policy.intervention` entry: advisory_id ID, action
ENUM, policy_version ENUM, external_system ENUM); allowlist_telltale.py:94-110
(`telltale.repo.commit`, the shape of a telltale-emitted entry with SIZE and PATH kinds);
cli_outcome.py:111-145 (`_post_outcome`: the only CLI path that appends an observation
without a launcher; copy its shape, do not import its private function); docs/log/W5-T1.md
(what `forecast candidate` prints and stores; the forecast_run_ids it returns).
Trap: an advisory is an OBSERVATION Telltale emits about itself: provider "telltale",
surface "telltale", its own capture_id "adv_" + 24 hex of a sha256 over (head_sha,
base_sha, series_id, target, created_at) so `sessions` and `show` can list it; never
attached to an agent's capture. Trap: the payload is allowlisted: advisory_id ID,
action ENUM (always "shadow" in this task), policy_version ENUM (the reducer version of
candidate.py), base_sha ID, head_sha ID, series_id ID, target ENUM, forecast_run_ids ID
(list), label ENUM (the decision label of the latest true-order backtest of the target
on that series, or "not assessable"), readiness ENUM ("ready" or the first failing check's
name), features SIZE per A-block column (the six values; None stays None). No free text,
no prompt, no path beyond the A block counts. Trap: "confidence" is NOT a number you
invent: it is the label plus the readiness word, and the report prints both beside every
forecast number. Trap: with no passing backtest for a target the advisory prints "no
assessable forecast" and stores that as its label; it still stores the A block. Trap:
the report is passed through `forecast.refuse_words` before printing and before storing.
Trap: nothing is posted to any receiver or network; `--post` does not exist.

DO:
(1) cli_advise.py `advise(args)`: resolve base (default: the repository's default branch
    merge-base as repo.identity does) and head; `candidate.features`; for each candidate
    target with a stored true-order backtest on the repository's change series
    (store reads over forecast_runs by series_id and target; the latest by created_at):
    readiness checks, the decision label, and `candidate.conditioned` at origin N with
    `advise_row` (both runs' point and quantiles for H = 1); build the payload; sanitize
    with the allowlist; `Store.append` + `flush`; print the record through
    report_advise.render.
(2) report_advise.py `render(record) -> str`: header (advisory_id, action shadow,
    base..head, created_at), the A block table, then per target one block: readiness,
    label with the inequalities the decision evaluated (from the stored decision),
    unconditioned and conditioned point and 0.1/0.5/0.9 quantiles, the paired
    difference, the sentence, and the license line when a licensed model produced the
    run. Ends with "This advisory is shadow: it is stored and printed and nothing acts
    on it." `refuse_words` over the whole text.
(3) `telltale show <adv_capture>` lists the advisory's payload (coverage line reads
    "advisory"); `telltale sessions` shows provider telltale for it.
(4) test_advise.py: on a temp repo with a change series written by
    synthetic_series.write_change (store it under the temp home) and a stored backtest
    for `merge_verification_failed` produced by `forecast backtest` with the baselines,
    `telltale advise --base <c0> --head <c2>` prints the A block, the label, the
    sentence, and stores one observation of type policy.advisory whose payload carries
    the six features and the forecast_run_ids; `show` lists it; without any stored
    backtest it prints "no assessable forecast" and stores that label; the stored
    payload survives sanitize with no unknown_field diagnostic; a record containing
    "would" is refused (ForbiddenWord) before anything is stored. Break-and-restore:
    make render omit the sentence and watch the sentence assertion fail; restore.
(5) docs/design/amendments/W5-T2.md: the observation type, its allowlist, the capture id
    rule, and that policy.intervention (14.6) is the observation a future policy writes
    when it ACTS; advise never writes one.

VERIFY: `uv run telltale advise --head <this branch>` on this repository (prints the record;
paste it; the label per target is whatever the stored runs say, and "not assessable" is a
valid answer); `uv run telltale show <adv_...>` (the payload); `sqlite3 ~/.telltale/telltale.db
"select count(*) from diagnostics where kind='unknown_field' and detail like '%policy.advisory%'"`
on a COPY of the home store you ran against (0); pytest -m integration; mypy; ruff check (in
full); ruff format --check; deptry; pre-commit.

STOP if a value the advisory must store has no allowlist Kind that fits (report the field
and the Kinds; do not add a text kind). STOP if `forecast candidate` (W5-T1) is not merged.

REPORT: docs/log/W5-T2.md, same order as every report, plus EXPLAIN FOR THE OWNER in the
Teacher register with the real advisory printed for this branch. Commit message:
"W5-T2 shadow advisory".
