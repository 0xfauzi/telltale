Task W6-F1: two one-line defects W6-T1 found while verifying, each with the test that
fails against the un-fixed code: an empty `TELLTALE_HF_CACHE` no longer sends the
checkpoint download into the working directory, and `telltale advise` no longer mistakes
a scenario row for the newest scored backtest.

OWNS: src/telltale/forecast/__init__.py (one new stdlib helper only),
src/telltale/forecast/timesfm.py (line 126 only), src/telltale/cli_advise.py (`_latest`
only, lines 366-378), tests/integration/test_advise.py (append tests),
tests/integration/test_forecast_env.py (new, small), docs/log/W6-F1.md. Nothing else:
three other implementers are editing cli_forecast.py, series*.py, forecast/regime.py,
export, store, doctor and launch at the same time.

READ (facts checked by the orchestrator on 2026-09-03 on main at 7925a4a):
- forecast/timesfm.py:67 `CACHE_ENV = "TELLTALE_HF_CACHE"`; line 126
  `self.cache_dir = cache_dir or os.environ.get(CACHE_ENV)`. An environment variable set
  to the empty string survives that `or` as `""`, and huggingface_hub resolves `""`
  relative to the current directory: W6-T1's implementer pulled 1.29 GB of checkpoint
  into a worktree root that way (docs/log/W6-T1.md, UNSURE). The module imports numpy at
  line 42 and torch inside `__init__` at line 122, so it cannot be imported in the
  default environment (numpy is in the `forecast` extra); CI runs the default
  environment. The helper therefore lives in forecast/__init__.py, which is stdlib:
  `hf_cache_dir(explicit: str | None) -> str | None` returning `explicit or
  os.environ.get(CACHE_ENV) or None`, and timesfm.py:126 calls it. Move `CACHE_ENV`
  beside it (timesfm.py imports the name back; the name must stay importable from
  timesfm.py because docs cite it).
- cli_advise.py:366-378 `_latest(store, series_id, target)` keeps rows where
  `row["target"] == target and row["ordering"] == ORDERING_TRUE` and returns the last.
  A scenario row (W6-T1, #54) is stored with ordering true, `metrics` `{}` and
  `decision` null, so on a candidate target it becomes "the newest true-order run" and
  `_label` (381-385) prints `decider.NOT_ASSESSABLE` for it, which reads as "the rule
  looked and found too few windows" when no rule looked. Every scored run (backtest,
  placebo, candidate) carries `metrics["forecasters"]`; forecast/scenario.py:575 already
  uses `(row["metrics"] or {}).get("forecasters")` as the same distinction. The fix is
  that guard in `_latest`. After it, a series with only a scenario row gives `_label`
  row None, which prints NO_FORECAST ("no assessable forecast"), the honest sentence.
- tests/integration/test_advise.py: `_candidate`, `_series`, `_backtest`, `_advise` and
  `_advisories` helpers (lines 61-107) drive the real commands on a temp home; the
  no-backtest test at line 189 is the model for the scenario-only case.
Trap: never import telltale.forecast.timesfm in a test that CI runs. Trap: the scenario
row in the advise test is stored by the real `telltale forecast scenario` command on the
same series and target (the change clock declares the A block; horizon 1 or 4; the
target is one of the advise targets), or, if that series cannot host a scenario, through
`store.put_forecast_run` with a row shaped exactly as `scenario.stored` writes it, and
the log says which and why. Trap: a test that passes against the un-fixed code is not a
test: run each new test with the fix reverted and paste the failure.

DO:
(1) forecast/__init__.py: `CACHE_ENV` and `hf_cache_dir`; timesfm.py:126 becomes
    `self.cache_dir = hf_cache_dir(cache_dir)`.
(2) cli_advise.py `_latest`: add `and (row["metrics"] or {}).get("forecasters")` to the
    filter; the docstring says why a scenario row is not a scored run.
(3) test_forecast_env.py: `hf_cache_dir(None)` is None with the variable unset, None
    with it set to "", the path with it set to a path, and the explicit argument wins
    over the variable (monkeypatch.setenv / delenv).
(4) test_advise.py: after a real backtest and then a scenario stored on the same series
    and target, `telltale advise` prints the backtest's label, not NOT_ASSESSABLE; with
    only a scenario stored, it prints "no assessable forecast".
(5) docs/log/W6-F1.md.

VERIFY: `uv run pytest -q tests/integration/test_forecast_env.py tests/integration/test_advise.py`
(pass); revert (1) and run test_forecast_env.py (fails naming the "" case; paste);
revert (2) and run test_advise.py (the two new tests fail; paste); restore; in a temp
directory with the forecast extra: `cd $TMP && TELLTALE_HF_CACHE= uv run --project
<repo> --extra forecast telltale forecast scenario --series <the synthetic 200-row
request series written as W6-T1's log shows> --target output_tokens --horizon 4 --future
tool_calls_since_prev=2,2,2,2 --forecasters timesfm` completes from the default cache
(`ls $TMP` shows no `models--google-*` directory afterwards; the process wall printed by
`time`, which was 8.63 s with the cache warm on this machine today); pytest -m
integration; mypy; ruff check (in full); ruff format --check; deptry; pre-commit.

STOP if the fix needs any line outside the OWNS list.

REPORT: docs/log/W6-F1.md, OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT
DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register. Commit message:
"W6-F1 hf cache env and advise scored-run guard".
