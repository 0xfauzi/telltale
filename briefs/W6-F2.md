Task W6-F2: preserve Codex tool outcomes when a shell chain masks its verification outcome.

OWNS: src/telltale/activities_codex.py, one integration test file, docs/log/W6-F2.md.
READS: activities_tools.py, correlate.py, measures_intervals.py, report.py, provider fixtures and AGENTS.md.

Context: docs/gates/wave-4.md records this unresolved defect at lines 111-113.
activities_codex.py calls `_outcome` only when `exit_masked` is false.
The conditional drops a stated tool failure together with the unknown verification result.
Claude's W4-F3 repair already separates these facts.

DO:
1. Reproduce the Codex failure through the real receiver, store, reducer, and report before editing code.
2. Preserve the tool call's stated success or failure while leaving masked verification results unknown.
3. Keep the verification exit code absent and retain the `exit_masked` marker.
4. Check both successful and failed tool calls, plus a call whose outcome remains unknown.
5. Keep the change small and follow the existing Claude behavior where the sources support it.

VERIFY:
- The regression fails before the fix and passes afterwards.
- Break the fix, confirm failure, and restore it.
- Timeline names the tool failure and masked check separately.
- Failed-test counts remain unknown when the check outcome is masked.
- Run integration tests, mypy, ruff check, ruff format --check, deptry, and pre-commit.

Use temporary stores. Do not launch a live agent or a paid experiment.
Never kill a process by name pattern.
Do not change the provider's recorded observations or invent a verification outcome.

REPORT: docs/log/W6-F2.md with the reproduced failure, commands, measurements, and remaining limits.
Use commit message `W6-F2 preserve Codex tool outcomes on masked checks`.
Push a pull request and leave merging to the coordinator.
