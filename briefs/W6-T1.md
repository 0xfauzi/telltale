Task W6-T1: `telltale forecast scenario`: a conditional forecast at the end of a series
under declared future-covariate paths, refused for any covariate the registry does not
mark as declarable, stored as a predictive ForecastRun whose `scenario` records the paths
and the sentence "a scenario is a conditional forecast, not a plan".

OWNS: src/telltale/forecast/scenario.py (new), src/telltale/forecast/__init__.py (the
declarable-covariate registry and scenario horizons only), src/telltale/cli_forecast.py
(the `scenario` subcommand only; the file is at 660 lines, 140 of headroom),
tests/integration/test_forecast_scenario.py (new), docs/design/amendments/W6-T1.md,
docs/log/W6-T1.md. Do NOT edit forecast/backtest.py (798 lines) or forecast/candidate.py
(798 lines): both sit two lines under the 800-line ratchet; reuse their functions and
put new code in scenario.py. Never edit store.py, model.py, series*.py, timesfm.py.

READ (facts checked by the orchestrator on 2026-09-03):
- forecast/__init__.py: `TargetSpec` (162-173: clock, unit, nonnegative, c_min,
  horizons=HORIZONS, k_min, threshold_rule, variant), `TARGETS` (180-222), `HORIZONS =
  (1, 4)` (44), `MAX_VARIATES = 15` (61), `MAX_CONTEXT = 512` (64), `Window` (124-153)
  with `future: dict[str, list[float]] | None` (145) documented as spanning `[ctx_start,
  o + H)`, length n_ctx + H; `Forecaster` protocol (156-159). There is NO registry of
  which covariates may carry a future path: `role="past_covariate"` is hardcoded at
  series.py:160 and series_lineage.py:464, and ColumnSpec's `future_covariate` role
  (model.py:246) is never emitted.
- forecast/timesfm.py: `_arrays` (179-212) reads `Window.future` into
  `past_future_covariates` (264), asserts length n_ctx + H by name (`_span`, 215-233,
  `WrongLength`), asserts 1 + past-only + future <= 32 (199-206, `TooManyVariates`,
  module `MAX_VARIATES = 32` at 64). `padding_mode="edge"`. The baselines read the
  target only, so under a baseline two scenarios give the same forecast; under TimesFM
  they differ.
- forecast/candidate.py: `_attach` (231-251) is the model for filling `Window.future`
  from series rows; `FUTURE_PREFIX = "future:"` (93), `future_names` (96-99).
- forecast/backtest.py: `persist(store, backtest)` (671-711) writes a forecast_runs row
  through `store.put_forecast_run` with `scenario = {command, covariates, tau, placebo,
  constants}` (698-707; `scenario` is a nullable JSON column, schema.py:112);
  `refuse_words` runs over warnings, assumptions and decision before the insert;
  `_licences` (786-794) is the license line; `run(...)` takes a `prepare` hook.
- cli_forecast.py: `_forecast_commands` (521-575) registers backtest, readiness, placebo,
  ablate; `_candidate_command` (577-606); `--horizon` is `choices=HORIZONS` (only 1 and
  4); `--forecasters` is a comma string split at 642; `--device` `choices=DEVICES`;
  `forecast(args)` dispatches at 640-657.
- docs/design/01-design.md 6.12 "Where TimesFM-3 bites": the horizon is rounded up to
  64 internally, so H = 1 costs as much as 64 and past-future covariates must span
  context + H with edge padding beyond.
- README.md:207-210 (the v0.6 gate text): "longer horizons only with explicit
  future-workload assumptions".
Trap: a scenario is NOT a backtest: its origin is N (the end of the series), there is no
actual, so `metrics` is empty and the row says why; calibration comes from the latest
stored true-order backtest of the same (series, target, horizon) when one exists and is
quoted by run id, never recomputed here. Trap: refuse a path whose length is not H, a
path for a column that is the target, a path for a column not in the declarable set, a
non-finite value, and a negative value for a nonnegative column. Trap: the sentence is
appended to assumptions, printed last, and `refuse_words` must pass (the word "would"
is refused: do not write it). Trap: H beyond the registry's horizons is allowed only
from `SCENARIO_HORIZONS`; every one costs a 64-step TimesFM call and the wall is printed.

DO:
(1) forecast/__init__.py: `DECLARABLE: dict[str, tuple[str, ...]]` keyed by clock: request
    clock `("tool_calls_since_prev", "files_edited_since_prev",
    "verification_runs_since_prev", "compaction_before")`; change clock the six A-block
    columns (`ABLATION_A`). Every other column is past-only and a path for it is refused
    with the reason "past-only: this column is observed, never planned".
    `SCENARIO_HORIZONS = (1, 4, 8, 16)`. `SCENARIO_SENTENCE = "a scenario is a conditional
    forecast, not a plan"`.
(2) forecast/scenario.py: `Scenario` (frozen: name, horizon, paths: dict[str, list[float]]);
    `parse_paths(specs: list[str], horizon) -> dict` for `col=v1,v2,...` strings;
    `check(series, target, scenario)` with the refusals of the traps, each a named
    exception with the column in the message; `window_at_end(series, target, scenario,
    past_only)` builds the Window at origin N: context `rows[max(regime_start, N -
    MAX_CONTEXT):N]`, `future` = each declared column's known context values followed by
    its path (length n_ctx + H; a None in the context refuses, no policy imputes);
    `run(series, target, scenario, forecasters, model) -> dict` runs every forecaster on
    that one window and returns point and quantiles per forecaster, wall_ms, the
    declared paths, the covariates seen, warnings; `persist(store, found)` writes one
    forecast_runs row through `store.put_forecast_run` with ordering "true", horizon H,
    one window (origin N, actual None), metrics `{}` plus the warning "no actual: a
    scenario has no error", `scenario = {name, horizon, paths, declarable, sentence,
    command, calibration_from: <run id or None>, constants}`, `claim_class` predictive,
    assumptions carrying SCENARIO_SENTENCE; `compare(a, b)` prints the two scenarios'
    point paths side by side with the difference per step; `report(found)` ends with the
    sentence and the license line (reuse backtest's `_licences` through a public name
    you add in scenario.py by importing the function; do not edit backtest.py).
(3) cli_forecast.py `forecast scenario --series ID --target T --horizon H --future
    col=v1,...,vH [--future ...] [--name NAME] [--forecasters a,b] [--device] [--model]
    [--compare-with RUN_ID]`: runs, persists, prints; with `--compare-with` prints
    `compare` against a stored scenario row. Exit 2 with the refusal text on any refusal.
(4) test_forecast_scenario.py (mark integration): on `synthetic_series.write` (request
    clock, 200 rows) with the stub and baselines: (a) two scenarios differing in one
    declared path store two rows, both `claim_class` predictive, both with the sentence in
    assumptions and in `scenario.sentence`, `scenario.paths` equal to the declared paths,
    one window with `actual` None; (b) a path for a past-only column is refused naming
    the column; a path of the wrong length is refused; a path for the target is refused;
    a horizon outside SCENARIO_HORIZONS is refused; (c) `compare` prints a difference
    line per step; (d) a covariate-reading stub defined in the test (point = last
    context value + the future path's last value) makes the two scenarios' points differ
    by exactly the difference of the paths' last values. Break-and-restore: make
    `window_at_end` drop the path (future = context only) and watch (d) fail; paste it.
(5) docs/design/amendments/W6-T1.md: the declarable registry per clock and why those
    columns, SCENARIO_HORIZONS, the row shape, the sentence, what a scenario is not.

VERIFY: `uv run telltale forecast scenario --series <the synthetic request series id
printed by the test, or one you write with synthetic_series.write into a temp home>
--target output_tokens --horizon 4 --future tool_calls_since_prev=2,2,2,2 --name quiet
--forecasters persistence,rolling_median` and the same with `=8,8,8,8 --name busy` (two
rows in forecast_runs; `--compare-with` prints the per-step difference and the sentence;
under the baselines the points are equal and the report says the baselines do not read
covariates); `uv run --extra forecast telltale forecast scenario ... --forecasters timesfm`
for both (points differ; wall_ms printed; the license line); a past-only path
(`--future fresh_input_tokens=1,1,1,1`) exits 2 naming the column; `--horizon 3` exits
2; pytest -m integration; mypy; ruff check (read in full); ruff format --check; deptry;
pre-commit (forecast-isolation passes; backtest.py and candidate.py untouched:
`git diff --stat main -- src/telltale/forecast/backtest.py src/telltale/forecast/candidate.py`
is empty).

STOP if the TimesFM call for H = 16 exceeds 120 s on this machine for one window (report
the wall and reduce SCENARIO_HORIZONS to what was measured, saying so). STOP if a change
to backtest.py or candidate.py turns out to be unavoidable (report which function and why).

REPORT: docs/log/W6-T1.md in the usual order plus EXPLAIN FOR THE OWNER in the Teacher
register with the quiet-versus-busy example and its real numbers. Commit message:
"W6-T1 scenario horizons".
