Task W6-T2: policy intervention regimes: `telltale intervention` posts a
`policy.intervention` observation, `series build --regime pre|post --intervention ID`
segments a lineage series at it, an unsegmented series carries the boundary as a
changepoint and in its cohort, and every forecast command refuses to pool across it
unless `--pooled` is given and then prints the boundary.

OWNS: src/telltale/cli_intervention.py (new; registered from cli.py with one line each in
the parser and the dispatch table, as cli_advise.py and cli_outcome.py are),
src/telltale/series_regime.py (new), src/telltale/series_lineage.py (793 lines: the
minimum hook to call series_regime, split the file first if a line does not fit),
src/telltale/series.py (`build` signature: regime and intervention arguments),
src/telltale/cli_forecast.py (the `--regime`, `--intervention` and `--pooled` flags and
the refusal; W6-T1 added `scenario` there, so start from main after #W6-T1 merged),
src/telltale/forecast/regime.py (new: the refusal check; forecast/backtest.py is at 798
lines and is NOT to be edited), tests/integration/test_regime.py (new),
docs/design/amendments/W6-T2.md, docs/log/W6-T2.md. Never edit store.py, model.py,
sanitize.py, receiver.py, backtest.py.

READ (facts checked by the orchestrator on 2026-09-03):
- `policy.intervention` is allowlisted at allowlist_telltale.py:162-167 with exactly
  four fields (advisory_id ID, action ENUM, policy_version ENUM, external_system ENUM),
  routed by the receiver at receiver.py:75 (`/v1/policy_interventions`), and READ BY
  NOTHING: grep of src/ finds only the allowlist, the route and three comments
  (cli_advise.py:29,77; report_advise.py:43; cli.py:58). `advise` writes a
  `policy.advisory` with action "shadow" and never an intervention.
- cli_outcome.py `_post_outcome` (111-145) is the model for a CLI that appends a
  telltale-emitted observation: sanitize, `Store.append`, `flush`, `rebuild`. An
  observation carries `provider_ts` and `ingest_ts` (model.py Observation, 124-143).
- Changepoints: series.py `env_column(fingerprints) -> (name, column, changepoints)`
  (517-535), a changepoint where two known fingerprints differ; used at series.py:206
  (request clock) and series_lineage.py:489; stored at series.py:231 and
  series_lineage.py:520. `Series.changepoints` is the list the backtester reads.
- series.py `build(store, clock, key, missingness_policy="exclude")` (172-193) dispatches
  lineage clocks to series_lineage.build (168-172) -> attempt_series / change_series;
  the cohort is derived (`_cohort`, series_lineage.py:545-561: repo_id, providers,
  captures, dropped; plus unknown_columns on the change clock). `series_id` hashes
  clock, cohort, columns, reducer_version and rows (design 6.12).
- forecast/backtest.py `plan` (271-284) and `_at_origin` (325-351): `ctx_start = max(
  regime_start(changepoints, origin), origin - MAX_CONTEXT)`; a window with fewer than
  c_min rows since the last changepoint is dropped `regime_too_short`; a changepoint
  inside the horizon drops the window. There is no regime or pooled parameter anywhere
  (grep "regime" matches only backtest.py; "pooled" only stats.py and report_probe.py).
- cli_forecast.py: `series build` (`_series_commands`, `--repo`, `--policy`), the five
  forecast subcommands plus W6-T1's `scenario`; handlers resolve the series by id
  through the store.
- spec 14.6: "When a policy begins acting on them, Telltale writes a policy.intervention
  observation containing the advisory ID, action taken and policy version. Subsequent
  performance is evaluated as a new regime." README.md:207-210 (v0.6 text).
Trap: an intervention has a position on a lineage clock by TIME: the boundary is the
first row whose `row_end_ts` is at or after the intervention's `provider_ts` (or
`ingest_ts` when none). The request clock is one capture; an intervention inside a
capture is refused with the reason "regimes are lineage-clock statements". Trap: the
boundary is recorded as a changepoint AND in the cohort (`interventions: [{advisory_id,
boundary_index, ts}]`), so the series_id changes when an intervention lands, which is
right: it is a different frame. Trap: `--regime pre` keeps rows before the boundary and
`post` rows at or after it; both record `regime` and the intervention id in the cohort;
neither pads. Trap: the refusal to pool is a check on the SERIES (its cohort lists an
intervention boundary and no regime was chosen), made in forecast/regime.py and called
from every cli_forecast handler before `backtest.run`; `--pooled` bypasses it and the
report's first line names the boundary index and the advisory id. Trap: a pooled run
stores `pooled_across: [advisory_id]` in the forecast_runs `scenario` JSON through the
existing persist path (add it to the dict the handler passes; do not edit backtest.py).

DO:
(1) cli_intervention.py: `telltale intervention --advisory-id ID --action ACTION
    --policy-version V --external-system NAME [--at ISO8601] [--db PATH]`: builds the
    payload, sanitizes it with the allowlist (ACTION and NAME are ENUM kinds: bounded
    lowercase tokens), appends one `policy.intervention` observation (provider telltale,
    surface telltale, its own capture id `pol_` + 24 hex of a sha256 over the payload
    and the timestamp, as `advise` does with `adv_`), flushes; prints the observation id
    and the timestamp. `--at` sets `provider_ts` (an intervention recorded late still
    positions by when it happened); default now.
(2) series_regime.py: `interventions(store, repo_id) -> list[Intervention]` (read the
    observations of that type; each with advisory_id, ts, observation_id);
    `boundary(rows_end_ts, ts) -> int | None`; `segment(series_rows, meta, regime,
    boundary)`; `cohort_entry(...)`. series_lineage.py calls it once after assembling
    rows: adds boundaries to `changepoints` (deduplicated, sorted), the cohort entry, and
    applies the regime filter when one was asked for. series.py `build(store, clock, key,
    missingness_policy, regime=None, intervention=None)`; `regime` without
    `intervention` when exactly one intervention exists for the repo is allowed, more
    than one refuses naming them.
(3) forecast/regime.py: `check(series, pooled: bool) -> list[str]` returns the refusal
    lines (empty when no boundary, or when a regime was chosen, or when pooled); every
    forecast handler in cli_forecast.py calls it and exits 2 on a refusal; with
    `--pooled` it prints "pooled across intervention <id> at row <i>" as the first line
    and the run's `scenario` carries `pooled_across`.
(4) cli_forecast.py: `series build --regime pre|post --intervention ID` and `--pooled` on
    backtest, placebo, ablate, candidate and scenario.
(5) test_regime.py: six fake-agent attempts over two task ids through `telltale run`
    (conftest `launched` and W3-T1's test are the model) with outcomes posted, an
    intervention posted with `--at` between attempts 3 and 4: `series build --clock
    attempt` gives 6 rows, changepoints containing 3, cohort `interventions` naming the
    id and boundary 3; `--regime pre` gives 3 rows and `post` 3 rows, each cohort saying
    so, and the three series ids differ; `forecast backtest` on the unsplit series exits
    2 naming the boundary, `--pooled` runs (with the stub and baselines on a longer
    synthetic attempt series if 6 rows cannot backtest: write it with synthetic_series
    and post the intervention by `--at` inside its time span) and prints the boundary
    line and stores `pooled_across`; an intervention on the request clock is refused;
    the observation survives sanitize with 0 unknown_field diagnostics. Break-and-restore:
    make `boundary` return None and watch the changepoint and cohort assertions fail;
    paste it.
(6) docs/design/amendments/W6-T2.md: the intervention command, the boundary rule, the
    cohort entry, the refusal and `--pooled`, what changes in series_id and why.

VERIFY: on a temp home with the fake-agent attempts of the test, the exact commands of
(5) pasted with their output; on a copy of the home store: `uv run telltale intervention
--advisory-id adv_test --action shadow --policy-version test --external-system telltale
--at 2026-09-03T12:00:00Z` then `uv run telltale series build --clock change --repo
d9f783b1d318c0d65ec6b86fe729f6ff1a3581983e4287a71f6510eb80d84d6c` (the cohort names the
intervention and its boundary index; changepoints include it; paste), `--regime pre`
and `--regime post` (row counts add up to the unsplit count), `uv run telltale forecast
backtest --series <unsplit id> --target attempts_to_land` (exit 2 naming the boundary)
and with `--pooled` (runs or refuses on readiness, but past the pooling check, with the
boundary line first); pytest -m integration; mypy; ruff check (in full); ruff format
--check; deptry; pre-commit.

STOP if the boundary rule needs a field the observation does not carry (report it; do
not add a field to the allowlist beyond the four). STOP if a change to backtest.py is
unavoidable (report which function).

REPORT: docs/log/W6-T2.md plus EXPLAIN FOR THE OWNER in the Teacher register with the
six-attempt example. Commit message: "W6-T2 policy intervention regimes".

ADDENDUM for attempt 2 (written by the orchestrator on 2026-09-03). Attempt 1 was
killed at 17:28:48 UTC by a sibling session's `pkill -f "pytest -m integration"` while
it was running its VERIFY lines (its last output was the `telltale intervention` run on
the home-store copy). Its worktree, which is your working directory, holds attempt 1's
uncommitted work: modified cli.py, cli_forecast.py, series.py, series_lineage.py and
new cli_intervention.py, forecast/regime.py, series_regime.py,
tests/integration/test_regime.py; no docs/log/W6-T2.md and no amendment yet. Start by
reading `git status` and `git diff`, treat that code as a draft you own and must check
against every DO item rather than trust, then complete VERIFY, the amendment and the
log, commit and push. Never kill a process by name pattern on this machine: other
sessions run here; kill by pid only.
