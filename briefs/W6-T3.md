Task W6-T3: `telltale export --format jsonl|parquet --out DIR`, its round trip back into an
empty store, `purge --diagnostics-older-than N`, a `retention` section in `doctor`, and
`telltale schema`.

OWNS: src/telltale/export.py (new), src/telltale/cli_export.py (new; registered from
cli.py with one line each in the parser and the dispatch table, as cli_advise.py is),
src/telltale/cli.py (the purge flag and the two registrations only; 678 lines),
src/telltale/doctor.py (the retention section; 293 lines), src/telltale/importer.py or
cli_import.py (one `import export` kind only; importer.py is at 727 lines),
pyproject.toml (delete the deptry DEP002 entry for pyarrow when its importer lands, as
the comment at 261-262 instructs), tests/integration/test_export.py (new),
docs/design/amendments/W6-T3.md, docs/log/W6-T3.md. Never edit store.py (796 lines,
and the only INSERT site for derived tables by hook), schema.py, sanitize.py, model.py.

READ (facts checked by the orchestrator on 2026-09-03):
- store.py public surface: `append` (302), `diagnose` (323), `purge(capture_id)` (400,
  deletes observations and diagnostics by capture then rebuilds), `purge_diagnostics
  (older_than_days)` (406-410: the only age-based deletion, cutoff `now - days`, NO CLI
  caller), `rebuild` (373), `health` (473). Reads live in store_reads.py `Reads`:
  `captures` (32), `observations_of_type` (35), `observations` (50), `activities` (87),
  `evidence` (106), `series` (111), `series_ids` (134), `forecast_runs` (143),
  `diagnostics` (149). Column tuples per table are in store.py around 96-105.
- schema.py: six STRICT tables and the captures view; `diagnostics.kind` CHECK
  ('parse_failure','dropped','conflict','unknown_field','level2_raw','launcher') (120-125).
- cli.py: `purge` parser at 537-538 takes one positional `capture_id` and no flag;
  `_COMMANDS` dispatch table at 634-660; `cli_advise.add_commands` is the registration
  model (557). There is no `export` and no `schema` command; design 6.13 lists both.
- doctor.py: `roundtrip()` (175-207) uses a THROWAWAY store under config.home() and
  rmtree's it; `_diagnostic_kinds` (223-229) counts kinds in that throwaway store only;
  `tool_rows` (268-283); cli.py `doctor(port)` (112-126) prints three blocks and exits 1
  naming the first failing surface. Nothing reports the real store's diagnostics.
- pyproject.toml: `export = ["pyarrow"]` extra (31-32); deptry `DEP002 = ["pyarrow"]`
  (261-262) with the instruction to delete the entry when the importer lands; mypy
  override for pyarrow at 167-168. No parquet or jsonl writer exists in src/.
- sanitize.py `sanitize(obs_type, raw_payload, level, ctx)` (236) and the allowlist
  tables (allowlist.py `ALLOWLIST`, allowlist_telltale.py `telltale_tables`,
  allowlist_codex.py): `Kind` enum at allowlist.py:14-22.
- cli_import.py and importer.py: the import kinds (`claude-transcripts`,
  `codex-rollouts`), `--root`, `--dry-run`, idempotence by capture id.
- The pre-commit hook `derived-writes-only-in-store` forbids INSERT INTO activities,
  evidence, series_snapshots, forecast_runs outside store.py: the export round trip
  therefore re-imports OBSERVATIONS only and rebuilds derived rows with `store.rebuild`;
  it never inserts a derived row.
Trap: the export must carry `claim_class` in every derived table's output (activities,
evidence, forecast_runs; series_snapshots rows carry the reducer_version) and must not
invent a column: export exactly the stored columns. Trap: jsonl export is stdlib only;
`import pyarrow` happens inside the parquet branch and nowhere else (forecast-isolation
is about torch and timesfm; deptry is about pyarrow). Trap: paths under `--out DIR`
only; refuse `--out` inside `$TELLTALE_HOME`. Trap: the re-import is idempotent
(observation_id is the primary key; a second import adds 0) and refuses a file whose
rows fail `sanitize` at the level recorded on the observation (a row that was sanitized
once must pass again: that is the check that the export did not leak). Trap:
`purge --diagnostics-older-than 0` deletes everything older than now: refuse 0 and
negatives. Trap: `doctor` must open the REAL store read-only for the retention section
and never write to it.

DO:
(1) export.py: `export(store, out_dir, fmt) -> dict[str, int]` writing one file per table
    (`observations`, `activities`, `evidence`, `series_snapshots`, `forecast_runs`,
    `diagnostics`) as `<table>.jsonl` (one JSON object per row, stored column names) or
    `<table>.parquet` (pyarrow, imported inside the branch), plus `MANIFEST.json` {tables:
    {name: rows}, exported_at, reducer versions seen, telltale version}. Streams rows;
    never loads a table into memory at once.
(2) cli_export.py: `telltale export --format jsonl|parquet --out DIR [--capture ID]`
    (`--capture` restricts observations, activities, evidence and diagnostics to one
    capture; series and forecast rows are not per capture and are exported whole);
    `telltale schema` prints JSON: the observation types with their allowlisted fields
    and Kinds (from the allowlist tables), the four durable shapes' field names (from
    the dataclasses), the claim classes and coverage words.
(3) the round trip: `telltale import export --root DIR [--dry-run]` reads
    `observations.jsonl`, sanitizes each payload at its own level, appends through
    `store.append`, flushes, rebuilds each capture touched; prints added and skipped
    counts; the diagnostics file is re-imported through `store.diagnose` only for kinds
    the CHECK allows.
(4) cli.py: `purge --diagnostics-older-than N` (mutually exclusive with the capture
    positional) calling `store.purge_diagnostics`, printing the count and the cutoff.
(5) doctor.py: a fourth block `retention` from the real store opened read-only: total
    diagnostics, count and oldest `ingest_ts` per kind, and the age in days of the
    oldest, with the line "purge --diagnostics-older-than N removes rows older than N
    days"; never part of the exit code.
(6) test_export.py: (a) replay claude S1 and codex S1 through the receiver, export jsonl,
    import into an EMPTY temp home, assert identical observation counts per type and
    per capture, `show` output identical for both captures after rebuild (compare the
    JSON minus reducer_version), a second import adds 0; (b) the parquet branch under
    `uv run --extra export`: file exists, row counts equal (skip with a reason when
    pyarrow is not importable: `pytest.importorskip`); (c) every derived table's export
    carries `claim_class` on every row; (d) purge one of the two captures and rebuild:
    the other capture's activities and evidence rows are byte-identical before and after
    (compare the stored JSON); (e) `purge --diagnostics-older-than 1` on a store with a
    diagnostic backdated by 2 days (write it with `store.diagnose`, then adjust nothing
    else: use a helper that inserts through the public API with a frozen clock if the
    store offers one, else mark the test as needing the age to be set by waiting, and
    STOP to report) removes it and leaves a fresh one; (f) `doctor` prints the retention
    block and its exit code is unchanged by it; (g) the export-then-import of a payload
    that was sanitized at level 0 passes sanitize at level 0 again. Break-and-restore:
    make the importer skip `sanitize` and plant an unsanitized prompt field in the jsonl
    to watch (g) fail; paste it.
(7) docs/design/amendments/W6-T3.md: the files, the manifest, the round trip rule (observations
    only, derived rows rebuilt), the retention section, `schema`.

VERIFY: on a copy of the home store (`sqlite3 ~/.telltale/telltale.db ".backup /tmp/x/telltale.db"`,
TELLTALE_HOME=/tmp/x): `uv run telltale export --format jsonl --out /tmp/x-out` (prints
rows per table; `wc -l` of each file equals the table's count from sqlite3, paste both);
`uv run --extra export telltale export --format parquet --out /tmp/x-parquet` (files
exist; row counts equal); `uv run telltale import export --root /tmp/x-out --dry-run`
against an EMPTY temp home (counts), then the real import (counts equal the export;
`telltale sessions` lists the same captures; `show` of two captures identical to the
source store's after rebuild); `uv run telltale purge --diagnostics-older-than 7` on the
copy (prints the count and cutoff; the count equals a sqlite3 count you paste beside it);
`uv run telltale doctor` (the retention block; exit 0); `uv run telltale schema | python3
-c "import json,sys; d=json.load(sys.stdin); print(len(d['observation_types']))"`;
pytest -m integration; mypy; ruff check (in full); ruff format --check; deptry (the
DEP002 entry deleted and deptry still clean); pre-commit.

STOP if a stored column cannot be exported without transformation (report the column
and why). STOP if the round trip changes any observation count (report the type and the
diagnostic the import wrote).

REPORT: docs/log/W6-T3.md plus EXPLAIN FOR THE OWNER in the Teacher register with the
real counts of the home-store copy. Commit message: "W6-T3 export, purge, retention, schema".
