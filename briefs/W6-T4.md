Task W6-T4: `doctor --matrix`, the launcher's three failure modes provoked and made
legible, and `setup --print --daemon` printing a launchd plist.

OWNS: src/telltale/doctor.py (293 lines) and src/telltale/doctor_matrix.py (new),
src/telltale/cli.py (the `--matrix` flag, the `--daemon` flag on setup, and the daemon
bind error only; 678 lines), src/telltale/launch.py (734 lines; the locked-database
and signal paths only), src/telltale/setup_daemon.py (new; the plist text),
tests/integration/test_hardening.py (new), docs/design/amendments/W6-T4.md,
docs/log/W6-T4.md. Do NOT edit providers/codex.py (800 lines, zero headroom) or
providers/claude.py; read their DRIFT lists. Never edit store.py, receiver.py.

READ (facts checked by the orchestrator on 2026-09-03):
- DRIFT lists: providers/claude_drift.py:80 `DRIFT: list[str]` (re-exported at
  providers/claude.py:39), providers/codex.py:99 `DRIFT: list[str]`; `CAPABILITIES` at
  claude_drift.py:24 and codex.py:63; the Provider protocol declares `DRIFT` at
  providers/__init__.py:90.
- Runtime version per capture: `launch.runtime_version(executable)` (launch.py:328-362)
  probes `<binary> --version`, cached under `$TELLTALE_HOME/<VERSIONS_FILE>`; it is part of
  the environment fingerprint and `sessions` prints it as `runtime`. Per-record
  `service_version` from the OTel resource (claude.py:320, codex.py:551). Coverage per
  capture is stored on the lifecycle activity (design 6.7/6.10).
- doctor.py: `roundtrip()` (175-207), `daemon_row` (241-249), `_probe_daemon` (252-265),
  `tool_rows` (268-283). cli.py `doctor(port)` (112-126) takes only `--port`.
- launch.py: `_open` (213-249) builds the Receiver with no port so the OS picks one (a
  port-in-use failure is impossible for `run`; any exception there prints
  "telltale: not recording this run" and returns None: fail open); `_child` (470-496):
  `code if code >= 0 else 128 - code` (493-495) so a child killed by signal N exits
  128 + N; `_signals` (512-546) relays SIGTERM to the child and absorbs SIGINT (the
  terminal delivers SIGINT to the whole group); store `BUSY_TIMEOUT_MS = 5000`
  (store.py:61), `RETRY_DELAYS_S` (67), `_give_up` (620-639) writes a launcher-kind
  diagnostic when a batch is lost. `daemon` (cli.py:252-284) passes an explicit port
  (266) and a bind failure propagates uncaught from `receiver.start()`.
- cli.py `setup` (163-172): `--apply` prints `_REFUSAL`; codex prints `_CODEX_PENDING`;
  claude prints the settings JSON derived from the real launch plan (`_claude_snippet`,
  128-137). No `--daemon`, no plist anywhere (grep of src, README, 03-capture-howto).
- AGENTS.md invariant 7 (never edit global config; setup PRINTS) and 8 (capture fails
  open; every Telltale exception is a diagnostic; the child's exit code is preserved).
Trap: the plist is printed, never written; it names `telltale daemon --port N --level L`
with the absolute path of the `telltale` binary that is running (`sys.argv[0]` resolved)
and `$TELLTALE_HOME` in `EnvironmentVariables`; the label is `com.telltale.daemon`; the
report says exactly what the owner may do with it (`launchctl load ~/Library/LaunchAgents/
com.telltale.daemon.plist`) and that Telltale never does it. Trap: provoking "database
locked" means a SECOND process holding a write lock (`BEGIN IMMEDIATE` and sleep) on
the same telltale.db while a `telltale run` captures; measure what happens today before
changing anything, paste it, and only then make the outcome legible (a launcher
diagnostic that names the lock and the number of batches lost, and `doctor` showing the
last such diagnostic). Trap: the child's exit code is never changed by any of this.
Trap: `--matrix` reads the real store read-only.

DO:
(1) doctor_matrix.py: `matrix(store) -> list[dict]`: one row per (provider,
    runtime_version) seen in captures: captures n, first and last seen, the coverage
    words per capability as the lifecycle activities recorded them (the set, e.g.
    "compaction: observed 12, partial 3, unavailable 1"), surfaces seen, and the
    provider's DRIFT entries; rendered as a table plus the DRIFT list per provider once.
    cli.py `doctor --matrix` prints it after the existing blocks; never part of the exit
    code.
(2) Failure modes, each provoked in test_hardening.py through the real launcher with the
    fake agent (conftest `launched` is the model) and each ending in a diagnostic of kind
    `launcher` whose detail names the mode, with the child's exit code preserved:
    (a) receiver port in use: for `run` the port is OS-chosen, so the test asserts a run
        succeeds while another process holds the daemon port; for `daemon`, a held port
        makes `telltale daemon --port N` exit 1 with one line naming the port and the
        holder ("port N held, not answering" or "answered by something else", reusing
        `_probe_daemon`), no traceback.
    (b) child killed by signal: `telltale run -- python fake_agent.py --sleep 30`
        (add `--sleep` to the fake agent if no flag lets it idle; say so) and SIGTERM to
        the launcher: the child receives it, the launcher exits 143, `capture_ended`
        records exit_code 143 and `terminal: signal 15`; SIGKILL to the child directly:
        the launcher exits 137 and the capture says so.
    (c) database locked: a second process holds `BEGIN IMMEDIATE` on the store for
        longer than the 1.5 s retry ladder while a run captures; the run completes with
        the child's exit code, the store afterwards holds a `launcher` diagnostic naming
        the lock with the count of lost batches, and `telltale doctor` prints that last
        diagnostic in the retention or a new `last launcher diagnostic` line.
(3) setup_daemon.py: `plist(port, level, home, binary) -> str` and cli.py
    `setup claude|codex --print --daemon [--port N] [--level L]`: prints the plist text
    (XML, `plutil -lint -` clean) and, after it, the same provider snippet as without
    `--daemon` but pointing at the daemon's fixed port. `--apply` still refuses.
(4) docs/design/03-capture-howto.md gains the daemon-plist paragraph (what it is, that
    Telltale prints and never installs it).
(5) docs/design/amendments/W6-T4.md: the matrix row shape, the three failure modes with
    the measured behaviour before and after, the plist.

VERIFY: `uv run telltale doctor --matrix` on a copy of the home store (rows for claude
2.1.257 and 2.1.259 and codex 0.150.1 at least, with coverage counts; the DRIFT lists
printed once each; paste the table); `uv run telltale setup claude --print --daemon
--port 4318 | head -40` and `... | sed -n '/<?xml/,/<\/plist>/p' | plutil -lint -`
("OK"); provoke (a), (b), (c) by hand exactly as the tests do and paste the launcher
line, the exit code (`echo $?`) and the diagnostic row from sqlite3 for each; `ls
~/Library/LaunchAgents | grep -c telltale` is 0 before and after (nothing written);
pytest -m integration; mypy; ruff check (in full); ruff format --check; deptry;
pre-commit.

STOP if making the locked-database outcome legible needs an edit to store.py (report the
function and what it lacks; do not edit it). STOP if a signal path changes the child's
exit code in any measured case (report the case).

REPORT: docs/log/W6-T4.md plus EXPLAIN FOR THE OWNER in the Teacher register with the
three provoked failures and their real numbers. Commit message: "W6-T4 compatibility
matrix and hardening".
