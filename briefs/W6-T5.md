Task W6-T5: the public release pass: make the repository presentable the day the owner
flips it public, with every claim in the README executed rather than asserted, a
documentation index that a hook keeps complete, a link checker, the research status
ticked from the gate reports and nothing else, and a CHANGELOG that lists what merged.

OWNS: README.md, docs/README.md, CHANGELOG.md, CITATION.cff, CONTRIBUTING.md,
SECURITY.md, .github/ISSUE_TEMPLATE/*, .github/PULL_REQUEST_TEMPLATE.md,
scripts/check_links.py (new), .pre-commit-config.yaml (one local hook added at the end
of the telltale-only block, nothing else touched), docs/log/W6-T5.md. READS: everything
else. Never edit src/, tests/, experiments/, docs/spec/, docs/gates/, docs/log/* (other
than your own), docs/experiments/*, briefs/*, pyproject.toml.

READ (facts checked by the orchestrator on 2026-09-03; re-check each on the day you
start, wave 6 merges land before you):
- `uv run telltale --help` lists 20 subcommands: doctor, setup, run, daemon, experiment,
  purge, resanitize, import, sessions, timeline, show, explain, rebuild, vector, compare,
  outcome, advise, series, forecast, profile. Wave 6 adds at least `export`, `schema`,
  `intervention`, `forecast scenario`, `doctor --matrix`, `setup --daemon`, `import
  export`. The list you document is the list `--help` prints on the day, not this one.
- README.md is stale from wave 0: line 65 "Two commands exist today", line 74 "`telltale
  doctor` is a stub", lines 78-92 wave markers; the Research status checklist (lines
  190-211) has every gate unticked. Section order: What Telltale is, What it is not,
  Quickstart, What gets stored and what never does, Claim classes, Architecture,
  Documentation, Research status, Licensing, Citation. Keep that order.
- docs/README.md indexes docs/spec, docs/design (four files; the amendments directory is
  not listed), docs/experiments (template row only; E01 to E12 exist), docs/log (2 of 50
  files listed), briefs (pattern row), docs/assets. docs/gates/ (wave-0.md to wave-5.md,
  wave-6.md arrives) is not indexed at all.
- CHANGELOG.md "Unreleased" describes W0-T1 only. `gh pr list --state merged --limit 100
  --json number,title,mergedAt` is the record of what landed; every PR title starts with
  its task id.
- scripts/ holds only precommit/ (four ratchet and hygiene scripts). No link checker
  exists anywhere: `ls scripts; grep -rn "check_links\|link" .pre-commit-config.yaml`
  finds nothing.
- Repository: visibility PRIVATE, description "A flight recorder for coding agents. Every
  number carries its claim class.", 7 topics set. CITATION.cff, both issue templates and
  the PR template exist. LICENSE is MIT; README "Licensing" already names the TimesFM-3
  weights license `timesfm-non-commercial-license-v1.0`; the owner acknowledged it on
  2026-09-03 (docs/gates/wave-5.md).
- The gate verdicts, which are the ONLY source for a tick: docs/gates/wave-0.md (v0.0),
  wave-1.md (v0.1), wave-2.md (v0.2), wave-3.md (v0.3), wave-4.md (v0.4, "Exit criterion
  (spec 21 v0.4)", final on 2026-09-03), wave-5.md (v0.5: NOT met, "keep it a research
  command", the advisory stays shadow), wave-6.md (v0.6: written by the orchestrator
  after your siblings merge; if it does not exist when you start, v0.6 stays unticked
  and the report says so).
Trap: the quickstart is executed, line by line, in a FRESH CLONE under a temp HOME and a
temp TELLTALE_HOME, and every command's output is pasted into the report. The wrapped
session in the quickstart is documented as the real command (`telltale run --provider
claude -- claude -p ...`), but you do not run it: run the documented fallback `uv run
telltale run -- bash -c 'echo hello; exit 0'` and say in the README that the same
command wraps any process. Never start an agent session. Trap: a tick in Research
status is a citation: each ticked line links the gate file; an unticked line quotes the
gate's one-sentence verdict. Trap: `gh repo edit --visibility` is NOT run; the flip is
the owner's click and the report says exactly which page and button. Trap: badges that
query the repository (the CI workflow badge) do not render for anonymous viewers while
private; curl each badge URL without auth and record the HTTP status; do not replace a
gated badge with a static lie. Trap: no emoji, no em dashes, anywhere, including the
templates. Trap: the index completeness rule is enforced by the hook, not by prose:
every file under docs/ (except docs/spec/media/*, docs/assets/*.png and docs/log/*.md,
which the index lists by pattern with a count the hook checks) must be linked from
docs/README.md.

DO:
(1) scripts/check_links.py: stdlib, under 60 lines: walks README.md, CONTRIBUTING.md,
    SECURITY.md, docs/**/*.md and briefs/*.md; for every relative Markdown link
    `[..](path[#anchor])` checks the target file exists relative to the linking file;
    for `#anchor` on a .md target checks a heading slug matches (GitHub slug rule:
    lowercase, spaces to hyphens, punctuation dropped); then checks index completeness
    as in the trap; prints every failure as `file:line: target` and exits 1 on any.
    Register it as a local pre-commit hook `docs-links-resolve` (language: python,
    pass_filenames: false, always_run: true) with a comment stating the invariant.
(2) docs/README.md: add sections for docs/gates/ (one row per wave file with its verdict
    in one line), docs/design/amendments/ (README plus whatever files remain after the
    orchestrator's folds), docs/experiments/ (one row per E##.md with the question in
    one line), docs/log/ (one row per file is acceptable, or the pattern row plus the
    count the hook checks: `N reports, one per merged task, immutable`), briefs/.
(3) README.md: rewrite Quickstart from the shipped commands (install, doctor, wrap one
    session, sessions, show, timeline, explain; the forecast commands in one sentence
    with the license line); add a "Commands" table after Quickstart: one row per
    subcommand as `--help` prints it, one line each, no invented flags (copy the
    first sentence of each subcommand's own `--help`); Documentation section links
    docs/README.md, the gates directory and the experiments directory; Research status
    ticked per the gate files; What gets stored: check each row against
    `uv run telltale schema` output (W6-T3) and correct rows, add none. Do not touch the
    hero, the tagline, Claim classes, What it is not, Licensing or Citation except to
    fix a link.
(4) CHANGELOG.md: "Unreleased" rewritten from the merged PR list, grouped by wave, one
    line per PR with its number, in merge order; the version stays 0.0.1 and no tag is
    cut (the release version is the owner's decision at the flip; say so in the report).
(5) CITATION.cff: check `git log --format=%an | sort -u` against the author entry; leave
    the date-released and DOI absent. Templates: read all three; fix stale references
    only.
(6) The fresh-clone run of (3)'s quickstart: `git clone <repo> $TMP/telltale-clone`
    with `HOME=$TMP/home TELLTALE_HOME=$TMP/home/.telltale`, `uv sync`, then every
    quickstart command in order; paste every output in the report.

VERIFY: `uv run python scripts/check_links.py` (0 failures; then break one link in a
scratch copy of docs/README.md and watch it name file:line: target; restore);
`uv run pre-commit run docs-links-resolve --all-files` (passes); the fresh-clone
transcript pasted in full; `for u in $(grep -o 'https://img.shields.io[^)]*\|https://github.com/0xfauzi/telltale/actions/workflows[^)]*' README.md); do curl -s -o /dev/null -w "%{http_code} $u\n" "$u"; done`
(statuses pasted; the gated ones named); `grep -c "\[x\]" README.md` equals the number
of gate files whose verdict says met, with the list; `gh pr list --state merged --limit
100 --json number | python3 -c "import json,sys; print(len(json.load(sys.stdin)))"`
equals the number of PR lines in CHANGELOG "Unreleased"; pytest -m integration; mypy;
ruff check (in full); ruff format --check; deptry; pre-commit run --all-files.

STOP if a quickstart command fails in the fresh clone (report the command and output; do
not document a command that did not run). STOP if a badge or link needs the repository
to be public to check (record it as access-gated; do not flip anything).

REPORT: docs/log/W6-T5.md plus EXPLAIN FOR THE OWNER in the Teacher register, ending
with "What to click": the GitHub Settings page, section and button for the visibility
change, and the social preview upload of docs/assets/logo-512.png. Commit message:
"W6-T5 public release pass".
