Task W7-T1: give Codex a request clock: one model_request activity per rollout token_count record, and a derived request duration on the OTel surface, so a Codex session (imported or captured) builds a request-clock series instead of 0 rows.

OWNS: src/telltale/activities_codex.py, src/telltale/providers/codex.py (one CAPABILITIES cell, one new CAPABILITIES row, DRIFT entries), fixtures/golden/codex/**, one integration test file (tests/integration/test_usage_surfaces.py or a new one), docs/log/W7-T1.md.
READS: src/telltale/activities.py (the Claude request rule), correlate.py, series.py, fixtures/sources/codex/0.150.1/{S1,S3,S6,rollout-import}, AGENTS.md, briefs/README.md.
Do NOT edit series.py, activities.py, providers/claude_drift.py, importer.py or env.py: W7-T2 and W7-T3 own them and run beside you.

WHAT WAS MEASURED (2026-09-05, by the orchestrator; every number reproduces from the named files):
- The module docstring of activities_codex.py ("A TURN IS NOT A REQUEST ... the rollout's token_count are TURN totals") is wrong about cardinality. The owner's largest rollout of the month (~/.codex/sessions/2026/09/01/rollout-2026-09-01T12-53-09-01a05cd0-e511-7441-8a95-d55be0c16fe8.jsonl, read-only) has 2 turn_context records, 1 task_started and 69 event_msg/token_count records in that one turn, each with a distinct info.last_token_usage and a monotonically growing info.total_token_usage.total_tokens (24062, 50668, 81746, 113264, ...).
- On the E02 fixtures, info.last_token_usage of each rollout token_count equals, field for field (input_tokens, cached_input_tokens, output_tokens), exactly one OTel codex.sse_event response.completed record (input_token_count, cached_token_count, output_token_count): S1 6 of 6, S3 2 of 2, S6 10 of 10. The one OTel response per session with no rollout twin is the same pre-turn record on all three: input 11437, cached 0, output 0.
- So token_count.last_token_usage IS one model request's usage. info.total_token_usage is the running turn total, which `_turn_totals` reads today and keeps reading.
- request_duration_ms on the OTel surface: codex.websocket_request carries event.timestamp = when the request was sent (S1 first: 2026-09-01T22:54:25.267Z) and codex.sse_event response.completed carries event.timestamp = completion (22:54:25.757Z); S1 has 7 websocket_request records against 7 responses. No surface carries a duration field for a model request: codex.api_request.duration_ms is the /models HTTP call, codex.turn_ttft is a turn's first token.
- request_duration_ms on the rollout: the token_count timestamp is NOT the response time; it equals the following tool output's timestamp (S1: custom_tool_call_output and token_count both at 22:54:30.241Z). The last response_item of a response (message, function_call, custom_tool_call) carries the closest thing to a completion time; the previous tool output, user_message or task_started carries the closest thing to a start time.
- Owner's store (copy at experiments/E13/out/home/telltale.db; read it, never write it; never touch ~/.telltale): 1,459 imported Codex captures, 149,560 codex.rollout.event_msg.token_count observations, 6,695 turn activities, 0 model_request activities, and `series build --clock request` gives 0 rows on every one (measured on imp_869276e15eb90a31ac51856f: 5,736 token_count rows, 0 series rows).

DO:
(1) activities_codex.py `_requests`: the sse_event path stays primary. When a capture has NO codex.sse_event response.completed record, emit one model_request per codex.rollout.event_msg.token_count from payload.last_token_usage: total_input_tokens = input_tokens (the wire value), input_tokens = fresh = input_tokens minus cached_input_tokens through `_fresh` (the same rule `_turn_totals` uses), cache_read_tokens = cached_input_tokens, cache_creation_tokens = cache_write_input_tokens, output_tokens, reasoning_output_tokens; `usage_source` = "codex.rollout.event_msg.token_count.last_token_usage"; model and effort from the latest turn_context before it by provider_ts (None when there is none); started_at = the token_count's provider_ts, with a comment stating that this is when the usage was RECORDED (after the tool ran), which is the row's position on the request clock and not the response's completion; request_index by provider_ts then observation id. A token_count whose last_token_usage is absent or not a mapping becomes no request and is counted in one `dropped` diagnostic naming the observation ids; never a zero row.
    When BOTH surfaces exist (S1, S3, S6 replay every surface into one capture): sse stays primary, each rollout token_count is attached as provenance to the sse request it equals field for field (pair in order within the capture; the three counters must be equal), and a rollout token_count with no equal sse twin is a `conflict` diagnostic naming both ids (spec 7.2), never a second request. The pre-turn (11437, 0, 0) response has no twin by construction and stays a request from sse alone.
(2) Request duration on the OTel surface, coverage `derived`: duration_ms = response.completed event.timestamp minus the nearest preceding codex.websocket_request event.timestamp (pair in order; when the two counts differ within the capture, no duration and one diagnostic naming both counts); `duration_source` = "otel:websocket_request->sse_event".
    Request duration on the rollout: MEASURE FIRST, do not decide first. On S1, S3 and S6 compute for every response its rollout-derived duration (timestamp of the response's last response_item before its token_count, minus the timestamp of the last custom_tool_call_output, function_call_output, user_message or task_started before the response's first response_item) and compare with the sse-derived duration of its twin. Decision rule, written here before the run: adopt the rollout derivation only if at least 90 percent of the paired responses (three fixtures pooled, n = 18) agree within 10 percent relative; otherwise the rollout duration stays None, the rollout cell in (3) is "unavailable", and the report carries the measured table. The table goes in the report either way, with this rule quoted beside it.
(3) providers/codex.py: CAPABILITIES request_usage on the rollout surface becomes "observed" (it was "partial" on the strength of the turn-total claim); add a DRIFT entry stating the fixture counts and the 11437/0/0 pre-turn record. Add a CAPABILITIES row `request_duration` = ("unavailable", "derived", "unavailable", "unavailable", <"derived" or "unavailable" per (2)>) for (exec_json, otel_logs, otel_metrics, hook, rollout). W7-T3 adds the same row to Claude's table and maps series.py's request_duration_ms column onto it; until W7-T3 merges the new row is read by nothing, which is expected.
(4) Correct the module docstring to the measurement. Keep the "INPUT TOKENS INCLUDE THE CACHED ONES" paragraph. Keep `turn` activities: they carry model_context_window, effort and the running totals, and nothing sums a turn into a request.
(5) Regenerate fixtures/golden/codex/{S1,S3,S6}/{show.json,timeline.txt}; explain every changed number in the report. Usage totals on those three must be UNCHANGED (sse is primary there); model_requests counts must be unchanged (7, 3, 11). Add fixtures/golden/codex/rollout-import goldens if that scenario has none, and explain each number in them.
(6) Tests: replay fixtures/sources/codex/0.150.1/rollout-import (rollout only) through the real receiver (conftest's replay helper) and assert model_request count equals the file's token_count line count (count it in the test with the file), usage_source as above, fresh = input minus cached on one hand-checked row (paste the row's three numbers in the test's docstring), and `series build --clock request` row count equal to that count; replay S1 (all surfaces) and assert model_request count 7 and zero conflict diagnostics. Break the pairing rule (attach every rollout row as a request) and watch the S1 assertion fail, then restore it.

VERIFY (paste every output in the report):
- In a temp TELLTALE_HOME: the receiver replay of rollout-import, then `uv run telltale show <capture>` (usage.model_requests equals `grep -c token_count fixtures/sources/codex/0.150.1/rollout-import/rollout.jsonl`, both pasted) and `uv run telltale series build --clock request --capture <capture>` (rows equal that count; the coverage line shows fresh_input_tokens, cache_read_tokens and output_tokens observed).
- On a COPY of experiments/E13/out/home/telltale.db (cp to a temp dir; never the original): `sqlite3 <copy> "select capture_id from captures where provider='codex' and capture_id like 'imp_%' order by observation_count desc limit 1"`, then with TELLTALE_HOME pointed at the copy's directory `uv run telltale rebuild <id>` and `uv run telltale series build --clock request --capture <id>` (rows > 0; paste the row count beside the capture's token_count observation count from sqlite).
- uv run pytest -m integration; uv run mypy .; uv run ruff check . && uv run ruff format --check .; uv run deptry .; pre-commit run --all-files.

STOP if a rollout token_count on the fixtures pairs with no sse response other than the 11437/0/0 record (report the counts; never pair by guess). STOP rather than edit any file outside OWNS.
Never touch ~/.telltale, ~/.codex or ~/.claude (the one rollout named above is read-only, for the docstring's numbers). Never kill a process by name pattern; kill by pid only. Temporary stores only; no live agent; no paid experiment.

REPORT: docs/log/W7-T1.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/Teacher.md): one real capture with its real counts before and after. Commit message `W7-T1 Codex request clock from rollout token_count`. Push the branch and open the PR with `gh pr create --body-file docs/log/W7-T1.md`; leave merging to the coordinator.
