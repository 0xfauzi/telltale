Task W7-T2: give every imported session an environment fingerprint, so env_changed is observed on backfill captures and a mid-session model or version switch becomes a changepoint.

OWNS: src/telltale/importer.py, src/telltale/env.py (one new constructor), tests/integration/test_import.py (extend) or one new test file, a hand-written fixture pair under fixtures/sources/, docs/log/W7-T2.md.
READS: src/telltale/providers/__init__.py (ParseCtx is a frozen dataclass with an environment_fingerprint_id field), providers/claude_transcript.py, providers/codex.py (the rollout parse), series.py (`env_column`), launch.py `_environment` (the launcher's version of this), allowlist_telltale.py (the telltale.environment entry), AGENTS.md, briefs/README.md.
Do NOT edit series.py, activities.py, activities_codex.py, providers/*.py or the forecast package: W7-T1 and W7-T3 own them and run beside you.

WHAT WAS MEASURED (2026-09-05, by the orchestrator):
- series.env_column: env_changed is `unavailable` when no observation row of the capture carries a fingerprint id, `observed` when every row does, `partial` otherwise; a changepoint is recorded only between two KNOWN and different ids. Every imported capture today is unavailable: 1,841 Claude imports and 1,459 Codex imports on the owner's store, and readiness check 1 refuses all 930 Claude imports with 32 or more requests on exactly this column plus request_duration_ms.
- The launcher (launch.py `_environment`) calls env.fingerprint(provider, argv, cwd, runtime_version=..., extra={"capture_modes": surfaces, "content_level": level}), emits `telltale.environment` with the payload minus fingerprint_id, and binds the id so every later observation row carries it. Both parsers stamp ParseCtx.environment_fingerprint_id onto each Observation (claude.py `_observe`; codex.py line 346). The importer builds one ParseCtx per file (`_import_one`) with no fingerprint id, and `_replay` passes it to every line.
- What a Claude transcript carries, measured on the owner's largest telltale transcript (2,262 assistant lines): top-level `version` on 2,262, `effort` on 2,245, message.model on 2,262, plus cwd and gitBranch. What a Codex rollout carries: session_meta.cli_version ("0.150.1") once, and turn_context.model ("gpt-5.6-sol") and turn_context.effort ("high") per turn.
- env.fingerprint hashes instruction files from the cwd ON DISK at call time (`_instruction_files` then `_digest`). For a backfill that would hash today's CLAUDE.md against a session that ran weeks ago: a value nobody measured. That is the trap; the constructor below never reads the disk.
- The telltale.environment allowlist (allowlist_telltale.py line 58) types instruction_hashes as Kind.PATH with path keys; runtime_version, model, effort as ENUM; capture_modes ENUM; content_level SIZE.

DO:
(1) env.py: `backfill_fingerprint(provider, runtime_version, model, effort, surface, content_level) -> dict[str, Any]` producing the same payload keys as `fingerprint`: provider, runtime_version, model, effort as given (None stays None), tool_set_hash None, mcp_names_hash None, instruction_hashes {} (an empty mapping: nothing was observed; check that sanitize accepts it, and STOP if it needs a new Kind), settings_hash None, sandbox_posture None, capture_modes [surface], content_level; id = "env_" + sha256 of the canonical payload exactly as `fingerprint` computes it, so two imports of one version+model+effort share an id and a launcher capture never collides with an import (its capture_modes list differs). The docstring says which fields an import can observe and which it cannot, and that None is "not observed", never "absent".
(2) importer.py: before `_replay`, a pre-pass over the file with the same reader and `_loads` that yields the (runtime_version, model, effort) regime per line number. Claude: from an assistant line's top-level `version`, message.model and top-level `effort`; a non-assistant line belongs to the current regime; lines before the first assistant line belong to the first assistant line's regime. Codex: runtime_version from session_meta.cli_version; model and effort from each turn_context, applying from that line on. Emit one `telltale.environment` observation (through the existing `_emit`, payload minus the id) the first time each distinct id appears, in file order and BEFORE the lines it governs; from then on parse each line with `dataclasses.replace(ctx, environment_fingerprint_id=<current id>)`. A file with no assistant line and no turn_context emits nothing and stays unavailable: never a fingerprint built from Nones alone. Record `fingerprints` (the count of distinct ids, an int) on the capture_started payload; add that field to the telltale.capture_started allowlist entry if the gate refuses it.
(3) Idempotence unchanged: a capture already in the store is skipped whole; nothing appends fingerprints to an existing capture. `telltale import --dry-run` still writes nothing.
(4) Tests: import the synthetic transcript fixture W2-T2 wrote (find it: `ls fixtures/sources/claude/*/transcript*`) into a temp home and assert one telltale.environment observation with runtime_version, model and effort filled and the other hashes None or {}; that every observation of the capture except the environment row itself carries that id (sqlite count of null environment_fingerprint_id = 0); and that `series build --clock request` reports env_changed `observed` with no changepoint. Then a hand-written transcript fixture (about 40 lines, real shape, no secrets, probes allowed) whose assistant lines switch model half way: 2 fingerprints, env_changed observed, exactly one changepoint at the switch row. The same for a hand-written rollout with two turn_context records differing in model. Break the stamping (do not replace ctx) and watch the null-count assertion fail, then restore it.

VERIFY (paste every output in the report):
- `uv run telltale import claude-transcripts --root <the fixture root>` into a temp TELLTALE_HOME; `uv run telltale series build --clock request --capture <id>` (the env_changed coverage word is observed; paste the coverage line); `uv run telltale show <id>` (the environment is visible: runtime_version, model, effort filled).
- On a COPY of experiments/E13/out/home/telltale.db (cp to a temp dir; never the original; never ~/.telltale): pick one imported Claude capture (`sqlite3 <copy> "select capture_id from captures where provider='claude' and capture_id like 'imp_%' order by observation_count desc limit 1"`), `uv run telltale purge <id>` on the copy, re-import its file from ~/.claude/projects (read-only; find it by the session id in the capture_started payload's provider_session_id_requested, or by filename), then series build: env_changed observed. Paste the coverage line before and after.
- uv run pytest -m integration; uv run mypy .; uv run ruff check . && uv run ruff format --check .; uv run deptry .; pre-commit run --all-files.

STOP if telltale.environment's allowlist refuses an empty instruction_hashes mapping and the only fix is a new Kind (report instead). STOP rather than edit any file outside OWNS.
Never write under ~/.telltale, ~/.codex or ~/.claude; read ~/.claude/projects only for the one VERIFY re-import. Never kill a process by name pattern; kill by pid only. Temporary stores only; no live agent; no paid experiment.

REPORT: docs/log/W7-T2.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/Teacher.md). Commit message `W7-T2 backfill environment fingerprints`. Push the branch and open the PR with `gh pr create --body-file docs/log/W7-T2.md`; leave merging to the coordinator.
