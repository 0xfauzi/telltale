Task W7-T3: make request_duration_ms a measured column on every surface that can carry it (Claude transcript: derived from timestamps, validated against OTel), map it to its own capability so its coverage word is measured, and amend readiness check 1 so a column a surface cannot carry is excluded by name instead of refusing the whole capture.

OWNS: src/telltale/activities.py (the Claude `_request` path), src/telltale/providers/claude_drift.py (one new CAPABILITIES row), src/telltale/series.py (one `_REQUEST_COLUMNS` entry and its comment), src/telltale/forecast/readiness.py (`_coverage`), src/telltale/forecast/backtest.py (record excluded columns on the run and in the report), docs/design/01-design.md (one amendment under a new "## Amendments from wave 7 (2026-09-05)" heading), experiments/E14/ and docs/experiments/E14.md, tests (test_capture_to_summary.py, test_forecast_contracts.py or new files), docs/log/W7-T3.md.
READS: providers/claude_transcript.py, correlate.py, forecast/__init__.py (REQUEST_VARIANT), experiments/E07/run.py and experiments/E13/run.py (how an experiment pools runs), docs/experiments/README.md (the write-up template), AGENTS.md, briefs/README.md.
Do NOT edit activities_codex.py or providers/codex.py (W7-T1 owns them and runs beside you; its Codex `request_duration` row is described below so the two tables agree) or importer.py and env.py (W7-T2).

WHAT WAS MEASURED (2026-09-05, by the orchestrator; you re-measure it in DO (6)):
- On the build's own launcher captures (128 Claude sessions, 6,533 OTel api_request records carrying request_id, 6,518 of them matched to the same requestId in the session's transcript under ~/.claude/projects), the transcript-derived duration, defined as timestamp(LAST assistant line of the request) minus timestamp(nearest preceding NON-assistant line: user, system or attachment), reproduces OTel duration_ms with median absolute difference 23 ms, median relative difference -0.004, p90 relative -0.001, and 99.5 percent of requests within 10 percent relative (median OTel 4,684 ms, median derived 4,656 ms). Defined against the FIRST assistant line instead, only 54.8 percent are within 10 percent (median absolute difference 424 ms): the first line lands when the first content block completes. Use the last line.
- A transcript request is 1 to 5 assistant lines sharing one requestId (one file: 2 lines 632 times, 3 lines 188, 1 line 179, 4 lines 37, 5 lines 9). The parser already emits one claude.transcript.assistant observation per line with provider_ts = the line's timestamp and correlation request_id, and claude.transcript.user observations with provider_ts.
- Readiness check 1 (`readiness._coverage`) counts every column and refuses when any one is not observed or derived. All 930 imported Claude captures with 32 or more requests fail it with "coverage: measured 9, needed 11" (request_duration_ms and env_changed unavailable on the transcript surface). Every Codex capture will fail it on request_duration_ms after W7-T1 unless W7-T1's rollout derivation passes its bar. The backtester (`backtest._variant`) already drops an unavailable column by name and records the reason; readiness is the only place that refuses. docs/experiments/E13.md had to pre-register a narrower named variant to run at all: read its "Decision rule" section.
- Coverage words: series.COVERAGE_RANK = observed, derived, partial, unavailable; readiness FORECASTABLE = observed, derived. `partial` means holes in rows; `unavailable` means all None by construction.

DO:
(1) activities.py `_request`: for a request whose head observation is claude.transcript.assistant (the "stream" grouping through `_by_request`), duration_ms = provider_ts of the group's LAST assistant observation minus provider_ts of the nearest preceding non-assistant transcript observation of the capture by provider_ts (claude.transcript.user or claude.transcript.system.*), in whole milliseconds; `duration_source` = "transcript_timestamps"; provenance = both observation ids. No preceding observation, or a negative gap: None, and one `dropped`-kind diagnostic per capture counting them. An OTel api_request keeps its own duration_ms with `duration_source` = "otel_api_request". Never mix: a request that has an OTel record never takes the transcript number, and a stream-only request (no transcript) stays None.
(2) claude_drift.py CAPABILITIES: new row `request_duration` = ("observed", "unavailable", "unavailable", "unavailable", "derived") for (otel_logs, otel_metrics, hook, stream, transcript), with the measurement above in the comment. W7-T1 adds Codex's row in providers/codex.py as (exec_json unavailable, otel_logs derived, otel_metrics unavailable, hook unavailable, rollout derived or unavailable per its own measurement); do not add it yourself.
(3) series.py `_REQUEST_COLUMNS`: request_duration_ms rests on ("request_duration",) instead of ("request_usage",). Rewrite that comment block to the measured facts for both providers (keep W3-T3's turn-timestamp finding as history). `columns()` then yields coverage `derived` on a transcript capture and `observed` on an OTel one with no new code path; confirm `blank_unobservable` still blanks a capture whose capability is unavailable.
(4) readiness.py `_coverage` (check 1) amendment: PASS when the target is forecastable and every non-forecastable column has coverage exactly `unavailable`; FAIL when any non-forecastable column is `partial` or the target itself is not forecastable. The detail names every excluded column with its coverage either way; `measured` and `needed` keep their meanings (forecastable count, column count) so the stored summary line still reads "coverage: measured 9, needed 11", now with passed True and the names beside it. backtest.py: the stored run and the printed report already carry `covariates`; add `excluded` (name and coverage pairs, from `_variant`'s second return) to the stored scenario and to the report text, with the sentence "a variant is named by its column set". 01-design.md amendment: the rule, the reason (2,389 captures on the owner's store refused by a column their surface cannot carry), and the pooling rule: two runs pool only when their `covariates` lists are equal; experiments/E13/run.py's `pool` will key on it (do not edit E13; say so).
(5) Tests: import the synthetic transcript fixture (`ls fixtures/sources/claude/*/transcript*`) into a temp home and assert model_request.duration_ms equals a hand-computed gap for one request (paste the two timestamps in the test docstring), duration_source transcript_timestamps, and series column coverage `derived`; assert an OTel replay of fixtures/sources/claude/<version>/S1 keeps duration_source otel_api_request and coverage observed. Forecast contract: a series with one all-None column of coverage unavailable passes check 1 with that column named in the detail; a series with a partial column fails it. Revert the amendment and watch the contract test fail, then restore it.
(6) experiments/E14/measure_transcript_duration.py plus docs/experiments/E14.md: the measurement script (store copy at experiments/E13/out/home/telltale.db, read-only: claude.otel.api_request payload request_id and duration_ms per launcher capture, matched by provider_session_id to ~/.claude/projects/*/<session>.jsonl, read-only), printing n matched, medians, and the share within 10 percent for the first-line and last-line definitions. E14.md follows docs/experiments/README.md's template; its decision rule is pre-registered as "adopt the last-line derivation if at least 90 percent of matched requests agree within 10 percent relative", and its numbers are the ones YOUR run prints.

VERIFY (paste every output in the report):
- `uv run python experiments/E14/measure_transcript_duration.py` (must reproduce about 99.5 percent within 10 percent on the last-line rule; if it does not, paste what it printed and apply the STOP below).
- Import one real transcript from ~/.claude/projects (read-only source) into a temp TELLTALE_HOME; `uv run telltale series build --clock request --capture <id>` (request_duration_ms coverage derived; paste the coverage line); `uv run telltale forecast readiness --series <series id> --target output_tokens` (check 1 passes and names env_changed as excluded, or, if W7-T2 has merged by then and you rebased, passes with nothing excluded; paste the checklist).
- uv run pytest -m integration; uv run mypy .; uv run ruff check . && uv run ruff format --check .; uv run deptry .; pre-commit run --all-files.

STOP if your re-measurement is below 90 percent within 10 percent: then request_duration_ms stays None on transcripts, the capability row's transcript cell is "unavailable", and the report and E14.md carry the table. STOP rather than edit activities_codex.py, providers/codex.py, importer.py, env.py or experiments/E13.
Never write under ~/.telltale, ~/.codex or ~/.claude; read ~/.claude/projects only for the measurement and the one VERIFY import. Never kill a process by name pattern; kill by pid only. Temporary stores only; no live agent; no paid experiment.

REPORT: docs/log/W7-T3.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/Teacher.md). Commit message `W7-T3 derived request duration and coverage-named variants`. Push the branch and open the PR with `gh pr create --body-file docs/log/W7-T3.md`; leave merging to the coordinator.
