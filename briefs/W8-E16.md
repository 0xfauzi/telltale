Task W8-E16: write experiments/E16/run.py, run it on the E16 store, and fill docs/experiments/E16.md's empty sections with what it measured, under the decision rule that file pre-registered. Change nothing above "## What we did" in that file.

OWNS: experiments/E16/run.py, experiments/E16/out/ (gitignored; every number in the write-up maps to a file there), docs/experiments/E16.md (the sections "What we did", "What we measured", "What it means", "What we rejected", "How sure we are", "What downstream may now say"), README.md (one paragraph under "The goal, and where it stands", after E13's), CHANGELOG.md (one line), docs/log/W8-E16.md.
READS: docs/experiments/E16.md (the pre-registration: population, targets, constants, readings, the missingness paragraph, the corrected population paragraph; every rule you apply is there and you may not add one after seeing numbers), docs/experiments/E11.md and experiments/E11/run.py (the previous runner of this protocol on one repository; reuse its functions by importlib path where their signatures still hold on today's API, else re-implement the smallest thing), experiments/E13/run.py (`copy_store`, `point_home_at_the_copy`, `load_model`, `mae_table` idioms), docs/log/W8-T3.md, docs/log/W8-F1.md, docs/log/W8-T4.md, docs/log/W8-T5.md, docs/design/04-forecast-reading-guide.md, AGENTS.md, briefs/README.md.
Do NOT edit anything under src/. STOP rather than edit any file outside OWNS.

WHAT IS MEASURED TODAY (by the orchestrator, 2026-09-06):
- The store: /Users/wumpinihussein/Documents/code/telltale/experiments/E16/out/home/telltale.db (absolute: experiments/*/out/ is gitignored, so your worktree's experiments/E16/out/ starts empty and you write your outputs there, under the worktree), a backup of the owner's rebuilt store plus `import git-history` with check runs of the four repositories on main at W8-T5 (path cells stored). It is READ-ONLY for you: the runner copies it with `sqlite3 .backup` into <worktree>/experiments/E16/out/run-home/ (about 2.1 GB, about 20 s) and points TELLTALE_HOME there, as E11's `snapshot()` and E13's `copy_store` do. Never ~/.telltale.
- Population, `series build --clock change --repo <id>` on that store (repo_id in the first column; the four in full: telltale d9f783b1d318c0d65ec6b86fe729f6ff1a3581983e4287a71f6510eb80d84d6c 179 rows (5 captured, 46 off-base commits dropped, 5 collapsed links), systemap 5b8cb70af348fd97bbfcc1304c01a43c8de20d5f25cdb6e2e67c2b9406354a83 108, deckgen 0ee20334979eb72c72c53cd29874eec55246e073a7d5240eedd3b4f07f627ee4 118, kstrl 2cae1649bab6d950ab76cec0325fa86fe8b23288ac13894ccdc6e4ce6541d762 252. Targets' unknown cells on that store (census after the last re-import, 2026-09-06 15:33): merge_verification_ms telltale 33, systemap 66, deckgen 64, kstrl 60; rework_within_3_lag3: 3 on each (rows 0..2). Block holes (lines_added and lines_removed unknown on commits touching a binary file): telltale 2, systemap 7, deckgen 27, kstrl 0; after W8-F1 those rows are excluded from the conditioned pair's frame and counted as `excluded_for_block`, so deckgen's conditioned runs may fall under k_min: report it as the rule says, do not widen anything.
- Retained rows and windows are the backtester's, not yours: `frame.retained(series, target[, columns])` and the plan. Measured on the final store with W8-F1's code on telltale: rework_within_3_lag3 176 retained rows, 40 windows at H 4 stride 4, `ready` (check 7 reads the minority share 0.2102); `forecast candidate` on it with baselines only: 175 retained after the block, excluded_for_block 2, 39 paired windows scored on step 4 of 4. merge_verification_ms before the last re-import: 140 retained rows, 124 windows at H 1, ready. Under E16's rule a (repository, target) with fewer than k_min = 20 windows is not assessable: systemap's 42 known verification rows give 26 windows and deckgen's 54 give 38, so both are assessable but thin, and the write-up says so.
- The API on main after W8-T3 and W8-F1 (read the docstrings; do not guess): `telltale.forecast.backtest.run(series, target, horizon, forecasters, ...)` (unconditioned, variant from the registry: CHANGE_VARIANT "change_past_only"; a `partial` target is admitted and run over its retained frame; partial covariates are excluded by name and listed on the run); `telltale.forecast.placebo.paired(series, target, horizon, forecasters, model, ...)` (true order, R block placebos with B = max(2, H) and the B = 1 controls, the persistence validity check, and `decide`'s label with the inequalities); `telltale.forecast.candidate.conditioned(store_or_None, series, target, forecasters, model, origin_range=..., a_block=ABLATION_A, past_only=...)` (the pair, at `candidate.horizon(target)`, scored on `SCORED_STEPS[target]` where declared, over `frame.retained(series, target, block)` after W8-F1 so binary-file commits cost rows and never the run); `telltale.forecast.readiness.check(series, target, horizon)`; constants in `telltale.forecast` (DELTA, W, K_MIN, C_MIN_SHORT, PLACEBO_SEEDS, ABLATION_A, CANDIDATE_TARGETS, CANDIDATE_HORIZON, SCORED_STEPS, CANDIDATE_SENTENCE). The TimesFM adapter: `telltale.forecast.timesfm` through `FORECASTERS`/`make(name, device)` as E13's `load_model` does; device `mps`; another TimesFM process (E13b) is running on the same GPU for hours, so expect a slower wall and record it, never a number you did not measure.
- E11's `origin_ranges` (whole range and its two halves), `paired_signs`, `changes_the_forecast`, `labelled`, `column_facts` and `control` (the synthetic random-walk control that tells a broken runner from a "not assessable" result) are the pieces to reuse; E11 was single-repository with a module HORIZON, so wrap, do not copy blindly.

DO:
(1) experiments/E16/run.py: for each repository (never pooled) and each of the three targets: readiness (over the retained frame; print the checklist and store it), the unconditioned true-order run with placebo and label through `placebo.paired`, the conditioned pair through `candidate.conditioned` over the whole origin range and each half, the paired median MAE difference (conditioned minus unconditioned) per range, both runs' calibration flags, the pre-registered reading ("the candidate's features move the forecast" iff the paired median difference is below zero in both halves and the conditioned calibration is not flagged where the unconditioned one is not; else "no measurable conditioning at this n"); for the two flag targets the base rate on the retained rows printed beside every MAE. Per (repository, target) write experiments/E16/out/<repo>/<target>.json with: rows, retained, excluded (count and reason), windows, dropped, excluded columns, E_M, E_B and the best baseline's name, E_P (median over seeds) and its range, W_MB, W_MP, the inequalities decide evaluated with their values, the label, placebo validity, the conditioned pair's numbers per range, calibration of each run, `excluded_for_block`, wall_ms; plus summary.json across all twelve, and the E11-style control. A `--repo` and `--target` filter and `--forecasters` for a stub-only dry run.
(2) Run the stub-only dry run first on one repository (`--forecasters stub,persistence,rolling_median,rolling_mean,local_drift` or whatever the registry's names are: read `telltale.forecast.FORECASTERS`), then the full run with TimesFM on mps: `TELLTALE_HOME=<unused: the runner sets its own> uv run --extra forecast python experiments/E16/run.py --device mps > experiments/E16/out/run.log 2>&1`. Record the wall.
(3) docs/experiments/E16.md: fill the six sections in the Teacher register with tables of the twelve (repository, target) rows: rows / retained / excluded / windows / E_M / E_B (name) / E_P / W_MB / W_MP / label / placebo valid; a second table for the conditioned pair: paired median MAE difference whole / half 1 / half 2, calibration flags, reading; base rates for the flag targets; every number traceable to an out/ file by name. "What it means" takes the branch of the pre-registered rule the numbers took, per row, and says in one paragraph what the twelve rows say about H5 on the change clock and H8, in the words the reading guide allows (a report writer refuses cause, impact, would). "What we rejected": the readings you could have taken and did not, with the number that forbids each. "How sure we are": measured / reasoned / judgement / unknown per claim. README.md: one paragraph with the headline numbers. CHANGELOG.md: one line.
(4) docs/log/W8-E16.md as REPORT below; note which E11 functions were reused and which re-implemented and why.

VERIFY (paste every output in the report):
- The stub dry run on telltale: twelve is not required, three (repository, target) JSON files exist with `windows` equal to the readiness line's planned count and `excluded` equal to the target's unknown cells on the retained frame.
- The full run: `ls experiments/E16/out/*/` shows twelve target files and summary.json; the control's label is what E11's control produced (read E11.md for its expected shape) or the difference is explained.
- `grep -nE "\b(cause|impact|would)\b" docs/experiments/E16.md` prints nothing in the sections you wrote (the pre-registration above them already uses "would" once in a quotation of the rule; leave it).
- uv run ruff check . && uv run ruff format --check .; uv run mypy . (experiments are type-checked too: read pyproject's mypy config); pre-commit run --all-files.

STOP if a (repository, target) refuses for a reason the pre-registration did not anticipate (write it in the report with the refusal text; do not patch src/). STOP if the copied store cannot be built for one of the four repo_ids (paste the build output). Never write under ~/.telltale, ~/.codex or ~/.claude. No live agent; no paid experiment. Never kill a process by name pattern; kill by pid only. Never run `gh` for anything but `gh pr create`.

REPORT: docs/log/W8-E16.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/Teacher.md): idea before name, one real (repository, target) row walked through with its real values, how each number was measured, the reading rejected and why. Commit message `W8-E16 git-backfilled change clock: H5 and H8 on four repositories`. Push the branch and open the PR with `gh pr create --body-file docs/log/W8-E16.md`; leave merging to the coordinator.
