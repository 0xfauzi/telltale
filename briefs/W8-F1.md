Task W8-F1: three fixes W8-T3's report measured and left outside its OWNS, so E16 can run: readiness check 7 recognises a flag that varies; the candidate protocol's frame excludes by row the rows where the candidate block is unknown instead of refusing the whole run; `telltale advise` finds the run of a holed target and asks the checklist at the target's own horizon.

OWNS: src/telltale/forecast/readiness.py (`_variation` only), src/telltale/forecast/frame.py, src/telltale/forecast/candidate.py, src/telltale/cli_advise.py, tests/integration/test_forecast_frame.py, tests/integration/test_forecast_candidate.py, the advise test file (find it: `grep -l "advise" tests/integration/*.py`), docs/design/amendments/W8-F1.md, docs/log/W8-F1.md.
READS: docs/log/W8-T3.md (its UNSURE section is the whole case for this task; read it first), docs/design/amendments/W8-T3.md, docs/experiments/E16.md (do not edit), AGENTS.md, briefs/README.md.
Do NOT edit backtest.py, series_*.py, importer_*.py, sanitize.py, model.py or store.py. STOP rather than edit any file outside OWNS.

WHAT IS MEASURED TODAY (W8-T3's report, 2026-09-06, on this repository's lineage of 169 first-parent commits imported with check runs; main includes W8-T3, W8-T4 and W8-T5's brief):
- Check 7 (`readiness._variation`) is the scaled MAD of the target over its known rows, needed above 0. On a 0/1 column whose median is 0 the MAD is 0 whatever the base rate: `rework_within_3_lag3` carries 34 reworks over 169 rows (base rate 0.20) and reads `variation FAIL, measured 0`; `merge_verification_failed` will read the same. Nothing blocks a backtest, which does not call the checklist, so E16 would print `NOT ready: variation` beside every flag target it runs. The check's stated purpose is to refuse a constant target (S1's fresh_input_tokens is 2 on all seven rows), and a flag with two values present is not constant.
- `candidate.conditioned` refuses at origin 16 with "candidate column lines_added holds an unknown inside [ctx_start, o]" because `lines_added` and `lines_removed` are unknown on rows 0 and 5 of the lineage (commits touching a binary file: git numstat reports `-`, and a count that is partly unknown is unknown). `frame.retained(series, target)` excludes by row only where the TARGET is unknown, so a hole in a block column anywhere below the origin refuses every origin above it. W8-T3 produced its paired numbers by dropping the two columns from the block; E16's question is whether the whole block moves the forecast, on four repositories, and any binary commit in any of them refuses the run.
- `cli_advise._latest` looks a run up by the parent series id, and W8-T3 stores the run of a holed target under `<parent>:<target>`, so `advise` says "no stored true-order backtest of this target on this series" after `forecast backtest --target merge_verification_ms` stored one; `cli_advise.HORIZON = 1` is a module constant, so the checklist for `rework_within_3_lag3` is asked at H = 1 and refuses with `horizon 1 is not one of [4]`. `candidate.horizon(target)` exists since W8-T3.

DO:
(1) readiness._variation: unchanged for every unit but `flag`; for a target whose ColumnSpec unit is `flag`, measured is the share of the minority value over the known rows (min(p, 1 - p) with p the share of 1s), needed above 0, and the detail names both counts ("34 of 169 known rows are 1: minority share 0.2012"). A flag with one value present still fails, with the same sentence as before about a constant target. The MAD line's docstring gains the two sentences that say why a flag is measured differently (a median of 0 makes the MAD 0 at any base rate below a half).
(2) frame.retained(series, target, columns=()) keeps a row only when the target AND every column named in `columns` is known; the cohort's `excluded_reason` names the columns that cost rows and how many each cost (a row missing two of them is counted once in `excluded_rows` and under each column); identity is still returned when nothing was excluded. `candidate.conditioned` and the unconditioned twin it pairs with run over `retained(series, target, block)` with `block` the candidate block after the target is removed, so both runs have identical origins (the protocol's pairing rule) and the block is never narrowed; the run's `candidate` dict records `excluded_for_block` (count and row keys). `_known`'s refusal stays as the guard behind it and is now unreachable on a retained frame; say so in its docstring rather than deleting it. The plain backtester (`telltale forecast backtest`) is unchanged: partial covariates stay excluded by name there (E13 continuity).
(3) cli_advise: `_latest` tries the suffixed id `f"{series_id}:{target}"` after the parent id and says which it found; the checklist is asked at `candidate.horizon(target)` (the HORIZON constant goes, or becomes a fallback for targets the map does not name, whichever is smaller).
(4) Tests, each broken once against the un-fixed code and watched failing: a flag series with base rate 0.2 passes check 7 with the minority share printed and a constant flag fails it; a synthetic 60-row change series with `lines_added` unknown on rows 0 and 5 runs `conditioned` end to end with origins starting at c_min over 58 retained rows, `excluded_for_block` 2, and the block whole; `advise` over a series whose only stored run is under the suffixed id prints that run's label; `advise` on `rework_within_3_lag3` asks readiness at horizon 4.

VERIFY (paste every output in the report):
- In a temp home, `uv run telltale import git-history --repo . --no-checks` is not enough for the flag target (it needs rework outcomes, which the importer computes without gh: confirm by reading importer_git, and if so use --no-checks), then `series build --clock change --repo <id>`, `forecast readiness --series <id> --target rework_within_3_lag3 --horizon 4` (check 7 passes with the minority share; paste the whole checklist) and `forecast candidate` on that target with the default block (runs; paste the header lines with `excluded_for_block` and the paired summary).
- `uv run telltale forecast backtest --series <id> --target merge_verification_ms` followed by `uv run telltale advise --head HEAD` (or the invocation the command takes; read cli_advise): the advisory names the stored run instead of "no stored true-order backtest".
- uv run pytest -m integration; uv run mypy .; uv run ruff check . && uv run ruff format --check .; uv run deptry .; pre-commit run --all-files.

STOP if (2) changes any number in test_forecast_candidate's existing assertions for a series without block holes (report which). Never write under ~/.telltale, ~/.codex or ~/.claude. No live agent; no paid experiment. Never kill a process by name pattern; kill by pid only. Never run `gh` for anything but `gh pr create`.

REPORT: docs/log/W8-F1.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/Teacher.md). Commit message `W8-F1 flags vary, the candidate block costs rows not runs, advise finds a holed target's run`. Push the branch and open the PR with `gh pr create --body-file docs/log/W8-F1.md`; leave merging to the coordinator.
