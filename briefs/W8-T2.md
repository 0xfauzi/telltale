Task W8-T2: let the change clock build a row for a commit no capture landed, with its process columns unavailable by name and its post-merge columns read from outcomes that name the commit, so a git-backfilled lineage is a series the laboratory can assess.

OWNS: src/telltale/series_lineage.py, src/telltale/series_outcomes.py, src/telltale/forecast/readiness.py and backtest.py (only the row-flag handling named in DO (4)), docs/design/amendments/W8-T2.md, tests/integration/test_series_lineage.py or the existing lineage test file (find it: `grep -l change_series tests/integration/*.py`), docs/log/W8-T2.md.
READS: src/telltale/series.py (`_measured` is in series_lineage; `env_column`, `blank_unobservable`, `column_report`), src/telltale/importer_git.py once W8-T1 lands (until then, the payload shapes below are the contract), src/telltale/forecast/candidate.py (REWORK_TAIL and the origin ceiling), docs/design/01-design.md 6.12 "Change clock", docs/experiments/E11.md, AGENTS.md, briefs/README.md.
Do NOT edit importer.py, importer_git.py, cli_import.py or the allowlist: W8-T1 owns them and runs beside you. If W8-T1's PR is open when you start, read its branch for the exact payloads; if it is merged, rebase onto main.

WHAT W8-T1 WRITES (the contract, fixed by the coordinator on 2026-09-06):
- One capture per repository, provider "git", capture id `imp_<24 hex>` from importer.capture_id("git", repo_id), surfaces_configured ["git_history"].
- One telltale.repo.commit per first-parent commit with the launcher's payload (sha, parents, tree, committed_ts, files_changed, additions, deletions, per_file) and `link_confidence: "unlinked"`.
- Zero or one external.outcome kind mechanical_verification per commit: external_system "github:check-runs", component_id = sha, status pass|fail, duration_ms, timestamp.
- Zero or one external.outcome kind revert_or_repair per commit: external_system "git:line-overlap", component_id = the reworked sha, external_run_id = the reworking sha, status "reworked", timestamp = the reworking commit's time. Absent when no rework within the next three first-parent commits, and absent when fewer than three later commits exist.

WHAT IS MEASURED TODAY (by the orchestrator, 2026-09-06):
- series_lineage._changes keeps a sha only when `_rung` finds a link_confidence on LADDER (explicit, provider_reported, tree_match_during, tree_match_after, heuristic); anything else is dropped with `_NO_RUNG`. `_landed_by` finds the attempts whose captures recorded the commit; `_change_row` sums the process columns over those attempts' activities and calls series_outcomes.columns(landed, identities, deadline), which returns rework None with NO_IDENTITY when identities is empty, and verification None with NO_VERIFICATION when no outcome is among the landed activities.
- Rows carrying tree_match_after or heuristic get the flag `low_confidence`. Search for who reads that flag downstream (`grep -rn low_confidence src/`), cite the result in the report, and keep that behaviour unchanged.
- The delayed label: series_outcomes._rework decides row j using outcomes observed up to the commit time of change j + 3 (`_deadline`). forecast/candidate.py caps origins at o <= N - 3 for the ACTUAL side only. In a backtest at origin o the context rows j in [o - 3, o) therefore carry labels decided by commits at index >= o: look-ahead inside the context, invisible so far because E11 had 0 windows and every label was 0. This task does not change the compiler's cells; it records the fact in the amendment, and E16 (the coordinator's experiment) forecasts the label through a lagged frame built from the Series (row j carries the label of change j - 3, decided by change j) at H = 4, which is the pre-registered way round it. Write that paragraph in docs/design/amendments/W8-T2.md so the next reader of 6.12 sees it.

DO:
(1) series_lineage.py: accept `link_confidence: "unlinked"` as a row. Not on LADDER (it is not a link) and not a low_confidence rung (nothing was guessed): a new constant UNLINKED = "unlinked", `_rung` returns it when it is the only word present, and a change whose rung is UNLINKED and whose `landed_by` is empty gets the row flag `uncaptured`. A sha recorded BOTH by a launcher capture (any LADDER rung) and by the git backfill is one row at the LADDER rung with the launcher's process columns, never two rows and never the backfill's flag: duplicate is not one.
(2) `_change_row` for an uncaptured change: the six repository columns from the payload as today (files_changed, lines_added, lines_removed, subsystems_touched, test_files_changed, dependency_delta); attempts_to_land None; the seven process columns None (fresh_input_tokens_total, cache_read_tokens_total, compactions, verification_cycles, edit_turnover_ratio, stable_state_intervals, unique_files_read), each with the unknown-cell sentence "no capture landed this change: the row is a repository fact and the process that produced it was not observed" in `unknown_columns`; env fingerprint None (env_changed is then unavailable through the existing `_measured` rule; check that and paste the coverage line).
(3) series_outcomes.columns: identities may be empty for an uncaptured change; then the verification and rework outcomes are the ones IN THE SAME CAPTURE AS THE COMMIT whose component_id equals the sha. Thread the commit's own capture activities through (the Lineage already holds every capture's activities in `activities`; pick the rows of the commit's capture). NO_IDENTITY becomes "no capture landed this change and no outcome names its sha". The deadline rule is unchanged: a revert_or_repair outcome counts only when its timestamp is within the deadline, and a row with fewer than three later changes stays None with NO_TAIL. A mechanical_verification outcome for an uncaptured row gives merge_verification_ms and merge_verification_failed exactly as for a captured one.
(4) readiness.py and backtest.py: find how row flags reach the backtester (search; `_variant` and the W7-T3 coverage-named exclusion are the neighbours). An `uncaptured` row is NOT excluded: it is a row whose process columns are unavailable, and W7-T3's rule already excludes an unavailable column by name and records `excluded` on the run. Add the count of uncaptured rows to the run's assumptions as one sentence ("N of M rows are uncaptured: their process columns are unavailable and were excluded by name") so a reader of forecast_runs sees it. If a flag other than low_confidence is refused anywhere, fix the refusal by name and say where.
(5) Cohort: `providers` includes "git" for a lineage with backfill rows; add `captured_rows` and `uncaptured_rows` counts to the cohort dict, so two frames that differ only in how many rows are backfilled get different series ids.
(7) The delayed label, made forecastable without look-ahead: one more change-clock column, `rework_within_3_lag3` (flag), appended immediately after `rework_within_3`: row j carries the rework_within_3 label of change j - 3, which is decided by change j's commit time (the deadline of change j - 3 is exactly that), and None for j < 3 or when that label is None. Its provenance is the same outcome activity, its coverage word comes from its cells like the other post-merge columns. This is the column E16 forecasts at H = 4 (the fourth step at origin o is change o's own label), so the context never holds a cell decided after the origin. Record the column and its reason in the amendment file.
(6) Tests: build a store with (a) a hand-written git-backfill capture of 8 commits with the four payload shapes above (one commit with a check-run outcome pass 90000 ms, one fail, one with a rework outcome inside the window, one with a rework outcome outside it, three trailing commits) and (b) one launcher capture that links one of those shas at tree_match_during with a model_request activity. Assert: 8 rows; the launcher-linked row has fresh_input_tokens_total from that capture and no `uncaptured` flag; the other seven carry the flag; merge_verification_ms 90000 on the pass row, merge_verification_failed 1 on the fail row; rework_within_3 1 inside, 0 outside, None on the last three with NO_TAIL; the seven process columns read `unavailable` on a frame of only uncaptured rows and `partial` on the mixed frame; `rework_within_3_lag3` on row 5 equals `rework_within_3` on row 2 and rows 0 to 2 are None; `series check` passes; `forecast readiness` on the mixed frame with target merge_verification_ms (lines_added is not a registered target; the registry is forecast/__init__.py TARGETS) excludes the unavailable columns by name and passes or fails check 2 on the row count (paste). Break (1)'s dedup (let a sha appear twice) and watch the row-count assertion fail, then restore.

VERIFY (paste every output in the report):
- Against the W8-T1 branch or main once merged: `TELLTALE_HOME=<temp> uv run telltale import git-history --repo . --no-checks`, then `uv run telltale series build --clock change --key <repo_id>` (rows equal the commit count; the coverage lines for the six repository columns say observed or partial, the seven process columns unavailable, rework_within_3 partial with the last three None; paste the whole column report) and `uv run telltale series check` on it (ok).
- `uv run telltale forecast readiness --series <id> --target merge_verification_ms` (check 1 passes with the unavailable columns named as excluded, or fails on a later check with its numbers; paste the checklist).
- uv run pytest -m integration; uv run mypy .; uv run ruff check . && uv run ruff format --check .; uv run deptry .; pre-commit run --all-files.

STOP if W8-T1's payload contract above turns out not to match its branch when you rebase (report the difference; do not adapt silently). STOP rather than edit any file outside OWNS. Never write under ~/.telltale, ~/.codex or ~/.claude. No live agent; no paid experiment. Never kill a process by name pattern; kill by pid only.

REPORT: docs/log/W8-T2.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/Teacher.md). Commit message `W8-T2 uncaptured change rows: git-backfilled lineages on the change clock`. Push the branch and open the PR with `gh pr create --body-file docs/log/W8-T2.md`; leave merging to the coordinator.
