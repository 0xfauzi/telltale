Task W8-T3: a target whose column has holes is forecast over the rows where it is known (row-level exclusion, named and counted), so the change clock's post-merge targets can be run at all; and the candidate protocol at a four-step horizon for the lagged rework label, scored on its last step only, so H8 can be assessed on `rework_within_3_lag3` without look-ahead; and the registry entry that names it.

OWNS: src/telltale/forecast/frame.py (new), src/telltale/forecast/__init__.py (one TargetSpec, CANDIDATE_TARGETS), src/telltale/forecast/candidate.py, src/telltale/forecast/backtest.py (`_variant`, the plan taking the retained frame, the run record; and `metrics` gaining a scored-steps parameter if that is where the step selection has to live; the file is at 800 lines, the ratchet's limit, so the projection lives in frame.py and backtest.py may not grow), src/telltale/forecast/readiness.py (check 1's verdict, the header line, every check over the retained frame), tests/integration/test_forecast_contracts.py or the candidate test file (find it: `grep -l "conditioned(" tests/integration/*.py`), docs/design/amendments/W8-T3.md, docs/log/W8-T3.md.
READS: src/telltale/series_lineage.py (W8-T2 adds the column `rework_within_3_lag3` after `rework_within_3`: row j carries the rework_within_3 label of change j - 3, None for j < 3 or when undecided), docs/design/amendments/W8-T2.md, docs/experiments/E16.md (the pre-registered protocol this task implements; do not change it), docs/experiments/E11.md, AGENTS.md, briefs/README.md.
Do NOT edit series_lineage.py, series_outcomes.py, importer_git.py or anything outside the forecast package and the files named above.

WHAT IS MEASURED TODAY (by the orchestrator, 2026-09-06):
- The change clock's post-merge targets cannot be run on the merged tree. `backtest._variant` admits a target only with coverage `observed` or `derived` (FORECASTABLE), and readiness check 1 fails on any `partial` column. On a lineage the post-merge columns are `partial` by construction: `rework_within_3` has no label on the last three rows, `rework_within_3_lag3` none on the first three, and `merge_verification_ms` is unknown on every commit without a check run. Measured on this repository's first-parent history (162 commits, `import git-history --repo .` with check runs, 78 s wall, 135 check-run outcomes, 24 commits with no check run, 3 diagnostics): merge_verification_ms and merge_verification_failed `partial` with 27 unknown cells, rework_within_3 `partial` with 3, rework_within_3_lag3 `partial` with 3; `forecast readiness --target merge_verification_ms` prints `target merge_verification_ms has coverage partial: a target must be one of ['observed', 'derived']` and stops. On the same lineage, window-level exclusion (drop every window whose context or actual holds an unknown target cell, which is what the backtester does for a hole today) keeps 16 of 146 windows for merge_verification_ms at H 1 and c_min 16, below k_min 20, and 0 of 143 for the lagged label at H 4, because every context of a lineage shorter than 512 rows starts at row 0 and rows 0..2 are unknown. Row-level exclusion (the target's series is the rows where the target is known) keeps 135 rows and 119 windows for merge_verification_ms, 159 rows and 140 windows for rework_within_3_lag3 at H 4, 159 rows and 143 windows for rework_within_3 at H 1. The formula is windows = N' - c_min - H + 1 at stride 1 with N' the retained row count; your HEAD has more commits than 162, so recompute and paste your own numbers with your commit count.
- Design 6.12 step (2) of the backtester reads "keep only rows where the target's coverage is observed ... drop any window containing None in the target or a selected covariate". The first clause is row-level and is what (0) below implements for the target; the second clause stays as it is for covariates. AGENTS.md invariant 5 allows a gap to be excluded by a named policy and forbids imputation; excluding a row because its target is unknown is an exclusion, and the run must say how many rows and which.
- candidate.py: `HORIZON = 1` module-wide; `conditioned()` runs `backtester.run(series, target, HORIZON, ...)` twice, unconditioned and with `_attach`, whose `prepare` hook fills the A block over the span `range(window.ctx_start, window.origin + window.horizon)` from the SERIES ROWS. At H = 4 that span reads rows origin + 1 .. origin + 3: the A blocks of the next three changes, which are not known at the origin. `_known` refuses an unknown inside the span. `_limit` caps origins at N - REWORK_TAIL for REWORK_TARGET only. `backtester.metrics(windows, tau)` scores every step of every window together.
- forecast/__init__.py: TARGETS carries the three candidate targets with `horizons=(1,)`, c_min C_MIN_SHORT (16), variant CHANGE_VARIANT ("change_past_only"); `CANDIDATE_TARGETS` is the tuple candidate.check_target reads; HORIZONS = (1, 4).
- The reason for all of this: rework_within_3 on row j is decided at the commit time of change j + 3, so at origin o the context rows o - 3 .. o - 1 carry cells decided after the origin. The lagged column moves the label to the row where it is decided. Forecasting it at H = 4 from origin o gives, at step 4, the label of change o: the candidate's own. Steps 1 to 3 are the labels of changes o - 3 .. o - 1, which are not the question, so the protocol scores step 4 alone and says so.

DO:
(0) forecast/frame.py (new): `retained(series: Series, target: str) -> Series`: the same Series with only the rows whose target cell is not None, in order; `row_meta` filtered the same way; `changepoints` remapped (a changepoint at series index i becomes the number of retained rows with index below i; duplicates and the values 0 and len(rows) dropped); `cohort` extended with `{"retained_rows": N', "excluded_rows": k, "excluded_row_keys": [...], "excluded_reason": "target <name> unknown"}`; `series_id` = the parent id with the suffix `:<target>` so a stored run names both. When k is 0 the parent Series is returned unchanged (identity: `retained(s, t) is s`), so every request-clock run with an observed target is byte-identical to today, which is the E13 invariant the STOP line protects. backtest.py: `run` and `plan` operate on `retained(series, target)`; `_variant` admits a target with coverage `partial` (FORECASTABLE stays as it is for covariates: a partial covariate is still excluded by name, and a derived covariate's hole inside a window still drops that window with its reason); the run record carries `"excluded_rows": {"count": k, "row_keys": [...], "reason": "target unknown"}` and, only when k > 0, the assumption sentence "k of N rows were excluded because the target was unknown on them; the context is the lineage without those rows, so the previous row means the previous change with a known target". readiness.py: every check runs over the retained frame; check 1's verdict is `target coverage in (observed, derived, partial)` with every excluded column still named with its word (a partial covariate is excluded by name, exactly as `_variant` does, and is no longer a failure: readiness was refusing a column the run would have excluded anyway); the header line prints `rows N  retained N'  excluded k` after the uncaptured count. candidate.py: `_limit` is deleted, because a row whose rework label is unknown is not in the frame and the backtester's own `o <= N' - H` bound is the whole ceiling for every target; say so where `_limit` was called. Contract test 1 (no look-ahead) must pass unchanged. Tests: on the W8-T2 backfill test's frame (tests/integration/test_series_backfill.py builds an 8-row lineage) and on a synthetic 60-row lineage with 7 unknown target cells scattered, assert `retained` drops exactly those rows, remaps a changepoint placed after a hole by the number of holes before it, and that a backtest run records `excluded_rows.count` 7 and forms `60 - 7 - c_min - H + 1` windows.
(1) forecast/__init__.py: `TARGETS["rework_within_3_lag3"] = TargetSpec(clock="change", unit="flag", nonnegative=True, c_min=C_MIN_SHORT, horizons=(4,), variant=CHANGE_VARIANT)`; add the name to CANDIDATE_TARGETS; a module constant `CANDIDATE_HORIZON = {"merge_verification_ms": 1, "merge_verification_failed": 1, "rework_within_3": 1, "rework_within_3_lag3": 4}` and `SCORED_STEPS = {"rework_within_3_lag3": (4,)}` (every other target: all steps), each with the two-sentence comment saying why (the paragraph above, in your words).
(2) candidate.py: replace the module-wide HORIZON by the per-target lookup everywhere it is read (`conditioned`, `one_step`, `_run`, `_window_at`, the reports); `_attach`: the future block is the candidate row's A block ONLY: values over [ctx_start, origin] from the series, then the value at row `origin` repeated for steps 2 .. H (edge padding done here, explicitly, and recorded in the run's `candidate` dict as `"future_beyond_step_1": "edge-replicated from the candidate row"`), never a read of any row past the origin; `_known` therefore spans `range(window.ctx_start, window.origin + 1)`. `_limit`: no REWORK_TAIL ceiling for the lagged target (the backtester's own `o <= N - H` bound is the whole ceiling; say so in the reason string). Scoring: for a target in SCORED_STEPS, `_within` (or `metrics`, whichever is cleaner) computes every metric over those steps only, and the run records `scored_steps` beside `horizon`; the paired report prints "scored on step 4 of 4" on its face. The mandatory sentence and the refused words are unchanged.
(3) The plain backtester (`telltale forecast backtest`) on this target is unchanged apart from accepting horizon 4 from the registry; do not add step selection there unless (2) needs it inside `metrics`, and if it does, the default is every step and the only caller passing steps is the candidate protocol.
(4) Tests: on a synthetic change-clock Series written by the test (60 rows; `rework_within_3_lag3` as the label of row j - 3 of a seeded 0/1 column; the six A-block columns filled; the seven process columns None so the W7-T3 exclusion applies), run `conditioned(None, series, "rework_within_3_lag3", {stub, baselines}, "echo")` and assert: horizon 4 on both runs; every window's future block has length n_ctx + 4 with its last four values equal (the candidate row's value); metrics on the conditioned and unconditioned runs are computed from step 4 only (compute the expected MAE by hand from the recorded windows and compare); origins run to N - 4; the sentence is in both runs' assumptions. Then break the padding (read row origin + 1 instead) and watch the "last four equal" assertion fail; restore. Contract test 1 (no look-ahead) must still pass: run it.

VERIFY (paste every output in the report):
- `TELLTALE_HOME=<temp> uv run telltale import git-history --repo .` (with check runs; needs `gh auth status` ok; about 80 s at 162 commits), then `uv run telltale series build --clock change --repo <repo_id>` (repo_id from `uv run python -c "from telltale import repo; print(repo.identity('.')['repo_id'])"`; paste the coverage table: merge_verification_ms partial with its null count), `uv run telltale forecast readiness --series <id> --target merge_verification_ms` (check 1 passes naming the excluded columns; the header shows retained and excluded; check 2 measures N' - 16 windows against 20; paste it), `uv run telltale forecast readiness --series <id> --target rework_within_3_lag3 --horizon 4` (retained N - 3; windows N' - 19), and `uv run telltale forecast backtest --series <id> --target merge_verification_ms` with the stub and baselines (windows equal the readiness number; `excluded_rows.count` equals the null count; 0 windows dropped for a hole).
- `uv run pytest -m integration -q tests/integration/test_forecast_contracts.py <the candidate test file>` (all pass; paste the names with -v).
- On the synthetic series through the CLI if a command reaches `conditioned` (`uv run telltale forecast candidate --help`; if it only takes --base/--head, say so and skip), else the test output alone.
- uv run pytest -m integration; uv run mypy .; uv run ruff check . && uv run ruff format --check .; uv run deptry .; pre-commit run --all-files.

STOP if `metrics` cannot be given a step selection without changing a number on the existing goldens or E-files (report what would change). STOP rather than edit any file outside OWNS. Never write under ~/.telltale, ~/.codex or ~/.claude. No live agent; no paid experiment. Never kill a process by name pattern; kill by pid only.

REPORT: docs/log/W8-T3.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/Teacher.md). Commit message `W8-T3 targets with holes run over their known rows; candidate protocol at H = 4 for the lagged rework label`. Push the branch and open the PR with `gh pr create --body-file docs/log/W8-T3.md`; leave merging to the coordinator.
