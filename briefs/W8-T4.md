Task W8-T4: when a git-history import defines the tracked base, the change clock's rows are that base's commits and nothing else, one row per sha, with a captured session's process columns attached to the base commit its work landed as; and a first-parent merge commit reports the diff main took when it landed instead of unknown.

OWNS: src/telltale/series_lineage.py, src/telltale/importer_git.py, src/telltale/repo_link.py (`_commit_stats` and its callers only), tests/integration/test_series_backfill.py, tests/integration/test_import_git.py, docs/design/amendments/W8-T4.md, docs/log/W8-T4.md.
READS: docs/design/amendments/W8-T1.md and W8-T2.md, docs/log/W8-T1.md and W8-T2.md, docs/experiments/E16.md (the population this task makes buildable; do not edit it), src/telltale/series_outcomes.py, AGENTS.md, briefs/README.md.
Do NOT edit anything under src/telltale/forecast/ (W8-T3 is editing it in parallel), series_outcomes.py, cli*.py, store.py or model.py. STOP rather than edit any file outside OWNS.

WHAT IS MEASURED TODAY (by the orchestrator, 2026-09-06, on a store holding the owner's captures plus `import git-history` with check runs of four repositories; main at fef41ed):
- telltale's change series builds 219 rows: 167 `unlinked` rows from the git import (main's first-parent history) plus 52 captured `repo_commit` activities of 47 distinct shas, and prints `captured_rows 51`. Every one of the 47 captured shas is a commit on a task branch that was later squash-merged: none is on main's first-parent history (`git rev-list --first-parent main`). So a lineage that the pre-registration calls "the first-parent history of main" has 52 rows that never landed on main as themselves, ordered among the 167 that did, and 5 shas counted twice (e5aa5c5d, a3585324, 111ff108, 5d42f2cd, 65ccb108: two `repo_commit` activities in one capture, at `tree_match_during` twice or at `tree_match_during` and `provider_reported`). Five of the 47 share a TREE with a first-parent commit: the branch tip after the gate protocol merged main into the branch has exactly the tree the squash commit put on main (723dc1b3 -> 13f88fa4 "W6-F1 hf cache env and advise scored-run guard (#55)", 441c6262 -> 2114f7ea, 593e276d -> 79c9073e, 2aff5886 -> 863d950a, 0a867064 -> 681d027b). The other 42 are intermediate branch commits whose trees landed nowhere. `tree` is a field of every `repo_commit` activity, captured and imported alike (646 of 646 imported rows carry it).
- `repo_link._commit_stats` returns every count and per_file as unknown for a commit with two parents ("a merge's numbers depend on which parent you pick"). `importer_git` walks `git log --first-parent`, where the parent is chosen by the walk, and reuses that function, so on a merge-based workflow the A block is unknown on most rows: deckgen 118 rows with files_changed `partial`, 79 nulls; kstrl 252 rows, 100 nulls; systemap 108 rows, 2; telltale 168 rows, 2 (a root commit reports its whole tree through --root, so the residual nulls are truncated per_file lists or the root). Those nulls take subsystems_touched, test_files_changed and dependency_delta with them, which are the candidate features E16 conditions on.
- Where the rows are assembled: series_lineage.lineage() and assemble() (grep `def lineage`, `def assemble`, `def _add`, `_primaries`); the imported capture is the one whose capture_started carries provider `git` (W8-T1: `imp_` + sha256("git", repo_id)[:24], surfaces_configured ["git_history"]).

DO:
(1) repo_link._commit_stats gains a keyword `first_parent: bool = False`. When it is True and the commit has more than one parent, the numbers and per_file come from `diff-tree -r -M --no-commit-id parents[0] sha` (the diff the base took when the merge landed on it). When it is False, unchanged: a merge inside a launcher capture (a `git merge main` into a branch, say) still reports unknown, and the docstring says why the two cases differ. importer_git passes `first_parent=True`. Acceptance: in a temp home, `import git-history --repo ~/Documents/code/deckgen --no-checks` then `series build --clock change --repo <id>`: files_changed nulls 79 -> at most 1 (a truncated per_file list still counts files; say what the residual is if any); kstrl 100 -> at most 1.
(2) series_lineage: when the repository has a git-history capture, the tracked base is that capture's commit set, and the lineage's rows are exactly those commits in committed order. A captured `repo_commit` activity joins the row whose sha it names, or, when its sha is not on the base, the row whose `tree` equals its tree; the row keeps the base commit's sha and committed_ts as its key and clock position, takes the process columns from the capture that produced the captured commit, keeps the captured link's rung as `link_confidence`, and records the captured sha in row_meta (`captured_sha`) when it differs from the row's sha. A captured commit whose sha and tree are both absent from the base is not a row: it is listed in the build's dropped table with the reason "not on the tracked base: a commit that did not land as itself" and counted in the cohort as `off_base_commits`. Without a git-history capture nothing changes: the rows are the captured commits as today, and every existing test passes unchanged.
(3) One row per sha, always: two `repo_commit` activities naming one sha (two rungs from one capture, or two captures) collapse to one row at the highest LADDER rung, provenance carrying both; the cohort counts `collapsed_links`. `captured_rows` counts rows, not activities.
(4) Tests, each broken once against the un-fixed code and watched failing: test_import_git gains a merge commit on main (a branch merged with `git merge --no-ff`) whose row reports the first-parent diff's files_changed and per_file; test_series_backfill gains a lineage with a git import plus (a) a capture whose commit is on the base by sha, (b) a capture whose commit is a branch tip with the same tree as a base commit and a different sha, (c) a capture whose commit is off the base entirely, (d) one sha linked twice from one capture; assert rows equal the base's count, captured_rows 2, the tree-twin row carries the capture's process columns and `captured_sha`, the off-base commit is in the dropped table with the reason above, and the twice-linked sha is one row at the higher rung.

VERIFY (paste every output in the report):
- The deckgen and kstrl lines of DO (1), with the null counts before (79, 100) and after.
- On a COPY of the E16 store (`sqlite3 experiments/E16/out/home/telltale.db ".backup <temp>/telltale.db"`, about 2.1 GB; read the copy, never the original and never ~/.telltale): `TELLTALE_HOME=<temp> uv run telltale series build --clock change --repo d9f783b1d318c0d65ec6b86fe729f6ff1a3581983e4287a71f6510eb80d84d6c` prints rows equal to the count of `unlinked` rows in that store (167 or 168: paste the number and `git rev-list --first-parent --count main` beside it), captured_rows 5, off_base_commits 42, collapsed_links 5, and `series check` ok; paste the dropped table's first lines and the five twin rows' `captured_sha`.
- uv run pytest -m integration; uv run mypy .; uv run ruff check . && uv run ruff format --check .; uv run deptry .; pre-commit run --all-files.

STOP if attaching a capture by tree equality would attach one capture to more than one base commit on the E16 copy (report the shas; do not pick). STOP if the merge-diff rule changes any number on an existing golden or on test_import_git's non-merge commits (report what moved). Never write under ~/.telltale, ~/.codex or ~/.claude. No live agent; no paid experiment. Never kill a process by name pattern; kill by pid only. Never run `gh` for anything but `gh pr create`; the imports above use --no-checks.

REPORT: docs/log/W8-T4.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/Teacher.md). Commit message `W8-T4 the tracked base defines the change clock's rows; first-parent merges report their landed diff`. Push the branch and open the PR with `gh pr create --body-file docs/log/W8-T4.md`; leave merging to the coordinator.
