Task W8-T5: the three path-derived cells of a commit (subsystems_touched, test_files_changed, dependency_delta) are computed from the commit's whole path list when the commit is recorded and stored on the observation, so the 8 KB payload bound that truncates per_file no longer turns them into unknowns.

OWNS: src/telltale/repo_link.py (`_commit_stats` and what it returns), src/telltale/importer_git.py (where the payload is fitted to the bound), src/telltale/series_paths.py, src/telltale/sanitize.py (three fields on the `telltale.repo.commit` allowlist only), tests/integration/test_import_git.py, tests/integration/test_series_backfill.py, docs/design/amendments/W8-T5.md, docs/log/W8-T5.md.
READS: docs/design/amendments/W8-T4.md, docs/log/W8-T4.md, src/telltale/series_changes.py (reads `series_paths.columns`; do not edit it), AGENTS.md, briefs/README.md.
Do NOT edit anything under src/telltale/forecast/ (W8-T3 is editing it in parallel), series_changes.py, series_lineage.py, model.py or store.py. STOP rather than edit any file outside OWNS.

WHAT IS MEASURED TODAY (by the orchestrator, 2026-09-06, main at 3122d74, the E16 store re-imported with the W8-T4 importer):
- `series_paths.columns(payload)` answers subsystems_touched, test_files_changed and dependency_delta from `payload["per_file"]`, and returns three Nones when the list is absent or was truncated (`per_file_truncated`, set by importer_git's fit-to-bound around line 518-532; on the launcher path the sanitizer's 8 KB bound does the same to a linked commit's payload). files_changed, additions and deletions are scalars and survive the bound.
- After W8-T4 the merge rows are filled, and the residual is exactly the truncated lists: deckgen 118 rows, subsystems_touched/test_files_changed/dependency_delta `partial` with 5 nulls; kstrl 252 rows, 4 nulls; telltale 172 and systemap 108, 0. A `partial` covariate is excluded BY NAME from every forecast variant (readiness check 1 and backtest._variant), so on deckgen and kstrl those five and four commits remove three of the six candidate features from every run of E16, whose whole question is whether those features move the forecast.
- The three cells are questions about paths taken from the string alone (series_paths' docstring), which git answers in full at import time; only the stored list is bounded. files_changed is already the precedent: "files_changed stays the true count whatever the launcher's payload bound does to the list" (repo_link._commit_stats docstring).

DO:
(1) repo_link._commit_stats returns, beside files_changed, additions, deletions and per_file, the three cells computed by `series_paths.columns({"per_file": <the whole list>})` BEFORE any bound is applied, under the keys `subsystems_touched`, `test_files_changed`, `dependency_delta`, plus `path_rules_version` = a short hash of series_paths.py's source (compute it the way reducer versions are computed elsewhere; grep `reducer_version` for the idiom), so a later change to the test-path or manifest rule is visible on the row. When the list itself is unknown (a merge without first_parent, a diff git could not read) the three are None as today.
(2) sanitize.py: the three cells and `path_rules_version` are allowlisted on `telltale.repo.commit` as scalars (numbers and a short string), nothing else changes in the allowlist. importer_git: the fit-to-bound keeps the scalars and cuts only per_file, as it does for files_changed.
(3) series_paths.columns prefers the stored scalars when the payload carries all three, and falls back to the per_file rule otherwise (every commit recorded before this task, and the launcher captures already in the owner's store). The NO_PATHS sentence gains the words that say a stored cell is missing only when the commit predates W8-T5 or its list was unknown; the truncation clause of the sentence goes, because a truncated list no longer costs the cells.
(4) Tests, each broken once against the un-fixed code and watched failing: test_import_git gains a commit touching more files than the 8 KB bound holds (write 400 small files under four top-level directories including a tests/ dir and a pyproject.toml change), asserts `per_file_truncated` is True on the stored payload and that the change series shows subsystems_touched 5 (four directories plus the root), test_files_changed equal to the count written, dependency_delta 1, all `observed`; and asserts on a commit whose list is NOT truncated that the stored scalars equal `series_paths.columns` over the stored per_file (the two rules agree wherever both can answer).

VERIFY (paste every output in the report):
- In a temp home, `uv run telltale import git-history --repo ~/Documents/code/deckgen --no-checks` then `series build --clock change --repo <id>`: the three columns `observed` with 0 nulls (before: 5); the same for `~/Documents/code/ralph` (before: 4); `~/Documents/code/telltale` and `~/Documents/code/systemap` still 0.
- On the deckgen build, a script that, for every row whose payload has a complete per_file list, compares the stored scalar cells with `series_paths.columns` over per_file: 0 disagreements out of N (paste N).
- uv run pytest -m integration; uv run mypy .; uv run ruff check . && uv run ruff format --check .; uv run deptry .; pre-commit run --all-files.

STOP if adding three fields to the repo.commit payload changes any golden under fixtures/golden (report which; do not regenerate silently). STOP if the launcher path cannot reach the whole list before the sanitizer bounds it (report where the bound is applied). Never write under ~/.telltale, ~/.codex or ~/.claude. No live agent; no paid experiment. Never kill a process by name pattern; kill by pid only. Never run `gh` for anything but `gh pr create`; the imports above use --no-checks.

REPORT: docs/log/W8-T5.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register (~/.claude/output-styles/Teacher.md). Commit message `W8-T5 path-derived commit cells survive the payload bound`. Push the branch and open the PR with `gh pr create --body-file docs/log/W8-T5.md`; leave merging to the coordinator.
