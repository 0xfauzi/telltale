Task W9-F1: three fixes from today's day-to-day capture: the Codex setup snippet must be a
`[otel]` table, the Claude Code 2.1.263 hook and OTel fields the allowlist has not met, and
the five account-identity attributes that are known and refused rather than unknown.

OWNS: src/telltale/cli.py (the `telltale setup codex` snippet text only), src/telltale/
allowlist.py, src/telltale/sanitize.py (the refused set), src/telltale/providers/
claude_drift.py (DRIFT entries), tests/integration/test_doctor_setup.py (the setup case),
tests/integration/test_privacy.py (one probe), docs/log/W9-F1.md.
READS: providers/codex_launch.py or wherever the `-c` overrides `telltale run` passes to
codex live (search for "otlp-http"), docs/experiments/E02.md.

READ: src/telltale/cli.py from line 200 (the snippet and its comment); the codex launch
plan's `-c` values; src/telltale/sanitize.py lines 60-72 (NEVER_PERSIST) and 250-262 (where
a NEVER_PERSIST hit is noted as "never_persist" and where an unknown field becomes an
unknown_field diagnostic); src/telltale/allowlist.py around lines 465 and 568 (the Claude
hook common fields, `cwd` and `transcript_path` as PATH).

Measured today, 2026-09-06:
(a) The owner pasted the printed Codex snippet at the END of ~/.codex/config.toml, after a
    `[projects."..."]` table. tomllib parsed that file with NO top-level `otel` key: dotted
    keys after a table header are keys of that table. A `codex exec` run under that file
    delivered nothing to the daemon (otel_logs 8 before, 8 after); the same values passed
    as `-c` overrides delivered 4 batches. A copy rewritten as a `[otel]` table at the end
    parses with top-level otel keys environment, exporter, metrics_exporter, tool_result.
(b) unknown_field diagnostics between 22:00 and 22:16 UTC, from Claude Code 2.1.263:
    every hook event now carries `scratchpad_dir` (a path); SubagentStop carries
    `agent_transcript_path`, `background_task_count`, `session_cron_count`; PostModelSwitch
    carries `cache_ttl`, `context_tokens`, `estimated_cache_write_usd`, `pricing`,
    `prompt_cache_warm`, `requested_model`, `source`; PostToolUseFailure showed a top-level
    `command`; the OTel event permission_mode_changed carries `from_mode`, `to_mode`,
    `trigger`. Inspect the raw shape of each before choosing a Kind: pull one record of
    each from a live haiku session against a temporary daemon on a temp TELLTALE_HOME (one
    session, 12 to 15 s measured), or from the receiver's diagnostics; never guess a Kind.
    `pricing` may be a nested object: if it is, drop it and say so.
(c) Every OTel event on 2.1.263 carries organization_id, user_account_id, user_account_uuid,
    user_email and user_id. They are refused on purpose (design 6.3 never persists account
    identity) and today each event records them as unknown_field: 41 diagnostics for one
    short session. Known and refused is a different statement from unknown.

Trap: the allowlist is the schema; every new field is a DRIFT entry in claude_drift.py
naming the version 2.1.263. Trap: no emoji, no em dashes. Trap: allowlist.py is 719 lines
and cli.py 766 against the 800 ratchet.

DO:
(1) cli.py: the printed Codex snippet becomes a `[otel]` table with the same four values the
    launch plan passes as `-c`, and its comment says why a table header is the only safe
    form (dotted keys appended after another table's header become that table's keys) and
    that the values are the ones `telltale run` passes. Do not add `--apply`; setup never
    writes (AGENTS.md invariant 7).
(2) A test in test_doctor_setup.py: `tomllib.loads('[projects."/x"]\ntrust_level = "trusted"\n'
    + snippet)["otel"]` has exactly the four keys, and the endpoint and protocol values equal
    the ones parsed out of the codex launch plan's `-c` strings (one source of truth checked
    against the other, not two copies of one string).
(3) allowlist.py: add the (b) fields with the Kind their raw shape warrants (paths PATH,
    counts SIZE, model and enum-like strings ENUM, money SCALAR); claude_drift.py: one DRIFT
    entry per event naming 2.1.263.
(4) sanitize.py: a REFUSED frozenset for the five (c) names (and the E01-era `user.id` if
    it is distinct), dropped at every depth like NEVER_PERSIST, noted as "refused" in the
    trace, and never counted as unknown_field. test_privacy.py gains one probe: a synthetic
    OTel api_request record carrying user_email "telltalefake@example.com" and
    organization_id "org_TELLTALEFAKE" replayed through the receiver; the strings are absent
    from the db and wal bytes and the run adds zero unknown_field diagnostics.
VERIFY: uv run pytest -m integration -q (all pass); replay the E01 fixtures through
`python -m telltale.receiver --replay` for S1 and S4: observation counts per type identical
to before your change (paste both); one live haiku session against a temporary daemon on
port 47398 with a temp TELLTALE_HOME: after it, `select kind, detail from diagnostics` shows
zero unknown_field rows for the (b) and (c) names (paste the query output); stop the daemon
BY PID. uv run mypy . ; uv run ruff check . && uv run ruff format --check . ; uv run deptry . ;
pre-commit run --all-files.
STOP if a (b) field's raw value is content (a prompt, a response, a file body): refuse it,
add it to NEVER_PERSIST, and say so; never widen the allowlist to admit content.
REPORT: docs/log/W9-F1.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR
NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register with the config.toml
placement as the real example. Commit message: "W9-F1 codex setup table, 2.1.263 fields,
refused identity attributes".
