Task W9-T1: give a daemon capture its repository, so day-to-day sessions land on the
change clock: bind repo identity from the first hook's cwd, take the launcher's snapshots,
and link commits at session end.

OWNS: src/telltale/daemon_capture.py (new), src/telltale/receiver.py (one call site),
src/telltale/launch_commits.py (link_commits takes a root), src/telltale/cli.py (daemon
wiring only), src/telltale/repo_link.py (short sha, if needed),
tests/integration/test_daemon_capture.py (new), docs/design/03-capture-howto.md (one
paragraph), docs/log/W9-T1.md.
READS: launch.py (`_Capture`: emit, snapshots, ctx), repo.py (identity, git_root), sanitize.py
(Ctx), providers/claude_launch.py (the http hook list and its timeout), facts.py.

READ: src/telltale/receiver.py lines 100-130 (constructor: `derive_captures`,
`ctx_for_capture`), 157-200 (register_session, bind_capture, on_new_capture, on_repo_change),
236-262 (`_ctx`: repo_id and fingerprint come from `_capture_ids`, the Ctx from `_ctx_for`);
src/telltale/launch.py: the `_Capture` class and where it wires `on_repo_change` and
`ctx_for_capture` (line 250); src/telltale/launch_commits.py in full (103 lines);
src/telltale/cli.py `def daemon` (line 343: `Receiver(store, level=level, port=port,
derive_captures=True)`).

Measured today, 2026-09-06, on Claude Code 2.1.263 through the running daemon, capture
cap_b0be0e0da48a5a1a78bdb8fa (a haiku session in a temp git repo that wrote hello.txt and
ran `git add hello.txt && git commit -m E17-smoke2`):
- PreToolUse payload: `"cwd":"<outside>/14fee89e"`, `"file_path":"<outside>/0cc7d242"`. The
  daemon has no repo root, so every path is outside and hashed. After this task the same
  session stores `file_path` as `hello.txt`.
- tool_result payload: `"git_commit_id":"0881c21"`. SEVEN characters: Claude Code reports a
  short sha. Trap: check what repo_link.reported_commits and commits_since do with a short
  sha today (search for where provider_reported ids are compared to `git log` output); if
  a short sha never matches, resolve it with `git rev-parse --verify <short>^{commit}`
  inside the repo, and only there. This is the provider_reported rung of spec 12.3 and the
  only rung a daemon capture had before this task.
- Every observation of that capture has repo_id NULL. `sessions --link-commits` skips it:
  launch_commits.link_commits compares `known.repo_id` to the cwd's identity and NULL never
  matches. That is the defect this task removes.
- repo.identity on this repository: 56, 45, 48 ms over three runs. The http hook timeout the
  snippet declares is in claude_launch.py; read it and state both numbers in the report.
- Sessions of this shape take 12 to 15 s of wall time and two api_requests (three runs).
- File lengths: receiver.py 755, cli.py 766, launch.py 762, allowlist.py 719. The ratchet is
  800. New code goes in daemon_capture.py; the touched files take a call site each.

Trap: the receiver is the standard-library collector; nothing new is imported. Trap: the
hook record that carries cwd is the one whose own paths must come out relativized, so bind
before parsing that record, not after. Trap: a cwd outside any git checkout binds nothing
and records nothing: repo_id None is the truth for that session, not an error. Trap: the
root is never stored (repo_id is the sha256 of it); it lives in memory for the daemon's
lifetime only. Trap: linkage at SessionEnd must not run on the hook's request thread past
the timeout; hand it to a worker thread after the flush that committed the session's own
tool_result records, since provider_reported ids come out of the store.

DO:
(1) src/telltale/daemon_capture.py: `Binder(store, receiver, level)`. `observe(capture_id,
    surface, raw)` is called by the receiver for every hook record before parse. For a
    capture not yet bound whose raw hook JSON carries an absolute `cwd`: `root =
    repo.git_root(Path(cwd))`; when root is not None build the launcher's own `_Capture`
    for this capture id with cwd=root (the same object link_commits builds; if its
    constructor needs a child process or argv, split the smallest piece out of launch.py
    rather than copying it), call `receiver.bind_capture(capture_id, identity["repo_id"],
    None)`, keep `{capture_id: _Capture}`, and emit `telltale.repo.identity` through the
    capture. `ctx(capture_id) -> Ctx` returns the bound capture's Ctx (repo_root=root) or
    `Ctx()`. `repo_changed(capture_id, trigger)` forwards to the bound capture's debounced
    snapshot, exactly as the launcher wires on_repo_change. `session_ended(capture_id)`
    runs `launch_commits.commits(capture)` on a worker thread after `store` has flushed,
    emitting one `telltale.repo.commit` per linked commit, then forgets the capture.
(2) receiver.py: one call to `binder.observe` at the top of the hook path (the `_HOOKS_PREFIX`
    route), before `_ctx`; `on_repo_change` and `ctx_for_capture` are the existing hooks and
    take the Binder's methods. The receiver keeps working with no Binder (the launcher's
    path is unchanged: it binds through bind_capture as today).
(3) launch_commits.link_commits(store, capture_id, level=1, root: Path | None = None): the
    repository is `root` when given, else Path.cwd() as today. `sessions --link-commits` is
    unchanged in behaviour and now links daemon captures bound to the current repository.
(4) cli.py daemon: construct the Binder and pass its methods into the Receiver. The daemon's
    one line per capture may add the repo_id when bound.
(5) docs/design/03-capture-howto.md: one paragraph in the Teacher register: a day-to-day
    session started inside a git checkout now carries the repository (as the sha256 of its
    root, never the path), its file paths are repo-relative, its commits are linked at
    session end by the provider_reported rung, and `telltale series build --clock change
    --repo <id>` includes it. A session started outside a checkout keeps repo_id None.
(6) tests/integration/test_daemon_capture.py, against the real receiver over HTTP with
    `derive_captures=True` and a Binder, temp TELLTALE_HOME, a temp git repo with one commit:
    (a) POST a synthetic PreToolUse hook JSON {session_id, cwd=<repo>, hook_event_name,
    tool_name "Write", tool_input {file_path "<repo>/hello.txt", content "hi"}} and wait for
    drain: the derived capture's observations carry repo_id == repo.identity(repo)["repo_id"],
    a telltale.repo.identity observation exists, the PreToolUse payload has file_path
    "hello.txt" and no "<outside>" anywhere, and the string "hi" is absent from the db bytes;
    (b) commit hello.txt in the temp repo, POST an OTLP JSON tool_result log record with
    tool_name Bash and git_commit_id = the SHORT sha, POST SessionEnd, wait: one
    telltale.repo.commit observation with the FULL sha and link_confidence provider_reported;
    (c) a PreToolUse whose cwd is a temp dir that is not a checkout: repo_id stays None, no
    repo.identity observation, no diagnostic; (d) `sessions --link-commits` run with cwd =
    the temp repo after (b) adds 0 (already linked) and prints the capture with its repo_id.
    Run (a) and (b) against main before your change and record that they fail (repo_id None,
    0 commits): a test that passes against the un-fixed code is not a test.
VERIFY: uv run pytest -m integration -q (all pass, names of the new tests printed in the
report); a live run, the only one this task spends: start `uv run telltale daemon --port
47399` from your branch with TELLTALE_HOME set to a temp dir, and in a temp git repo run
`env -u CLAUDECODE claude -p "Create a file hello.txt containing the word hi, then run: git
add hello.txt && git commit -m W9-smoke. Reply done." --model haiku --permission-mode
acceptEdits --allowedTools Write Bash --settings '<the JSON `telltale setup claude --print`
prints, with the port replaced by 47399>'` (12 to 15 s measured); then `telltale sessions`
shows the capture with the temp repo's repo_id; `telltale show <capture>` lists one
repo_commit; `telltale series build --clock change --repo <repo_id>` from the temp repo
prints 1 row whose fresh_input_tokens_total is not None; stop the daemon BY PID (never
pkill -f or killall). uv run mypy . ; uv run ruff check . && uv run ruff format --check . ;
uv run deptry . ; pre-commit run --all-files (file-length ratchet passes at 800).
STOP if `_Capture` cannot be built without a child process and the split would move more
than about 60 lines out of launch.py: report the constructor's needs and the split you
propose, with line numbers, and do not copy code. STOP if the live session sends nothing
to the daemon (one of five such sessions did today, with a ~110 s stall before its first
transcript line and no telemetry at all; cause unknown): retry once, and if it happens
again report both session ids and the transcript timestamps, do not loosen the VERIFY.
REPORT: docs/log/W9-T1.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR
NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register: idea before name, the
E17 capture above as the real example with its real values before and after, how every
number was measured, and the answer rejected (storing the cwd path) and why (a path is a
disclosure; the sha256 of the root is the repo_id the launcher already uses). Commit
message: "W9-T1 daemon captures bind their repository".
