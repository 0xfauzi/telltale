Task W9-T2: a capture that recorded a change contributes its process columns to that
change's row on the change clock, whether or not it is a numbered attempt.

OWNS: src/telltale/series_changes.py, src/telltale/series_lineage.py (only if a field must
move; it is at 789 lines), docs/design/amendments/W9-T2.md (new; the amendments directory
holds the W8-T2 precedent), tests/integration/test_series_lineage.py (extend),
docs/log/W9-T2.md.
READS: series_outcomes.py (`columns` at line 164, unchanged), series_paths.py, lineages.py,
docs/design/01-design.md "Change clock", docs/design/amendments/W8-T2.md (the shape of a
recorded semantic change).

READ: src/telltale/series_changes.py lines 360-518: `_change` builds a Change with
`landed_by=_landed_by(found, rows)` (the ATTEMPTS whose captures recorded the commit) and
`own=_own(found, rows)` ("every activity of the capture(s) that recorded this commit,
attempt or not"), and `_change_row` folds every process column from `landed` = the
activities of `landed_by`, so a recording capture that is not an attempt contributes
nothing; `own` is read only by series_outcomes when the change is `uncaptured`.
src/telltale/series_lineage.py lines 195-215: `Lineage.activities` and `.providers` exist
"so the change clock can read a repo_commit written by a capture that carried no attempt
id"; lines 329-355: `_identity` returns the sentence "no task_id and attempt: not an
attempt of this lineage", which is how a day-to-day session ends up in `dropped`.

Measured (read the E16 store COPY at experiments/E16/out/home/telltale.db; never
~/.telltale):
- 713 telltale.repo.commit observations: 657 unlinked (the tracked base, from git
  history), 54 tree_match_during, 2 provider_reported. 33 captures recorded a commit at a
  ladder rung; 61 captures carry an external.correlation (are attempts). 0 linked shas are
  themselves on the base: every captured commit joined its base row by tree twin
  (`captured_sha`), which is why E16 could report process columns on only 5 of 657 telltale
  rows.
- W9-T1's live capture cap_786c594857d404824628c2ae (a haiku session in a temp checkout,
  one commit, provider_reported): `series build --clock change --repo <id>` prints 1 row
  with the six repository columns observed and all seven process columns unavailable, and
  the cohort's dropped list says "cap_786c59...: no task_id and attempt: not an attempt of
  this lineage". That row is the whole of what day-to-day capture can offer H7 today.

Decision, made by the orchestrator so the row's meaning is stated before the code moves:
a change-clock row is one change landed on the tracked base; its process columns are
folded over the attempts that landed it when there are any, else over the captures that
recorded it at a ladder rung. `attempts_to_land` stays the max attempt ordinal and is None
when no attempt exists: a day-to-day session is not "attempt 1", because how many sessions
tried before it is not observable. Trap: unknown stays None, never 0 and never 1.
Trap: the five E16 rows that already fold from attempts must not change by one byte.
Trap: series_lineage.py is 789 lines against the 800 ratchet; new code lives in
series_changes.py (518) or a new small module. Trap: no emoji, no em dashes.

DO:
(1) series_changes.py: `Change` gains `recorded_by`: the captures that recorded this commit
    or its tree twin at a ladder rung, attempt or not, in start order, each with
    capture_id, provider, activities (the `_READ_TYPES` slice, as Attempt has), coverage
    and fingerprint read off the same lifecycle row `series_lineage._capture_row` reads.
    `landed_by` stays the attempts among them. `_change_row` folds the seven process
    columns over `landed_by` when non-empty, else over `recorded_by`; `attempts_to_land`
    as decided above; `env_changed` from the recorders' fingerprints when they carry one
    (a daemon capture carries none: None). The post-merge columns keep their inputs
    (outcomes attach by task_id and attempt, which a recorder without one cannot have):
    say so in a comment at the call.
(2) Coverage of the process columns comes from the recorders' coverage maps exactly as it
    comes from attempts today (find where `worst_coverage` is applied over `landed_by`
    and apply it over whichever list was folded).
(3) The cohort names what happened: `process_from_recording_captures` = the list of shas
    whose process columns came from recorders rather than attempts, and the dropped list
    stops calling such a capture "not an attempt of this lineage": its sentence becomes
    "recorded <sha> at <rung>: process columns folded, attempts_to_land unknown". A
    capture that recorded nothing keeps today's sentence.
(4) docs/design/amendments/W9-T2.md: the decision above in the Teacher register, with the
    E16 numbers, and the cell-by-cell statement of what changes for a row that no attempt
    landed. docs/design/01-design.md is not edited.
(5) tests/integration/test_series_lineage.py: a lineage with two captures recording two
    commits at provider_reported, one an attempt (correlation task_id/attempt) and one
    daemon-shaped (no correlation, no fingerprint); the attempt's row folds from the
    attempt and has attempts_to_land; the other row has fresh_input_tokens_total equal to
    its model_request tokens, attempts_to_land None, env_changed None, and the cohort names
    its sha. Run it against main before your change and record the failure (process cells
    None on the second row).
(6) The E16 measurement, on the store copy, for each of the four repo_ids in
    docs/experiments/E16.md: `series build --clock change --repo <id>` before and after;
    row counts identical; the rows that folded from attempts before are byte-identical
    after (compare the JSON of rows and row_meta); N rows gained process columns, listed
    with their rungs. Put the table in the report.
VERIFY: uv run pytest -m integration -q (all pass); the E16 before/after table; when PR
#74 (W9-T1) is on origin/main, merge it into your branch and run W9-T1's live procedure
once (daemon on port 47399 with a temp TELLTALE_HOME, a temp checkout, `env -u CLAUDECODE
claude -p "Create a file hello.txt containing the word hi, then run: git add hello.txt &&
git commit -m W9-smoke. Reply done." --model haiku --permission-mode acceptEdits
--allowedTools Write Bash --settings '<setup claude --print, port 47399>'`, 9 to 15 s
measured): `series build --clock change --repo <id>` from the checkout prints 1 row with
fresh_input_tokens_total not None and attempts_to_land None; stop the daemon BY PID (never
pkill -f or killall). uv run mypy . ; uv run ruff check . && uv run ruff format --check . ;
uv run deptry . ; pre-commit run --all-files.
STOP if any of the five E16 attempt rows changes: report the diff and do not proceed.
STOP if the fold would need a recorder's coverage and the lifecycle row has none: report
which captures and what the row would have said; do not default coverage.
REPORT: docs/log/W9-T2.md in the order OUTCOME / WHAT WAS WRONG / WHAT CHANGED / UNSURE OR
NOT DONE / NEXT, plus EXPLAIN FOR THE OWNER in the Teacher register: the W9-T1 live row
before and after as the real example, how each number was measured, and the rejected
answer (attempts_to_land = 1 for a single session) and why. Commit message: "W9-T2 change
rows fold process columns from the captures that recorded them".
