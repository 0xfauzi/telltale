"""Two functions. divide is unguarded on purpose: the agent's job is to guard it."""


def add(a: float, b: float) -> float:
    return a + b


def divide(a: float, b: float) -> float:
    return a / b
